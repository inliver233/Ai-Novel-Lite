{% if mode == 'append' %}<APPEND_MODE>
追加模式（硬规则）：

你只输出追加到已有正文末尾的新增片段，不得重写整章或复述已有内容。

衔接要求：
- 从 <CURRENT_DRAFT_TAIL> 的最后一句之后自然续写
- 保持语气、时态、叙事视角、人称的完全连续——读者不应察觉到拼接痕迹
- 如果草稿尾部停在对话/动作中途，直接接续该对话/动作
- 不要重复章节号、章节标题或已有的段落开头
- 不要用总结前文的方式开始追加——直接从故事正在发生的那个瞬间续写

声音一致性要求：
- 句式长度分布应与前文保持相似——如果前文以短句为主，追加部分不要突然变成长段抒情
- 角色的说话方式必须与前文中的声音完全一致——用词范围、语气特征不可突变
- 描写密度与前文匹配——如果前文描写克制，追加部分不要突然变得浓墨重彩
- 叙事距离保持不变——前文是贴近角色内心的近景，追加部分不要拉远到旁观者视角

内容要求：
- 追加内容必须向前推进剧情，不得原地踏步或用描写凑字数
- 如果草稿尾部已完成一个场景，从新场景的压力点切入
- 如果草稿尾部处于场景中间，继续推进当前场景的目标→阻碍→结果
- 追加部分的每500字内至少包含一次局势变化或新信息——防止叙事注水
</APPEND_MODE>
{% endif %}