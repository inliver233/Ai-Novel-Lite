从以下文本中提取章节结构（分块 {{chunk_index}}/{{total_chunks}}）：

{{analysis_context}}

---
{{chunk_text}}
---

请严格按照系统提示的 JSON 格式输出。注意：
- outline_md 中的换行必须转义为 \n
- beats 数组中每条为单行字符串
- 确保输出的 JSON 完整且可解析
- 仅输出 JSON，无其他文本
