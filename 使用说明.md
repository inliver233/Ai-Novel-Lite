# ainovel Atelier 使用说明（给完全零基础用户）

> 目标：让“完全没学过编程、没用过 AI”的人，也能从 0 开始：创建项目 → 生成大纲 → 创建章节 → 写作/AI 生成 → 预览 → 导出。
>
> 本说明以仓库 `README.md` + 前端页面/按钮文案为准（代码阅读整理）。如果你的界面文案略有不同，以你看到的按钮文字为准。

---

## 0. 你需要知道的 3 句话

1) 这是一个“本地网页应用”：启动后，用浏览器打开一个地址（例如 `http://localhost:5173`），像用网页一样点按钮即可。  
2) “项目 = 一本小说”；“大纲 = 整本的计划”；“章节 = 每一章正文”；“导出 = 下载整本 Markdown 文件”。  
3) 任何生成失败/报错时，优先记录页面里出现的 **请求 ID（request_id）**，它是定位问题的“编号”。

---

## 1. 最快上手（推荐流程：开工向导）

如果你只想最快写出东西，照着做：

1) 启动应用（见下方 **2. 如何启动**）。  
2) 打开浏览器进入首页（Dashboard / 首页）。  
3) 点 **“新建项目”** → 填一个项目名 → 点 **“创建”**。  
4) 进入 **“项目设置”**：把“世界观/风格/约束”随便先填几行 → 点 **“保存”**。  
5) 进入 **“角色卡”**：点 **“新增角色”** → 填主角 → 点 **“保存”**。  
6) 进入 **“模型配置”**：选择/新建一个“后端配置”，保存 API Key，然后点 **“测试连接”**。  
7) 进入 **“开工向导”**：点 **“一键开工”**（会自动生成大纲，并按大纲创建章节骨架）。  
8) 进入 **“写作”**：选中第 1 章 → 点 **“AI 生成”** 或自己写 → **保存** → 写完设为 **“定稿（done）”**。  
9) 进入 **“预览”**：通读整本。  
10) 进入 **“导出”**：下载 `.md` 文件。

---

## 2. 如何启动（两种方式：本地启动 / Docker 一键启动）

### 2.1 本地启动（开发模式，最常用）

你会启动两个东西：后端（数据/AI 接口）+ 前端（网页界面）。

#### A) 启动后端（FastAPI）

1) 打开 PowerShell，进入仓库根目录（你现在所在目录）。  
2) 进入后端目录：

```powershell
cd backend
```

3) 首次安装依赖（如果你已经装过，可跳过）：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

4) 配置环境变量（第一次需要）：

- 复制 `backend/.env.example` 为 `backend/.env`
- 然后按需要修改（详见 **3. 登录与账号**、**4. 模型/API Key**）

5) 启动后端（重要：SQLite 只能单 worker）：

```powershell
.\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --workers 1 --port 8000
```

后端默认地址：`http://localhost:8000`（API 在 `/api` 下，例如 `/api/health`）

#### C)（可选）后台任务队列（“无感更新/批量生成”不运行时必看）

有些功能会在后台自动更新（例如世界书/向量检索 RAG/搜索索引/批量生成）。本地启动时你有两种选择：

1) **推荐（最省事）**：在 `backend/.env` 设置 `TASK_QUEUE_BACKEND=inline`（不需要 Redis；进程内单线程 worker）。  
2) **rq 模式（更接近部署）**：启动 Redis + worker（见仓库根 `README.md` 的“后台任务队列”小节）。

#### B) 启动前端（Vite）

另开一个 PowerShell 窗口：

```powershell
cd frontend
npm install
npm run dev
```

前端默认地址：`http://localhost:5173`

---

### 2.2 Docker Compose（一键启动/更接近部署）

适合：你不想折腾 Python/Node 依赖，或者你想用 Postgres/Redis/Worker。

1) 复制 `.env.docker.example`：

```powershell
copy .env.docker.example .env.docker
```

2) 编辑 `.env.docker`，至少要填（不需要懂是什么，照着填即可）：

- `POSTGRES_PASSWORD`：数据库密码（自己随便设一个强一点的）
- `DATABASE_URL`：连接串（示例通常在 `.env.docker.example` 里）
- `SECRET_ENCRYPTION_KEY`：用于加密保存 Key（必须）
  - 生成方式（在任意 Python 环境执行）：
    ```powershell
    python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
    ```

3) 启动：

```powershell
docker compose up --build
```

访问：
- 前端：`http://localhost:5173`
- 后端：`http://localhost:8000`

---

## 3. 登录与账号（你看到“登录页”时怎么做）

### 3.1 两种登录状态

应用里可能出现两种情况：

1) **需要登录**：你会看到“登录”页面，需要输入用户名/密码。  
2) **开发演示模式（DEV fallback）**：登录页上会提示“已启用开发演示模式：无需登录也可进入应用”，并出现按钮 **“跳过登录，继续进入应用”**。

> 重要安全提示：DEV fallback 只能用于本地开发/演示；如果你把它部署到公网且仍开启，会造成“绕过登录”的高风险。

### 3.2 管理员账号怎么来？

后端启动时会根据环境变量尝试创建管理员（见 `backend/.env.example`）：

- `AUTH_ADMIN_USER_ID`
- `AUTH_ADMIN_PASSWORD`（至少 8 位）

如果你需要“用户管理”功能，建议配置管理员账号，然后用它登录。

### 3.3 退出登录

侧边栏/页面里通常会有 **“退出登录”**（或在用户菜单里）。退出会清理浏览器里的会话信息。

---

## 4. 模型与 API Key（“为什么我点生成没反应/报错？”）

### 4.1 你必须先做的：在“模型配置”里保存 Key 并测试连接

入口：左侧导航 → **项目配置 → 模型配置**

你会看到两块很重要的内容：

1) **模型预设（Preset）**：provider / base_url / model / temperature / max_tokens 等（影响“怎么生成”）  
2) **后端配置库（Profile）+ API Key**：把 Key 安全地存在后端数据库里（影响“能不能调用模型”）

最常见的正确操作：

1) 在 **“API 配置库（后端持久化）”**：
   - “选择配置”下拉框：如果没有，就在“新建配置名”里输入名字（例如“我的 OpenAI”）
   - 点 **“保存为新配置”**
2) 在 **“API Key（后端安全存储）”**：
   - 在输入框粘贴 Key
   - 点 **“保存 Key”**
3) 回到上面，点 **“测试连接”**
4) 最后点 **“保存”**（保存模型预设参数）

如果你没做完这些，很多页面的 **AI 生成/一键开工/分析/重写/RAG** 都会失败。

### 4.2 你不需要懂的概念（但你会看到它们）

- `provider`：服务商/接口类型（openai / openai_compatible / anthropic / gemini 等）
- `base_url`：接口地址（用默认就行；自建网关才需要改）
- `model`：模型名（如 `gpt-4o-mini`）
- `extra (JSON)`：高级参数（不要把 API Key 写在这里）

---

## 5. 项目里最重要的“东西”（像词典一样解释）

### 5.1 项目（Project）

- **是什么**：相当于“一本小说工程”。  
- **在哪里创建**：首页（Dashboard）点“新建项目”。  
- **会保存什么**：设定、角色卡、大纲、章节正文、生成记录、世界书、记忆等。  

### 5.2 大纲（Outline）

- **是什么**：整本小说的结构计划。  
- **一个项目可以有多个大纲版本**：你可以“新建/重命名/删除/切换当前大纲”。  
- **大纲里可包含章节结构**：如果 AI 生成解析出了“章节列表”，才能“一键创建章节骨架”。  

### 5.3 章节（Chapter）

每章通常有：
- **章号**（第几章）
- **标题**
- **本章要点**（计划/大纲摘要）
- **正文（Markdown）**
- **摘要（可选）**
- **状态**：`planned`（计划中）/ `drafting`（草稿）/ `done`（定稿）

> 重要：很多“写入记忆”的动作要求章节必须是 `done`（定稿）。  
> 另外：如果你修改了“定稿”章节的标题/要点/正文/摘要，系统会提示并把状态自动回退到“草稿”，避免“定稿被污染”。

### 5.4 角色卡（Characters）

- **用途**：让生成时的人物更稳定（不容易把性格/关系写错）。  
- **建议至少 1 个核心角色**：主角/反派/关键 NPC。  

### 5.5 世界书（WorldBook）

可以把“世界观设定/术语解释/人物设定片段”做成一条条条目。生成时可以按关键词触发注入，让 AI 记住世界观。

每条世界书条目常见字段：
- **标题（title）**：条目名称
- **关键词（keywords）**：触发该条目的关键词（可多行）
- **启用（enabled）**：是否允许触发
- **常驻（constant）**：是否“无论如何都注入”（不靠关键词）
- **优先级（priority）**：重要程度（影响排序/注入）
- **字数上限（char_limit）**：该条最多注入多少字符（防止提示词过长）

世界书页面还有：
- **预览触发**：输入一段文本，看会触发哪些条目，以及最终注入的文本是什么。  
- **批量模式**：批量启用/停用/改优先级/改字数上限/批量删除。  
- **导入/导出 JSON**：用于备份或迁移；`overwrite` 会先删光再导入，非常危险（页面会二次确认）。  

### 5.6 风格（Styles）

这是“写作风格提示词”的管理页。你可以：
- 使用预设风格（is_preset）
- 新建自己的风格（写一段 prompt_content）
- 设为项目默认风格（生成时默认注入）

### 5.7 记忆（Memory）与“记忆注入”

你可能会看到：
- **上下文预览（Context Preview）**：看“本次生成会带哪些记忆/世界书片段进去”。  
- **记忆更新（Memory Update）**：把章节分析结果/结构化信息写入长期记忆（通常三步：提议→审核→应用）。  
- **结构化记忆**：用表格方式浏览实体/关系/事件/伏笔/证据。  
- **伏笔面板**：查看“未回收伏笔”。  

> 不想这么复杂也可以：先只用“项目设置 + 角色卡 + 大纲 + 写作 + 导出”，其他都当高级功能。

### 5.8 请求 ID（request_id）与 run_id（生成记录）

当你看到报错弹窗/提示条里带：
- `request_id`：一次请求的定位编号（排障第一重要）
- `run_id`：一次生成记录的编号（用于查看生成记录、下载排障包）

---

## 6. 页面说明（按左侧导航顺序）

> 说明：页面 URL 里会有 `projectId`，例如：`/projects/xxx/writing`。
> 你不需要手动输入 URL，绝大多数都能从左侧导航进入。

### 6.0 左侧导航菜单怎么用（先学这个，后面所有页面都靠它）

左侧导航栏（侧边栏）里包含：

- **项目切换器（Project Switcher）**：先选中一个项目，才会出现“写作/大纲/角色卡/世界书…”等入口。  
  - 如果你没选项目，会看到提示：**“请选择一个项目以进入编辑页。”**
- **首页（首页）**：回到项目列表（Dashboard）。  
- **术语/帮助**：打开一个“术语速查 + 小贴士”的帮助面板（不跳转页面）。  
- **显示高级调试**（开关）：打开后会出现“知识库（RAG）/术语表/图谱/分形/结构化记忆/任务中心”等排障页面。  
  - 新手建议先关掉，避免信息太多。
- **移动端**（屏幕窄时）：通常会出现“打开导航/关闭导航”的按钮（点一下弹出侧边栏）。

### 6.1 登录（/login）

你会看到：
- 用户名、密码输入框
- “登录 / 清空”
- 如果是 DEV 演示模式，会有“跳过登录继续进入应用”

你该怎么做：
- 正式使用：输入管理员/普通账号 → 点“登录”
- 仅本地体验：点“跳过登录”

### 6.2 首页（Dashboard，/）

用途：创建项目、进入最近项目、删除项目。

你会看到：
- 顶部欢迎语
- 按钮：**创建第一个项目 / 继续写作 / 继续向导**（视项目情况变化）
- 卡片：**“+ 新建项目”**
- 每个项目卡片上的按钮：**向导 / 设置 / 删除**

常用操作：
1) 新建项目：点“新建项目” → 填项目名 → 点“创建”
2) 进入项目：点项目卡片（或点“向导/设置”）
3) 删除项目：点“删除” → 二次确认（不可恢复，会删除设定/角色/章节/生成记录）

### 6.3 开工向导（/projects/:projectId/wizard）

用途：把“从 0 到导出”的步骤变成清单；并提供“一键开工”。

你会看到：
- 顶部：当前项目、完成度、建议流程
- 按钮：
  - **一键开工**：自动生成大纲并创建章节骨架（默认 12 章，tone/pacing 有默认值）
  - **改用按步骤**：跳到“步骤清单”
- 步骤清单：每步都有“前往 / 跳过 / 取消跳过”

一键开工会发生什么：
1) 调用后端生成大纲（需要模型可用、Key 可用）  
2) 把生成的大纲保存为一个新的大纲版本  
3) 根据解析出的章节结构，创建章节骨架  
4) 成功后跳转到“写作”

失败时最常见原因：
- 没配置模型/没保存 Key
- 生成结果没解析出章节结构（提示会让你去“大纲”页手动调整）

### 6.4 项目设置（/projects/:projectId/settings）

用途：写“世界观/风格/约束”，以及一些高级检索/向量化配置；可管理成员协作（项目 owner 才能邀请）。

最重要的三块（新手先填这三块就够）：
- **世界观**：故事背景、世界规则、时代、地点、组织……
- **风格**：叙事视角、文风、节奏、禁忌表达……
- **约束**：必须遵守的规则（例如“不能出现现代词汇”“不要出现 AI 自称”等）

按钮：
- 顶部 **保存**
- 快捷键：**Ctrl/Cmd + S**

高级内容（不懂就先别动）：
- Query Preprocessing / Rerank / Embedding：用于 RAG/知识库检索增强
- 成员（Memberships）：邀请其他用户加入项目（viewer/editor）

### 6.5 角色卡（/projects/:projectId/characters）

用途：创建/编辑角色，生成时会引用角色信息。

你会看到：
- 搜索框（按姓名/定位）
- 角色列表卡片
- 按钮：**新增角色**

新增/编辑角色（右侧抽屉）：
- 姓名（必填）
- 角色定位（可选：主角/反派/同伴…）
- 人物档案（建议写详细）
- 备注（记录未定稿信息）
- 按钮：关闭 / 保存

### 6.6 模型配置（/projects/:projectId/prompts）

用途：把“能调用模型”这件事配置好。**不配这个，所有 AI 生成/分析/重写 都会失败。**

页面主要分 2 块：

#### A) 模型预设（Preset：决定“怎么生成”）

你会看到的关键输入项（从上到下常见）：

- **服务商（provider）**：例如 `openai / openai_compatible / anthropic / gemini ...`
- **接口地址（base_url）**：一般用默认；只有你用“网关/自建代理”才改
- **模型名（model）**：例如 `gpt-4o-mini`
- **temperature / top_p**：越高越“发散”，越低越“稳”
- **max_tokens**：最大输出长度（太小会写不完，太大可能更慢/更贵）
- **presence_penalty / frequency_penalty**（OpenAI 系列常见）或 **top_k**（部分 provider）
- **stop（逗号分隔）**：遇到这些停止词就提前停
- **timeout_seconds**：超时（默认 90 秒，最大 1800）
- **extra（JSON）**：高级参数（可以点“一键格式化”；**不要在这里填写 API Key**）

按钮：
- **测试连接**：用当前配置尝试调用后端测试接口（成功后向导会把“配置模型并测试连接”判定为完成）
- **保存**：保存当前预设参数

#### B) API 配置库（Profile）+ API Key（决定“能不能调用”）

这块的逻辑是：先创建/选择一个“配置”，再把 Key 存进去。

你会看到的控件：
- 下拉框：**选择配置**（可能显示：`配置名 · provider/model`）
- 输入框：**新建配置名**
- 按钮：
  - **保存为新配置**
  - **更新当前配置**
  - **删除当前配置**

然后是 API Key 区：
- 显示状态：
  - `已保存：sk-***`（只显示 masked，不会回显明文）
  - 或 `未保存：请在下方输入并保存`
- 输入框：**输入新 Key（不会回显已保存的 Key）**
- 按钮：
  - **保存 Key**
  - **清除 Key**

最常见的“正确姿势”：
1) 先“保存为新配置”（或选择已有配置）  
2) 粘贴 Key → “保存 Key”  
3) 回到上面点“测试连接”  
4) 最后点“保存”（保存 preset 参数）

### 6.7 提示词工作室（/projects/:projectId/prompt-studio）

用途：高级模式——管理“提示词预设（Preset）/提示块（Block）”，并生成“后端渲染预览”。

你会看到的典型结构（从左到右）：
- **预设列表**：选择一个预设
- **编辑区**：编辑该预设下的提示块（system/user、模板、触发条件等）
- **预览区**：请求后端渲染，看到最终 Prompt（不一定会调用 LLM）

常见按钮/操作（不同版本文案可能略有不同）：
- 新建/删除预设
- 一键启用推荐预设（大纲/章节）
- 导入/导出预设（JSON）
- 预览某个任务（例如：outline_generate / chapter_generate）

新手建议：
- 如果你只是想“更像你想要的文风”，优先在“项目设置/风格/角色卡”里写清楚；提示词工作室属于进阶。

### 6.8 风格（/projects/:projectId/styles）

用途：管理“写作风格提示词（prompt_content）”，并可设为项目默认风格。

你会看到：
- 预设风格列表（预设一般不可删除）
- 你的自定义风格列表
- 按钮：**新建风格**（输入名称/描述/prompt_content）
- 每条风格旁按钮：**设为默认 / 编辑 / 删除（仅自定义）**

风格会在生成时被注入（如果你在 AI 生成里选择了该风格，或设置了项目默认风格）。

### 6.9 世界书（/projects/:projectId/worldbook）

用途：把世界观/规则/设定做成“条目”，生成时按关键词触发注入（或常驻注入），提高一致性。

你会看到的常用控件：
- 顶部按钮：**刷新 / 导出 JSON / 导入 JSON / 新建条目**
- 搜索框：搜索标题/关键词（示例占位符：`dragon`）
- 排序下拉：按 `updated_at / priority / enabled` 等排序
- 开关：**批量模式**

#### A) 新建/编辑条目（编辑抽屉）

常见字段（按页面顺序）：
- **标题**（必填）
- **启用（enabled）**：不开启就不会触发注入
- **常驻（constant）**：开启后“始终注入”，不需要关键词（注意占用预算）
- **关键词（每行一个）**：只对“非 constant 条目”生效
- **字符上限（char_limit）**：限制该条注入长度（防止提示词过长）
- **优先级（priority）**：影响触发条目的排序与注入顺序
- **内容（Markdown）**：条目的正文

常见按钮：
- **保存 / 删除 / 关闭**
- “丢弃未保存修改？”提示：如果你改了内容没保存就想关抽屉，会弹二次确认

#### B) 预览触发（非常推荐用）

用途：你输入一段 `query_text`，系统告诉你：
- 触发了哪些条目（触发列表）
- 最终拼接出来的 `text_md`（真正会注入给模型的文本）

常见选项：
- **包含常驻（constant）**
- **拼接字符上限**
- 按钮：**预览**

#### C) 批量模式（谨慎）

批量模式打开后：
- 点击条目 = 选择/取消选择
- 可 **全选 / 清空选择**
- 可批量：启用/停用、改优先级、改 char_limit、批量删除、复制并编辑

> 批量删除/overwrite 导入是不可恢复操作，务必确认。

### 6.10 大纲（/projects/:projectId/outline）

用途：编辑当前大纲；AI 生成大纲；从大纲创建章节骨架；管理多个大纲版本。

你会看到：
- “当前大纲”下拉框（可切换大纲版本）
- 按钮：新建 / 重命名 / 删除
- 按钮：**AI 生成大纲**（打开生成弹窗）
- 按钮：**从大纲创建章节骨架**（需要章节结构）
- 编辑器：Markdown 大纲正文
- 按钮：保存大纲

AI 生成大纲（弹窗）里常见选项：
- 章数（chapter_count）
- 语气（tone）
- 节奏（pacing）
- 是否包含世界观/角色信息
- 是否开启“流式生成”（会显示进度条与 raw 输出预览）

生成后你会看到“生成结果预览”，可以选择：
- **覆盖当前大纲并保存**：危险，会替换当前内容
- **保存为新大纲并切换**：更安全，推荐

从大纲创建章节骨架会发生什么：
- 根据大纲解析出的章节列表，批量创建章节（章号/标题/要点）
- 如果检测到已有章节，会提示你“覆盖创建”（会删除原章节，危险）

### 6.11 写作（/projects/:projectId/writing）

用途：管理章节；编辑正文；AI 生成/批量生成；章节分析；记忆注入与工具面板。

页面结构（常见）：
1) 顶部工具条：
   - 当前大纲选择
   - “新增章节”
   - 生成：AI 生成 / 批量生成 / 生成记录
   - 工具：伏笔面板 / 表格面板 / 上下文预览 / 记忆更新 / 任务中心
2) 章节列表（移动端按钮打开；桌面端可能在侧边）
3) 编辑区：标题、状态、要点、正文（Markdown）、摘要
4) 顶部右侧按钮（选中章节后）：
   - **分析**（章节分析弹窗）
   - **标注回溯**（跳转到“章节标注回溯”页）
   - **删除**
   - **保存**

章节状态怎么用（很重要）：
- `planned`：只有计划，还没写
- `drafting`：正在写/草稿
- `done`：写完定稿（向导判定“完成章节”用它；记忆更新也通常要求它）

AI 生成（从工具条点“AI 生成”打开抽屉）：
- 你会看到：本次使用的 provider/model
- 常见选项（简化理解）：
  - “指令（instruction）”：你希望这一章怎么写/怎么改
  - “替换/追加”：替换=重写本章正文；追加=在正文后续写
  - “世界书注入/记忆注入”：开启后，会先检索世界书/记忆并注入提示词（更一致但更复杂）
  - “包含上下文”：是否把世界观/风格/约束/大纲/上一章等带进来

生成记录（工具条“生成记录”）：
- 可按时间查看每次生成
- 可下载“排障包”（注意：可能包含章节内容等隐私，分享前自查）
- 可按 `request_id` 串联一次生成流水线

章节分析（点“分析”）：
你会看到：
- 分析重点（可选）
- 按钮：
  - 开始分析/重新分析
  - 保存到记忆库（写入长期记忆，不改正文）
  - 复制 run_id
- 结果区：摘要、钩子、伏笔、节奏点、修改建议、总体备注
- “按建议重写并应用”：会把重写结果覆盖到编辑器正文（不会自动保存，记得保存）

### 6.12 预览（/projects/:projectId/preview）

用途：像读者一样通读章节内容（支持左右键切换章节）。

你会看到：
- 章节列表（可隐藏/移动端抽屉）
- 上一章/下一章按钮
- 当前章节内容（渲染 Markdown）
- 按钮：阅读标注 / 编辑

提示：
- 若你勾选“仅定稿”，只会显示状态为 done 的章节。

### 6.13 阅读标注（/projects/:projectId/reader）

用途：阅读章节时，同时查看“该章相关记忆预览”（story_memory/伏笔/结构化记忆等）。

你会看到：
- 章节列表 + 正文阅读
- 一个“记忆”侧栏/抽屉（移动端可能是按钮打开）
- 记忆侧栏会显示：各模块是否启用、命中数量、注入文本等

常见用法：
- 用它检查“记忆注入是否真的带进来了”
- 发现某段信息缺失 → 回到写作页 → 用世界书/记忆更新补齐

### 6.14 导出（/projects/:projectId/export）

用途：下载整本书为 Markdown 文件。

选项：
- 包含：设定 / 角色卡 / 大纲
- 章节范围：全部章节 / 仅定稿章节（done）

点 **“导出 Markdown”** 后：
- 浏览器会下载一个 `.md` 文件
- 如果浏览器拦截下载，请允许该站点下载

---

## 7. 高级功能（你问的重点：世界书 / RAG / 术语表 / 图谱 / 分形 / 结构化记忆 / 任务中心）

> 你说的“那些高级功能怎么触发/怎么用/点了会怎么样”，核心都绕不开一句话：  
> **写作时，系统会在“发送给 AI 的提示词”里额外塞一些“记忆/设定片段”**，让 AI 不容易忘设定、写错人物关系、前后矛盾。  
> 这一章就是手把手教你：这些“额外塞进去的东西”从哪来、怎么开、怎么查、怎么排错。

如果你在左侧看不到 **知识库（RAG）/术语表/图谱/分形/结构化记忆/任务中心**：
1) 先在左侧“项目切换器”选中一个项目（没选项目不会显示项目内页面）  
2) 打开 **“显示高级调试”** 开关  
3) 点一下“高级调试”标题把折叠展开（默认会折叠起来）

---

### 7.0 先把话说明白：这些“高级模块”到底是什么（说人话版）

把它们想成你写作时旁边的 6 本小册子：

1) **世界书（WorldBook）** = “设定卡片/词典条目”  
   - 你手动写：某个名词/人物/地点/规则是什么。  
   - 生成时：只要你写的文本里出现关键词，它就会被“自动翻出来塞给 AI”。  

2) **结构化记忆（Structured Memory）** = “把故事拆成表格”  
   - 系统把内容整理成：实体（人物/地点）/关系/事件/伏笔/证据。  
   - 生成时：会把这些表格的摘要塞给 AI，让它“记住已经发生过什么、谁和谁什么关系”。  

3) **向量 RAG（Vector RAG / 知识库）** = “项目内搜索引擎”  
   - 它会把你项目里的文本切成一小段一小段，做成可检索的索引。  
   - 生成前先搜“跟你这次要写的内容最相关的几段”，把那几段原文塞给 AI。  

4) **图谱（Graph）** = “人物关系网（从结构化记忆里来）”  
   - 你输入一个名字/片段，它会找出相关实体，并把 1 跳关系（A 和 B 的关系）组织成一段注入文本。  

5) **分形（Fractal）** = “整本书高层摘要”  
   - 用于更长跨度的一致性（例如：主线目标、阶段性转折、主角长期动机）。  

6) **任务中心（Task Center）** = “记忆更新的工单/队列/日志”  
   - 你点了记忆更新、应用、回滚……都在这里看状态与错误信息。

最重要的两把“放大镜”（用来确认到底有没有生效）：

- **写作页 → 工具 → 上下文预览**：看“这次生成，会塞哪些记忆进去”以及原因（最推荐）。  
- **写作页 → AI 生成抽屉底部 → 预检/审查**：看“最终要发给 AI 的提示词长什么样”（不调用模型）。

---

### 7.1 最关键：写作时到底怎么“触发”这些高级模块？

你妹妹只需要记住下面这套固定动作（像点外卖一样照着点）：

#### 第 1 步：打开“记忆注入”总开关（否则后面全没用）

入口：**写作页** → 顶部工具条点 **“AI 生成”** → 抽屉里找到 **“记忆注入”** 卡片  

你会看到一个开关，文字可能叫：**“世界书注入”**（注意：名字叫世界书，但它其实是“总开关”）。  

- 关着：上下文预览会提示“世界书注入已关闭”，RAG/图谱/分形等都不会注入。  
- 打开：才会去检索世界书/记忆，并把结果塞进提示词。

#### 第 2 步：决定“这次检索用什么关键词”

在“记忆注入”里有一行：**“记忆查询关键词（可选）”**  

- **留空**：系统会自动用“你这次的指令 + 章节计划”来当检索关键词（新手推荐）。  
- **自己填**：当你发现它老是检索不到你想要的东西，就手动填关键名词  
  - 例：你要写“龙骑士第一次见导师”，就填：`龙骑士 导师 试炼`  

#### 第 3 步：勾选你要启用的模块（就像勾插件）

“注入模块”里最先看到的是：
- **世界书（worldbook）**：是否把世界书触发结果塞进去

展开 **“更多模块（高级）”** 后，你会看到（从易到难解释）：

- **剧情记忆（story_memory）**：像“重要剧情摘抄”，通常来自“保存到记忆库/导入”。  
- **结构化记忆（structured）**：把实体/关系/事件/伏笔组织成摘要塞进去（前提：你已经做过记忆更新并应用）。  
- **向量 RAG（vector_rag）**：用搜索引擎找相关原文片段塞进去（前提：配置向量化并重建索引）。  
- **关系图（graph）**：从结构化记忆里提取关系网塞进去（前提：结构化记忆里有数据）。  
- **Fractal（fractal）**：更高层摘要塞进去（前提：分形已构建）。  
- （更偏排障）**语义历史（semantic_history）**、**未回收伏笔（foreshadow_open_loops）**：一般新手不用，想排“伏笔忘了”再开。

#### 第 4 步：用“上下文预览”确认真的生效（强烈推荐）

入口：写作页顶部工具条 → “工具” → **上下文预览**  

你会看到：
- 顶部按钮：**下载预览 bundle / 刷新 / 关闭**  
- 一个开关：还是那个 **“世界书注入”**（它关了就啥也不显示）  
- 一个大区域：会列出 **Pack sections**（每个模块 enabled/disabled + 原因）  
- 你还能点 **“同步生成设置”**：让预览参数跟你 AI 生成抽屉里设置完全一致（非常好用）

如果你在这里看到：
- `worldbook enabled` 且有 `text_md`：世界书确实注入了  
- `vector_rag disabled_reason: ...`：RAG 没配好/没重建/没关键词  
- `structured empty`：结构化记忆还没生成或没应用

这就是排障最快的“真相”。

你在“上下文预览”里会看到的主要区域（从上到下，按人话解释）：

1) **预览参数（preview API）**（最上面那块）：
   - **同步生成设置**：把预览参数强制对齐到你当前 AI 生成抽屉的设置（新手最该点的按钮）  
   - `memory_query_text`：预览用的检索关键词（一般留空=自动；不准再手动填）  
   - `modules（section_enabled）`：你想预览哪些模块（勾选/取消勾选只影响“预览”）  
   - `预算覆盖（budget_overrides）`：只为了测试“字数上限”对结果的影响（**仅影响预览**）  

2) **Pack sections**：一个清单，告诉你每个模块现在是 `enabled` 还是 `disabled`，以及原因（`disabled_reason`）。  
   - 你想找“为什么没生效”，优先看这里的原因。  

3) **Memory text_md**：最重要的“正文区”。  
   - 你能直接看到 `worldbook / vector_rag / graph / fractal ...` 各自最终会注入的那段文本。  
   - 你想确认“AI 到底看到了什么”，就看这里。  

4) **原始数据（JSON）/ Context Optimizer / Tables / Items**（更偏排障/高级）：
   - **原始数据（JSON）**：复制 pack JSON、复制预览设置 JSON（适合发给开发者排查）  
   - **Context Optimizer**：优化前后对比（很高级，不懂可以忽略）  
   - **Tables（project_tables）**：项目表格的注入预览（一般新手可忽略）  
   - **Items（semantic_history / foreshadow_open_loops）**：这两个模块的条目列表（没开模块时通常为空）  

5) **下载预览 bundle**：会导出一个 `context_preview_bundle_...json`（排障材料）。  
   - 按设计不应包含 API Key，但包含项目内容，分享前仍建议自己快速检查一遍。

#### 第 5 步（可选）：用“预检/审查”看看最终提示词（更底层、更明确）

入口：写作页 → AI 生成抽屉最底部按钮 **“预检/审查”**  

它会：
- **不调用模型**  
- 直接把“最终 system prompt / user prompt / memory_pack 等”展示出来  
- 你可以用它确认：世界书/RAG/图谱/分形到底有没有被拼进提示词

你打开“预检/审查”后会看到什么（对照表，看完就不慌）：

- **顶部按钮**：`刷新预检 / 关闭`；如果你之前启用了“覆盖提示词”，还会出现 `回退默认`  
- **预检参数**：`替换/追加`（对应你这次生成模式）、`macro_seed`（随机种子）、`task`（本次生成任务类型）  
- **最终 Prompt（只读）**：
  - `system`：规则/设定/注入块通常主要在这里（最重要）  
  - `user`：你本次的指令、章节计划等通常在这里  
  - `messages`：某些接口会用对话消息格式（看不懂可忽略）  
  - `render_log`：模板渲染日志（排障用，看不懂也没关系）  
- **注入段落（memory_pack）**：这里最直观：展开 `worldbook / vector_rag / graph / fractal ...` 就能看到每段 `text_md`（这就是注入给 AI 的那段文字）  
- **覆盖文本（可选）**（非常高级，新手不建议乱用）：
  - 你可以改 `system（覆盖）/ user（覆盖）`，再点 **“使用覆盖文本执行”**  
  - 一旦用了覆盖，之后“生成/追加生成”也会继续沿用覆盖，直到你点 **“回退默认”**

---

### 7.2 世界书（WorldBook）——条目到底怎么触发？（最常见疑问）

入口：左侧导航 → **世界书**（/projects/:projectId/worldbook）

#### 7.2.1 世界书条目 = 一张“设定卡”

你可以把任何容易忘的东西做成条目，例如：
- 人物设定：年龄、口头禅、恐惧、与谁的关系
- 地点规则：进城要交税、夜晚不能点火
- 术语解释：某个组织、某件神器的规则

#### 7.2.2 你看到的每个字段，是什么意思（按编辑抽屉顺序）

打开方式：世界书页右上角点 **“新建条目”** 或点击已有条目  

字段解释：
- **标题**：条目名字（必填）  
- **启用（enabled）**：不开启就永远不会触发（等于“禁用这张卡”）  
- **常驻（constant）**：开启后“无论你写什么都注入”（强力但占预算，建议只留 1～5 条最核心规则）  
- **排除递归（exclude_recursion）**：  
  - 人话：**这张卡“只看你输入的原文”，不看其他卡片内容**  
  - 什么时候开：你不想它被别的条目“连锁触发”时  
- **阻止递归（prevent_recursion）**：  
  - 人话：**这张卡的内容不会拿去触发别的卡**  
  - 什么时候开：这张卡内容很长、到处都是关键词，容易引发“连锁触发爆炸”时  
- **关键词（每行一个）**：触发用的词（只对“非 constant 条目”生效）  
  - 例：`dragon` / `龙` / `巨龙` / `黑龙`  
- **字符上限（char_limit）**：这条注入最多允许多少字符（太长会被截断）  
- **优先级（priority）**：注入排序用  
  - `must` 必须保留  
  - `important` 重要  
  - `optional` 可选  
  - `drop_first` 优先丢弃（当提示词太长时它最先被牺牲）  
- **内容（Markdown）**：真正要塞给 AI 的文本（建议写得像“百科词条”：简短、明确、有规则）

#### 7.2.3 “预览触发”怎么用（建议每次改完都点一下）

世界书页右侧有 **“预览触发”** 面板（也可能在编辑抽屉里有同款）。  

你要做的动作：

1) 在 `query_text` 里粘贴一段“你打算让 AI 看到的文字”  
   - 最常用：粘贴你本章要点/指令里会出现的关键词  
2) 点按钮 **“预览”**  
3) 看两个输出：
   - **触发条目**：会列出触发了哪些条目，并显示 `reason`（为什么触发）  
   - **text_md（最终注入文本）**：这就是最终会被塞进提示词的文本（带 `<WORLD_BOOK>...</WORLD_BOOK>` 包裹）

面板里的开关含义：
- **包含常驻（constant）**：把常驻条目也算进来一起预览  
- **允许递归**：允许“连锁触发”  
  - 举例：如果条目 A 的内容里写了“龙”，而条目 B 的关键词是“龙”，开递归后，A 触发会导致 B 也触发  
- **拼接字符上限**：限制最终 text_md 总长度（超了会出现 **“已截断”**）

注意：写作时只要你在“记忆注入”里勾选了世界书（worldbook），系统默认会 **包含常驻** 且 **允许递归**；预览面板这些开关主要是为了让你对比“开/关递归时会触发哪些条目”。

#### 7.2.4 常见“为什么没触发”的原因（按出现概率排序）

1) 条目没开 **启用（enabled）**  
2) 条目是非 constant，但 **关键词为空**  
3) `query_text` 里压根没出现关键词（最常见）  
4) 你改了条目但没点 **保存**（预览只基于已保存内容）  
5) 被截断了：你设置的 char_limit 太小，导致真正想要的那段被砍掉  
6) 连锁触发被你阻止了：你开了 “排除递归 / 阻止递归”，导致链式触发不再发生（这是正常现象）

#### 7.2.5 批量模式（bulk mode）：一次改很多条（避免手点到手酸）

你会在世界书页左侧“条目列表”上方看到一个开关：**批量模式**。  
打开后：
- **点条目** 不会进入编辑，而是“选择/取消选择”  
- 页面会显示：**已选 N 条**（如果你开了分页/过滤，也可能提示“含未显示”）

你会用到的按钮（按出现顺序解释）：
- **全选**：选中“当前过滤结果”里的所有条目（不是全库，是你现在搜索/过滤出来的这批）  
- **清空选择**：把已选清空  
- **批量启用 / 批量停用**：把选中的条目统一打开/关闭（会二次确认）  
- **复制并编辑**：**只有当你“只选了 1 条”时才可用**；它会复制出一条新条目并直接打开编辑抽屉（最适合做“同模板不同人物/地点”的条目）  
- **批量删除**：危险操作，删除后无法恢复（会二次确认）  
- **优先级（priority）/ 字符上限（char_limit） + 应用**：你先在下拉/输入框里选好值，再点“应用”，才会真正写入到选中的条目上

小提示：如果你开着批量模式又想编辑某条，先把批量模式关掉再点条目。

#### 7.2.6 导入/导出 JSON（备份/迁移/换电脑）

**导出 JSON**（建议养成习惯）：  
1) 世界书页右上角点 **“导出 JSON”**  
2) 浏览器会下载一个 `.json` 文件（文件名通常含 projectId 和时间戳）  
3) 这是你的“世界书备份”；大改之前先导出一次，最稳妥

**导入 JSON**（强烈建议先 dry_run 预演）：  
1) 世界书页右上角点 **“导入 JSON”**（会打开一个抽屉）  
2) 选择导入模式（mode）：
   - `merge`：合并/更新（推荐，安全）  
   - `overwrite`：覆盖导入（会先删除你当前所有条目再导入，非常危险）  
3) 选择文件（导入文件必须是导出时的 schema；否则会提示解析失败）  
4) 先点 **“dry_run 预览”**：只做演算，不写入  
5) 看“导入报告”里这些数字：
   - `created` 新建了多少条  
   - `updated` 更新了多少条（按 **标题 title** 匹配）  
   - `deleted` 删除了多少条（overwrite 才会大）  
   - `skipped` 跳过了多少条（常见原因：冲突）  
   - `conflicts` 冲突列表（例如：同标题在你当前项目里出现多条）  
   - `actions` 每条做了什么（可展开查看）  
6) 确认没问题再点 **“应用导入”**  
7) 如果你选了 `overwrite`，系统会再弹一次“确认覆盖导入？”（别手滑）

#### 7.2.7 搜索 / 排序（找条目用，不影响触发）

世界书左侧有：
- **搜索（标题 / 关键词）**：输入名词就能搜到条目  
- **排序**：按 `updated_at / priority / enabled` 等排序  

提示：列表搜索支持“拼音/首字母”的模糊匹配（方便你找条目）；但这只是“找条目”，不等于触发规则变了。

#### 7.2.8 高级关键词写法（看不懂也没关系，默认不用）

绝大多数情况下，你只需要写普通关键词（每行一个）即可：例如 `龙`、`dragon`。  
如果你在网上/别人的项目里看到这种写法，意思如下（有些需要后端开关才生效）：

- `word:xxx`：英文/数字的“整词匹配”（避免 `he` 匹配到 `the` 这种误触发）  
- `alias: a | b | c` / `aliases: a b c`：别名列表（是否生效取决于后端是否开启 worldbook 的 alias 匹配）  
- `re:...` / `regex:...`：正则匹配（一般默认关闭；且需要后端 allowlist 才允许生效，防止安全问题）  
- 拼音匹配：一般默认关闭；开启后，可能支持用 `long` 去匹配 `龙` 这类（具体以你的后端配置为准）

---

### 7.3 知识库（RAG）（/projects/:projectId/rag）——“项目内搜索引擎”怎么用？

入口：左侧导航 → 打开 **“显示高级调试”** → **知识库（RAG）**

#### 7.3.1 先说门槛：RAG 不配置就一定用不了

RAG 需要“向量化（Embedding）”配置。你不需要懂原理，只要记住：**没配就会显示 disabled_reason**。

最简单配置路径（UI 里有按钮）：

1) 进入 RAG 页面右上角点 **“模型配置（Vector RAG）”**  
2) 找到 **“Embedding（向量化）配置”**（通常是折叠的，点开）  
3) 最懒人做法（适合新手）：先不折腾环境变量，直接在项目里填：
   - provider：`openai_compatible`（或你实际用的提供方）
   - base_url：如果你用官方 OpenAI，可留空；如果你用网关/第三方，就填它给你的 base_url
   - model：填你要用的 embedding 模型名
   - API Key：粘贴 Key（只显示 masked）
4) 点 **“保存 RAG 配置”**
5) 回到 RAG 页，点 **“刷新状态”** 看 `enabled:true/false`

#### 7.3.2 你会看到哪些区块（按页面从上到下）

**A. 顶部按钮区（最常用）**
- **刷新状态**：只查看，不改数据  
- **触发入库（Ingest）**：把当前项目内容切块并写入索引（可能耗时）  
- **重建索引（Rebuild）**：重新构建索引（更耗时，但最能解决“索引过期/命中不稳定”）  

> 小白判断要不要点 Rebuild：看顶部提示的 `索引过期（dirty）`  
> - dirty = true：建议点 Rebuild  
> - dirty = false：一般不用点

**B. 状态（Status）**
- 会显示：是否启用、禁用原因、是否混合检索、rerank 情况等  
- 还可以展开 **“原始 status 结果（raw）”** 看更详细信息（不用也行）

**C. 知识库（KB）**

你可以把“资料”分到多个 KB（比如：世界观 KB / 角色 KB / 设定 KB）。  

你会看到：
- **刷新 KB**：重新拉取 KB 列表  
- **保存排序**：你拖拽 KB 改了顺序后必须点它才会生效  
- 每个 KB 一张卡片，常见控件：
  - “选择 KB”：决定本次 Query 用哪些 KB（不选 = 默认用已启用的 KB）
  - “启用”：允许它作为默认 KB 被使用
  - “权重（weight）”：数值越大，这个 KB 越容易被选中（像音量旋钮）
  - “名称”：给 KB 起名
  - “保存”：改完名称/启用/权重后要点保存
  - “删除”：**只有禁用且不是 default 才能删**（避免误删）
- 底部可以 **创建 KB**（输入名称 → 点“创建 KB”）

重要提示（避免你以为“写作不生效”）：
- 你在本页看到的“当前选择（selected_kb_ids）”会影响你在 **本页** 点的 ingest / rebuild / query。  
- 但写作时的 `vector_rag` 注入目前更偏向使用 **default KB**（新手先把 default 用明白就够了）。  
- 所以：**不建议把 default KB 禁用**；如果你折腾了多 KB，记得让 default 也重建过。

**D. 查询来源（Sources）**
你可以勾选：
- 世界书（worldbook）
- 大纲（outline）
- 章节（chapter）

> 注意：这里的 sources 主要用于“这页的调试查询”。  
> 写作时真正的注入检索，通常还会综合更多来源（你以写作页“上下文预览”为准）。

**E. 查询（Query）**
- 输入框：`query_text`
- 按钮：**查询 / 复制注入文本 / 复制排障信息**

查询后你会看到：
- `注入预览（prompt_block.text_md）`：这就是会塞给 AI 的那段文本  
- `final.chunks`：系统最终选中的原文片段（按 source/chapter 分组），你能看到它到底引用了哪几段

**F. 高级调试（Advanced Debug）**（新手可以先不碰）
里面主要有两块：
- **重排（Rerank）**：对候选结果二次排序，命中可能更准，但可能更慢/更贵  
- **Super sort**：只影响这页的调试 Query，用来强行偏向某个来源（一般不需要）

#### 7.3.3 新手推荐的“最小可用”操作流程

1) 先把世界书/大纲/章节写一点内容（空项目检索不到东西是正常的）  
2) 去 Prompts 页把 Embedding 配好（见 7.3.1）  
3) 回 RAG 页点 **重建索引（Rebuild）**  
4) 在 Query 输入框里打：`dragon`（或你项目里的关键词）点 **查询**  
5) 展开 **注入预览** 看有没有文本  
6) 写作页开启“世界书注入”（总开关）+ 勾选 `vector_rag`，再生成并用“上下文预览”确认

#### 7.3.4 “触发入库（Ingest）” vs “重建索引（Rebuild）”怎么选？

你可以把它们理解成：
- **Ingest = 把新内容补进索引**（适合你刚改了世界书/章节，想让索引“跟上”）  
- **Rebuild = 把索引整套推倒重做**（适合 dirty=true、命中很怪、换了 embedding 配置之后）  

点完之后你会看到什么：
- 页面通常会自动展开 **“高级调试”**  
- 里面会出现：**入库结果（Ingest result）** / **重建结果（Rebuild result）** 的 raw JSON  
- 如果失败：记得抄 **request_id**（排障最有用）

#### 7.3.5 “高级调试”里的重排（Rerank）怎么用（按钮级）

入口：RAG 页往下滚 → 展开 **高级调试** → 找到 **重排（Rerank）** 卡片  

你会看到：
- 按钮 **刷新配置**：重新读取当前项目的 rerank 生效值（只看不改）  
- 开关 **启用重排（rerank）**  
- 下拉 **方法（rerank method）**：`auto / rapidfuzz_token_set_ratio / token_overlap`  
- 数字框 **Top K（rerank top_k）**：候选数量（越大越慢，但可能更准）  
- 数字框 **hybrid_alpha（0~1）**：页面会提示“仅影响 Query 调试请求”  
- 按钮 **应用重排配置**：点了才会真正保存到项目设置（会影响写作时的检索/注入）

新手怎么用才不容易踩坑：
1) 先保持默认（别动）  
2) 当你发现 Query 明显“找到了但顺序很怪”，再尝试打开 rerank 并把 top_k 调到 30~50  
3) 觉得变差了：关掉 rerank 再点“应用重排配置”即可回退

#### 7.3.6 KB 的“选择 / 启用 / 权重 / 排序”分别影响什么？

- **选择（selected_kb_ids）**：你勾选了哪些 KB，RAG 页本次 Query/入库/重建就用哪些；不勾选=默认用“启用”的 KB  
- **启用（enabled）**：决定它能不能作为默认 KB 被使用；也会影响你能否删除（一般要求先禁用再删）  
- **权重（weight）**：你可以把它当“偏好程度”，越大越容易从这个 KB 取到内容（想临时弱化某个 KB 就调小）  
- **排序（order）**：多 KB 同时命中时的“优先顺序/合并时参考顺序”（你拖拽完要点“保存排序”才生效）

#### 7.3.7 RAG 没效果时的快速自检（从最常见到最少见）

1) 写作页 **“世界书注入”总开关** 是不是关着？（关着 = 不注入）  
2) 你在 AI 生成里勾选了 `vector_rag` 吗？（没勾 = 不注入）  
3) RAG 页状态是不是 `enabled:false` + `disabled_reason: ...`？（先去 Prompts 配 Embedding）  
4) 顶部是不是提示 `dirty:true`？（点 Rebuild）  
5) Query 的 `query_text` 是不是太空/太泛？（填具体名词；或在写作里填“记忆查询关键词”）  
6) 你选的 KB 是否都被你禁用了？或权重被你调得很极端？（回到 KB 面板检查）

---

### 7.4 术语表（Glossary）（/projects/:projectId/glossary）——“别名/同义词词典”

入口：左侧导航 → 显示高级调试 → **术语表（Glossary）**

它是干什么的（人话）：
- 你给某个术语写一堆别称/缩写/昵称  
- 当你在查询里写了“别名”，系统可以把“正式名”也加进查询（需要后端开关）

页面上你会看到的按钮与区块：
- 顶部按钮：**刷新 / 重建（抽取）/ 导出 JSON**  
- 查询框：输入术语或别名 → 点“查询”  
- 新增术语：输入 `term` + `aliases`（逗号分隔）→ 点“创建”  
- 术语列表：每条都有
  - 启用开关（启用/禁用）
  - 编辑/保存/取消
  - 删除（不可撤销）
  - 引用来源（章/导入文档的跳转链接）

“重建（抽取）”按钮是什么：
- 它会从 **章节正文 + 导入文本** 里自动抽取候选术语  
- 风险：会抽到一些没用的词，所以你需要手动禁用/删除不想要的

很重要的一句话（很多人卡在这里）：
- 页面会提示：`GLOSSARY_QUERY_EXPAND_ENABLED=true` 默认关闭  
- 意思是：**你就算在这里加了别名，如果不打开这个后端开关，世界书/图谱的“别名增强”也不会生效**  

怎么打开（不改代码，只改配置）：
1) 打开 `backend/.env`（或 docker 的环境变量文件）  
2) 加一行：`GLOSSARY_QUERY_EXPAND_ENABLED=true`  
3) 重启后端  
4) 回到世界书“预览触发”/图谱“查询”，你会发现别名更容易命中

---

### 7.5 图谱（Graph）（/projects/:projectId/graph）——“人物关系网调试页”

入口：左侧导航 → 显示高级调试 → **图谱**

你在这里能得到什么：
- 你输入一个名字/片段，它会从结构化记忆里找相关实体  
- 展示：命中实体、节点、关系、证据  
- 最关键：**注入预览（prompt_block.text_md）**（这就是 graph 模块会塞进提示词的文本）

页面按钮怎么用（手把手）：
1) 确认右上角开关 **“启用（enabled）”** 打开  
2) 在“查询文本（query_text）”输入：
   - 最推荐：人物名、地点名、组织名（例如：`Alice`）  
3) 点右上角 **“查询”**  
4) 看结果：
   - “结论摘要”：状态、节点数、关系数、证据数  
   - “注入预览”：能复制（按钮叫“复制”）  
   - “节点/关系/证据”：帮助你确认它到底识别了谁、证据来自哪里

为什么可能“查不到任何东西”：
- 你的结构化记忆表还是空的（没做过记忆更新/没应用）  
- 你输入的名字和实体表里的 name/alias 对不上  
- 你把 graph 模块关了（或总开关关了）

---

### 7.6 分形（Fractal）（/projects/:projectId/fractal）——“整本书的高层摘要/长期一致性”

入口：左侧导航 → 显示高级调试 → **分形（Fractal）**

这页最核心就两个按钮：
- **重建（确定性）**：不依赖大模型（更稳、更便宜），适合先用  
- **重建（LLM 摘要）**：会调用模型做摘要（可能更聪明，但更慢/更可能报错/可能产生费用）

你会看到两个预览框：
- 确定性（deterministic）
- LLM 摘要（v2）

如何判断当前到底用哪个：
- “状态与结论”里会写：  
  - 分形记忆是否启用  
  - LLM 摘要是否启用  
  - 结论：当前注入会优先用 v2 还是回退到 deterministic

常见失败原因（你需要看 request_id）：
- 模型没配/Key 不可用（v2 常见）
- 解析失败（v2 输出不是预期结构）
- 生成时间太长（网络/模型拥堵）

---

### 7.7 结构化记忆（/projects/:projectId/structured-memory）——“把故事拆成表格”怎么用？

入口：左侧导航 → 显示高级调试 → **结构化记忆**

#### 7.7.0 先解决“为什么这里是空的”：结构化记忆不是自动来的

很多人第一次打开会发现：实体/关系/事件全是 0。别慌，这通常是正常的。  
**结构化记忆的内容主要来自“记忆更新（Memory Update）→ 应用（Apply）”之后**，不是你写完正文就自动出现。

最推荐的“让它有东西”的做法（新手版）：
1) 去写作页把某一章写完 → 章节状态改成 **done（定稿）** → 点保存  
2) 写作页顶部工具条 → “工具” → 点 **记忆更新（Memory Update）**  
3) 抽屉里按 1/2/3 步走：
   - **步骤 1：一键生成提议（Auto Propose）**（会调用模型，可能花钱/耗时）  
   - **步骤 2：审核（Review）**：你觉得对的就点“接受”，不对的就别接受  
   - **步骤 3：应用（Apply）**：点 **应用已接受项（Apply）** 才会真正写入数据库  
4) 去 **任务中心** 看 change set 状态是否变成“已应用”  
5) 回到结构化记忆页点 **刷新**

常见卡点：如果你点 Apply 报“仅定稿章节可进行记忆更新”，说明你那章还不是 done（或没保存）。

#### 7.7.1 这页能干啥（人话）

它是一个“查表页”：
- 你想查“人物现在叫什么/关系是什么/发生了哪些大事/哪些伏笔还没回收”，来这里搜最快  

它也能做一个危险动作：
- 你选中一些条目，生成“删除/标记回收”的 JSON 指令  
- 再交给 Memory Update 应用（会真正改数据库）

#### 7.7.2 页面上每个东西怎么用（按你看到的顺序）

**A. 顶部按钮**
- **刷新**：重新加载当前表  
- **批量操作（N）**：只有你选中了 N 条才会出现  
- **Memory Update**：打开记忆更新抽屉  
  - 如果按钮是灰的：说明你没有带 `chapterId`（建议从写作页打开，系统会自动带上）

**B. 五个标签页（Tabs）**
- 实体（entities）：人物/地点/物品  
- 关系（relations）：A 与 B 的关系  
- 事件（events）：发生了什么  
- 伏笔（foreshadows）：埋下但未回收的点（可标记 resolved）  
- 证据（evidence）：来自文本的引用片段（用于“你为什么这么认为”的依据）

**C. 查询**
- 输入框：先输入关键词，然后点“应用搜索”（或回车）  
- 开关：**包含已删除（include_deleted）**（一般不用开）

**D. 选择条目（为批量操作做准备）**
- 每行都有选择框  
- 选中后，右上角会出现 **“批量操作（N）”**

#### 7.7.3 “批量操作”抽屉：你点了会发生什么？

打开后它会按 1/2/3 步写得很清楚：

1) **选择条目**：你可以“全选当前页 / 清空选择”  
2) **生成操作**：  
   - 默认会生成“删除操作 JSON”  
   - 如果你在伏笔表，还会多一个“标记已解决 JSON”  
3) **复制并打开 Memory Update**：  
   - 点 **“复制删除操作 JSON”**（复制到剪贴板）  
   - 点 **“打开 Memory Update”**（然后你把 JSON 粘进去并 Apply）

> 重点提醒：生成 JSON ≠ 已经删除。  
> 真正删除发生在你去 Memory Update 点 **Apply** 的那一刻。

---

### 7.8 任务中心（/projects/:projectId/tasks）——我点了记忆更新，它在干嘛？

入口：左侧导航 → 显示高级调试 → **任务中心**

你会看到左右两栏：

**左边：变更集（Change Set）**  
- 你可以理解成“一次记忆更新的改动清单”  
- 状态含义：
  - 未应用（proposed）：只是提议，还没写入数据库  
  - 已应用（applied）：已写入数据库  
  - 已回滚（rolled_back）：写入后又被撤销  
  - 失败（failed）：执行异常（点开看 JSON）

**右边：任务（Task）**  
- 你可以理解成“后台在执行某个步骤的工单”  
- 状态含义：
  - queued：排队中  
  - running：执行中  
  - done：完成  
  - failed：失败（通常会有 error_message）

怎么用它排障（手把手）：
1) 先在写作页点“记忆更新”或“保存到记忆库/应用”之类的动作  
2) 打开任务中心  
3) 看有没有新出现的 Change Set 或 Task  
4) 如果失败：
   - 点那条记录 → 右侧抽屉会打开  
   - 点 **“复制 Debug 信息”**  
   - 把 request_id + 复制出来的 JSON 给维护者（定位最快）

---

### 7.9 导入（/projects/:projectId/import）——把外部 txt/md 变成可用的世界书/记忆

用途（人话）：把你已有的资料（设定文档/旧稿/大纲）扔进去，系统帮你拆段、抽取，最后你确认后写入项目。  

你会看到：
- 上传文件（txt/md）
- 导入记录列表（状态：排队/处理中/完成/失败）
- 详情里会显示：chunk（切块结果）、提案摘要、raw JSON、重试等按钮

新手建议：
- 先导入一小份测试（比如 1～2 页），确认抽取效果，再导入大的。

---

## 8. 管理功能（管理员才看得到）

### 8.1 用户管理（/admin/users）

用途：管理员创建用户、重置密码、启用/禁用用户。

关键点：
- “一次性密码”只在页面显示一次，复制后会隐藏；刷新页面后无法找回（务必当场保存并安全发送）。

---

## 9. 常见问题（FAQ）

### Q1：我打开 `http://localhost:5173` 但页面空白/打不开

按顺序排查：
1) 前端有没有启动？终端是否显示 `Local: http://localhost:5173/`  
2) 后端有没有启动？浏览器打开 `http://localhost:8000/api/health` 看是否返回 OK  
3) 端口是否被占用？（5173/8000 常见）  

### Q2：我点“AI 生成”提示没配置/报错

去 **模型配置** 页确认：
- 已选择/新建配置（Profile）
- 已保存 Key（显示 `已保存：sk-***` 的 masked 形式）
- 已“测试连接”成功

### Q3：我写完了但向导完成度不涨

向导的“完成章节”判定是：章节状态必须是 **done（定稿）**。  
去写作页把章节状态改成 done，并保存。

### Q4：为什么我改了“定稿”章节，它又变回草稿？

这是设计行为：编辑 done 章会提示“草稿污染风险”，系统会把状态回退为 drafting。  
你修改完并确认无误后，可以再手动改回 done 并保存。

### Q5：报错里有 `request_id`，我该怎么用？

把 `request_id` 复制下来，连同：
- 你在什么页面
- 点了什么按钮
- 输入了什么内容
一起发给维护者/开发者，定位会快很多。

---

## 10. 风险提示（非常重要）

1) **不要泄露 API Key**：不要截图发群、不要粘到 extra JSON、不要提交到 git。  
2) **DEV fallback 只能本地用**：部署到公网必须关闭，否则可绕过登录。  
3) **SQLite 单 worker**：后端用 SQLite 时必须 `--workers 1`，否则可能出现 `database is locked`。  
4) **危险操作不可恢复**：删除项目/删除大纲/覆盖创建章节/overwrite 导入世界书，都会永久删除数据。  
5) **生成会消耗成本与时间**：流式生成、JSON 修复可能会额外调用模型。  

---

## 11. 附录：你可能会用到的地址/命令

常用地址：
- 前端：`http://localhost:5173`
- 后端：`http://localhost:8000`
- 健康检查：`http://localhost:8000/api/health`

后端启动（SQLite 单 worker）：
```powershell
cd backend
.\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --workers 1 --port 8000
```

前端启动：
```powershell
cd frontend
npm run dev
```

E2E 测试（可选，开发者用）：
```powershell
cd test
npm test
```
