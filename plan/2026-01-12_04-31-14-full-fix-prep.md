# Plan: 2026-01-12 Full Fix Prep（全面修缮/全面修复准备）

## Background（为什么要做 full fix）
- 上一轮已完成并合入 `origin/test` 的 review backlog（`issues/2026-01-11_22-46-16-review-backlog.csv`）以“审计 + 最小修复 + 补测”为主，`docs/reviews/REV-001..REV-028.md` 中仍包含若干**后续建议/缺口/加固项**，需要在开工前拆成可执行 Issue。
- 本计划的目标是把这些“需要改/需要补测/需要加固/需要清理/需要文档化”的事项固化为新的 Issue CSV（全量 TODO），并给出可追踪的 Review→Issue 映射、风险分级与执行顺序建议。

## Goal
- 产出可执行的 full-fix Issue 列表（见下方 Issue CSV），每条 Issue 可独立一个 commit 完成。
- 将 review 文档中的后续建议转换为：明确范围（Files）、验收标准（Acceptance）与可执行验证方式（Test_Method）。
- 为后续“全面修复执行”提供：按模块分组、风险总览、执行顺序建议与回归策略。

## Scope
- In（仅限 demo 仓库内）：
  - Backend：FastAPI / SQLite / Alembic / services / schemas / RBAC / logging / Dockerfile
  - Frontend：React/Vite/Tailwind 的 Auth/Drawer/Graph/Markdown 等相关组件
  - Test：Playwright E2E/契约/DB schema 回归与后续补测
  - Deploy/Docs：docker-compose、nginx、部署与安全文档
- Out：
  - demo 之外目录（明确禁止）
  - 本次 baseline meta commit 不做任何业务修复/重构（只提交 plan/csv/必要文档）

## Evidence（事实来源）
- 上一轮计划与执行索引：
  - `plan/2026-01-11_22-46-16-review-backlog.md`
  - `issues/2026-01-11_22-46-16-review-backlog.csv`（REV-001..REV-028 全部 DONE）
- Review 证据文档（唯一事实来源）：`docs/reviews/REV-001.md` … `docs/reviews/REV-028.md`
- 本轮启动前 git 检查（2026-01-12）：
  - `git branch --show-current` => `test`
  - `git status --porcelain` => clean
  - `git remote -v` => `origin https://github.com/inliver233/Ai-Novel-Demo.git`

## Assumptions / Dependencies
- Docker 相关 hardening 需要本机或 CI 具备可运行的 Docker 环境（否则只能做受限验收并记录风险）。
- FF-012 属于“先决策后实现”，FF-013 依赖 FF-012 的设计结论。
- FF-022（pgvector 集成测试）依赖可用的 Postgres + pgvector 环境变量；无环境时必须 skip 并记录 `validation_limited`。

## Workstreams（按模块分组）
- **security/backend**：FF-001 FF-002 FF-003 FF-005 FF-006 FF-011 FF-024
- **security/deploy + docs**：FF-007 FF-008 FF-009 FF-010
- **data/perf/backend**：FF-014 FF-018 FF-019 FF-021
- **feature/backend+frontend**：FF-016 FF-020 FF-023
- **test**：FF-004 FF-015 FF-017 FF-022
- **frontend/auth**：FF-025 FF-026

## Issue CSV
- Path: `issues/2026-01-12_04-31-14-full-fix-prep.csv`
- Must share the same timestamp/slug as this plan.

## Feature Map（Review → Full-fix Issues）
- REV-001：session cookie/CORS 安全形态后续加固 → FF-006 FF-007
- REV-002：dev fallback 主要为运维护栏（已落盘）→ FF-007（汇总为 prod checklist）
- REV-003：RBAC 403/404 语义统一建议 → FF-003
- REV-004：schema fail-closed 与输入边界补强建议 → FF-001 FF-002
- REV-005：SQL/template 注入面未发现高风险 → none
- REV-006：Markdown XSS 防回归建议 → FF-004
- REV-007：secrets 脱敏审计建议补测试 → FF-005
- REV-008：SQLite 单 worker 与长事务审计无缺口 → none
- REV-009：Alembic 与 db_schema 基线一致 → none
- REV-010：SQLite→Postgres 搬运脚本审计无缺口 → none
- REV-011：worldbook keyword 子串误命中改进建议 → FF-023
- REV-012：analysis/apply hash 漂移风险与契约建议 → FF-024
- REV-013：annotations fallback 长文误判已修复 → none
- REV-014：pack.logs schema 与 UI 摘要建议 → FF-016
- REV-015：AuthContext dev_fallback 护栏与 refresh 调度优化建议 → FF-025 FF-026
- REV-016：RBAC role 数据约束建议 → FF-011
- REV-017：docker-compose/Dockerfile/nginx 生产化安全加固建议 → FF-007 FF-008 FF-009 FF-010
- REV-018：VectorRAG 可观测性 counts 建议 → FF-021
- REV-019：pgvector array binding 集成测试与性能建议 → FF-022
- REV-020：structured memory 软删 tombstone 与索引优化建议 → FF-012 FF-013 FF-014
- REV-021：MemoryUpdate v1 测试缺口建议 → FF-015
- REV-022：GraphContext prompt_block 预算与性能建议 → FF-018 FF-019
- REV-023：FractalMemory 集成测试与规模 guardrail 建议 → FF-020
- REV-024：Style Profiles 契约与边界补测建议 → FF-017
- REV-025：style/post_edit 审计无缺口 → none
- REV-026：applyRunId race 已通过回归用例补强 → none
- REV-027：Playwright 稳定性建议（当前未观测 flaky）→ none
- REV-028：外部项目标识清理扫描无命中 → none

## Phases（执行顺序建议）
1) **P0（阻塞/安全/上线护栏）**
   - FF-001（fail-closed 基线）
   - FF-007（prod 安全清单）
   - FF-008（prod compose 降暴露面）
2) **P1（稳定性/一致性/关键补测）**
   - FF-002 FF-003 FF-009 FF-010 FF-011 FF-012 FF-013 FF-015 FF-017
3) **P2（性能/体验/非阻塞加固）**
   - FF-004 FF-005 FF-006 FF-014 FF-016 FF-018 FF-019 FF-020 FF-021 FF-022 FF-023 FF-024 FF-025 FF-026

## Tests & Verification
- 单 Issue：以 CSV 的 `Test_Method` 为准（能跑就跑；跑不了需在 Notes 记录受限验收与风险）。
- 批次回归（建议每完成一个 Phase 或所有 issues 完成后）：`pwsh test/run-all.ps1`

## Tools / MCP
- 默认仅使用本地命令：`rg` / `git` / `pwsh` / `python` / `npm`
- MCP 仅必要时使用，且单轮对话最多 2 次（见 `docs/mcp-tools.md`）。

## Risks / Blockers（风险总览）
- High
  - Deploy hardening（FF-008~FF-010）若缺少 Docker 环境将导致验证受限，需要明确记录风险与补跑计划。
  - FF-012 决策未定会阻塞 FF-013 的实现与测试。
- Medium
  - 校验策略收紧（FF-001 FF-002）可能引入兼容性变化，需要用契约/E2E 确认前端与测试用例不回归。
  - DB 迁移与索引（FF-011 FF-014）可能触发 db_schema 基线更新，需要同步更新 `test/contracts/db_schema.json` 并回归通过。
- Low
  - 多数 P2 属于可解释性/性能/体验提升，不影响核心功能路径，但仍需回归覆盖避免引入 flaky。

## Rollback / Recovery
- 每条 Issue 独立 commit：出现问题可按 Issue 回滚单个 commit。
- DB 迁移类 Issue：必须提供可逆的 downgrade（或写明不可逆并提供恢复路径），并在回归前备份测试 DB 基线。

## Checkpoints
- 建议在完成每个 Phase 后做一次 checkpoint（可选）：
  - Phase 1 完成后跑一次 `pwsh test/run-all.ps1`
  - Phase 2 完成后跑一次 `pwsh test/run-all.ps1`
  - 全量完成后再跑一次 `pwsh test/run-all.ps1` 并更新 Regression_Status（另起 meta commit）

## Acceptance Checklist（本计划完成标准）
- [ ] 生成新的 plan 文件与 issues CSV 文件，timestamp/slug 一致
- [ ] 新 CSV 通过 `python .codex/skills/plan/scripts/validate_issues_csv.py`
- [ ] Feature Map 覆盖 REV-001..REV-028 且可追踪到 Issue IDs
- [ ] baseline meta commit 已在 `test` 分支提交并 push 到 `origin/test`

## References
- `AGENTS.md`
- `issues/README.md`
- `docs/testing-policy.md`
- `.codex/prompts/issues_csv_execute.md`
- `docs/reviews/REV-001.md` … `docs/reviews/REV-028.md`
- `docs/mcp-tools.md`

