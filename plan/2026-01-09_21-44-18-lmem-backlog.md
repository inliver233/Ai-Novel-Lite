---
mode: plan
task: "LMEM backlog (Phase 0~7)"
created_at: "2026-01-09T21:44:18"
complexity: complex
---

# Plan: LMEM backlog (Phase 0~7)

## Goal
- 产出可执行的 LMEM backlog：Issue CSV + 闭环执行规范，使后续每条 LMEM issue 都能按 CSV 推进（实现/验收/提交/回归）。

## Scope
- In:
  - 将 LMEM 既有草案迁移为标准 `issues/*.csv`（固定表头，详见 `issues/README.md`）
  - 对齐：`AGENTS.md` / `issues_csv_execute.md` / `docs/mcp-tools.md`
- Out:
  - 不实现任何 LMEM 功能（本计划只做“准备与分解”）

## Assumptions / Dependencies
- 以 `test` 分支作为 AI 提交分支（从 `dev` 创建）。
- 本机具备运行 backend/frontend/test 的基础环境；若不具备，按“受限验收”写入 CSV `Notes`。

## Phases
1. 对齐 Issue CSV 规范与执行闭环文档
2. 迁移 LMEM 草案为标准 Issue CSV（固定表头）
3. 校验 CSV（schema + 非空 + 状态枚举）
4. （可选）按 phase 拆分多个 CSV 批次

## Tests & Verification
- `python .codex/skills/plan/scripts/validate_issues_csv.py issues/2026-01-09_21-44-18-lmem-backlog.csv` -> exit 0
- `git status` -> 只包含与本计划相关的文档/CSV/plan 变更

## Issue CSV
- Path: issues/2026-01-09_21-44-18-lmem-backlog.csv
- Must share the same timestamp/slug as this plan.

## Tools / MCP
- context7:resolve-library-id / context7:query-docs（查官方文档与用法）
- chrome-devtools:*（UI 验证/快照/网络检查）

## Acceptance Checklist
- [ ] `AGENTS.md` 与 Issue CSV 规则一致
- [ ] `issues_csv_execute.md` 与 CSV 表头一致
- [ ] `docs/mcp-tools.md` 与 `codex mcp list --json` 一致
- [ ] `issues/2026-01-09_21-44-18-lmem-backlog.csv` 通过 `validate_issues_csv.py`

## Risks / Blockers
- 旧 LMEM 草案字段与新表头不一致，需要信息压缩到 `Title/Notes`
- 旧字段（priority/phase/area/owner/review_requirements）无法作为独立列存储，需要约定编码

## Rollback / Recovery
- 使用 git 回滚到变更前版本；并保留 `*.bak.*` 文件作为额外备份。

## Checkpoints
- Commit after: docs 对齐 + CSV 生成

## References
- `长期记忆系统完整实现规划.md`
- `issues/README.md`
- `docs/testing-policy.md`
- `test/README.md`
