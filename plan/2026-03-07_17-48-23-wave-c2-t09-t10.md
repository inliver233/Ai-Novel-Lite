---
mode: plan
task: Wave C-2 T09 T10 runtime convergence
created_at: "2026-03-07T17:48:23+08:00"
complexity: complex
status: in_progress
---

## Goal
- Complete `T09 + T10` on top of Wave C-1 by adding a unified runtime/event contract for high-value task flows and closing the frontend consumption/recovery loop without regressing legacy `generation_runs`, `batch_generation`, or Wave B chapter store behavior.
- Land additive batch orchestration controls (`pause / resume / retry_failed / skip_failed`) and richer runtime checkpoints so batch generation becomes recoverable, observable, and production-safe under the existing SQLite single-worker constraint.
- Make TaskCenter and Writing converge on the same project runtime stream with replay-aware reconnect handling, cross-route consistency, and auditable per-run timelines.

## Scope
- In:
  - Add a runtime mapping/projection layer on top of `ProjectTaskEvent` for `worldbook_auto_update` and batch-driven `chapter_generate` steps.
  - Extend batch orchestration persistence/contracts with recovery-safe status, checkpoint, retry/skip/pause metadata, and a linked project runtime task.
  - Emit runtime-aware `ProjectTaskEvent` payloads for batch step start/success/failure/pause/resume/checkpoint/recovery.
  - Add backend runtime API(s) and TaskCenter timeline UI to inspect run/step/attempt/event/checkpoint state.
  - Connect Writing batch UI to the project SSE stream with replay-aware reconnect and fallback polling.
  - Add backend, frontend, and Playwright coverage for replay ordering, batch recovery, and cross-page convergence.
- Out:
  - `T11 / T12 / T13 / T14 / T15`.
  - Replacing legacy chapter generation SSE or deleting legacy `batch_generation` / `generation_runs` routes.
  - Multi-worker SQLite execution or complex billing/运营计费系统.

## Issue Breakdown
1. `MVP-638`: add Wave C-2 plan/issue contract, batch-runtime persistence/linkage, and base runtime helpers/migration.
2. `MVP-639`: extend batch orchestration worker/routes with checkpoint events, pause/resume/retry_failed/skip_failed, and quota guards while preserving legacy contracts.
3. `MVP-640`: add project task runtime/timeline APIs and runtime-focused backend contract tests for replay/order/idempotency.
4. `MVP-641`: connect TaskCenter + Writing to replay-aware SSE consumption, shared convergence logic, and batch/runtime UI state recovery.
5. `MVP-642`: run Wave C-2 regression, update docs/issues, append objective 5.4 writeback, and close out production readiness.

## Verification
- Backend baseline:
  - `cd backend && .\.venv\Scripts\python.exe -m compileall -q app alembic`
  - `cd backend && .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v`
- Frontend baseline:
  - `cd frontend && npm run lint`
  - `cd frontend && npm test`
  - `cd frontend && npm run build`
- DB snapshot after schema change:
  - `pwsh test/scripts/snapshot-db.ps1`
- Mandatory regressions:
  - `cd test && $env:E2E_BACKEND_URL="http://127.0.0.1:51116"; $env:E2E_FRONTEND_URL="http://127.0.0.1:51117"; npx playwright test --workers=1 specs/ui/task-center.spec.ts specs/ui/taskcenter-projecttasks.spec.ts specs/ui/batch-generation.spec.ts specs/ui/taskcenter-projecttasks-sse.spec.ts specs/api/generation-runs.contract.spec.ts specs/api/batch-generation-cancel.contract.spec.ts specs/api/taskcenter-structured-memory.contract.spec.ts specs/api/project-task-events.contract.spec.ts`
- Wave C-2 targeted additions:
  - `cd test && $env:E2E_BACKEND_URL="http://127.0.0.1:51116"; $env:E2E_FRONTEND_URL="http://127.0.0.1:51117"; npx playwright test --workers=1 specs/api/project-task-runtime.contract.spec.ts specs/ui/batch-generation-runtime-sync.spec.ts`

## Acceptance Checklist
- [ ] `worldbook_auto_update` and batch-driven `chapter_generate` steps have a unified runtime representation with run/step/attempt/event/checkpoint semantics.
- [ ] Batch generation supports pause/resume/retry_failed/skip_failed and no longer aborts the whole batch on a single chapter failure.
- [ ] Project runtime APIs can reconstruct timeline/checkpoints/artifacts by run ID without breaking legacy contracts.
- [ ] TaskCenter can inspect runtime timeline; Writing batch UI stays converged after reconnect/route changes.
- [ ] Project SSE replay remains additive and old `generation_runs` / `batch_generation` contracts continue to work.
- [ ] Required backend/frontend/Playwright/DB snapshot commands complete successfully.
- [ ] `5.4大调查详细记录.md` includes an objective Wave C-2 writeback with residual boundaries.

## Risks / Blockers
- Batch orchestration currently runs as a legacy queue worker, so runtime linkage must remain additive and not create double-execution or unsafe side effects.
- Batch pause/resume/retry must preserve chapter-write boundaries: generated content stays in generation records until the user explicitly applies/saves.
- SSE recovery across route changes depends on replay cursor handling; client caching must dedupe events and avoid status rollback.

## Rollback
- Revert by issue commit (`[MVP-638]` ... `[MVP-642]`) if needed.
- The new batch controls and runtime views stay additive; rollback can disable them without removing legacy routes.
- If batch orchestration runtime linkage causes production risk, fall back to the legacy batch worker contract while retaining old `generation_runs` and batch endpoints.

## References
- `AGENTS.md:1`
- `issues/README.md:1`
- `docs/testing-policy.md:1`
- `README.md:1`
- `5.4大调查详细记录.md:1508`
- `5.4大调查详细记录.md:1555`
- `5.4大调查详细记录.md:2078`
- `5.4大调查详细记录.md:2124`
- `backend/app/models/project_task.py:12`
- `backend/app/models/project_task_event.py:12`
- `backend/app/services/project_task_event_service.py:46`
- `backend/app/services/project_task_runtime_service.py:67`
- `backend/app/services/batch_generation_service.py:191`
- `backend/app/api/routes/tasks.py:39`
- `backend/app/api/routes/batch_generation.py:20`
- `frontend/src/hooks/useProjectTaskEvents.ts:33`
- `frontend/src/pages/TaskCenterPage.tsx:284`
- `frontend/src/pages/writing/useBatchGeneration.ts:9`
- `frontend/src/services/chapterStore.ts:93`
