---
mode: plan
task: Wave D final T11 T12 T13 T14 T15 closeout
created_at: "2026-03-07T20:54:42+08:00"
complexity: complex
---

﻿## Goal
- Complete the final Wave D (`T11`~`T15`) on top of stable Wave C-2 by hardening LLM configuration contracts, prompt resource governance, production env/security boundaries, delivery gate layering, and reproducible performance baselines without regressing legacy generation/batch/runtime compatibility.
- Keep compatibility-first behavior for existing `generation_runs`, legacy generation SSE, `batch_generation`, Wave B `chapterStore`, and Wave C project-task runtime/SSE consumers while moving new governance to audit-first / enforce-ready.
- Finish the operational closeout loop: repo-local plan + issue CSV, executable guards/runners, performance evidence, full regression, objective `5.4大调查详细记录.md` writeback, and per-issue commit/push on `test`.

## Scope
- In:
  - `T11`: build a single source of truth for provider/model/capability/pricing metadata, add audit/enforce-ready validation, and wire LLM profile/preset/task/runtime consumers to the registry with compatibility mappings.
  - `T12`: add prompt preset integrity validation, template/marker/task binding checks, default-resource canary smoke, and drift detection without overwriting user-customized preset content.
  - `T14`: define `APP_ENV=dev|test|prod`, tighten prod-only constraints for dev fallback/session/CORS/secrets/default passwords/deploy boundaries, and expose executable deployment/security checks.
  - `T15`: create route/task/config/prompt coverage matrix, smoke/contract/critical/full regression layers, and project-level gate/regression runners.
  - `T13`: add isolated perf/baseline scripts and result docs for `100/300/800` chapters + `1/5/20` concurrency, with explicit dev/prod interpretation.
  - Final closeout: update docs, issues, and `5.4大调查详细记录.md`; run backend/frontend/DB snapshot/Playwright full validation.
- Out:
  - Reworking completed `T01`~`T10` features or rewriting the Wave B/Wave C compatibility layers.
  - Building a full billing platform, prompt CMS, enterprise IAM, or cloud-heavy CI system.
  - Changing SQLite single-worker assumptions or mixing perf stress traffic into default regression.

## Assumptions / Dependencies
- Work stays on `test` and commits start at `MVP-643` unless a collision is discovered.
- Existing stable baseline is `daf1a1e` (`[MVP-642] ... Wave C-2 ...`).
- `5.4大调查详细记录.md:1603`, `:1651`, `:1698`, `:1746`, `:1795`, `:2124`, `:2163`, `:2229` remain the source of truth for task scope and closure.
- Existing prompt default resources under `backend/app/resources/prompt_presets/*` are authoritative inputs and must be guarded rather than silently mutated.
- Performance scripts must use isolated test data / temp DBs and must not touch production-like persistent databases.

## Phases
1. Land plan + issues contract and implement `T11` registry/audit foundation.
2. Wire strict-ready LLM validation through routes/services and add contract tests.
3. Add `T12` prompt integrity guard + canary and protect default-resource drift.
4. Harden `T14` env/security/deploy checks with executable guards and docs.
5. Build `T15` coverage matrix + layered runners and repo-local gate entrypoints.
6. Add `T13` reproducible perf baseline tooling and evidence docs.
7. Run full regression, close issues, append `5.4` writeback, commit/push each issue.

## Tests & Verification
- Backend baseline -> `cd backend && .\.venv\Scripts\python.exe -m compileall -q app alembic && .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v`
- Frontend baseline -> `cd frontend && npm run lint && npm test -- --run && npm run build`
- DB snapshot -> `pwsh test/scripts/snapshot-db.ps1`
- Full Playwright regression -> `cd test && npx playwright test --workers=1`
- `T11` contract -> add backend unit/integration coverage for invalid provider/model/capability handling, audit/enforce behavior, and compatibility mappings.
- `T12` integrity -> add backend tests + repo guard/smoke for missing template, orphan template, invalid marker/task binding, and canary drift.
- `T14` env/security -> add backend config/auth tests plus guard tests for prod fallback/session/CORS/default-password/deploy boundary rules.
- `T15` gate layering -> execute repo runners for smoke / contract / critical / full and verify failure reporting paths.
- `T13` perf -> run isolated benchmark scripts and record `100/300/800` + `1/5/20` outputs in docs.

## Issue CSV
- Path: issues/2026-03-07_20-54-42-wave-d-final-t11-t15.csv
- Must share the same timestamp/slug as this plan.

## Tools / MCP
- `none` for issue execution contract rows unless manual reproduction is clearer.
- Repo guards/runners under `scripts/guards` and `backend/scripts` will be extended as the primary executable governance surface.

## Acceptance Checklist
- [ ] `T11` has a single LLM config registry with provider/model/capability/pricing metadata, audit-first validation, strict-ready integration, and compatibility mappings.
- [ ] `T12` default prompt resources have executable integrity checks, canary smoke, and user-customized presets remain untouched unless explicitly reset.
- [ ] `T14` documents and code agree on `dev|test|prod` behavior and production-dangerous defaults are blocked by executable checks.
- [ ] `T15` exposes a route/task/config/prompt coverage matrix plus smoke/contract/critical/full runner entrypoints for daily use.
- [ ] `T13` ships isolated baseline scripts plus documented results for chapter scale and concurrency.
- [ ] Backend, frontend, DB snapshot, and full Playwright regression pass after all changes.
- [ ] `issues/...csv` is fully closed (`DONE`) and `5.4大调查详细记录.md` contains an objective Wave D writeback.

## Risks / Blockers
- Over-enforcing unknown models/providers could break custom gateway usage; mitigate with compatibility mappings and audit-first defaults.
- Prompt integrity checks must not treat intentionally blank marker-driven templates as invalid; the guard needs explicit marker-aware rules.
- Security hardening must preserve dev/test ergonomics while making prod misconfiguration fail loudly.
- Perf baselines must stay reproducible and isolated so they do not destabilize ordinary regression runs.

## Rollback / Recovery
- Revert per-issue commits `[MVP-643]` ... `[MVP-649]` if a specific governance layer regresses behavior.
- Keep new validation paths additive and mode-driven (`audit` by default, `enforce` opt-in/ready) so rollback can disable enforcement without deleting data.
- Keep new guards/runners repo-local; if one is too strict, drop that commit or remove it from the gate runner while preserving existing suites.

## Checkpoints
- Commit after: each issue row (`MVP-643` through `MVP-649`) with matching CSV status update and immediate push to `origin/test`.

## References
- `AGENTS.md:1`
- `issues/README.md:1`
- `docs/testing-policy.md:1`
- `README.md:1`
- `README.md:263`
- `README.md:281`
- `5.4大调查详细记录.md:1603`
- `5.4大调查详细记录.md:1651`
- `5.4大调查详细记录.md:1698`
- `5.4大调查详细记录.md:1746`
- `5.4大调查详细记录.md:1795`
- `5.4大调查详细记录.md:2124`
- `5.4大调查详细记录.md:2163`
- `5.4大调查详细记录.md:2229`
- `backend/app/core/config.py:1`
- `backend/app/api/routes/llm_profiles.py:1`
- `backend/app/api/routes/llm_preset.py:1`
- `backend/app/services/prompt_preset_resources.py:1`
- `backend/app/services/prompt_presets.py:1`
- `backend/app/models/llm_profile.py:1`
- `backend/app/models/llm_preset.py:1`
- `backend/app/models/prompt_preset.py:1`
- `backend/scripts/run_quality_gate.py:1`
- `scripts/guards/registry.py:1`
- `scripts/guards/run.py:1`
- `docs/deployment/security-checklist.md:1`
- `docs/deployment/docker-compose-prod.md:1`
- `test/playwright.config.ts:1`
- `test/scripts/snapshot-db.ps1:1`
