# REV-009 — [data][migrations] Alembic 与 db_schema.json 基线一致性审计

## 背景
- `backend/alembic/versions/*` 定义数据库迁移历史；`test/contracts/db_schema.json` 是 E2E/契约层的 schema 基线。
- 本项目标：
  - 保证迁移与 schema 基线一致（`db-schema` spec 通过）；
  - 审计 `ensure_db_schema` 对 legacy SQLite（缺少 `alembic_version`）的 `stamp` 行为是否安全、与文档一致。

## 检查项
- `test/specs/db/db-schema.spec.ts` 是否通过（代表基线与当前迁移一致）。
- Alembic 迁移链是否包含 init revision，且 head 合并（merge）合理。
- `ensure_db_schema`：
  - legacy SQLite 无 `alembic_version` 时的判定逻辑；
  - prod 环境是否 fail-closed（不自动 stamp）。

## 结果与差异清单
- `db-schema` spec：**通过**（无差异；无需更新 `test/contracts/db_schema.json`）。
- `backend/alembic/versions/0f24b611cf21_init.py` 与 `INIT_REVISION="0f24b611cf21"` 对齐。

## ensure_db_schema（legacy SQLite stamp）审计结论
文件：`backend/app/db/migrations.py`
- 当检测到 SQLite 且库非空但缺少 `alembic_version`：
  - 通过表/列存在性推断是否为“新 schema”（`outlines`/`llm_profiles` + `projects.active_outline_id/llm_profile_id`）。
  - `has_new_schema=True` → `stamp head`
  - 否则 → `stamp INIT_REVISION`
- **prod（`APP_ENV=prod`）直接跳过自动 stamp 并抛错**，要求人工处理（fail closed），与根 `README.md` 的注意项一致。

## 证据（命令与关键输出摘要）
- `cd test; npm test -- specs/db/db-schema.spec.ts`
  - `1 passed`（db schema matches baseline snapshot）
- `backend/app/db/migrations.py`：
  - `INIT_REVISION = "0f24b611cf21"`
  - prod 下 `stamp_skipped` + raise（禁止自动 stamp）

## 修复方案
- 本次无需修复（基线一致且策略符合预期）。

## 测试结果
- 按 `Test_Method` 运行 `db-schema` spec：通过；未执行 `snapshot-db.ps1`（因为无需更新基线）。
