# demo（Atelier）vs mumu（MuMuAINovel）学习优化详细区别（基于已实现功能的逐项对比）

> 目标：让“下一次对话”的协作者只看这一份文档，就能像重新完整调查了一遍一样，准确理解：
> 1) `demo/` 当前已经实现了什么（前端交互/按钮逻辑/提示词构建/上下文构建/章节组织等具体实现）  
> 2) `mumu/MuMuAINovel/` 在相同维度上是怎么做的（对应文件/关键逻辑位置）  
> 3) 以“demo 已实现功能”为主线，哪些点可以借鉴 mumu 做得更稳/更顺滑/更高质量（落地到 demo 需要改哪些文件、改成什么样、怎么改）

---

## 0. 范围与原则（非常重要）

### 0.1 本文“对比”的口径

- **主线只讨论 demo 已经实现的功能**：项目/设定/角色/模型配置/大纲/章节写作/流式生成（beta）/预览/导出/Prompt Studio/生成记录等。
- mumu 中大量“更高级功能”（多用户、OAuth、职业/组织/关系、记忆系统、章节分析、批量生成、导入导出等）会作为 **“可迁移优化点”** 出现在“建议”里，但不会喧宾夺主。

### 0.2 关键约束（demo 的红线）

- SQLite 单 worker（见 `demo/README.md`）：后端必须 `--workers 1`；LLM 调用期间不能持有长事务（demo 已按此做了拆 session）。
- **API Key 绝不出现在日志/响应/导出/前端回显**：demo 已采用 `llm_profiles` + `has_api_key/masked_api_key` 口径（见 `demo/backend/app/api/routes/llm_profiles.py`、`demo/backend/app/services/llm_key_resolver.py`）。
- demo 的 Prompt Engine（Prompt Studio：blocks + Jinja2 宏 + token 预算 + render_log 可观测）是 demo 的核心优势之一：**不要为了“学习 mumu”而降级回“字符串模板”**。

---

## 1. 两个项目的定位差异（理解“为什么实现不同”）

### 1.1 demo（Atelier）的定位

- 单用户本地 MVP（更强调：**工坊式写作工作台**、统一 UI 设计语言、工程卫生、Prompt Engine 可编辑/可观测）。
- 关键产品思路：**写作流程闭环**（向导/完成度）、**提示词系统可控**（Prompt Studio）、**生成结果不自动落库**（减少误写入，用户确认后保存）。
- 主要技术：React + TS + Vite + Tailwind + Framer Motion；后端 FastAPI + SQLAlchemy + Alembic + SQLite。

### 1.2 mumu（MuMuAINovel）的定位

- 生产级平台（更强调：**多用户隔离**、部署、长流程、批量任务、分析/记忆系统、工具链齐全）。
- 关键产品思路：**生成即落库**、**长流程后台任务**、**上下文/记忆/分析系统增强产出质量**、**可视化管理（章节关系/标注/分析）**。
- 主要技术：React + TS + Ant Design + Zustand；后端 FastAPI + PostgreSQL + 异步数据库 + ChromaDB + Embedding +（可选 MCP 工具链）。

---

## 2. demo 已实现功能（逐页 + 逐 API + 逐关键逻辑）

> 本节把 demo 现状写清楚：页面有什么按钮、怎么交互、调用什么 API、后端怎么构建 prompt 与上下文、怎么解析输出、如何记录 generation_runs。

### 2.1 前端页面与交互（demo/frontend/src/pages）

#### 2.1.1 AppShell（导航/主题/响应式）

- 文件：`demo/frontend/src/components/layout/AppShell.tsx`
- 功能要点：
  - 左侧 Sidebar（可折叠/展开）+ 移动端抽屉式导航（overlay）。
  - 标题由路由后缀映射（`ROUTE_TITLES`）。
  - 主题系统使用 `data-theme="paper-ink"` 与 `.dark` 正交（见 `demo/frontend/src/index.css`）。

#### 2.1.2 Dashboard（项目列表/新建/删除/进入/完成度）

- 文件：`demo/frontend/src/pages/DashboardPage.tsx`
- 交互要点：
  - “新建项目”卡片 → 弹 Modal 输入 name/genre/logline → 创建后刷新列表并进入项目。
  - 每个项目卡：
    - 点击进入：若完成度 >=100 进入写作，否则进入向导。
    - “向导”按钮直接进入 wizard。
    - “删除”按钮：二次确认（Confirm）→ 调用删除 API。
  - 完成度来源：
    - Dashboard 会额外拉取 `/api/projects/summary`，用 `computeWizardProgressFromSummary` 计算进度条与下一步。

#### 2.1.3 Project Wizard（开工向导 + 自动模式）

- 文件：`demo/frontend/src/pages/ProjectWizardPage.tsx`
- 交互要点：
  - 显示步骤（settings/characters/llm/outline/chapters/writing/preview/export）+ 跳转链接。
  - 支持“跳过某步”（localStorage 记录）。
  - “自动模式（MVP）”：一键调用 LLM 生成大纲 → 保存为新 outline 版本 → 从结构 bulk_create 章节骨架 → 跳到 writing。
    - 调用：`POST /api/projects/{projectId}/outline/generate`（非流式）→ `POST /api/projects/{projectId}/outlines` 保存版本 → `POST /api/projects/{projectId}/chapters/bulk_create` 创建章节。

#### 2.1.4 Settings（设定页）

- 文件：`demo/frontend/src/pages/SettingsPage.tsx`
- 交互要点：
  - 编辑项目元信息（genre/logline）+ 项目设定（world_setting/style_guide/constraints）。
  - dirty 保护 + Ctrl/Cmd+S 保存。

#### 2.1.5 Characters（角色卡 CRUD）

- 文件：`demo/frontend/src/pages/CharactersPage.tsx`
- 交互要点：
  - 列表展示角色；Drawer 编辑/新建；删除确认。
  - 角色字段目前是轻量：name/role/profile/notes（对应后端 `demo/backend/app/models/character.py`）。

#### 2.1.6 Prompts（模型配置：profiles + preset + 测试连接）

- 文件：`demo/frontend/src/pages/PromptsPage.tsx`
- 关键口径：
  - demo 把“模型连接信息/Key”放到后端 `llm_profiles`（加密存储），项目只绑定 `project.llm_profile_id`。
  - `llm_preset` 是“请求参数/模型参数”的单例（provider/model/base_url/temperature/max_tokens/stop/timeout/extra...），**不存 Key**。
- 交互要点：
  - profiles CRUD（创建/改名/删除）、绑定到 project。
  - API Key：保存到 profile（后端加密）、支持清除；前端只展示 `masked_api_key`。
  - 测试连接：`POST /api/llm/test`，成功后写向导完成度 localStorage。

#### 2.1.7 Prompt Studio（提示词系统：presets/blocks/预览/导入导出/拖拽排序）

- 文件：`demo/frontend/src/pages/PromptStudioPage.tsx`
- 交互要点：
  - preset 列表（带 active_for 标签）→ 选中后加载 blocks。
  - block 编辑：role、enabled、template 或 marker_key、预算（priority/maxTokens）、触发任务 triggers、injection（relative/absolute + depth）等。
  - 拖拽排序：调用 reorder API。
  - import/export：以 JSON 文件导入导出一个 preset（包含 blocks）。
  - Preview：统一走后端 `POST /api/projects/{projectId}/prompt_preview`，保证“前端预览=后端实际渲染”。

#### 2.1.8 Outline（大纲编辑 + AI 生成 + 预览应用 + 创建章节骨架）

- 文件：`demo/frontend/src/pages/OutlinePage.tsx`
- 交互要点：
  - 编辑/保存 outline（单例 `GET/PUT /api/projects/{id}/outline`）。
  - AI 生成（可选流式 beta）：
    - 非流式：`POST /api/projects/{id}/outline/generate`
    - 流式：`POST /api/projects/{id}/outline/generate-stream`（SSE）
  - 生成结果预览：
    - 预览结构（chapters[number/title/beats]）+ outline_md。
    - “应用生成结果”：把 `outline_md` 写入 outline；并把结构 `structure_json` 存入 outline（后端字段）。
  - 从大纲创建章节骨架：`POST /api/projects/{id}/chapters/bulk_create`；若冲突可 replace。

#### 2.1.9 Writing（章节 CRUD + Markdown 编辑 + AI 生成 replace/append + plan_first + post_edit + 生成记录）

- 文件：`demo/frontend/src/pages/WritingPage.tsx`
- 章节 CRUD：
  - 新建章节（number/title/plan）、保存、删除、切换章节 dirty 确认。
  - 编辑器字段：title/plan/content_md/summary/status。
- AI 生成（核心链路）：
  - 入口在 `AiGenerateDrawer`（`demo/frontend/src/components/writing/AiGenerateDrawer.tsx`）：
    - 模式：流式（beta）/ plan_first / post_edit
    - 目标字数：target_word_count
    - 上下文开关：世界观/风格/约束/大纲；角色多选；上一章注入（none/summary/content）
    - 指令：instruction（本次生成的“作者指令”）
  - 生成按钮逻辑（WritingPage）：
    - 如果当前章节 dirty，会先弹确认：保存并生成 / 直接生成 / 取消。
    - 非流式：`POST /api/chapters/{chapterId}/generate` → 写入编辑器但**不自动保存**。
    - 流式：`POST /api/chapters/{chapterId}/generate-stream`（SSE）→ 边流式边写入编辑器；支持取消；失败时无 chunk 会回退非流式。
  - 流式解析（非常关键）：
    - demo 约定章节输出契约为 markers：`<<<CONTENT>>> ... <<<SUMMARY>>> ...`。
    - 前端流式解析器：`demo/frontend/src/services/chapterMarkerStreamParser.ts`（带单测 `chapterMarkerStreamParser.test.ts`）。
      - 支持 marker 跨 chunk；支持 whitespace drift；marker 缺失会进入 raw fallback。
- 生成记录（generation_runs）：
  - Writing 页能打开最近 5 条运行记录 drawer（RunHistoryDrawer），并可拉取详情。
  - 后端 generation_runs 记录存储：渲染后的 prompt（system/user/messages）+ render_log_json + params + output/error。

#### 2.1.10 Preview（阅读预览）

- 文件：`demo/frontend/src/pages/PreviewPage.tsx`
- 交互要点：
  - 左侧章节列表（PC 可折叠；移动端抽屉）。
  - 章节正文用 `ReactMarkdown + remarkGfm` 渲染。
  - “编辑”按钮跳到 Writing 页并带 query `chapterId=...`。
  - 会写 localStorage 标记“已预览”（wizard preview step）。

#### 2.1.11 Export（导出 Markdown）

- 文件：`demo/frontend/src/pages/ExportPage.tsx`
- 交互要点：
  - 勾选：include_settings / include_characters / include_outline / chapters=all|done。
  - 调用 `GET /api/projects/{id}/export/markdown?…`，下载 `.md`。
  - 导出成功写 localStorage（wizard export step）。

---

### 2.2 前端基础设施（demo/frontend/src/services & components/ui）

#### 2.2.1 API 客户端与错误口径

- 文件：`demo/frontend/src/services/apiClient.ts`
- 统一约定：后端 JSON envelope 为 `{ok: true, data, request_id}` / `{ok:false,error,request_id}`（导出例外：text/markdown）。
- `ApiError` 带 `code/message/details/requestId/status`，Toast 展示时常带 request_id 便于定位。

#### 2.2.2 SSE 客户端（POST over fetch + ReadableStream）

- 文件：`demo/frontend/src/services/sseClient.ts`
- 协议：
  - 服务端发送 `data: {type: "progress"|"chunk"|"result"|"error"|"done", ...}\n\n`
  - 前端读取流，按 `\n\n` 切块，解析 `data:` 行 JSON。
- 错误策略：
  - HTTP 非 2xx：尝试按 demo 的 ApiErrorPayload 解析，否则 SSE_BAD_RESPONSE。
  - Content-Type 不是 `text/event-stream`：SSE_PROTOCOL_ERROR。
  - 支持 AbortController 取消。

#### 2.2.3 流式章节 marker 解析器（避免把 marker 字面串写入编辑器）

- 文件：`demo/frontend/src/services/chapterMarkerStreamParser.ts`
- 关键行为：
  - phase: before → content → summary；若 preamble 太长仍未找到 marker，会 raw fallback。
  - tailKeepChars 保持 buffer 上限，避免长输出导致内存爆。

#### 2.2.4 UI 组件（Confirm/Toast/Drawer/Modal）

- 目录：`demo/frontend/src/components/ui/`
- Toast：
  - `toast.ts` 定义 context API；`ToastProvider.tsx` 渲染堆叠 toast（带 success/error）。
- Confirm：
  - `confirm.ts` + `ConfirmProvider.tsx` 提供 confirm / choose（双按钮）模式。

#### 2.2.5 设计系统落地（Paper & Ink）

- 规范：`demo/ui设计规范.md`
- 变量与组件类：`demo/frontend/src/index.css`
  - CSS variables 定义色彩/字体/动效；
  - `.btn/.panel/.input/.textarea/...` 等统一组件样式；
  - `prefers-reduced-motion` 降级策略；
  - `.atelier-content` 统一正文排版。

---

### 2.3 后端 API 与业务实现（demo/backend）

#### 2.3.1 路由汇总

- 入口：`demo/backend/app/api/router.py`（prefix `/api`）
- 核心路由文件：`demo/backend/app/api/routes/*`
  - projects/settings/characters/outline/outlines/chapters/llm_preset/llm_profiles/llm/ export/generation_runs/prompts(prompt engine)

#### 2.3.2 项目（projects）

- 文件：`demo/backend/app/api/routes/projects.py`
- 特点：
  - `POST /projects` 新建项目后会 **自动 seed 推荐 Prompt Engine presets** 并激活 outline_generate/chapter_generate（`ensure_default_outline_preset`、`ensure_default_chapter_preset`）。
  - `GET /projects/summary` 为 Dashboard 完成度提供聚合数据（章节 total/done、是否有 Key 等）。

#### 2.3.3 模型配置（llm_profiles + llm_preset + llm_test）

- profiles：`demo/backend/app/api/routes/llm_profiles.py`
  - API Key 加密：`encrypt_secret`；只回 `has_api_key/masked_api_key`。
  - profile 更新会同步所有绑定 project 的 llm_preset（provider/model/base_url/默认 max_tokens）。
- resolve key：`demo/backend/app/services/llm_key_resolver.py`
  - 优先用 header `X-LLM-API-Key`（单次 override），否则从 project 绑定的 profile 解密取 key。
- preset：`demo/backend/app/api/routes/llm_preset.py`
  - 单例：`GET/PUT /projects/{id}/llm_preset`。
  - 默认 `max_tokens=32000`（降低截断风险）。
- LLM client：`demo/backend/app/llm/client.py`
  - `openai/openai_compatible/anthropic/gemini` 参数过滤（减少 400）。
  - 上游错误映射到统一 `AppError`（含 status_code，dev 环境可回截断后的 upstream_error）。

#### 2.3.4 Prompt Engine（Prompt Studio）后端

- API：`demo/backend/app/api/routes/prompts.py`
  - `GET /projects/{id}/prompt_presets`：保证 baseline presets 存在（plan/post_edit/推荐 outline/chapter）。
  - preset CRUD、block CRUD、reorder、import/export、preview（`POST /projects/{id}/prompt_preview`）。
- 渲染：`demo/backend/app/services/prompt_presets.py`
  - 读取 blocks → 逐块渲染（template/Jinja 或 marker_key）→ 处理 overrides（同 identifier 后者覆盖前者，除非 forbid_overrides）→ token 估算与裁剪（priority + maxTokens + 全局预算）→ 组装 messages。
  - 生成 `render_log`（缺失变量/每块 token/裁剪原因等）供可观测性与调试。
- 模板引擎：`demo/backend/app/services/prompting.py`
  - Jinja2 渲染 + 宏系统（date/time/isodate/random/pick/注释），支持 seed 复现。

#### 2.3.5 输出契约与解析（非常关键）

- 合同：`demo/backend/app/services/output_contracts.py`
  - outline_generate：JSON（解析 chapters 结构）
  - chapter_generate：markers（<<<CONTENT>>>/<<<SUMMARY>>>）
  - plan_chapter：tags（<plan>...</plan>）
  - post_edit：tags（<rewrite>...</rewrite>）
- 解析器：`demo/backend/app/services/output_parsers.py`
  - chapter：优先 markers；否则 fallback 从 JSON 找 content_md/summary；否则原文；finish_reason=length 会给 OUTPUT_TRUNCATED 警告。
  - outline：JSON 解析失败会提示可能截断；openai/openai_compatible 会尝试“修复 JSON”二次调用（见 outline.py）。

#### 2.3.6 大纲生成（outline）

- 文件：`demo/backend/app/api/routes/outline.py`
- 上下文构建：
  - settings（world/style/constraints）可整体关；
  - characters 可选；
  - requirements 以 JSON 写入 prompt values。
- 非流式：调用 LLM → parse JSON → 如 parse_error 且 openai/openai_compatible 尝试 fix_json。
- 流式：SSE progress/chunk/result/done；openai 系列走真正 stream，其它 provider fallback 为一次性调用再塞 result。

#### 2.3.7 章节生成（chapters）

- 文件：`demo/backend/app/api/routes/chapters.py`
- 关键决策：
  - **生成不自动落库**：返回结果交给前端写入编辑器，用户保存才落库（降低误写）。
  - 上下文由请求 body 控制（世界观/风格/约束/大纲/角色/上一章 summary or content）。
  - mode=append 时，会在 instruction 前加“追加模式”提示；mode=replace 则“替换模式”。
  - plan_first：
    1) 先运行 `plan_chapter`（contract `<plan>...</plan>`）得到 plan；
    2) 把 plan 注入 instruction（以 `<PLAN>...</PLAN>`）；
    3) 再运行 `chapter_generate`。
  - post_edit：
    - 章节生成后，再跑一次 `post_edit`（contract `<rewrite>...</rewrite>`），并用返回覆盖 `content_md`。
- 流式版本：
  - SSE 里会先 progress(准备/规划/渲染/调用模型/解析/润色)；
  - chunk 是原始模型 delta；前端用 marker parser “边读边写”；
  - 中途失败：
    - 若没产生 chunk，会在前端回退非流式；
    - 若产生了 chunk，则前端不回退，提示错误并保留已写入内容（现状如此）。

---

## 3. mumu 的对应实现（按“demo 的相同维度”去找）

> 本节不是把 mumu 全讲一遍，而是把 **和 demo 已实现链路强相关** 的实现拎出来，并标清文件位置。

### 3.1 前端页面与交互（mumu/MuMuAINovel/frontend/src/pages）

#### 3.1.1 章节管理（Chapters.tsx）

- 文件：`mumu/MuMuAINovel/frontend/src/pages/Chapters.tsx`
- 关键能力：
  - 单章生成（SSE）、批量顺序生成（创建任务→轮询 status→可取消→刷新后恢复 active task）。
  - 分析任务状态跟踪（analysis_tasksMap + 轮询 `/analysis/status`）。
  - 目标字数、写作风格、模型选择等参数更丰富。
  - 有统一的 SSE 进度 UI（`SSELoadingOverlay`、`SSEProgressModal`）。

#### 3.1.2 章节阅读（ChapterReader.tsx）

- 文件：`mumu/MuMuAINovel/frontend/src/pages/ChapterReader.tsx`
- 关键能力：
  - “上一章/下一章”导航（依赖后端 navigation API）。
  - 显示章节标注（hooks/foreshadows/plot_points/character_events），可点选高亮并在侧边栏看详情。
  - 可触发“重新分析”（`POST /chapters/{id}/analyze`）→ 轮询分析进度 → 刷新标注。

#### 3.1.3 提示词模板（PromptTemplates.tsx）

- 文件：`mumu/MuMuAINovel/frontend/src/pages/PromptTemplates.tsx`
- 关键能力：
  - 模板按分类展示；编辑/重置/导入/导出；导入结果会展示统计与“哪些模板被转为自定义”的细节提示。

#### 3.1.4 大纲（Outline.tsx / ProjectWizardNew.tsx）

- mumu 的大纲系统支持：
  - 生成/续写（SSE），并且支持“角色预测→用户确认”的中途交互（需要 SSE event 支持）。
  - 大纲展开（把一个 outline 展开成多个子章节规划）。
  - one-to-one 与 one-to-many 两种大纲模式（更贴近长篇生产）。

### 3.2 前端基础设施（mumu/MuMuAINovel/frontend/src/utils & services & store）

#### 3.2.1 SSE 客户端（支持 event: 行与特殊事件）

- 文件：`mumu/MuMuAINovel/frontend/src/utils/sseClient.ts`
- 特点：
  - 同时提供 SSEClient（EventSource GET）与 SSEPostClient（fetch POST 流）。
  - 解析 `event:` 行；支持特殊事件 `character_confirmation_required`（收到后暂停并等待用户确认）。
- 这点对 demo 的启示：如果未来 demo 也想做“生成中需要用户确认”的交互，SSE 协议与 client 需要提前设计 event 支持。

#### 3.2.2 API 层与状态管理

- API：`mumu/MuMuAINovel/frontend/src/services/api.ts`（Axios + 拦截器，返回 data）
- 状态：Zustand + hooks（例如 `mumu/MuMuAINovel/frontend/src/store/hooks.ts`）
- 影响：组件中写“业务逻辑”的比例更高（Chapters.tsx 非常大），而 demo 把通用交互拆到 Confirm/Toast/WizardNextBar 等组件里。

---

### 3.3 后端：章节生成 + 上下文 + 记忆 + 分析（mumu/MuMuAINovel/backend/app/api/chapters.py）

#### 3.3.1 生成前置条件（严格顺序）

- 函数：`check_prerequisites`（`mumu/MuMuAINovel/backend/app/api/chapters.py`）
- 行为：如果当前章不是第一章，会检查所有前置章节是否有 content；若有空章则拒绝生成并提示“需要先完成前置章节：第 x,y 章”。

#### 3.3.2 智能上下文构建（海量章节）

- 函数：`build_smart_chapter_context`（同文件）
- 策略（非常具体）：
  - 故事骨架：每 50 章采样 1 章（标题+摘要）
  - 相关历史：语义检索 15 个相关章节（依赖 memory_service/向量）
  - 近期概要：最近 30 章摘要（每章 200 字）
  - 最近完整：最近 3 章全文
- 并且额外做了 demo 当前没有的一个“强衔接策略”：
  - 从上一章全文中提取结尾 500~600 字，写入一个明确的“必读衔接区块”，并追加“不要重复、从新情节点开始”的硬规则。

#### 3.3.3 记忆系统（向量检索 + 伏笔追踪）

- 服务：`mumu/MuMuAINovel/backend/app/services/memory_service.py`
- 核心函数：`build_context_for_generation`
  - recent_context（最近章节记忆）
  - relevant_memories（语义相关记忆）
  - foreshadows（未回收伏笔）
  - character_states（角色相关记忆）
  - plot_points（重要情节点）
- 这部分是 demo 当前没有的“长期记忆层”，但其输出格式（分区块 + 使用指南）非常值得 demo 用 PromptBlocks 复刻（即使先用非向量的简化版）。

#### 3.3.4 章节分析（PlotAnalyzer + annotations）

- 服务：`mumu/MuMuAINovel/backend/app/services/plot_analyzer.py`
  - 维度：hooks/foreshadows/plot_points/character_states/conflict/scores/suggestions
  - 流程：截取前 8000 字→调用 AI→解析 JSON→生成建议与记忆片段
- API：
  - `/chapters/{id}/analysis/status`、`/chapters/{id}/analysis`、`/chapters/{id}/annotations`、`POST /chapters/{id}/analyze`
- 标注 API：`get_chapter_annotations`（`mumu/MuMuAINovel/backend/app/api/chapters.py`）
  - 返回每条记忆的位置 position/length，用于前端高亮渲染。

#### 3.3.5 批量顺序生成（BatchGenerationTask）

- API：`/chapters/project/{project_id}/batch-generate`、`/batch-generate/{batch_id}/status`、`/project/{project_id}/batch-generate/active`、`/batch-generate/{batch_id}/cancel`
- 行为：创建后台任务，严格按顺序生成；可选 enable_analysis；失败终止；支持取消。

#### 3.3.6 重新生成（regenerate-stream）

- 服务：`mumu/MuMuAINovel/backend/app/services/chapter_regenerator.py`
- 关键点：把“原章节内容 + 分析建议 + 用户自定义指令 + 保留元素”整合成提示词，流式输出新版本，并可计算 diff。

---

### 3.4 后端：Prompt 系统（mumu）

- mumu 的 prompt 体系是“模板 key + 字符串 format”为核心：
  - `mumu/MuMuAINovel/backend/app/services/prompt_service.py`
  - `mumu/MuMuAINovel/backend/app/api/prompt_templates.py`
- 特点：
  - 用户自定义模板覆盖系统默认模板；
  - UI 支持分类、导入导出、重置；
  - 章节生成模板 `CHAPTER_GENERATION_WITH_CONTEXT` 明确写了“防重复条款、字数严格范围、记忆上下文区块”。
- 对 demo 的启示：
  - 这些“条款内容/区块组织方式”很值得吸收；
  - 但 demo 更应把它们拆成 PromptBlocks，并利用预算裁剪与 render_log 可观测，而不是照搬字符串模板系统。

---

## 4. 核心链路逐项对比（demo 已实现功能 → mumu 对应做法 → 可借鉴点）

> 本节把“差异”讲清楚，并直接给出“落地到 demo 的改法”。

### 4.1 章节生成：输出契约与流式体验

#### demo 的做法

- 输出契约（强约束）：`<<<CONTENT>>>` + `<<<SUMMARY>>>`（后端 contract=markers，前端 stream parser 逐步抽取）。
  - 后端：`demo/backend/app/services/output_contracts.py`、`demo/backend/app/services/output_parsers.py`
  - 前端：`demo/frontend/src/services/chapterMarkerStreamParser.ts`
- 流式协议（统一）：progress/chunk/result/error/done（`demo/backend/app/utils/sse_response.py` + `demo/frontend/src/services/sseClient.ts`）。
- 回退策略：流式失败且无 chunk → 回退非流式（在 `demo/frontend/src/pages/WritingPage.tsx`）。

#### mumu 的做法

- 流式协议存在多种 type（start/progress/content/chunk/done...），且不同链路可能不完全一致。
- 进度 UI 有统一 overlay/modal（`SSELoadingOverlay`、`SSEProgressModal`）。

#### 可借鉴点（保持 demo 协议不变）

- 只学习 mumu 的“进度 UI 形态”（遮罩/Modal/取消按钮/提示文案），不要把 demo 的 SSE message type 搞乱。
- demo 现有的 `AiGenerateDrawer` + “右下角浮层进度条”已经接近了；可继续打磨到 mumu 那种“统一进度组件”。

---

### 4.2 章节生成：上下文构建（最重要差异）

#### demo 的做法（当前已实现）

- 上下文主要来自：
  - 项目设定：world_setting/style_guide/constraints（可按开关注入）
  - 角色卡：用户勾选角色 ids → `format_characters` 注入
  - 大纲：可注入 outline.content_md
  - 上一章：可注入 summary 或 content
  - 用户指令 instruction + target_word_count
- 具体逻辑见：
  - `demo/backend/app/api/routes/chapters.py`（values 构造）
  - `demo/backend/app/services/prompt_presets.py`（默认 chapter preset 的 block 结构）
  - `demo/frontend/src/components/writing/AiGenerateDrawer.tsx`（开关/选择 UI）

#### mumu 的做法（更强）

- 章节生成上下文 = 多层策略：
  - 海量章节 smart_context（骨架采样/相关历史/近期概要/最近全文）
  - 记忆系统 memory_context（recent/relevant/foreshadows/plot_points/角色状态）
  - “上一章结尾强衔接” + “禁止复述”硬条款
- 见：
  - `mumu/MuMuAINovel/backend/app/api/chapters.py`（build_smart_chapter_context + 结尾注入）
  - `mumu/MuMuAINovel/backend/app/services/memory_service.py`（build_context_for_generation）
  - `mumu/MuMuAINovel/backend/app/services/prompt_service.py`（CHAPTER_GENERATION_WITH_CONTEXT 防重复条款）

#### demo 可立即落地的优化（不引入向量库也能做）

1) **上一章“结尾片段”注入（强烈建议做成 P0）**  
   - demo 目前只有 summary/content；建议增加 `tail`（例如尾部 600~1200 字）选项。
   - 需要改：
     - `demo/backend/app/schemas/chapter_generate.py`：扩展 `previous_chapter` 枚举或新增字段。
     - `demo/backend/app/api/routes/chapters.py`：根据选项截取上一章尾部，并写入 values（建议单独变量，如 `previous_chapter_ending`，不要复用 `previous_chapter`）。
     - `demo/backend/app/services/prompt_presets.py`：默认 chapter preset 新增一个 block，写出“必须承接结尾/绝不复述/从下一动作开始”的硬规则（内容可以借鉴 mumu 的条款，但拆成 block）。
     - `demo/frontend/src/components/writing/AiGenerateDrawer.tsx`：UI 增加“上一章结尾（推荐）”选项与说明。
   - mumu 参考位置：`mumu/MuMuAINovel/backend/app/api/chapters.py`（上一章结尾提取 + 条款）

2) **append 模式注入“当前草稿尾部”**  
   - demo 的 append 目前只是一句提示，并没有把“当前已写内容”发给模型，容易导致重复或断裂。
   - 做法：append 时把当前章节 `content_md` 的尾部 N 字（例如 800~1500）注入 `<CURRENT_DRAFT_TAIL>`，并要求“从尾部继续写，不要重述”。
   - 需要改同上（schema + chapters.py + prompt_presets 默认 block + 前端说明）。

3) **轻量 smart_context（不引向量）**  
   - 用 demo 已有字段先实现：
     - 最近 K 章 summary（或 content 截断）；
     - 每 M 章采样一条“骨架摘要”（可直接用 summary 或 plan）。
   - 作为可选 blocks 注入，交给预算系统裁剪，避免 prompt 爆炸。

---

### 4.3 章节生成：前置条件与章节分配（顺序约束）

#### demo 的现状

- 章节骨架可由大纲 bulk_create；写作页也可手动创建。
- 生成任意章节不要求前置章节已写完（只要存在章节记录就能生成）。
- 优点：自由；缺点：容易“跳章生成”导致上下文断裂，生成质量不稳定。

#### mumu 的现状

- 默认严格顺序：前置章节无正文则拒绝生成（`check_prerequisites`）。
- 优点：保证连续性；缺点：用户自由度下降。

#### demo 的可借鉴方案（建议做成可配置而非强制）

- 增加 `require_sequential`（默认 false）：
  - true：若 `chapter.number > 1` 且任一前置章节为空，则返回明确错误码，前端提示“先补完第 X 章”。
  - false：保持现状。
- 文件改动（demo）：
  - `demo/backend/app/schemas/chapter_generate.py`（新增字段）
  - `demo/backend/app/api/routes/chapters.py`（校验前置章节）
  - `demo/frontend/src/components/writing/AiGenerateDrawer.tsx`（UI 开关）
  - `demo/frontend/src/pages/WritingPage.tsx`（错误提示与快速跳转）

---

### 4.4 生成结果：落库策略（demo vs mumu）

#### demo 的现状

- 生成结果写入编辑器，但不自动保存 DB（Writing 页也明确提示“生成不自动保存”）。
- 优点：不会把坏结果写进 DB；缺点：用户忘保存会丢。

#### mumu 的现状

- 流式生成结束会保存章节内容，并更新 word_count / 项目总字数；可触发分析任务。

#### demo 可选优化（不改变“默认不落库”口径也能提升体验）

- 做成“可选自动保存”：
  - 生成完成后弹出明确 CTA：`保存` / `放弃`（目前是 toast 提醒，仍可能被忽略）。
  - 或在 `AiGenerateDrawer` 里增加“生成完成后自动保存（beta）”开关（默认关）。
- 仅前端即可实现（调用现有 saveChapter），不必先改后端契约。

---

### 4.5 Prompt 系统：demo 的优势与 mumu 可借鉴的内容

#### demo 的优势（已经更强）

- PromptBlocks：可排序、可启停、可裁剪、可观测、可预览一致。
- Jinja2 + 宏 + seed：可复现。
- token 预算：按块裁剪 + 原因记录（render_log）。

#### mumu 值得借鉴的“内容层写法”

- prompt 中对“防重复/承接/字数范围/记忆使用指南”的明确条款，属于高质量写作 prompt 的成熟经验。
- 建议做法：把这些条款拆成 demo 的 blocks：
  - `sys.story.continuity_rules`
  - `sys.story.no_repeat_rules`
  - `sys.story.word_count_contract`（把 target_word_count 写成更强约束）
  - `sys.story.memory_guidelines`（未来接入记忆系统时使用）

---

### 4.6 预览阅读：交互细节（翻页/标注）

#### demo 的现状

- Preview 只有章节列表 + Markdown 渲染 + “编辑”跳转，没有“上一章/下一章”按钮。

#### mumu 的现状

- ChapterReader 有 navigation API + 翻页按钮 + 标注侧栏 + 重新分析。

#### demo 低成本可优化（P0）

- 仅前端实现翻页：
  - 在 PreviewPage 按 chapter.number 排序，增加 “上一章/下一章”按钮；
  - 增加快捷键（←/→）；
  - 移动端也保持可用。
- 文件：`demo/frontend/src/pages/PreviewPage.tsx`

---

### 4.7 导出：demo 已实现更简洁，但可学习 mumu 的“导入导出范围”

#### demo 的现状

- 导出 Markdown（可选包含设定/角色/大纲/章节 done/all）。
- 没有“项目导入导出 JSON”能力。

#### mumu 的现状

- 有完整项目导入导出服务（JSON），支持跨项目迁移与备份（`import_export_service.py`）。

#### demo 的可选优化（P1/P2）

- 先做“角色卡导入导出 JSON”（最小闭环），再做“项目全量导入导出”。
- 参考文件：
  - mumu：`mumu/MuMuAINovel/backend/app/services/import_export_service.py`
  - demo 需要新增：`demo/backend/app/api/routes/*` + schema + 前端按钮（CharactersPage 或 ExportPage）。

---

## 5. 面向 demo 的“可执行优化清单”（每条都给到：改成什么样、改哪些文件、参考 mumu 哪些实现）

> 这里把优化点拆到可以直接开工的粒度，按优先级排列。  
> 注意：以下每条都以“不改变 demo 的产品契约”为前提（除非特别标注）。

### P0-1：上一章结尾（tail）注入 + 防重复规则块（最推荐）

**目标**：显著降低“重复上一章内容/重复铺垫/断裂衔接”的概率。

**demo 当前行为**：
- 上一章注入只有 summary/content（`AiGenerateDrawer` 下拉）。
- 后端只把 summary/content 当作 `<PREVIOUS_CHAPTER>`，没有“结尾必读/强衔接”的规则块。

**mumu 的参考**：
- `mumu/MuMuAINovel/backend/app/api/chapters.py`：提取上一章结尾 500~600 字，并写硬条款。
- `mumu/MuMuAINovel/backend/app/services/prompt_service.py`：CHAPTER_GENERATION_WITH_CONTEXT 的“防重复条款”。

**建议落地设计（demo）**：
1) schema：
   - 修改 `demo/backend/app/schemas/chapter_generate.py`：
     - 将 `previous_chapter` 从 `str | None` 改成 `Literal["none","summary","content","tail"] | None`（或保持 str 但后端显式校验）。
2) 后端 values：
   - 修改 `demo/backend/app/api/routes/chapters.py`（非流式+流式两处 values 构造一致）：
     - 读取上一章时，如果 `tail`：取 `(prev.content_md or "").strip()` 的尾部 N 字（建议 600~1200 可配），写入 `values["previous_chapter_ending"]`。
     - 仍可保留 `values["previous_chapter"]` 用于 summary/content（兼容旧 blocks）。
3) Prompt preset：
   - 修改 `demo/backend/app/services/prompt_presets.py` 的默认章节 preset：
     - 新增 block：`sys.story.previous_chapter_ending`（素材：结尾，optional/important，maxTokens 建议 800~1400）。
     - 新增 block：`sys.story.no_repeat_rules`（必须规则 must，小文本，不需要裁剪）。
4) 前端 UI：
   - 修改 `demo/frontend/src/components/writing/AiGenerateDrawer.tsx`：
     - 下拉新增“结尾（推荐）”；
     - 文案解释：用来衔接、防重复。
5) 验证方式：
   - 手工：写完第1章后生成第2章，观察开头是否承接尾部且不复述；
   - 自动：无（MVP）。

---

### P0-2：append 模式注入“当前草稿尾部”，让续写变稳定

**目标**：append 真正做到“续写”，减少“模型不知道写到哪”导致的重复/跑题。

**demo 当前行为**：
- append 仅在 instruction 前加一句提示（`【追加模式】...`），但不提供当前章内容。

**mumu 的参考**：
- 章节重写会把原文作为输入（`chapter_regenerator.py`），这本质上就是“给模型一个锚点”。

**建议落地设计（demo）**：
1) schema：
   - 可新增 `context.include_current_chapter_tail: bool` + `context.current_chapter_tail_chars: int`（可选，默认开）。
2) 后端 values：
   - 读取当前章节 `chapter.content_md`，截取尾部 N 字写入 `values["current_draft_tail"]`。
3) Prompt preset：
   - 新增 `<CURRENT_DRAFT_TAIL>` block；
   - 新增规则块：append 时要求“从尾部继续写，不要重写已写内容”。
4) 前端 UI：
   - AiGenerateDrawer 增加一个开关与数字输入（可先 hardcode 默认 1200 字不暴露 UI，先保证质量）。

---

### P0-3：Preview 加入“上一章/下一章”翻页（纯前端）

**目标**：阅读更像“读书”，减少频繁点列表，提升 MVP 观感。

**demo 当前行为**：只有列表选章。

**mumu 参考**：`ChapterReader.tsx` 翻页按钮 + navigation API。

**建议落地设计（demo）**：
- 只改 `demo/frontend/src/pages/PreviewPage.tsx`：
  - 按 `chapter.number` 排序；
  - 计算 prev/next；
  - 增加按钮（上一章/下一章）+ 键盘快捷键。

---

### P1-1：生成前置条件（可选严格顺序）

**目标**：减少“跳章生成”导致的上下文断裂，提高平均生成质量。

**mumu 参考**：`check_prerequisites`。

**建议落地设计（demo）**：
- 新增 `require_sequential` 开关（默认关）；
- 后端校验：若开且前置章节为空 → 返回 `AppError(code="CHAPTER_PREREQ_MISSING", status_code=400, details={missing_numbers: [...]})`。
- 前端：toast + 一键跳转到缺失章节。

---

### P1-2：轻量 smart_context（不引向量）

**目标**：让长篇在章节多时仍能提供“全局骨架 + 近期上下文”，并交给 PromptBudget 系统自动裁剪。

**mumu 参考**：build_smart_chapter_context 的四层结构。

**建议落地设计（demo）**：
- 先用现有数据实现：
  - `recent_summaries`：最近 10~30 章 summary（没有 summary 的章跳过）
  - `recent_full`：最近 1~3 章全文（可截断）
  - `story_skeleton`：每 10~20 章采样一章 summary/plan
- 输出成分区块文本，再作为可选 blocks 注入。

---

### P1-3：批量顺序生成（后端任务化）

**目标**：替代 demo 当前的“前端 saveAndGenerateNext 纯串行”，做到可恢复、可取消、可观察进度。

**mumu 参考**：BatchGenerationTask + active/status/cancel。

**建议落地设计（demo）**：
- 后端新增 batch_tasks 表与相关 endpoints；
- 前端 Writing 或新增页面提供“生成接下来 N 章”的 UI；
- 进度 UI 可参考 mumu 的 SSEProgressModal 外观，但保持 demo 的设计系统。

---

### P2：章节分析/标注/建议→一键重写（情节控制闭环）

**目标**：让“写作质量控制”从“靠手感”变成“可分析、可追踪、可回收伏笔”。

**mumu 参考**：
- `plot_analyzer.py` 维度 + `annotations` API + ChapterReader 标注 UI + regenerate-stream。

**建议落地设计（demo）**：
- 分两阶段：
  1) 先做“分析结果展示”（不做向量记忆）：分析 hooks/foreshadows/plot_points/suggestions；
  2) 再做“标注定位”与“按建议重写”（复用 demo 的 post_edit/tag contract 或新增 regenerate contract）。

---

## 6. 你现在就应该怎么用这份文档

- 如果目标是“立刻提升 demo 章节生成质量”：优先做 **P0-1（上一章结尾注入）+ P0-2（append 续写锚点）**。
- 如果目标是“提升 MVP 展示观感”：优先做 **P0-3（Preview 翻页）+ 统一进度 UI**。
- 如果目标是“走向生产级平台”：参考 mumu 的 batch/analysis/memory，但要保持 demo 的 Prompt Engine 体系。

