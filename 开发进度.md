# ainovel MVP 开发进度

契约基线：`mvp开发计划.md` v2.4（不改契约语义不 bump）

## Sprint 0（初始化）

- [x] 项目骨架：`backend/` + `frontend/`
- [x] 主题系统（data-theme × dark）骨架落地（`frontend/index.html` + `frontend/src/index.css`）
- [x] DB/Alembic 可跑通（SQLite 默认；`backend/alembic.ini` + `backend/alembic/`）

## Sprint 1（App Shell + 项目CRUD + 主题）

- [x] Sidebar/AppShell（折叠、ThemeToggle、ProjectSwitcher）
- [x] Dashboard：项目列表/新建/删除/进入（`frontend/src/pages/DashboardPage.tsx`）
- [x] 设定页：项目字段 + project_settings 保存（`frontend/src/pages/SettingsPage.tsx`）

## Sprint 2（角色卡 + Prompts + LLM Test）

- [x] 角色 CRUD（Drawer）（`frontend/src/pages/CharactersPage.tsx`）
- [x] prompts/llm_preset 单例资源（GET 默认值 + PUT upsert）（后端已实现；前端 Prompts 页已接入）
- [x] `POST /api/llm/test` + 前端提示映射（含 request_id）（后端已实现；前端 Prompts 页已接入）

## Sprint 3（大纲生成 + bulk_create + 写作页骨架）

- [x] outline 单例资源（GET 默认值 + PUT upsert）（后端 + `frontend/src/pages/OutlinePage.tsx`）
- [x] `POST /api/projects/{projectId}/outline/generate`（解析 chapters）（后端 + `frontend/src/pages/OutlinePage.tsx`）
- [x] `POST /api/projects/{projectId}/chapters/bulk_create`（replace 规则）（后端 + 大纲页已接入）
- [x] 写作页：章节列表/详情/新增/保存/删除 + 章节切换 dirty 确认（`frontend/src/pages/WritingPage.tsx`）

## Sprint 4（章节生成 + 生成记录 + 导出）

- [x] `POST /api/chapters/{chapterId}/generate`（replace/append；不自动落库）（后端已实现）
- [x] generation_runs 记录（后端已实现：list/get）
- [x] Prompt 占位符渲染：修复 `render_template`，generation_runs 存渲染后的 prompt（`backend/app/services/prompting.py`）
- [x] `GET /api/projects/{projectId}/export/markdown`（text/markdown）（后端已实现）
- [x] RunHistoryDrawer（最近 5 条 + 详情）（前端已实现：`frontend/src/pages/WritingPage.tsx`）
- [x] 导出页（选项 + 下载 markdown）（前端已实现：`frontend/src/pages/ExportPage.tsx`）
- [ ] 第11节演示脚本全流程自测通过（仍需人工按 UI 跑通；LLM 成功路径需真实 Key；API 闭环与导出下载已自测）

## Sprint 5（P0 修复 + 丝滑流程向导）

- [x] P0 侧边栏折叠/展开可用（收起态 padding/按钮区/导航项居中/不溢出）（`frontend/src/components/layout/AppShell.tsx`）
- [x] P0 写作 vs Prompt & 模型图标去重；折叠按钮改为明确图标（`frontend/src/components/layout/AppShell.tsx`）
- [x] P0 `/projects/:projectId` 增加 index redirect，避免空白页（`frontend/src/App.tsx`）
- [x] P0 SidebarLink 显式禁用 underline（不受全局 `a:hover` 影响）（`frontend/src/components/layout/AppShell.tsx`）
- [x] P1 开工向导页（3.A 纯前端编排）+ 自动模式入口（生成大纲→章节骨架）（`frontend/src/pages/ProjectWizardPage.tsx`）
- [x] P1 Dashboard 展示完成度，并对未完成项目默认进入向导（`frontend/src/pages/DashboardPage.tsx`）
- [x] P1 向导完成度打点：测试连接成功/导出成功写入 localStorage（`frontend/src/pages/PromptsPage.tsx`、`frontend/src/pages/ExportPage.tsx`、`frontend/src/services/wizard.ts`）
- [x] Phase2 预留：前端 user_id 单点来源（减少散落常量）（`frontend/src/services/currentUser.ts`）
- [x] 前端构建通过：`cd frontend && npm run build`（chunk > 500kb warning，非阻塞）
- [x] 修复：大纲页“应用生成结果”后仍可“从大纲创建章节骨架”（`frontend/src/pages/OutlinePage.tsx`）
- [x] 修复：`outline_generate` 占位符注入补齐 `style_guide/constraints`；LLM 失败日志补齐 `error_code`（`backend/app/api/routes/outline.py`、`backend/app/api/routes/chapters.py`）
- [x] 修复：新建项目后跳转设定页（对齐第 11 节演示脚本第 1 步）（`frontend/src/pages/DashboardPage.tsx`）
- [x] P0 工程基线：前端 `npm run lint` 0 error（拆分 Confirm/Toast/ProjectsContext 的 hook 导出；修复 apiClient 正则）
- [x] P0 修复：Prompts 页占位符扫描/渲染正则修复（`frontend/src/pages/PromptsPage.tsx`）
- [x] P0 修复：Prompts `timeout_seconds` 统一口径（支持 1~1800；默认 90；保存预设/测试连接均自动 clamp 到 1800，不再因超上限导致 400）（`frontend\src\pages\PromptsPage.tsx`、`frontend\src\components\prompts\LlmPresetPanel.tsx`、`backend\app\schemas\llm_test.py`、`backend\app\schemas\llm_preset.py`）
- [x] P0 修复：导出文件名解析支持 `filename*=`（UTF-8）+ 修复导出下载 revoke 时机（`frontend/src/services/apiClient.ts`、`frontend/src/pages/ExportPage.tsx`）
- [x] P0 工程化：前端引入 prettier + `npm run format`/`format:check` + `lint:fix`（`frontend/package.json`）
- [x] P0 修复：后端兜底异常错误码改为 `INTERNAL_ERROR`，异常路径日志级别修正为 `error`（`backend/app/main.py`）
- [x] P0 修复：`DATABASE_URL` 配置统一来源（`settings.database_url`）（`backend/app/db/session.py`）
- [x] P0 安全：禁用 `httpx/httpcore` 的 request 日志（避免 Gemini `?key=...` 泄露）；Prompts 页 `LLM_UPSTREAM_ERROR` toast 显示上游 `status_code`（不再固定显示 502）；后端 dev 环境上游错误 `details.upstream_error`（脱敏+截断）便于排错；增强 `sk-` 脱敏（支持 `_`/`-` 网关 key）；`openai_compatible` 参数更保守（减少 400）
- [x] P0 修复：`openai_compatible` 遇到 400/422 时做“参数降级重试”+ OpenAI-like 响应解析兜底（解决“测试连接成功但大纲生成 400”类网关兼容性差异）
- [x] P0 工程卫生：新增根 `.gitignore`；移除 `backend/.env` 与 `backend/*.db`（运行产物）；清理 `__pycache__`；补齐 SQLite WAL/SHM（`*.db-wal/*.db-shm`）忽略
- [x] P1 前端：统一数据加载模式（新增 `frontend/src/hooks/useProjectData.ts` 并在多页复用），并统一网络错误为 `NETWORK_ERROR`（`frontend/src/services/apiClient.ts`）
- [x] P1 前端：localStorage key 前缀集中到 `frontend/src/services/storageKeys.ts`；侧边栏折叠 key 抽到 `frontend/src/services/uiState.ts`；AppShell 标题改为路由元信息表集中管理
- [x] P1 后端：main.py 迁移到 lifespan（移除 import-time side effects）；LLM httpx client 复用 + 超时拆分；base_url 校验抽到 `app/llm/utils.py`；schema provider 类型单点；LLMPreset 移除 key 字段
- [x] P1 后端：增强错误日志可观测性（`AppError`/`VALIDATION_ERROR` 在 JSON 日志输出 `error_code/message/details(白名单)`，不含明文 key，便于按 request_id 定位）
- [x] 丝滑流程（纯前端）：新增统一 `WizardNextBar`（进度/下一步 Dock），接入 Settings/Characters/Prompts/Outline/Writing/Export（`frontend\src\components\atelier\WizardNextBar.tsx`、`frontend\src\hooks\useWizardProgress.ts`）
- [x] 对齐第 11 节脚本：向导步骤顺序调整为「设定 → 角色 → 模型 → 大纲 → 章节 → 写作 → 导出」，并在关键步骤补齐 CTA（保存并下一步/测试连接并下一步/应用并下一步/导出后完成）

## Sprint 6（输出控制系统 v1：解析/长度/流式）

> 说明：`mvp开发计划.md` v2.3 仍以“非 SSE”作为 MVP 口径；本 Sprint 以 **新增可选能力** 的方式实现（不破坏原非流式接口与页面主流程）。

- [x] 后端：更鲁棒输出解析 + 大纲 schema 校验（Pydantic）+ 大纲 JSON 修复二次调用（`backend/app/services/output_parsers.py`）
- [x] 后端：OpenAI/OpenAI-compatible 流式调用（SSE 上游）+ `finish_reason` 记录（`backend/app/llm/client.py`）
- [x] 后端：章节 `target_word_count` + 动态 `max_tokens` 估算 override（不污染 DB preset）（`backend/app/schemas/chapter_generate.py`、`backend/app/services/length_control.py`、`backend/app/api/routes/chapters.py`）
- [x] 后端：SSE 工具 + 流式生成端点（outline/chapter）（`backend/app/utils/sse_response.py`、`backend/app/api/routes/outline.py`、`backend/app/api/routes/chapters.py`）
- [x] 前端：POST SSE 客户端 + Outline/Writing 流式 UI（可取消、失败自动回退非流式）（`frontend/src/services/sseClient.ts`、`frontend/src/pages/OutlinePage.tsx`、`frontend/src/pages/WritingPage.tsx`、`frontend/src/components/writing/AiGenerateDrawer.tsx`）
- [x] P0 修复（Review 热修）：outline 的 JSON 修复失败不再中断主请求（保持 v2.3“解析失败也不阻塞”）；流式调用异常映射为 `AppError`；SSE 流式失败也会写入 `generation_runs`；前端 SSE 早断开检测 + 避免重复 toast；新建项目 `llm_preset.max_tokens` 默认改为 `32000`
- [x] DoD 复核通过（2025-12-15，Review 热修后）：`python -m compileall -q backend\\app backend\\alembic`、`cd frontend && npm run lint`、`cd frontend && npm run build`

## Sprint 7（v2.4：Secrets 落库 + Preview + 响应式 + Writing 体验）

- [x] v2.4 契约文档：API Key/Base URL 等落库；响应/日志不回显明文 key；`X-LLM-API-Key` 仅单次 override（`mvp开发计划.md`）
- [x] 后端：`llm_profiles` 增加 `api_key_ciphertext/api_key_masked` + DPAPI 加密封装（新增 `backend/app/core/secrets.py`）+ 新 Alembic 迁移
- [x] 后端：`/api/llm/test` 支持从 DB 读取 key（`project_id` / `profile_id`），无 header 时可直接测试
- [x] 后端：outline/chapter generate 无 header key 时自动读取项目绑定的 `llm_profile_id`（避免前端再传明文）
- [x] 前端：Prompts 支持 profiles（CRUD/切换）+ Key 保存/清除（只回显 `masked_api_key/has_api_key`），刷新后仍可生成
- [x] 向导：完成度% 修复（按 `chapters.status=done` 统计；写作未全 done 不到 100%）+ 权重调整；Dashboard/WizardNextBar 口径一致
- [x] 向导：项目已完成后再次编辑会恢复“下一步”，同时保留次要按钮“已完成：回到项目概览”
- [x] Writing：流式生成中允许隐藏 AI 面板且不中断；隐藏时显示浮层进度/展开/取消；新增 CTA“保存并开始下一章生成”
- [x] 新增 Preview 页：章节列表（可折叠/移动端抽屉）+ Markdown 阅读渲染 + “编辑”跳转 Writing + 接入向导步骤（Preview→Export）
- [x] AppShell：布局更饱满（`max-w-screen-xl`）+ 平板/手机抽屉导航；Writing/Preview/AI drawer 适配移动端

## 下一步（本轮重点）

- [x] 全面 Code Review（前端/后端；逐字逐句；包含逻辑/契约一致性/可维护性/潜在边界问题）
- [x] DoD 命令复核通过（2025-12-14）：`python -m compileall -q backend\\app backend\\alembic`、`cd frontend && npm run lint`、`cd frontend && npm run build`（chunk warning 允许，属 P2）
- [x] DoD 命令复核通过（2025-12-15）：`python -m compileall -q backend\\app backend\\alembic`、`cd frontend && npm run lint`、`cd frontend && npm run build`（chunk warning 允许，属 P2；含 WizardNextBar 连续导航改动）
- [x] 安全验收（静态，2025-12-15）：全局检索确认无明文 key 入库/打印，SSE payload 不包含 key（搜索：`X-LLM-API-Key`、`Bearer`、`sk-`）
- [ ] 第11节演示脚本（v2.4）：按 UI 手工跑通闭环并记录问题（含 Preview 步骤；LLM 成功路径需真实 Key）
- [ ] 三端响应式回归：手机 390x844 / 平板 768x1024 / PC 1440x900 跑关键路径（Console 0 error；Network 无未处理 4xx/5xx）
- [ ] 最终 DoD（本轮改动后）：`python -m compileall -q backend\\app backend\\alembic`、`cd frontend && npm run lint && npm run build`
- [ ] 手工验收（Output Control v1）：流式大纲/流式章节能收到 progress/chunk/result/done；触发截断时能提示；“取消生成”能中止；SSE 失败能回退非流式
- [ ] （可选）更新项目内已存在的 prompt 模板：历史项目若仍是“章节输出 JSON”，可在 Prompts 页手动改为 `<<<CONTENT>>>/<<<SUMMARY>>>` 分隔格式（新项目默认模板已更新）

## 当前阻塞 / 技术债（必须优先处理）

- [x] React Router 已迁移为 Data Router：`createBrowserRouter` + `RouterProvider`（`useBlocker` 可用）
- [x] 写作/导出页 UI 打磨：按 `ui设计规范.md` 补齐动效与“纸墨质感”细节（生成中指示、转场、细节样式）
- [ ] 工具注意：Windows 下大段 `apply_patch` 仍可能触发 206，建议持续使用“小 patch”策略
- [ ] Vite build 提示 chunk > 500kb（仅 warning，非阻塞；可选后续拆分）
- [x] P1：后端 outline/chapter generate 逻辑去重抽 service（避免重复与 locals hack）
- [x] P1：前端拆分 `WritingPage.tsx` / `PromptsPage.tsx`，降低改动风险

## 已实现（后端接口概览）

- [x] 通用：统一 JSON `{ok,data|error,request_id}`（除导出）、响应头 `X-Request-Id`、CORS expose
- [x] `GET /api/health`
- [x] projects：list/create/get/update/delete
- [x] settings：`GET/PUT /api/projects/{projectId}/settings`（单例：GET 不 404、PUT upsert）
- [x] characters：list/create/update/delete（含归属校验）
- [x] prompts：`GET/PUT /api/projects/{projectId}/prompts`（单例：默认模板种子 + upsert）
- [x] llm_preset：`GET/PUT /api/projects/{projectId}/llm_preset`（单例：默认值 + base_url 校验/trim）
- [x] llm_profiles：`GET/POST/PUT/DELETE /api/llm_profiles`（配置库 + Secrets；响应只回 `has_api_key/masked_api_key`）
- [x] llm_test：`POST /api/llm/test`（可选 `project_id/profile_id`；无 header key 时自动从 DB 读取）
- [x] outline：`GET/PUT /api/projects/{projectId}/outline`（单例）
- [x] outline_generate：`POST /api/projects/{projectId}/outline/generate`（写入 generation_runs，且避免长事务）
- [x] outline_generate_stream（beta）：`POST /api/projects/{projectId}/outline/generate-stream`
- [x] chapters：list/create/bulk_create/get/update/delete（bulk_create replace 规则已实现）
- [x] chapter_generate：`POST /api/chapters/{chapterId}/generate`（不自动落库，仅返回结果 + 写 generation_runs）
- [x] chapter_generate_stream（beta）：`POST /api/chapters/{chapterId}/generate-stream`
- [x] generation_runs：`GET /api/projects/{projectId}/generation_runs?limit=5`、`GET /api/generation_runs/{runId}`
- [x] export：`GET /api/projects/{projectId}/export/markdown`（成功 text/markdown，失败走统一 JSON 错误）

## 已实现（前端概览）

- [x] 主题系统：`data-theme="paper-ink"` × `dark` 正交；本地存储 `ainovel::theme::<user_id>`（MVP 默认 `user_id=local-user`）
- [x] AppShell：Sidebar（折叠/ThemeToggle/ProjectSwitcher）+ 8 页面 + 向导页（含移动端抽屉导航）
- [x] 路由：已迁移 Data Router（支持 `useBlocker` 离开确认）
- [x] Dashboard：项目 CRUD（新建/删除/进入）
- [x] 设定：项目字段 + settings 保存（dirty + Ctrl/Cmd+S）
- [x] 角色卡：CRUD（Drawer）
- [x] 模型配置：LLM preset/profile + 测试连接（`/projects/:projectId/prompts`；API Key/Base URL 等落库到后端配置库 `llm_profiles`，响应只回 `has_api_key/masked_api_key`）
- [x] 大纲：编辑/保存 + AI 生成（预览→应用并保存）+ bulk_create
- [x] 写作：章节 CRUD + Markdown 编辑器 + Ctrl/Cmd+S + dirty 离开/切换确认 + AI 生成 replace/append + RunHistoryDrawer（流式可隐藏面板；支持“保存并开始下一章生成”）
- [x] 预览：章节列表 + Markdown 阅读渲染 + 编辑跳转 Writing（移动端抽屉；PC 可折叠）
- [x] 导出：选项（include_settings/characters/outline + chapters=all|done）+ 导出 Markdown 下载（`apiDownloadMarkdown`）

---

## Prompt System（提示词系统）

### M0（后端最小落地 + 数据迁移）

- [x] 新表：`prompt_presets` / `prompt_blocks`（Alembic migration）
- [x] 后端渲染链路切换：outline/chapter 生成从“块系统”渲染得到 system/user（旧流程无感可用）
- [x] DB 迁移：一次性导入旧 `prompt_templates` → `prompt_presets/prompt_blocks`，并删除 `prompt_templates` 表（不再维护双写/同步）
- [x] CRUD 最小集：presets/blocks + reorder/import/export/preview（供后续 Prompt Studio 接入）

### M1（Prompt Studio + 预览一致性）

- [x] 新页面：`/projects/:projectId/prompt-studio`（预设管理 + 块编辑 + 拖拽排序 + 导入导出）
- [x] 预览一致性：前端预览统一走后端 `POST /api/projects/{projectId}/prompt_preview`
- [x] 旧 Prompts 模板编辑器已移除：提示词仅在 Prompt Studio 编辑（模型配置保留在 `/projects/:projectId/prompts`）

### M2（宏系统 + token 预算 + 可观测）

- [x] 后端渲染升级：Jinja2 + 宏（date/time/isodate/random/pick/注释；seed 可复现）
- [x] token 估算/裁剪：按块统计 + per-block maxTokens + 全局预算；裁剪日志随预览返回
- [x] generation_runs 可观测：新增 `prompt_render_log_json`（缺失变量/每块 token/裁剪原因）

### M3（多阶段链路：规划→写作→润色）

- [x] `plan_chapter`：默认预设 + `POST /api/chapters/{chapterId}/plan` + 标签契约解析
- [x] `plan_first`：章节生成（非流式/流式）支持“先规划→注入 `<PLAN>`→再写作”
- [x] `post_edit`：默认预设 + 章节生成（非流式/流式）可选二次调用润色
- [x] 多厂商 messages 统一：内部 `messages[]` → provider payload；流式也走 messages 适配

### 默认推荐预设（写作学习模板）

- [x] 新增推荐预设：`默认·大纲生成 v3（推荐）`、`默认·章节生成 v3（推荐）`
- [x] 预设列表自动种子：`GET /projects/{projectId}/prompt_presets` 会确保两套推荐预设存在
- [x] 新建项目默认启用：`POST /projects` 会自动把推荐预设的 `active_for` 设为对应任务（老项目不自动接管）
- [x] Prompt Studio 顶部增加说明 + “一键启用推荐预设（大纲/章节）”
- [x] 2025-12-23：Prompt System 质量整顿（宏注释修复/预算按 provider 生效/模板错误可观测/Prompt Studio 预览与拖拽排序修复）
