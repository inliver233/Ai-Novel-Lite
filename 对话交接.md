# ainovel MVP 交接说明（用于下一次对话无缝接续）

契约基线：`mvp开发计划.md` v2.4（最高优先级；不要扩 scope；若改变契约语义必须 bump 并写变更说明）

工作目录：`E:\毕设小说项目\项目\demo`

## 必读文档（优先级）

1) `mvp开发计划.md`（v2.4，唯一契约/验收口径）
2) `ui设计规范.md`（Atelier “Paper & Ink”，必须落地主题系统与纸墨质感）
3) `ainovel开发计划_v2.md`（仅灵感参考，不纳入 MVP）
4) `开发进度.md`（当前实现进度与阻塞点）

## 当前状态（2025-12-21）

- ✅ 已完成：全面 Code Review（并合入最小修复补丁；详见 `开发进度.md`）
- ✅ P0 已修复：前端 `npm run lint` 0 error + prettier/format；Prompts 占位符正则；导出下载 revoke 时机；导出文件名解析支持 `filename*=`；后端兜底错误码 `INTERNAL_ERROR` + 异常路径日志级别修正；`DATABASE_URL` 单一来源；新增根 `.gitignore`；移除 `backend/.env` 与 `backend/*.db`（运行产物）；清理 `__pycache__/*.pyc`；移除 `frontend/dist` 等构建产物；以及侧边栏折叠/空白页等 UI P0
- ✅ 仓库卫生补齐：SQLite WAL/SHM（`*.db-wal/*.db-shm` 等）已纳入根 `.gitignore`（避免误入库/误打包）
- ✅ P1（3.A）已落地：开工向导页（完成度/下一步/跳过/自动模式入口）、Dashboard 展示完成度并对未完成项目默认进入向导
- ✅ P1 工程化已补齐：前端 `useProjectData` 统一数据加载；`apiClient` 统一网络错误为 `NETWORK_ERROR`；localStorage key 前缀集中到 `storageKeys`；sidebar key 抽到 `uiState`；AppShell 标题改为路由元信息表
- ✅ P1 后端工程化已补齐：`main.py` 迁移到 lifespan（无 import-time side effects）；LLM `httpx` client 复用 + 超时拆分；`normalize_base_url` 抽到 `app/llm/utils.py`；schema 的 `LLMProvider` 单点定义；`LLMPreset` 不存 Key（v2.4：Key/URL 等落库在 `llm_profiles`，响应只回 `has_api_key/masked_api_key`）
- ✅ P1 后端可观测性补齐：`AppError`/`VALIDATION_ERROR` 发生时，JSON 日志输出 `error_code/message/details(白名单)`（不含明文 key），便于按 request_id 定位 400/422 等问题
- ✅ 丝滑流程（纯前端）已落地：新增统一 `WizardNextBar`（进度/下一步 Dock），接入 Settings/Characters/Prompts/Outline/Writing/Export；Prompts 支持“测试连接并下一步”；大纲页“应用并下一步”；导出页导出后显示“完成/回到项目概览”
- ✅ 修复：Prompts `timeout_seconds` 口径（最大 1800/30分钟，默认 90）——保存预设/测试连接均自动 clamp 到 1800；后端校验对齐（不再因超上限导致 400）
- ✅ 安全补齐：禁用 `httpx/httpcore` 的 request 日志（避免 Gemini `?key=...` 泄露）；Prompts 页 `LLM_UPSTREAM_ERROR` toast 显示上游 `status_code`；后端 dev 环境上游错误 `details.upstream_error`（脱敏+截断）便于排错；增强 `sk-` 脱敏（支持 `_`/`-` 网关 key）；`openai_compatible` 参数更保守（减少 400）
- ✅ P0 修复：`openai_compatible` 上游 400/422 兼容性增强（参数降级重试 + OpenAI-like 响应解析兜底），用于解决“Prompts 测试连接成功但大纲生成 400”类网关差异
- ✅ DoD 复核通过（2025-12-14）：`python -m compileall -q backend\\app backend\\alembic`、`cd frontend && npm run lint`、`cd frontend && npm run build`（chunk warning 允许，属 P2）
- ✅ DoD 复核通过（2025-12-15）：`python -m compileall -q backend\\app backend\\alembic`、`cd frontend && npm run lint`、`cd frontend && npm run build`（chunk warning 允许，属 P2；含 Output Control v1 + WizardNextBar 连续导航改动）
- ✅ 已新增（可选 beta，不破坏原非流式契约）：Output Control System v1（更稳健解析/章节字数控制/流式 SSE 进度 + chunk）
- ❌ 未做：3.B（后端持久化 wizard_step/status）
- ⚠️ 仍需：按 `mvp开发计划.md` 第 11 节做一次完整 UI 演示脚本手工跑通（LLM 成功路径需真实 Key）

- ✅ P0 热修（2025-12-15）：outline 的 JSON 修复失败不再中断主请求（保持 v2.3“解析失败也不阻塞”）；`call_llm_stream` 捕获 httpx 异常并映射 `AppError`；SSE 流式失败也会写入 `generation_runs`；前端 SSE 早断开检测 + 避免重复 toast；写作页“直接生成”文案修正
- ✅ 默认值调整（2025-12-15）：新建项目 `llm_preset.max_tokens` 默认改为 `32000`（用于减少大纲/章节被截断概率；若上游不支持会自动降级重试）

- ✅ v2.4（P0）已落地：LLM Secrets 落库（`llm_profiles.api_key_ciphertext/api_key_masked` + Windows DPAPI 加密；响应不回显明文 Key；`X-LLM-API-Key` 仅单次 override）
- ✅ v2.4（P0）已落地：Prompts 页支持 profiles（CRUD/切换）+ Key 保存/清除；刷新/重开项目无需重新配置仍可生成（前端默认不再 localStorage/header 传 Key）
- ✅ 小任务已落地：向导完成度算法修复 + 权重调整（写作=全部章节 `status=done` 才 100%）；项目已完成后再编辑会恢复“下一步”，同时保留次要按钮“已完成：回到项目概览”
- ✅ Writing 已增强：流式生成中允许隐藏 AI 面板且不中断（隐藏时浮层显示进度/展开/取消）；新增 CTA“保存并开始下一章生成”
- ✅ 新增 Preview 页：章节列表（PC 可折叠；移动端抽屉）+ Markdown 阅读渲染 + 编辑跳转 Writing + 向导步骤（Preview→Export）
- ⚠️ 待回归：按 v2.4 E2E 脚本全流程 + 三端 viewport + 最终 DoD（compileall / lint / build）；当前工作区存在未提交改动（含新 migration/PreviewPage/secrets）

## 当前实现概况（已落地）

### 0) 仓库结构

- `backend/`：FastAPI + SQLAlchemy + Alembic + SQLite（默认）+ LLM adapters
- `frontend/`：React + TS + Vite + Tailwind（语义 CSS variables 分层主题）

### 1) 后端（已实现）

入口与中间件：
- `backend/app/main.py`：统一 `X-Request-Id` + JSON 日志；统一错误响应（除导出外）；CORS（expose `X-Request-Id`）；未知异常返回 `INTERNAL_ERROR`；使用 FastAPI lifespan 启动时配置日志并初始化 `local-user`
- `backend/app/db/session.py`：SQLite pragmas（WAL/busy_timeout 等）；SQLite 模式设计为单 worker
- `backend/alembic/`：已初始化迁移；默认 SQLite 数据库文件运行时生成 `backend/ainovel.db`（不提交）

数据模型（已按 v2.3 建表）：
- `users/projects/project_settings/characters/outline/chapters/prompt_templates/llm_presets/generation_runs`

核心 API（均已按“统一响应”实现；导出除外）：
- `GET /api/health`
- projects：`GET/POST/GET/PUT/DELETE /api/projects...`
- settings（单例）：`GET/PUT /api/projects/{projectId}/settings`（GET 不 404、PUT upsert）
- characters：`GET /api/projects/{projectId}/characters`、`POST .../characters`、`PUT/DELETE /api/characters/{characterId}`（含归属校验 404）
- outline（单例）：`GET/PUT /api/projects/{projectId}/outline`
- outline_generate：`POST /api/projects/{projectId}/outline/generate`（Header 校验 provider；写入 generation_runs；避免长事务）
- outline_generate_stream（beta）：`POST /api/projects/{projectId}/outline/generate-stream`（SSE：progress/chunk/result/done）
- chapters：`GET/POST /api/projects/{projectId}/chapters`、`POST .../chapters/bulk_create?replace=...`、`GET/PUT/DELETE /api/chapters/{chapterId}`
- chapter_generate：`POST /api/chapters/{chapterId}/generate`（replace/append；**不自动落库**；写 generation_runs；避免长事务）
- chapter_generate_stream（beta）：`POST /api/chapters/{chapterId}/generate-stream`（SSE：progress/chunk/result/done；支持 cancel；失败可回退非流式）
- prompts（单例）：`GET/PUT /api/projects/{projectId}/prompts`（默认模板种子 + upsert）
- llm_preset（单例）：`GET/PUT /api/projects/{projectId}/llm_preset`（默认值 + base_url trim + scheme 校验；不存 key）
- llm_test：`POST /api/llm/test`（要求 Header `X-LLM-Provider` 与 body.provider 一致）
- generation_runs：`GET /api/projects/{projectId}/generation_runs?limit=5`、`GET /api/generation_runs/{runId}`
- export：`GET /api/projects/{projectId}/export/markdown`（成功返回 `text/markdown`；失败走统一 JSON 错误）

LLM 适配器：
- `backend/app/llm/client.py`：支持 `openai/openai_compatible/anthropic/gemini`；新增 `call_llm_stream()`（仅 openai/openai_compatible）；`call_llm()` 与流式均记录/透出 `finish_reason`；base_url 校验由 `backend/app/llm/utils.py` 统一；LLM 调用使用 `backend/app/llm/http_client.py` 复用 client；`openai_compatible` 遇到上游 400/422 会做参数降级重试（动作记录在 `error.details.compat_adjustments`）；OpenAI-like 返回解析更兼容（如 `choices[0].text`、Responses-like `output[].content[].text`）；错误码映射按 v2.3；不输出明文 key（上游错误内容已做最小脱敏，不包含 key）
- Prompt 渲染：`backend/app/services/prompting.py`（`{{placeholder}}`）已修复；generation_runs 会记录渲染后的 prompt
- 输出控制（新增）：`backend/app/services/output_parsers.py`（更鲁棒 JSON 抽取；outline schema 校验；chapter 支持 `<<<CONTENT>>>/<<<SUMMARY>>>` 分隔；outline 失败可二次调用“修复 JSON”）
- 字数控制（新增）：`backend/app/services/length_control.py`（按 `target_word_count` 估算本次 `max_tokens` override；不污染 DB preset）
- SSE 工具（新增）：`backend/app/utils/sse_response.py`（统一事件格式 + 断线 close，避免资源泄漏）

### 2) 前端（已实现）

主题系统（Paper & Ink）：
- `frontend/index.html`：启动时读取 `ainovel::theme::<user_id>`（MVP 默认 `user_id=local-user`）并设置 `<html data-theme>` 与 `.dark`
- `frontend/src/index.css` + `frontend/tailwind.config.js`：按 v2.3 的“两层变量”结构；组件只消费语义变量

全局基础设施：
- `frontend/vite.config.ts`：`/api` 代理到 `http://localhost:8000`
- Toast：Provider `frontend/src/components/ui/ToastProvider.tsx` + hook `frontend/src/components/ui/toast.ts`（错误 toast 支持复制 request_id）
- ConfirmDialog：Provider `frontend/src/components/ui/ConfirmProvider.tsx` + hook `frontend/src/components/ui/confirm.ts`（含三选一 `choose()`，用于写作页生成/切换确认）
- ProjectSwitcher/ThemeToggle/AppShell：`frontend/src/components/layout/AppShell.tsx` 等
- API Client：`frontend/src/services/apiClient.ts`（统一解析 `{ok,data|error,request_id}`；网络错误统一为 `ApiError(code="NETWORK_ERROR")`）
- SSE Client（新增）：`frontend/src/services/sseClient.ts`（POST SSE；支持自定义 headers；支持 abort；消费 progress/chunk/result/error/done）
- 数据加载 hook：`frontend/src/hooks/useProjectData.ts`（统一项目页加载/刷新模式）
- user_id 单点来源：`frontend/src/services/currentUser.ts`（读取 `ainovel::auth::user_id`，默认 `local-user`）
- LLM Key（v2.4）：默认存后端数据库（`llm_profiles`；响应只回 `has_api_key/masked_api_key`）；`frontend/src/services/llmKeyStore.ts` 可保留为“临时 override”（可选/兼容旧规则）
- 开工向导进度：`frontend/src/services/wizard.ts`（skip/测试连接/导出成功等本地标记）
- localStorage key：`frontend/src/services/storageKeys.ts`（前缀/拼接单点）；`frontend/src/services/uiState.ts`（UI 状态 key 单点）
- Router：已迁移到 Data Router（`createBrowserRouter` + `RouterProvider`，`useBlocker` 可用）

已完成页面（可与后端联动）：
- Dashboard：`frontend/src/pages/DashboardPage.tsx`（项目新建/删除/进入；展示完成度；未完成默认进入向导；新建后按第 11 节脚本进入设定页）
- 开工向导：`frontend/src/pages/ProjectWizardPage.tsx`（完成度/下一步建议/自动模式入口）
- 设定：`frontend/src/pages/SettingsPage.tsx`（项目字段 + settings；dirty + Ctrl/Cmd+S）
- 角色卡：`frontend/src/pages/CharactersPage.tsx`（CRUD + Drawer）
- Prompts：`frontend/src/pages/PromptsPage.tsx`（preset + templates + preview + 测试连接；Key/URL 落库并可复用配置；错误码提示映射；测试成功会更新向导进度）
- 大纲：`frontend/src/pages/OutlinePage.tsx`（编辑/保存 + AI 生成预览→应用并保存 + 从大纲 bulk_create；可选 beta：流式生成）
- 写作：`frontend/src/pages/WritingPage.tsx`（章节 CRUD + Markdown 编辑器 + AI 生成 replace/append + RunHistoryDrawer；可选 beta：流式生成（可隐藏面板）+ 目标字数 + 取消生成；支持“保存并开始下一章生成”）
- 预览：`frontend/src/pages/PreviewPage.tsx`（章节列表 + 阅读器（Markdown）+ 隐藏/抽屉 + 编辑跳转 + 下一步到导出）
- 导出：`frontend/src/pages/ExportPage.tsx`（导出选项 + Markdown 下载；导出成功会更新向导进度）
- 页面转场：`frontend/src/components/layout/AppShell.tsx`（framer-motion Fade Up）
- 生成中提示：`frontend/src/components/atelier/GhostwriterIndicator.tsx`（写作/导出页复用）

前端构建验证：
- `cd frontend && npm run build` 已通过（TypeScript + Vite；仍有 chunk > 500kb 警告，非阻塞）

## 已知阻塞点 / 技术债（下一步重点）

1) **第 11 节演示脚本仍需按 UI 完整跑通**
   - 现状：核心页面均已落地，但仍需按 `mvp开发计划.md` 第 11 节从 UI 端到端验证闭环（LLM 相关步骤需要真实 API Key）。
   - 处理：启动前后端后，按脚本逐步操作并修复发现的问题；如改变契约语义必须 bump v2.4。

2) **UI 规范打磨（MVP基础已补齐）**
   - 已补齐页面转场（Fade Up）与生成中“墨迹”提示；AppShell/Preview/Writing 已做三端响应式基础适配；如需更细腻动效可继续微调（不影响 v2.4 验收）。

## 下一步执行清单（按第 11 节演示脚本闭环）

按优先级：
1) 按 `mvp开发计划.md` 第 11 节演示脚本（v2.4）UI 端到端跑通闭环，并记录/修复问题：
   - Prompts：新建/选择后端配置（profiles）→ 保存 Key → 测试连接；刷新确认无需重新配置仍可生成
   - Outline：生成（非流式至少 1 次；流式至少 1 次）→ 应用并保存 → 从大纲 bulk_create（建议 ≥10 章用于压测向导完成度）
   - Writing：生成第 1 章（replace/append）→ 验证“保存并开始下一章生成”；验证流式时 AI 面板可隐藏且不中断（浮层进度/展开/取消）；保存章节；按需要将章节状态设为 `done`（向导以 `done` 判定“写完”）
   - Preview：章节列表/阅读/隐藏/编辑跳转/下一步到导出（WizardNextBar 也应可用）
   - Export：导出 Markdown 成功下载；回到 Dashboard 检查完成度% 与“完成后编辑恢复下一步”
   - 若大纲/章节生成仍返回 `LLM_BAD_REQUEST`（HTTP 400/422）：在浏览器 DevTools → Network 里复制失败响应 JSON（重点看 `error.details.status_code`、`error.details.upstream_error`、`error.details.compat_adjustments`）+ `request_id`（响应体 `request_id` 或响应头 `X-Request-Id`），用于精确定位网关不兼容字段（不要复制/截图任何 API Key）
2) 三端响应式回归（必须用 chrome-devtools-mcp）：
   - 手机：390x844；平板：768x1024；PC：1440x900
   - 每个尺寸至少验证：Dashboard 新建/进入、Prompts 保存与测试、Outline 保存与生成、Writing 保存与生成（含流式）、Preview/Export
   - Console 0 error；Network 无未处理 4xx/5xx
3) DoD 回归（最终必须全通过）：
   - `python -m compileall -q backend\\app backend\\alembic`
   - `cd frontend && npm run lint && npm run build`

## 本地运行/验证（建议按顺序）

后端：
```powershell
cd backend
copy .env.example .env
.\.venv\Scripts\alembic.exe -c alembic.ini upgrade head
# 若 alembic.exe 不可用，可用：
# .\.venv\Scripts\python.exe -m alembic -c alembic.ini upgrade head
.\.venv\Scripts\uvicorn.exe app.main:app --reload --workers 1 --port 8000
# 若 uvicorn.exe 不可用，可用：
# .\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --workers 1 --port 8000
```

前端：
```powershell
cd frontend
npm install
npm run dev
```

（可选）构建校验：
```powershell
cd frontend
npm run lint
npm run build
# （可选）统一格式化：
# npm run format
```

后端（语法检查）：
```powershell
python -m compileall -q backend\\app backend\\alembic
```

## 重要约束提醒（别踩红线）

- 严格按 `mvp开发计划.md` v2.4：核心页面 + API 契约、错误码、生成不落库、bulk_create replace 规则、header/body provider 一致性
- LLM Secrets 必须落库（`llm_profiles`）；但严禁在日志/响应/导出/前端回显明文 Key；生成/测试连接默认不再从前端传 Key（允许 `X-LLM-API-Key` 单次 override）
- SQLite 模式必须单 worker；LLM 调用前不能持有 DB 长事务（当前实现已做：先读库→关 session→请求上游→新 session 落库）
