# ainovel · 长期记忆系统完整实现规划（v1.1 初版可执行：可落地施工图）

> 日期：2026-01-08  
> 适用范围：`demo/` 现有 MVP（FastAPI + SQLAlchemy + Alembic；React + TS + Vite + Tailwind；默认 SQLite；任务队列 RQ/Redis 可选）。  
> 参考实现：  
> - Amily：`Amily/ST-Amily2-Chat-Optimisation`（表格系统：白名单执行器 + 延迟删除；世界书：蓝/绿灯递归触发；RAG：分来源分块+rerank+超级排序；关系图谱：命中实体+1-hop 注入；分形记忆：scene→arc→saga；文本优化：标签块替换式重写）。  
> - MuMuAINovel：`mumu/MuMuAINovel`（Cookie 会话 + 过期刷新、Postgres+Alembic+Compose、PlotAnalysis+StoryMemory（含 position/length 标注）、Chroma PersistentClient 向量检索与组合上下文、章节标注 UI、写作风格资产（prompt_content + 项目默认风格））。  
> 产出目标：把“长期记忆系统”从概念升级为可开工的施工图（算法/数据/接口/改动范围/阶段 DoD/风险与降级/可观测与回溯）。  
> 过程记录：`demo/temp-process.md`；任务清单：`demo/todolist.md`。  
> 约束：本回合只改文档，不改业务代码，不提交。

---

## 0. 如何使用本文（读者 / 产物 / 版本）

- **读者**：未来负责把 ainovel 从 MVP 推进到“可解释、可回溯、可扩展的长篇一致性系统”的开发者。
- **使用方式**：
  1) 按 Phase 拆 Issue（每个 Phase 的 DoD = 合并门槛）；  
  2) 做任何关键决策（DB/向量库/embedding）都要回填本文件与 `demo/temp-process.md`；  
  3) 上线/迁移相关决策必须同时更新降级策略与回滚路径。
- **版本策略**：本文是 v1.1（初版可执行）。后续每完成一个 Phase：更新该 Phase 的 DoD 与“实现差异”。

### 0.1 v1.0 → v1.1 本轮增强点（以“能开工”为准）

- 修正/补齐“以代码为准”的 demo 关键事实：真实 API 形态（`/api/chapters/{id}/...`）、`render_values` 命名空间、`prompt_render_log_json` 结构、generation_runs 返回字段缺口、RQ 封装现状与可扩展点、auth 插入点（见 1.2/3.4/13）。
- 把 Amily 的“可迁移具体做法”写到可执行层：白名单执行器（函数白名单+参数容错+延迟删除）、蓝/绿灯递归触发细节、RAG 分来源分块+包装标签、rerank+超级排序排序键、GraphContext 输出结构、分形阈值压缩与注入（见 7~11）。
- 把 MuMuAINovel 的“工程化落地”写到 ainovel 的落点：Cookie 会话+刷新、Postgres+迁移+compose、PlotAnalysis/StoryMemory/annotations 的数据闭环、smart_context 升级策略、写作风格资产与注入优先级、任务状态表与后台分析流程（见 6/12/13）。
- 砍掉含糊“建议/可选”：为每个 Phase 给出默认决策、关键参数、失败/降级策略、DoD 与验证清单；并把“实验路线”单独标注（见 4/14）。

---

## 1. 现状事实基线（必须承认现实）

### 1.1 当前 demo 已具备的底座（可复用）

- Prompt System（块系统）：`backend/app/services/prompt_presets.py#render_preset_for_task`  
  - 支持：Jinja2 模板渲染、宏、缺失变量统计、覆写/forbid_overrides、预算裁剪（block maxTokens + 全局 budget）、absolute/relative 注入。
- 上下文拼装：`backend/app/services/chapter_context_service.py`  
  - 现有上下文来源：`project_settings`（世界观/风格/约束）、`characters`、`outlines.content_md`、上一章注入、`smart_context`（摘要/正文节选/骨架 stride）。
- 可观测：`generation_runs` 保存 prompt/output/error + `prompt_render_log_json`（块级 token 与裁剪/缺失/模板错误）。
- 队列：`backend/app/services/task_queue.py`（RQ/Redis 或 inline）+ `batch_generation_tasks`（任务表 + item 表）+ worker 入口 `backend/scripts/run_rq_worker.py`。
- 权限隔离雏形：`projects.owner_user_id` + `require_owned_*`（`backend/app/api/deps.py`）；但当前 `get_current_user_id()` 固定 `local-user`（`backend/app/main.py` 会创建本地用户）。

### 1.2 关键“对齐点”（本文后续设计必须贴合的真实细节）

#### 1.2.1 `render_values` 的真实形态（决定 prompt 注入方式）

`chapter_context_service.py#assemble_chapter_generate_render_values` 同时提供：
- 扁平 key：`world_setting/style_guide/constraints/characters/outline/...`
- 命名空间：`project.{...}`、`story.{...}`、`user.{...}`
- `plan_first` 注入：`inject_plan_into_render_values()` 把 `<PLAN>` 追加到 `instruction`，并设置 `story_plan` 与 `story.plan`

结论：**长期记忆建议统一挂载到 `memory.*`**，并复用 PromptBlock 的“点号路径解析”能力（`prompt_presets.py#_try_get_marker_value`）。

#### 1.2.2 `prompt_render_log_json` 的真实结构（决定可观测字段设计）

`prompt_presets.py#render_preset_for_task` 返回：
```json
{
  "task": "chapter_generate",
  "preset_id": "...",
  "prompt_budget_tokens": 24000,
  "prompt_tokens_estimate": 12345,
  "missing": ["story.xxx"],
  "blocks": [
    {
      "id": "...",
      "identifier": "sys.story.outline",
      "role": "system",
      "priority": "important",
      "max_tokens": 4200,
      "tokens_before": 5000,
      "tokens_after": 4200,
      "trimmed": true,
      "dropped": false,
      "reason": "block_max_tokens:4200",
      "render_error": null
    }
  ]
}
```
结论：记忆系统新增的“检索/注入可观测”应当沿用这种结构化日志风格（不要只塞一段大字符串）。

#### 1.2.3 RQ/Redis 能力现状（决定异步管线落地方式）

- 现有队列仅封装 `enqueue_batch_generation_task(task_id)`；RQ job 使用 `job_id=task_id` + `meta.kind=batch_generation`。  
- 现有 DB 任务表形态（批量生成）可复用到 memory/update/embed/rebuild（任务状态、幂等、失败可追踪）。

---

## 2. 目标 / 非目标 / 术语（避免团队跑偏）

### 2.1 目标（验收口径）

当写到第 N 章（N 很大）时：
- **一致性**：人物设定/状态变化、关系网、世界规则、关键事件与后果、伏笔与回收、时间线/地点迁移能被稳定记住并可检索。
- **可解释**：每次生成可以回答：注入了哪些记忆、来自哪里、为什么命中、被裁剪/去重/重排的原因。
- **可编辑可回滚**：记忆条目可人工修正；自动写入必须先走“提议→审核→应用”；出错可回滚或软删除。
- **可生产化**：支持多用户隔离；可迁移到 Postgres；可 Docker compose 一键启动；可观测与审计可用于排障。

### 2.2 非目标（本规划明确不做或后置）

- 不做“完全自动写书 agent”（自动拆任务/自动改全书）；优先做“可靠的人在环工具”。
- 不做模型微调/训练（先把数据资产与评测体系做出来）。
- 不追求一次性把 Amily 全量能力照搬（只迁移可落地、可验证的结构与算法）。

### 2.3 术语（统一命名）

- **WorldBook（世界书）**：人工维护的条目库（常驻/关键词触发/递归触发），生成时注入规则与资料。
- **PlotAnalysis（剧情分析）**：章节级结构化分析（hooks/foreshadows/plot_points/character_states/评分）。
- **StoryMemory（故事记忆）**：从章节抽取出的多类型记忆片段（可用于语义检索、伏笔追踪、标注高亮）。
- **Structured Memory（结构化记忆）**：可约束的事实表（entities/relations/events/foreshadows…），支持受限更新。
- **Vector Store（向量库）**：从章节/世界书/记忆分块生成 embedding，用语义召回。
- **Evidence（证据）**：对记忆更新/冲突提示的引用依据（章节版本、文本位置、原文片段、hash）。
- **ChangeSet（变更集）**：一次 memory_update 的“提议变更”载体（可审计/可回放/可回滚）。

---

## 3. 总体架构（双闭环：生成前检索 + 生成后写入）

### 3.1 生成前：ContextPack → PromptBlocks（不推翻现有 Prompt System）

数据流（推荐）：
```
WritingPage(AiGenerateDrawer 的 context 开关)
  -> POST /api/chapters/{id}/generate
      -> build_chapter_generate_render_values()  # demo 已有
      -> memory_retrieval_service.build_pack()   # 新增：输出 MemoryContextPack
      -> render_values["memory"] = pack          # 命名空间注入
      -> render_preset_for_task()                # demo 已有：预算/裁剪/日志
      -> call_llm_and_record()                   # demo 已有：generation_runs 审计
```

关键原则：
- **记忆注入是“块”而不是“大 prompt”**：新增 `sys.memory.*` PromptBlocks，并复用预算裁剪。
- **smart_context 保留为兜底**：Phase 0~4（记忆系统未覆盖/向量不可用/检索不稳定时）仍可用；当 Phase 4A/4B + Phase 5 的检索稳定后再逐步降权，但永远保留 fallback 开关。

### 3.2 生成后：Propose → Review → Apply → Jobs（安全写入闭环）

写入闭环（推荐）：
```
章节生成/保存/定稿 (status=done 或用户点击“更新记忆”)
  -> POST /api/chapters/{id}/memory/propose
      - LLM 输出严格 JSON（受限操作）
      - 后端校验 + 生成 ChangeSet(diff/warnings)
  -> UI 人在环：逐条 accept/reject
  -> POST /api/memory_change_sets/{id}/apply
      - 单事务落库（结构化表 + evidence + 审计）
      - 事务外 enqueue embed/rebuild/graph/fractal
```

关键原则：
- **LLM 调用前后必须切事务**（沿用 demo 的 generation_runs 写入模式：短 session/短事务）。
- **默认不允许“自由写表/自由删”**：必须走白名单操作 + 幂等键 + 审计。

### 3.3 选稿/定稿（Commit）策略：两档实现（必须先定口径）

长期记忆写入有一个硬问题：**草稿污染**。demo 目前 `chapters` 只有单份 `content_md/summary/plan`，但 `generation_runs` 会保存历史输出。这里给两档方案：

#### 方案 A（先做，成本最低）：以 `chapters.status=done` 作为“定稿”信号

- 规则：
  - 只有 `chapter.status == "done"` 才允许 `memory/propose` 与 `memory/apply`（默认）。
  - 写入的源文本固定使用 `chapters.content_md/summary/plan`（即“当前章保存稿”）。
  - UI 上必须明确：**保存 ≠ 定稿**；定稿需要用户额外确认（按钮/弹窗）。
- 需要改动（实现阶段）：
  - 后端：在 `backend/app/api/routes/chapters.py` 的 memory 相关接口里强制校验 `status=="done"`（或提供 override 权限）。
  - 前端：WritingPage 增加“标记为定稿/取消定稿”入口；定稿后显示 Memory Update 按钮。
- 风险：
  - 用户可能在 done 后仍修改正文导致“记忆与正文不一致”。需要 DoD：定稿后编辑必须自动把 status 打回 draft，或者对编辑动作做确认。

#### 方案 B（后置但更完整）：章节版本表 + selected_version_id（可回放/可比对）

- 规则：
  - 引入 `chapter_versions`，每次“保存为版本”都会固化一份 `content_md/summary/plan/source_run_id`。
  - `chapters.selected_version_id` 指向唯一选稿；memory_update 只读 selected_version。
- 需要新增表（实现阶段）：
  - `chapter_versions(id, project_id, chapter_id, outline_id, source_generation_run_id, content_md, summary, plan, created_at)`
  - `chapters.selected_version_id`（nullable，FK）
- UI（参考 mumu 的“标注回溯”思路）：
  - RunHistory → “保存为版本” → 版本对比 → “设为定稿”
  - Memory Update/检索/标注均以 selected_version 的内容为准（evidence pointer 也能更稳定）
- 依赖：Phase 0 的 UI/数据流插入点已就绪后再做（建议 Phase 2 之后落地）。

### 3.4 异步任务编排（RQ/Redis）：统一任务表 + 幂等 job_id

demo 已有可复用范式：`batch_generation_tasks` + `task_queue.py`（RQ job_id=task_id）。记忆系统建议完全复用这个“任务表 + worker”范式：

- 新增（或复用同构）表：`memory_tasks` / `memory_task_items`
  - `kind`：`memory_update_propose` / `memory_apply` / `vector_ingest` / `vector_rebuild` / `graph_update` / `fractal_update`
  - `status`：queued/running/succeeded/failed/canceled
  - `params_json/result_json/error_json`：用于回放与 UI 展示
- TaskQueue 扩展建议（实现阶段）：
  - `backend/app/services/task_queue.py`：从仅 `enqueue_batch_generation_task` 扩展为 `enqueue(kind, task_id)`（或新增 `enqueue_memory_task`），并统一写 `job.meta.kind`。
- 幂等键（必须第一天设计）：
  - `idempotency_key = hash(project_id + chapter_id + selected_version_id + kind + input_hash)`
  - 同 key 若已存在 succeeded：直接返回旧结果；若 running：返回 task_id（避免重复跑）

---

## 4. Phase 路线图（路线 + DoD + 依赖：按“最小可交付”拆）

> 说明：每个 Phase 都必须满足三条硬门槛：**不破坏现有写作链路、可回溯（能解释注入/写入）、可降级（缺依赖时仍可写作）**。

### 4.0 总览（推荐默认路线）

这份规划刻意拆成两条可选路线：
- **内容能力线（SQLite 也能跑）**：P0→P1→P2→(P4A Chroma)→P5→P6→P7（先把“能记住/能回溯/能手工修正”做出来）。
- **生产化线（多人/部署/性能）**：P3（Auth+Postgres+Worker）→(P4B pgvector)（把系统带到可部署、可扩展状态）。

两条线可以并行推进，但**P4B(pgvector)** 必须建立在 **P3(Postgres)** 之上（避免依赖循环）。

### 4.1 难度与风险评估（用于排期，不是空话）

| Phase | 交付核心 | 难度 | 主要风险 | 硬依赖 |
|---|---|---:|---|---|
| 0 | MemoryContextPack 骨架 + 调试面 + 观测结构 | 低 | 接口反复改导致返工 | 无 |
| 1 | WorldBook（蓝/绿灯 + 递归） | 低-中 | 触发噪声/截断策略不当 | 0 |
| 2 | PlotAnalysis + StoryMemory + Annotations UI | 中 | JSON 不稳、证据定位不准、标注重叠 | 0 |
| 3 | 生产化地基：Auth+RBAC+Postgres+Compose+Worker | 高 | 权限漏洞/迁移窗口/运维复杂度 | 0 |
| 4A | Vector RAG（Chroma 路线，SQLite 期） | 中 | 向量库持久化/重建、filter 局限 | 0 |
| 4B | Vector RAG（pgvector 路线，生产化） | 高 | 索引调参/overfiltering/线上观测不足 | 3 |
| 5 | Structured Memory + ChangeSet（安全写入闭环） | 高 | 幂等/回滚/冲突检测难 | 2 |
| 6 | GraphContext + Fractal Memory | 中-高 | 召回噪声/摘要漂移/注入膨胀 | 5（Graph），2/5（Fractal） |
| 7 | Style Profiles + 去味/一致性修复链 | 中 | 质量主观/评测口径不清/成本 | 0 |

### Phase 0：基础地基（不改变生成效果）

目标：把“插入点、接口形态、日志形态、调试入口”先固化，后续模块只填实现。

DoD（最小集）：
- 后端：定义 `MemoryContextPack` schema（允许空包，但字段结构稳定）。
- 后端：新增 `GET /api/projects/{project_id}/memory/retrieve`（调试用：仅返回 pack 与 logs，不参与生成）。
- 后端：在生成链路中预留 `memory_retrieval_log_json` 的结构（可先放进 `generation_runs.params_json`，后续再决定是否拆 artifact 表）。
- 前端：WritingPage 增加 `ContextPreviewDrawer`（能展示空包 + 日志占位），并在 `AiGenerateDrawer` 增加“记忆注入开关（占位）”。

### Phase 1：WorldBook（优先落地，快速见效）

目标：用最小成本获得“规则注入 + 可控递归触发”，并为后续结构化记忆提供资料源。

DoD：
- WorldBook CRUD + `preview_trigger`（含 enable_recursion / exclude_recursion / prevent_recursion / char_limit）。
- Prompt System：在推荐预设（例如 `chapter_generate_v4`）新增 `sys.memory.worldbook` block，可在 UI 勾选启用/禁用。
- 可观测：retrieval pack 中返回 `triggered[]`（含 reason）+ `truncated`；并在 ContextPreviewDrawer 可查看。

### Phase 2：PlotAnalysis + StoryMemory（mumu 风格：分析→记忆→标注）

目标：先做“统一记忆表”闭环：能抽取、能检索、能标注回溯；为 Phase 5 的结构化写入提供原材料。

DoD：
- 新表：`plot_analysis` + `story_memories`（必须含 `memory_type/importance/tags/story_timeline/text_position/text_length/is_foreshadow`）。
- 章节分析：在 demo 现有 `/api/chapters/{id}/analyze` 基础上增量，做到“可选择落库 + 生成 story_memories（含 chapter_summary）”。
- 标注页：新增 `ChapterAnalysisPage`（或在现有分析 modal 扩展）支持“正文高亮 + 右侧记忆侧栏”，并处理重叠标注（参考 mumu 的 AnnotatedText 分片策略）。
- 检索雏形：新增 `GET /api/chapters/{id}/annotations`（返回 position/length + type/title/importance/tags）。

### Phase 3：生产化地基（Auth + RBAC + Postgres + Compose + Worker）

目标：把 demo 从单机 local-user 演进到可部署多人系统，且不破坏现有项目数据；同时为 pgvector/多 worker 打地基。

DoD：
- Auth：至少一种登录可用（推荐 Cookie 会话，参考 mumu），并把 `get_current_user_id()` 改为从 `request.state.user_id` 解析（保留 dev fallback）。
- RBAC：新增 `project_memberships`（owner/editor/viewer），把 `require_owned_*` 升级为 `require_project_access`（读写分级）。
- Postgres：跑通 PG 迁移（含 `uuid-ossp`/`pg_trgm` 扩展）；SQLite 数据迁移脚本与验证清单齐全（见 13.3）。
- Compose：`frontend + backend + postgres + redis + rq_worker` 一键启动；卷挂载、日志、密钥管理写清楚。

### Phase 4A：Vector RAG（Chroma 路线：SQLite 期可用）

适用：不切 PG 先验证“语义召回是否有效”（对齐 mumu 的双存储思路）。

DoD：
- Chroma PersistentClient 目录可配置并可持久化（或明确可重建策略）；collection 按 `user_id + project_id` 隔离（参考 mumu 的哈希命名规避限制）。
- `POST /api/projects/{project_id}/vector/query` 调试接口可用：返回候选/去重/排序/裁剪与 logs。
- 生成注入：在 MemoryContextPack 中填充 `vector_rag.snippets[]` 与 `text_md`，并有 PromptBlock 可注入。

### Phase 4B：Vector RAG（pgvector 路线：生产化）

适用：准备进入多人/部署/性能；希望“一套 DB 覆盖结构化 + 向量”。**依赖 Phase 3(Postgres)。**

DoD：
- pgvector 跑通：`vector_chunks.embedding` 入库；HNSW/IVFFlat 策略可配置；处理 overfiltering（iterative scan）有明确开关与降级。
- hybrid：FTS + vector → 融合（RRF）→ 去重 → 超级排序（叙事顺序）。
- 调试页：展示 query、filters、candidates、final、scores、dropped reasons、注入预览（便于排障/调参）。

### Phase 5：Structured Memory + Memory Update（Amily 精华迁移：受限操作 + ChangeSet）

目标：把“记忆写入”升级为可控的结构化事实库：可审计、可回滚、可冲突提示，且能派生图谱与更强召回。

DoD：
- 表：entities/relations/events/foreshadows/evidence/change_sets（含 unique/索引/软删/幂等键）。
- 协议：`memory_update_v1` 严格 JSON（白名单 op）；`propose → review → apply → rollback` 闭环跑通。
- UI：Memory Update 面板（diff 分组、逐条 accept/reject、apply 任务状态、失败可追溯）。

### Phase 6：GraphContext + Fractal Memory（派生能力）

DoD：
- Graph：命中实体 + 1-hop 邻居扩散，输出 `<GraphContext>` 并可视化（参考 Amily executor）。
- Fractal：scene→arc→saga 阈值压缩、可配置触发点、注入预算控制（参考 Amily），并支持回放/重建。
- 归档：旧向量/旧事件软删与重建索引策略（必须写清触发条件与成本）。

### Phase 7：Style Profiles + 去 AI 味 + 一致性修复（与 post_edit 融合）

DoD：
- style_profiles/写作风格资产落库：支持“系统预设 + 用户自定义 + 项目默认风格”（参考 mumu 的 writing_styles + project_default_styles 模式）。
- 生成/后处理链：风格注入（系统优先级）+ 去味（AI_DENOISING 类模板）+ 一致性修复（可选），并可与 Memory Update 联动（例如：检测到设定冲突时提示“是否重写相关段落”）。

---

## 5. 统一输出结构：MemoryContextPack（Prompt / UI / 日志三方共享）

### 5.1 结构定义（推荐）

后端输出（用于 `/memory/retrieve`、生成时注入、以及 future logs）：
```json
{
  "worldbook": {
    "triggered": [{"id":"...","title":"...","reason":"constant|keyword:xxx"}],
    "text_md": "..."
  },
  "story_memory": {
    "items": [{"id":"...","type":"foreshadow","chapter_number":12,"importance":0.8,"content":"..."}],
    "text_md": "..."
  },
  "structured": {
    "entities": [{"id":"...","type":"character","name":"...","profile_md":"..."}],
    "relations": [{"source":"...","target":"...","relation_type":"...","description":"..."}],
    "events": [{"id":"...","chapter_number":12,"summary_md":"..."}],
    "foreshadows": [{"id":"...","status":"open","hook_md":"..."}],
    "text_md": "..."
  },
  "vector_rag": {
    "snippets": [{"chunk_id":"...","score":0.42,"source_type":"chapter_content","chapter_number":8,"text_md":"..."}],
    "text_md": "..."
  },
  "graph": {
    "matched_entity_ids": ["..."],
    "text_md": "..."
  },
  "fractal": {
    "saga_md": "...",
    "arc_md": "...",
    "recent_md": "...",
    "text_md": "..."
  },
  "logs": {
    "query_text": "...",
    "filters": {"chapter_range":[1,12], "character_ids":["..."]},
    "budgets": {"worldbook":2000,"story_memory":1500,"structured":2500,"vector_rag":3000},
    "dropped": [{"section":"vector_rag","reason":"budget|dedupe|overfilter"}]
  }
}
```

### 5.2 Prompt 渲染变量约定（与 demo 真实实现对齐）

在 `render_values` 中新增：
- `memory`（dict）= 上述 pack（必要时可裁剪掉 `logs`）
- 允许 PromptBlocks 通过 `memory.worldbook.text_md` 等路径取值（`prompt_presets.py` 已支持点号路径）。

对应 PromptBlock 模板示例：
```jinja2
{% if memory.worldbook.text_md %}<WORLD_BOOK>
{{memory.worldbook.text_md}}
</WORLD_BOOK>
{% endif %}
```

---

## 6. 模块细则：PlotAnalysis + StoryMemory（Phase 2）

> 这一模块来自 mumu 的成熟落地：  
> - 模型：`mumu/MuMuAINovel/backend/app/models/memory.py`  
> - 抽取：`mumu/MuMuAINovel/backend/app/services/plot_analyzer.py#extract_memories_from_analysis`  
> - 组合检索：`mumu/MuMuAINovel/backend/app/services/memory_service.py#build_context_for_generation`  
> - UI：`mumu/MuMuAINovel/frontend/src/pages/ChapterAnalysis.tsx`

### 6.1 数据模型（建议先做“统一记忆表”，后续再拆）

新增表（demo/backend，Alembic；字段命名尽量与 mumu 对齐，但保持 demo 的 UUID/Session 习惯）：

1) `plot_analysis`（每章 0/1 条，默认覆盖旧的）
- `id` (uuid str), `project_id` (FK), `chapter_id` (FK, unique)
- `analysis_json`（Text：存完整 analysis JSON，跨 SQLite/PG 一致；必要时另存结构化列）
- 评分冗余列（便于排序/过滤）：`overall_quality_score/coherence_score/engagement_score/pacing_score`（float，可空）
- 可读报告：`analysis_report_md`（Text，可空）
- `created_at`
- 索引：`(project_id, created_at desc)`、`(project_id, chapter_id)`

2) `story_memories`（统一记忆表：Phase 2 就闭环）
- `id` (uuid str), `project_id` (FK), `chapter_id` (FK, nullable)
- `memory_type`（enum string）：`chapter_summary|hook|foreshadow|plot_point|character_event|world_detail|dialogue|scene|...`
- `title`（短标题，可空）、`content`（Text，必须）、`full_context_md`（Text，可空）
- `importance_score`（float 0~1；用于检索/注入排序）
- `tags_json`（Text：JSON list[str]）
- `story_timeline`（int：默认=chapter.number；允许跨章记忆时可填“发生章”）
- 证据定位（用于标注 UI）：`text_position`（int，-1 表示未知）、`text_length`（int）
- 伏笔状态：`is_foreshadow`（0/1/2）、`foreshadow_resolved_at_chapter_id`（FK，可空）
- 可选：`related_character_ids_json` / `related_entity_ids_json`（Text：JSON list[str]；先不做强约束）
- 软删：`deleted_at`（可选，Phase 5 前也可不做）
- 索引（最小集）：`(project_id, chapter_id)`、`(project_id, story_timeline desc)`、`(project_id, memory_type, importance_score desc)`

**强制要求**：`chapter_summary` 每章至少 1 条（mumu 用它做“相关历史检索/海量章节 smart_context”锚点；我们也会复用）。

为什么先统一表：
- 能更快形成“抽取→检索→标注→回溯”闭环（Phase 2 就能大幅提升长文可控性）。
- 与 Phase 4 的结构化事实表并不冲突：统一表可作为原始事实层（raw memories），结构化表可作为精炼事实层（canonical facts）。

### 6.2 抽取算法（LLM 输出契约 + 后处理）

总体策略：把“稳定性”放在第一位——**LLM 负责输出 analysis（严格 JSON），后端负责把 analysis 转成 memories（确定性规则）**，减少二次 LLM 的不确定性。

分析契约（建议复用 demo 的 `output_contracts` 机制，任务名仍可沿用现有 `chapter_analyze`）：
```json
{
  "summary": "100~200字章节摘要（可选，但强烈建议输出）",
  "hooks": [{"type":"开头钩子","content":"...","strength":8,"position":"开头","keyword":"..."}],
  "foreshadows": [{"type":"planted|resolved","content":"...","strength":7,"keyword":"...","reference_chapter":3}],
  "plot_points": [{"type":"转折","content":"...","importance":0.8,"impact":"...","keyword":"..."}],
  "character_states": [{"character_name":"张三","state_before":"...","state_after":"...","psychological_change":"..."}],
  "scores": {"overall":8.5,"pacing":8.0,"engagement":9.0,"coherence":8.5},
  "suggestions": ["..."]
}
```

抽取规则（对齐 mumu 的实现，写死为默认值，可在 Settings 中配置）：
- `chapter_summary`：始终生成 1 条（优先用 analysis.summary；否则用 plot_points 前 3 条拼接；再不行用正文前 300 字）。
- hooks：`strength >= 6` 才落库；`importance_score = clamp(strength/10, 0, 1)`。
- plot_points：`importance >= 0.6` 才落库；`importance_score = importance`。
- foreshadows：全落库；`is_foreshadow = 1(planted) / 2(resolved)`；并保存 `reference_chapter` 到 metadata（Phase 5 再映射到结构化 foreshadows 表）。
- character_states：全落库；`importance_score = 0.7`（默认）。
- 证据定位（对齐 mumu）：优先用 `keyword` 在正文 `find()` 的位置；找不到则：
  1) 去标点后再找；2) 取 keyword 前 10~15 字做降级匹配；仍失败则 `text_position=-1,text_length=0`。
- **兜底策略（必须有）**：即使 `text_position==-1`，仍允许落库（否则记忆系统“因为定位失败而变成不可用”）；但 UI 需要明确标注“未定位到原文位置”。

### 6.3 API 设计（请求/响应/错误码）

原则：**沿用 demo 现有路由形态**（章节相关能力挂在 `/api/chapters/{id}/...`；跨项目列表挂在 `/api/projects/{id}/...`），避免未来实现时“全量重排路由”。

建议新增/扩展路由：

1) 现有（保持）：`POST /api/chapters/{chapter_id}/analyze`（`backend/app/api/routes/chapter_analysis.py`）
- 行为：仅跑 LLM 分析并返回 analysis（已有 generation_runs 审计）。
- 错误码：`PROMPT_CONFIG_ERROR` / `LLM_CONFIG_ERROR` / `VALIDATION_ERROR`。

2) 新增：`POST /api/chapters/{chapter_id}/analysis/apply`
- 行为：把“已经得到的 analysis”落库为 `plot_analysis`，并按确定性规则生成/覆盖 `story_memories`。
- 入参（示例）：
  ```json
  {"analysis": {...}, "draft_content_md": "用于定位位置（可选，默认用章节当前正文）"}
  ```
- 幂等：`idempotency_key = hash(project_id + chapter_id + analysis_hash)`，重复 apply 直接返回已存在结果。
- 错误码：`ANALYSIS_PARSE_ERROR`（analysis 结构不合规）、`CHAPTER_EMPTY`。

3) 新增：`GET /api/chapters/{chapter_id}/analysis`
- 返回：`plot_analysis`（若存在）+ `story_memories`（按 importance desc）。

4) 新增：`GET /api/chapters/{chapter_id}/annotations`
- 返回：用于标注 UI 的 `annotations[]`（字段对齐 mumu：`{id,type,title,content,importance,position,length,tags,metadata}`）。
- 兜底：当 `position==-1` 且正文可用时，允许基于 `keyword`/`content` 在正文中重新定位（参考 mumu `/chapters/{id}/annotations` 的 fallback）。

5) 新增：`GET /api/projects/{project_id}/memories`
- Query：`type` / `min_importance` / `chapter_range` / `limit` / `offset`。
- 用途：全局记忆页、筛选伏笔、调试。

6) 新增：`POST /api/projects/{project_id}/memories/search`
- Phase 2 先做 keyword/FTS（SQLite 简化版）；Phase 4 接入向量检索。

7) 新增：`GET /api/projects/{project_id}/foreshadows?before_chapter=...`
- 返回未回收伏笔：`is_foreshadow==1 AND story_timeline < before_chapter`（按 importance desc）。

### 6.4 前端 UI（最小可交付）

落点建议（贴合 demo 现状）：
- 复用现有分析入口：`frontend/src/pages/writing/useChapterAnalysis.ts` 已经对接 `/api/chapters/{id}/analyze` 与 `/rewrite`。
- Phase 2 增量交互：
  1) 分析 modal 增加“保存到记忆库”按钮 → 调用 `POST /api/chapters/{id}/analysis/apply`；成功后提示“已生成 N 条记忆/标注可用”。
  2) 新增 `ChapterAnalysisPage`（参考 mumu 的布局）：左正文（AnnotatedText 高亮）+ 右侧 MemorySidebar（类型分组 + 过滤 + 点击滚动）。
  3) 在 WritingPage 增加入口：“剧情分析/标注回溯”。

### 6.5 可观测与验证

- generation_runs：维持现状：`/chapters/{id}/analyze` 与 `/rewrite` 都会写 `generation_runs`（run_type=`chapter_analyze`/`chapter_rewrite`）。
- analysis/apply：属于“非 LLM 写入”，建议记到 `memory_tasks`（或先记 `generation_runs` 的新 run_type=`analysis_apply`，二选一但要统一）。
- DoD 验证清单：
  - analysis JSON 不合规：返回 `ANALYSIS_PARSE_ERROR`，不落库；UI 明确提示并保留原 analysis 供用户复制修正。
  - 重复 apply：幂等返回同一份结果（计数一致），不会重复插入 `chapter_summary`。
  - 标注定位缺失：允许 `position==-1` 落库；前端高亮过滤无效标注并提示“部分记忆未定位”。

---

## 7. 模块细则：WorldBook（Phase 1，Amily 风格）

> 参考实现：`Amily/ST-Amily2-Chat-Optimisation/core/lore.js#getPlotOptimizedWorldbookContent`

### 7.1 数据模型

新增表：`worldbook_entries`
- `id` (uuid), `project_id`
- `title`, `content_md`
- `enabled`（bool）
- `constant`（bool，蓝灯）
- `keywords_json`（list[str]，绿灯）
- `exclude_recursion`（bool）：只在 queryText 匹配，不参与递归
- `prevent_recursion`（bool）：内容不参与递归拼接
- `char_limit`（int，默认例如 12000）
- `priority`（drop_first/optional/important/must，映射到 PromptBlock budget priority）
- `updated_at`

索引：
- `(project_id, enabled)`
- `(project_id, constant, enabled)`

### 7.2 触发算法（蓝灯/绿灯/递归）

与 Amily 对齐的算法（简化伪代码）：
```python
def trigger(entries, query_text, enable_recursion=True):
    query = query_text.lower()
    triggered = {e for e in entries if e.enabled and e.constant}  # 蓝灯
    pending = [e for e in entries if e.enabled and not e.constant]  # 绿灯候选

    while enable_recursion:
        recursion_text = "\n".join(e.content_md for e in triggered if not e.prevent_recursion).lower()
        search_text = (query + "\n" + recursion_text).strip()

        changed = False
        next_pending = []
        for e in pending:
            keywords = [k.lower() for k in e.keywords if k.strip()]
            if not keywords:
                next_pending.append(e)
                continue
            hit = any(k in (query if e.exclude_recursion else search_text) for k in keywords)
            if hit:
                triggered.add(e)
                changed = True
            else:
                next_pending.append(e)
        pending = next_pending
        if not changed:
            break

    return list(triggered)
```

输出格式建议（可解释 + 可 debug）：
- 每条目附：标题、命中原因（constant / keyword:xxx / recursion from Y）
- 总输出按 `char_limit` 截断，并在 `logs.dropped` 标注 truncation。

### 7.3 API 设计

建议路由：`backend/app/api/routes/worldbook.py`
- `GET /api/projects/{project_id}/worldbook_entries`
- `POST /api/projects/{project_id}/worldbook_entries`
- `PUT /api/worldbook_entries/{id}`
- `DELETE /api/worldbook_entries/{id}`
- `POST /api/projects/{project_id}/worldbook_entries/preview_trigger`
  - 入参：
    ```json
    {"query_text":"...","include_constant":true,"enable_recursion":true,"char_limit":12000}
    ```
  - 出参：
    ```json
    {"triggered":[{"id":"...","title":"...","reason":"constant|keyword:xxx"}],"text_md":"...","truncated":false}
    ```

### 7.4 Prompt System 融合

默认预设升级（新资源目录，避免覆盖用户自定义）：
- `backend/app/resources/prompt_presets/chapter_generate_v4/`（示例）
  - 新增 block：`sys.memory.worldbook`（marker_key=`memory.worldbook.text_md`）
- 保持老项目不强行迁移：提供“一键启用推荐预设 v4”按钮（复用 Prompt Studio 的资源升级模式）。

---

## 8. 模块细则：Vector RAG（Phase 4A/4B）

> 参考实现：  
> - Amily：`core/rag-processor.js`（分块、预处理、rerank、超级排序）+ `core/super-sorter.js`  
> - MuMuAINovel：`backend/app/services/memory_service.py`（Chroma collection 隔离、metadata filter、组合检索）  
> - Web：pgvector / Postgres FTS / Cohere rerank（参考链接见文末）

### 8.1 选型建议（先做决定，避免长期摇摆）

默认决策（写清楚，避免长期摇摆）：
- **当 Phase 3（Postgres+部署+auth/worker）已落地**：默认走 **Phase 4B（pgvector）**，理由：备份/一致性/运维更简单，且向量与结构化记忆能统一一套权限与审计。
- **当仍处于 SQLite 单机期**：默认走 **Phase 4A（Chroma）** 作为“语义召回验证路线”，理由：不切 DB 也能跑 embedding + metadata filter；未来可迁移到 pgvector（以“重建”为主）。

本文后续以 pgvector 为主线，同时保留 Chroma 路线作为明确的降级/过渡方案（不是“半吊子备选”）。

### 8.2 向量数据结构（Phase 4B：pgvector 主线）

新增表：
- `vector_collections`
  - `id`, `project_id`, `name`, `source_type`（chapter_content/chapter_summary/worldbook/story_memory/…）
  - `enabled`, `embedding_provider`, `embedding_model`, `dimension`
  - `chunking_config_json`（chunk_size/overlap/…）
  - 索引：`(project_id, enabled)`
- `vector_chunks`
  - `id`, `project_id`, `collection_id`
  - `source_id`（chapter_id/worldbook_entry_id/memory_id…）
  - `chapter_number`（可空，但对小说强烈建议填）
  - `chunk_order`（同 source 内顺序）
  - `content_md`
  - `content_hash`（用于去重）
  - `embedding`（vector）
  - `metadata_json`（来源/标题/位置/…）
  - `idempotency_key`（重建/重跑幂等）
  - `deleted_at`（软删）
  - 索引（最小集）：
    - `(project_id, collection_id, deleted_at)`
    - `(project_id, chapter_number, chunk_order)`
    - `(project_id, content_hash)`（去重）
    - 向量索引：HNSW/IVFFlat（按 embedding 距离算子选择）
    - （hybrid）FTS：`content_tsv`（tsvector）+ GIN 索引（或对 title/keywords）

pgvector DDL 建议（实现阶段直接贴到 migration/文档里，便于调参）：
```sql
-- 扩展：vector + pg_trgm（模糊匹配）
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pg_trgm;

-- HNSW（默认推荐，参数后续可配）
CREATE INDEX ix_vector_chunks_embedding_hnsw
ON vector_chunks
USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);

-- FTS（可选，做 hybrid 时启用）
ALTER TABLE vector_chunks ADD COLUMN content_tsv tsvector;
CREATE INDEX ix_vector_chunks_content_tsv_gin
ON vector_chunks USING gin (content_tsv);
```

关于过滤与 overfiltering（必须在规划里写清楚）：
- HNSW 在“强过滤 + topK 很小”时可能返回不足结果；pgvector 0.8+ 支持 iterative index scan，可通过 `hnsw.iterative_scan`/`hnsw.max_scan_tuples` 等参数缓解。
- 调试页必须展示：filters、返回数是否不足、是否触发了“放宽扫描/降级策略”。

### 8.3 向量数据结构（Phase 4A：Chroma 路线）

对齐 mumu 的成熟落地（`mumu/MuMuAINovel/backend/app/services/memory_service.py`）：
- PersistentClient：目录建议 `data/chroma_db/`（需在 compose 中挂载卷；或明确为可重建目录）。
- Collection 命名：对 Chroma 的命名限制（长度/字符集）做规避，推荐 `u_{sha256(user_id)[:8]}_p_{sha256(project_id)[:8]}`。
- metadata 约束：Chroma metadata 值要求为基础类型（字符串/数字/bool）；tags 列表需 JSON 字符串化（或打平成 `tag_xxx=true` 便于 filter）。

DoD（Phase 4A 的关键点不是“跑通 query”，而是“可重建 + 可观测”）：
- 提供 `vector/rebuild`：可以一键重建 collection（删除并重嵌入），且重建可在后台任务里运行。
- `vector/query` 返回 logs：where/where_document、候选数、最终数、去重/排序原因、耗时。

### 8.4 分块策略（对齐小说场景）

借鉴 Amily 的“按来源分块 + metadata + 标签包装”：
- chapter_content：滑窗分块（例如 800~1200 字符，overlap 150~250），metadata：chapter_number、chunk_order、title
- chapter_summary：不分块或小分块（摘要本就短）
- worldbook_entry：按条目分块（必要时滑窗）
- story_memory：每条记忆作为一个 chunk（天然结构化）

推荐“可读性包装”（注入时可用，入库时保存原文与包装两份二选一）：
```
<RAG source="chapter_content" chapter="12" order="3">
[来源: 第12章, 第3块]
...
</RAG>
```

### 8.5 检索算法（hybrid + rerank + 去重 + 超级排序）

#### 8.5.1 Query 构建（检索质量的 80%）

对齐 demo 的 render_values：
- 基础：`instruction`（可能含 `<PLAN>`）、`story.outline`、`previous_chapter_ending`、`smart_context_story_skeleton`
- 可选：用户勾选角色（角色名/别名拼入 query）

#### 8.5.2 候选召回（pgvector + FTS）

建议做 “topK_vector + topK_fts → 融合”，并显式写清融合公式（避免调参时无从下手）：
- vector：按 `embedding <=> query_embedding` 取 topK（距离越小越相似）。
- fts：对 `content_tsv @@ plainto_tsquery(query)` 取 topK，按 `ts_rank` 排序。
- 融合（默认）：RRF（Reciprocal Rank Fusion），`rrf_score = Σ 1/(rank + k)`，建议 `k=60`。

注意：不要直接线性混合 raw `ts_rank` 与 `vector_distance`（尺度不一致）；要么做 rank 融合（RRF），要么做归一化后再加权。

#### 8.5.3 rerank（可选）

当候选池较大/噪声高时启用 rerank（外部服务可插拔，参考 Cohere Rerank 的形态）：
- 输入：query + documents[]（候选文本，建议先限制 50~200 条，避免延迟爆炸）。
- 输出：rerank_score（0~1 或任意尺度）。
- 最终分数（示例）：`final = alpha*rerank + (1-alpha)*rrf`，再乘来源权重/时间权重。
- 降级：无 key/成本过高/超时 → 直接跳过 rerank（但 logs 必须记录“为何跳过”）。

#### 8.5.4 去重与“超级排序”（叙事阅读顺序）

对齐 Amily 的 superSort 思路：
- 先按 `content_hash` 去重
- 再按 “chapter_number asc, chunk_order asc” 排成可读叙事顺序
- 若来源不同：可以按 source_type 分组（summary 先、content 后；worldbook 常驻靠前）

### 8.6 API 与 UI（必须有调试面）

后端：
- `POST /api/projects/{project_id}/vector/ingest`（异步任务：分块→embedding→入库）
  - 权限：require_owned_project
  - 入参示例：
    ```json
    {
      "source_type": "chapter_content",
      "source_ids": ["chapter_id_1", "chapter_id_2"],
      "collection_id": "optional",
      "chunking": {"chunk_size": 1000, "overlap": 200},
      "mode": "upsert"
    }
    ```
  - 出参示例：`{"task_id":"...","status":"queued"}`
  - 错误码：`VECTOR_CONFIG_ERROR`（collection/embedding 配置缺失）、`QUEUE_UNAVAILABLE`
- `POST /api/projects/{project_id}/vector/query`（调试用：返回候选/融合/重排/日志）
  - 入参示例：
    ```json
    {
      "query_text": "主角身世之谜与那把钥匙",
      "collections": ["chapter_summary", "chapter_content", "worldbook"],
      "top_k_vector": 30,
      "top_k_fts": 30,
      "hybrid": {"enabled": true, "rrf_k": 60},
      "rerank": {"enabled": false, "alpha": 0.5},
      "filters": {"chapter_range": [1, 50]}
    }
    ```
  - 出参示例（节选）：
    ```json
    {
      "results": [{"chunk_id":"...","final_score":0.82,"chapter_number":12,"source_type":"chapter_content","text_md":"..."}],
      "logs": {"candidates": 60, "deduped": 45, "sorted": "chapter_order", "dropped": [{"reason":"overfilter"}]}
    }
    ```
- `POST /api/projects/{project_id}/vector/rebuild`（重建 collection：软删旧 + 重嵌入）
  - 入参示例：`{"collection_id":"...","mode":"full"}`
  - 出参示例：`{"task_id":"...","status":"queued"}`

前端：
- `RagPage`（参考 mumu 的“Prompt 模板页 + 工具页”思路）
  - Collections 列表（enabled、chunk 数、embedding 模型、最近更新时间）
  - Query 调试：输入 query_text → 展示召回列表（score/source/chapter_number）+ 注入预览 + dropped reason

### 8.7 SQLite 降级 / 无 embedding key 降级

- 没有 embedding 能力：`vector_rag` 返回空（不影响写作），UI 置灰并提示“缺少 embedding 配置/密钥”。
- 只有 SQLite：Phase 4B（pgvector）不可用；默认走 Phase 4A（Chroma）或仅做 keyword/FTS 降级（SQLite FTS5 若引入需单独评估迁移成本）。

---

## 9. 模块细则：Structured Memory + Memory Update（Phase 5，Amily 表格系统精华迁移）

> 参考实现：  
> - Amily：`core/table-system/executor.js`（白名单解析与安全执行、容错、延迟删除）  
> - MuMuAINovel：`StoryMemory.text_position/text_length`（证据定位）、`RelationshipType/CharacterRelationship`（关系建模）  

### 9.1 表设计（第一期建议“固定表 + JSON 扩展”）

新增表（第一期最小集）：
- `entities`
  - `id`, `project_id`, `type`（character/location/faction/item/concept）
  - `name`, `aliases_json`
  - `profile_md`（注入用）
  - `attributes_json`（扩展）
  - `status`（active/archived/pending_deletion）
  - `updated_at`
  - unique：`(project_id, type, name_normalized)`（`name_normalized=lower(trim(name))`，建议实体表显式存一列 `name_normalized` 避免跨 DB 的函数索引差异）
- `relations`
  - `id`, `project_id`
  - `source_entity_id`, `target_entity_id`, `relation_type`（string；后续可升级 `relation_types` 表，参考 mumu 的 `RelationshipType`）
  - `description`, `weight`, `status`（active/broken/past/pending_deletion）
  - `evidence_id`
  - unique：`(project_id, source_entity_id, target_entity_id, relation_type)`
- `events`
  - `id`, `project_id`, `chapter_number`
  - `summary_md`, `consequences_md`, `tags_json`
  - `participants_json`（或 join 表，后续再拆）
  - `evidence_id`
  - unique：`(project_id, chapter_number, summary_hash)`
- `foreshadows`
  - `id`, `project_id`
  - `status`（open/resolved/pending_deletion）
  - `hook_md`, `payoff_plan_md`
  - `first_seen_chapter`, `resolved_chapter`
  - `related_entities_json`
  - `evidence_id`
  - unique：`(project_id, hook_hash)`
- `evidence`
  - `id`, `project_id`
  - `chapter_id`, `chapter_number`
  - `text_position`, `text_length`
  - `excerpt_md`（≤ 500~1000 chars）
  - `content_hash`（用于去重/定位）
  - 可选：`chapter_version_id`（若采用 3.3 方案 B 的版本表，用于证据稳定指针）
- `memory_change_sets`
  - `id`, `project_id`, `chapter_id`
  - `idempotency_key`（unique）
  - `status`（proposed/applied/rolled_back/failed）
  - `proposed_json`, `diff_json`, `warnings_json`, `error_json`
  - `created_at`, `applied_at`, `rolled_back_at`
- `memory_change_set_items`（强烈建议第一期就有：否则回滚/审计会非常痛苦）
  - `id`, `project_id`, `change_set_id`
  - `op_index`（int）、`op_json`（Text）
  - `decision`（accepted/rejected）、`applied`（bool）、`applied_ref_json`（Text：记录落库行 id/type）
  - `before_json`/`after_json`（Text，可选：用于回滚/对比）
  - `error_json`（Text，可选）
- `memory_tasks`（RQ 编排统一任务表，复用 batch_generation 的形态）
  - `id`, `project_id`, `kind`（memory_update/embed/rebuild/…）
  - `status/progress/error_json/result_json`

### 9.2 LLM 输出契约（严格 JSON + 白名单操作）

建议把 LLM 输出限制为“受限操作列表”，并且**只允许操作固定表**：
```json
{
  "ops": [
    {"op":"entity.upsert","key":{"type":"character","name":"张三"},"set":{"aliases":["阿三"],"profile_md":"...","attributes":{"age":"18"}},"evidence":{"chapter_number":12,"text_position":1234,"text_length":56,"excerpt_md":"..."}},
    {"op":"relation.upsert","key":{"source":"张三","target":"李四","relation_type":"盟友"},"set":{"description":"...","weight":0.7},"evidence":{...}},
    {"op":"foreshadow.upsert","key":{"hook_md":"..."},"set":{"status":"open","payoff_plan_md":"..."},"evidence":{...}}
  ],
  "warnings": [
    {"code":"CONSISTENCY_CONFLICT","message":"张三年龄从18变成30","evidence":{...}}
  ]
}
```

关键点（必须写进实现规范）：
- **key 与 set 分离**：key 用于幂等定位；set 用于更新字段。
- **名称解析**：op 内允许用 name，但执行时必须做 entity 归一（name/alias → entity_id）。
- **证据强制**：除非明确标注 `evidence_required=false`（默认 true），否则无 evidence 不允许落库（或降级为 warning）。

#### 9.2.1 必选“硬约束”（否则很快失控）
- `schema_version`：必须是固定字符串（例如 `"memory_update_v1"`），后端按版本解析（为未来升级留口子）。
- `max_ops`：默认 ≤ 50（或更小），超过直接拒绝并要求“分批提议”（避免一次 apply 影响过大）。
- 字段长度上限：例如 `name<=64`、`profile_md<=4000`、`excerpt_md<=1000`、`hook_md<=2000`；超过则截断并写入 warnings（或直接拒绝）。
- `fail-closed`：任何未知 `op`、未知字段、类型不匹配、一致性校验失败（如缺关键 key）→ 直接拒绝该 op（或整个 changeset），不能“尽量修复然后写库”。
- 允许 `dry_run=true`：同一份 proposed_json 可先生成 diff/warnings 给 UI 预览，再决定是否 apply（减少“apply 才发现问题”）。

### 9.3 安全执行器（迁移 Amily 思路到后端事务）

执行策略（核心闭环）：
1) `propose`：LLM 输出 JSON → Pydantic 校验 → 生成 change_set（status=proposed）  
2) `apply`：单事务执行 ops（upsert/soft delete）→ 写 evidence → 写审计（status=applied）  
3) `rollback`：优先用软删/反向补丁回滚；至少要做到“标记本次 change_set 的影响并停止继续 embed”

事务边界与幂等（建议写成实现时可直接照抄的伪代码）：
```python
def apply_change_set(change_set_id, user_decisions):
    # 1) 幂等：同 change_set 若已 applied → 直接返回 success
    # 2) 单事务：对被影响的行做锁定（PG: SELECT ... FOR UPDATE；SQLite: 事务内串行）
    with db.transaction():
        cs = lock_change_set(change_set_id)
        assert cs.status == "proposed"

        for op_index, op in enumerate(cs.ops):
            decision = user_decisions.get(op_index, "accepted")
            if decision == "rejected":
                write_item(op_index, decision="rejected", applied=False)
                continue

            # 3) 每个 op：resolve key → compute before/after → upsert/soft_delete → write evidence
            before = snapshot_target(op)
            after_ref = execute_op(op)  # returns {table, id}
            write_item(op_index, decision="accepted", applied=True, applied_ref=after_ref, before=before, after=snapshot(after_ref))

        cs.status = "applied"
        cs.applied_at = now()

    # 4) 事务外：enqueue 后续任务（vector/graph/fractal），并把 task_id 写回 cs 或 memory_tasks
```

回滚策略（最小可行但必须可审计）：
- 第一阶段（够用）：对“删除类”只做软删/两阶段删除，因此回滚=撤销软删标记；对“upsert 类”如果无法恢复 before，就至少标记 `rolled_back` 并停止继续派生任务（Graph/RAG 重建），同时 UI 显示“需要人工修复”。
- 第二阶段（推荐）：依赖 `memory_change_set_items.before_json/after_json` 做反向补丁，尽可能恢复到 apply 前状态（这也是建议第一期就有 items 表的原因）。

借鉴 Amily 的两条安全经验：
- **白名单**：只允许固定 op 名；任何未知 op 直接拒绝（像 `allowedFunctions`）。
- **延迟删除**：删除类操作默认先标记 `pending_deletion`，需要二次确认/二次 apply 才真正软删（避免一次模型误删不可逆）。

### 9.4 API 与 UI（人在环必须有）

后端：
- `POST /api/chapters/{chapter_id}/memory/propose`
- `POST /api/memory_change_sets/{id}/apply`
- `POST /api/memory_change_sets/{id}/rollback`
- `GET /api/chapters/{chapter_id}/memory/change_sets?limit=...`

前端：
- 写作页新增 “Memory Update” 面板：
  - diff 以“表→实体→字段”分组
  - 每条 op 可 accept/reject
  - apply 时显示任务状态（如果 embed/rebuild 异步）

### 9.5 冲突检测（先轻量，再升级）

第一期：只做提示级 warning（不阻断）：
- 同名实体属性冲突（年龄/身份突变）
- 时间线倒挂（events.chapter_number 逆序）
- 关系矛盾（同对实体出现互斥关系类型）

后续：阻断级（必须人工处理才能 commit）。

---

## 10. 模块细则：Graph Context（Phase 6，结构化派生）

参考 Amily：`core/relationship-graph/executor.js`（命中实体 + 1-hop 邻居扩散 + GraphContext 注入）。

### 10.1 检索逻辑

最小实现：
- 从 queryText 做字符串匹配（实体名/别名）
- 对每个命中实体取 1-hop 邻居（relations 表）
- 输出：
```text
<GraphContext>
[实体: 张三]
  - 来源: 直接匹配
  - 连接:
    * -> 李四 (盟友)
</GraphContext>
```

### 10.2 UI 与可观测

- GraphPage：节点=entity，边=relation；点击节点显示详情与证据；支持“本次生成命中实体高亮”。
- generation_runs（或 retrieval pack logs）记录：matched_entity_ids、扩散原因、裁剪原因。

---

## 11. 模块细则：Fractal Memory（Phase 6，宏观连续性）

参考 Amily：`core/fractal-memory.js`（scene→arc→saga，BUFFER_SIZE=5，滚动压缩）。

### 11.1 对 ainovel 的适配（按章节而非聊天楼层）

建议阈值（可配置）：
- scene：每章生成 1 条 scene（或每章拆 3~5 个 scene）
- arc：每 5 章压缩为 1 条 arc
- saga：每 5 条 arc 重写 saga（100~200 字）

触发点：
- 章节 status 变为 done（或用户点击“定稿/选稿”）时触发 fractal_update

### 11.2 数据结构

最小表：`fractal_memory`
- `id`, `project_id`
- `saga_md`
- `arc_json`（list[str]）
- `scene_json`（list[str]）
- `updated_at`

注入：
- PromptBlock：`sys.memory.fractal`（marker_key=`memory.fractal.text_md`）

---

## 12. 模块细则：Style Profiles + 去 AI 味 + 一致性修复（Phase 7）

> 参考实现：  
> - MuMuAINovel：`mumu/MuMuAINovel/backend/app/models/writing_style.py`、`mumu/MuMuAINovel/backend/app/models/project_default_style.py`、`mumu/MuMuAINovel/backend/app/api/writing_styles.py`、`mumu/MuMuAINovel/backend/app/api/chapters.py`（style_id 注入）。  
> - Amily：`Amily/ST-Amily2-Chat-Optimisation/core/summarizer.js`（标签块替换式重写；可用于 post_edit/去味/一致性修复）。  
> - demo：`demo/backend/app/services/generation_pipeline.py`（post_edit 链路）、Prompt System（`demo/backend/app/services/prompt_presets.py`）。

### 12.1 要解决的问题（验收口径）

- 让“风格”成为可持久化资产，而不是散落在 `project_settings.style_guide` 的一段字符串。
- 支持：系统预设 + 用户自定义 + 项目默认 + 生成时临时覆盖。
- 风格注入必须可观测：每次生成/后处理明确记录使用的 `style_id`/版本/对应 PromptBlocks。
- 去 AI 味与一致性修复必须可降级：失败不阻断写作，保留原文并给出原因。

### 12.2 数据模型（对齐 mumu，贴合 demo 的 UUID 与权限）

1) `writing_styles`（也可命名 `style_profiles`，但建议对齐 mumu 便于迁移学习）
- `id` (uuid str), `owner_user_id` (FK `users.id`, nullable；NULL=系统预设)
- `name`（<=100）, `style_type`（`preset|custom`）, `preset_id`（可空）
- `description`（Text）, `prompt_content`（Text, 必填）
- `order_index`（int default 0）
- `created_at`, `updated_at`,（可选）`deleted_at`
- 约束/索引（建议）：
  - `unique(owner_user_id, name)`（避免同用户重复风格名）
  - `(owner_user_id, style_type, order_index)`（列表排序/筛选）

2) `project_default_styles`（与 mumu 一致：每项目 1 条默认风格）
- `project_id` (unique FK `projects.id`)
- `style_id` (FK `writing_styles.id`)
- `created_at`, `updated_at`

3)（可选）`style_samples`（用于“从样稿抽取风格画像”，不直接注入）
- `id`, `project_id`, `user_id`, `name`, `content_md`, `source_chapter_id`(可空), `created_at`

4)（可选）style_extract 的审计：优先复用 `generation_runs`（run_type=`style_extract`），不额外造表

### 12.3 API 设计（对齐 demo 路由原则）

原则：章节相关能力挂在 `/api/chapters/{id}/...`；跨项目列表挂在 `/api/projects/{id}/...`；全局只读资源用 `/api/...`。

- `GET /api/writing_styles/presets`：系统预设列表（`owner_user_id IS NULL`）
- `GET /api/writing_styles/user`：当前用户可用风格（预设 + 自己的 custom）
- `GET /api/projects/{project_id}/writing_styles`：项目可用风格 + `is_default`
- `POST /api/projects/{project_id}/writing_styles`：创建自定义风格（`owner_user_id=current_user`）
- `PUT /api/writing_styles/{style_id}`：更新（仅 owner 可写；preset 禁改）
- `DELETE /api/writing_styles/{style_id}`：删除（可先硬删；若后续需要审计再改软删）
- `POST /api/writing_styles/{style_id}/set-default`：设置项目默认风格（校验 project 权限）
- （可选）`POST /api/projects/{project_id}/writing_styles/extract`：上传样稿/选章节 → 产出 `prompt_content` 草案（走 `generation_runs`）

错误与权限：
- 401：未登录
- 403：无项目权限/非 owner 修改他人风格
- 422：`prompt_content` 为空、name 超长、preset 不可编辑等

### 12.4 生成时注入逻辑（与 Prompt System 的落点）

风格在生成时只需要输出一段可控的 `prompt_content`（风格画像），并写入 `render_values`。

推荐优先级（与 mumu 保持一致）：
1) 生成请求显式 `style_id`（一次性覆盖）
2) 项目默认 `project_default_styles.style_id`
3) fallback：`project_settings.style_guide`（demo 现有）

`render_values` 建议新增：
- `style.profile_md`：拼装后的风格提示词（通常=writing_styles.prompt_content + 轻量包裹）
- `style.meta`：`{style_id, style_type, preset_id}`（用于日志/UI）

对应 PromptBlocks（建议）：
- `sys.style_profile`：marker_key=`style.profile_md`（system, priority important, 有预算上限）
- （可选）`sys.anti_ai_voice`：只在 post_edit 或显式开关时启用（避免前置成本）

### 12.5 去 AI 味 / 一致性修复：后处理链（与 post_edit 的关系）

最小可交付路线：把它实现为 **post_edit 的可选分支**，不要新增新的“神秘生成步骤”。

推荐管线：
- chapter_generate：只做风格“指导性”注入（不强求彻底去味，避免前置成本/延迟）
- post_edit：按开关执行 1~2 次重写
  - pass1：风格对齐（保持事实不变）
  - pass2（可选）：去味/口癖清理 + 一致性修复（仅在检测到冲突/口癖时触发）

可迁移的“标签块替换式重写”（来自 Amily summarizer，用于 post_edit 模板）：
```text
<CONTENT>原文</CONTENT>
<REWRITE_RULES>
- 不改动剧情事实/人名/时间线
- 目标风格：{{style.profile_md}}
- 去味规则：避免模板化总结句；减少高频口癖（词表可配置）
</REWRITE_RULES>
```

失败与降级：
- LLM 输出无法通过校验（空/过短/缺失关键段落）：直接保留原文，并在 `generation_runs.error_json` 记录 `STYLE_POST_EDIT_FAILED`（含原因与重试建议）。
- style_profile 缺失：降级到 `project_settings.style_guide`，并记录 `style.fallback=project_settings`（便于排障）。

### 12.6 UI 交互（参考 mumu，可落到 demo 页面结构）

- 新增 `StyleProfilesPage`：
  - 卡片列表：预设/自定义 Tag；默认风格显示 Star（参考 mumu `frontend/src/pages/WritingStyles.tsx`）
  - 支持创建/编辑/删除自定义风格（`prompt_content` 大文本编辑）
  - “设为项目默认”按钮（owner/editor 可用，viewer 只读）
- WritingPage/AiGenerateDrawer：
  - 下拉选择风格：默认=项目默认；支持“本次覆盖”与“恢复默认”
  - 生成参数里记录 `style_id`（写入 `generation_runs.params_json`，便于回放）
-（可选）Style Test Bench：输入一段文本 → 选择风格 → 触发 post_edit → 对比输出与日志

### 12.7 DoD 与验证清单（Phase 7）

- 数据：能创建/编辑/删除自定义风格；项目能设置默认风格；预设不可编辑
- 生成：选择 `style_id` 后 prompt 注入生效且可回放（能在 `generation_runs` 看到 `style_id` 与 block）
- 后处理：post_edit 的风格对齐可开关；失败自动降级，不阻断写作
- 安全：风格资产隔离（仅自己可写；系统预设只读；project default 受 RBAC 控制）

---

## 13. 多用户与生产化（Phase 3：auth/RBAC/迁移/compose）

> 参考 MuMuAINovel：  
> - Cookie 会话：`backend/app/middleware/auth_middleware.py`、`backend/app/api/auth.py`  
> - Postgres compose：`docker-compose.yml`、`Dockerfile`  

### 13.1 Auth 插入点（对齐 demo 真实结构）

后端最关键插入点：
- `backend/app/api/deps.py#get_current_user_id()`：从固定 `local-user` 改为解析会话/JWT（保留 dev fallback）。
- `backend/app/main.py#_ensure_local_user()`：仅 dev 执行；prod 必须禁用固定用户。

推荐最小路线（更贴近 mumu，前端负担小）：
- Cookie 会话（HttpOnly `user_id` + `session_expire_at`）+ 中间件写入 `request.state.user_id`  
- 保留现有 `UserIdDep` 形态：`UserIdDep = Annotated[str, Depends(get_current_user_id)]`，内部改成读 state

#### 13.1.1 最小可用的 Cookie 会话规范（建议直接复刻 mumu 的 shape）
- Cookie：
  - `user_id`：HttpOnly，`SameSite=Lax`（或 Strict，视前端部署形态而定），prod 建议 `Secure=true`。
  - `session_expire_at`：非 HttpOnly（供前端显示/刷新），与 `user_id` 同步过期。
- 端点（建议新增 `backend/app/api/routes/auth.py`）：
  - `POST /api/auth/local/login`（本地账号登录；Phase 3 最小实现）
  - `POST /api/auth/refresh`（临近过期才续期；减少无意义续期）
  - `POST /api/auth/logout`
  - `GET /api/auth/user`（前端启动时拉取当前用户）
  - （可选）`GET /api/auth/oauth/{provider}/url` + `GET /api/auth/oauth/{provider}/callback`（后置）
- CSRF 策略（必须写清楚）：
  - 若前后端同域：优先 `SameSite=Lax/Strict + 自定义 Header`（例如要求 `X-Requested-With: ainovel`）作为最小防护；
  - 若跨域且允许 credentials：必须引入 CSRF token（double-submit 或服务端 session），并在 CORS 中严格 allow_origins（不能 `*`）。

#### 13.1.2 密码与用户表（最小闭环）
- demo 现有 `users` 只有 `id/display_name`，没有密码；Phase 3 最小落地建议新增：
  - `user_passwords(user_id, password_hash, password_updated_at, disabled_at)`（hash 推荐 bcrypt/argon2）
  - `users.is_admin`（可选）或单独的 `user_roles`
- 对齐 mumu 的思路：支持 `.env` 里配置一个“本地管理员账号”用于首次部署（后续可改密码/禁用）。

### 13.2 RBAC 与协作（从 owner 单人演进）

建议新增：
- `project_memberships(project_id,user_id,role)`
  - role：owner/editor/viewer
  - 所有 require_owned_* 升级为 require_project_access（owner/editor/viewer）+ 单独 require_project_owner（敏感操作）

### 13.3 SQLite → Postgres 迁移策略（必须写清窗口与回滚）

分阶段建议：
- Phase 0~2：允许 SQLite（单 worker），不强制用户迁移
- Phase 4B（pgvector）起：必须切 Postgres（否则向量能力不可用）

迁移执行建议（实现阶段，必须可回滚）：
1) 先跑通“PG 空库 + Alembic 初始化”（包含扩展：`uuid-ossp`、`pg_trgm`，以及 Phase 4B 需要的 `vector`）。
2) 再做“一次性数据搬运”脚本（建议 Python/SQLAlchemy 方式逐表搬运，保留 id，不依赖自增）：
   - 顺序：users → projects → settings/characters/outlines/chapters → prompt_* → generation_runs → batch_tasks → 新增 memory 表（若有）
   - 校验：每表行数一致；关键表（chapters/outlines/characters）做内容 hash 抽样；外键完整性（随机抽样 join）。
3) 回滚策略：
   - SQLite 文件永远不删；迁移失败时允许切回 SQLite 继续只读导出；
   - PG 侧迁移失败可直接 drop schema 重来（因为数据源仍在 SQLite）。

### 13.4 Docker compose（目标形态）

建议拆分服务：
- `frontend`（Vite build 静态）
- `backend`（FastAPI）
- `postgres`（Phase 3B+）
- `redis`（RQ）
- `rq_worker`（跑 memory_update/embed/batch_generation）

必须明确：
- logs/数据卷挂载
- embedding key/密钥管理（prod 必须）
- 若使用 Chroma（Phase 4A）：必须决定“向量库是否持久化”。
  - 持久化：挂载 `data/chroma_db` 到卷；
  - 不持久化：明确提供 `vector/rebuild`，并把 rebuild 成本写进 DoD（否则线上重启后质量直接崩）。

---

## 14. 可观测 / 审计 / 回放（贯穿所有 Phase）

### 14.1 观测数据去向（不重复造轮子）

- **LLM 调用审计**：统一写入 `generation_runs`（demo 已有，建议把 memory_update/plot_analyze/style_extract 也用它记录）。
- **非 LLM 任务审计**：写入 `memory_tasks`（或复用 batch_generation_tasks 的形态）。
- **Prompt 注入日志**：复用 `prompt_render_log_json`；新增记忆 pack 的结构化日志（建议单独 JSON 字段或 artifact 表）。

### 14.2 Memory 检索日志（建议结构）

`memory_retrieval_log_json`（可落到 generation_runs 或单独表）：
- query_text（构建后的）
- per_section：命中条目 ids、原因、分数、去重、裁剪原因
- budgets：每段预算与最终 token
- overfilter：若过滤导致结果不足的补救策略记录

### 14.3 回放路径（排障必备）

最小回放要求：
- 能通过 generation_run_id 找到：当时 prompt、render_log、注入的 memory pack（至少能重建）
- 能通过 change_set_id 找到：LLM 输出、diff、apply 结果、后续 embed 任务状态

补充：demo 已有 `generation_runs.request_id`，并且一次“plan_first → chapter_generate → post_edit”会复用同一个 request_id（见 `backend/app/api/routes/chapters.py`）；因此实现阶段应明确：
- UI 回放入口优先按 `request_id` 聚合展示同一链路的多个 run（否则用户只能看到其中一个 `generation_run_id`）。
- 若增加新的 run_type（`analysis_apply` / `memory_retrieve` / `memory_update_propose`），也必须沿用同一 request_id（或写 parent_run_id），保证链路可追踪。

---

## 15. 风险与降级策略（必须提前写）

- 没有向量库/embedding key：Vector RAG 关闭，不影响基本写作（Phase 4 相关 UI 置灰）。
- LLM 抽取失败：不落库，只返回 parse_error；允许用户手工录入 WorldBook/记忆。
- 记忆膨胀：预算裁剪优先级 drop_first/optional/important/must；RAG 先去重再超级排序；必要时只注入摘要。
- 多 worker + SQLite：明确不支持（demo 已有 warning）；Phase 4B 起必须切 PG（Phase 3 是前置）。
- 误删/误更新：删除默认两阶段（pending_deletion）；所有 apply 必须可审计与回滚（软删优先）。

---

## 16. 附录：代码落地清单（按 demo 目录对齐）

> 说明：下面是“实现阶段预计会新增/改动的文件路径建议”。  
> 不是强制命名，但必须遵循 demo 现有分层：`api/routes → services → models → schemas → alembic`。

### 16.1 后端（FastAPI）

**Phase 0（接口/骨架）**
- 新增：`backend/app/schemas/memory_pack.py`（MemoryContextPack）
- 新增：`backend/app/services/memory_retrieval_service.py`（空实现返回空 pack）
- 新增：`backend/app/api/routes/memory.py`（`GET /api/projects/{project_id}/memory/retrieve` 调试接口）
- 更新：`backend/app/api/router.py`（include memory router）

**Phase 1（WorldBook）**
- 新增：`backend/app/models/worldbook_entry.py`
- 新增：`backend/app/schemas/worldbook.py`
- 新增：`backend/app/services/worldbook_service.py`（递归触发算法 + 输出格式化）
- 新增：`backend/app/api/routes/worldbook.py`（CRUD + preview_trigger）
- 新增 Alembic：`backend/alembic/versions/*_add_worldbook_entries.py`
- 更新：`backend/app/api/router.py`

**Phase 2（PlotAnalysis + StoryMemory）**
- 新增：`backend/app/models/plot_analysis.py`
- 新增：`backend/app/models/story_memory.py`
- 新增：`backend/app/schemas/memories.py`（analysis/memory 的请求响应）
- 新增：`backend/app/services/plot_analyzer_service.py`（LLM 调用 + JSON 解析/清洗）
- 新增：`backend/app/services/story_memory_service.py`（抽取规则、text_position 定位、查询）
- 新增：`backend/app/api/routes/memories.py`（analyze/list/search/foreshadows/stats）
- 新增 Alembic：`backend/alembic/versions/*_add_plot_analysis_and_story_memories.py`

**Phase 3（Auth/RBAC/迁移/compose）**
- 新增：`backend/app/api/routes/auth.py`（cookie 会话：login/refresh/logout/user）
- 新增：`backend/app/middleware/auth_middleware.py`
- 新增：`backend/app/models/project_membership.py`
- 新增：`backend/app/models/user_password.py`（或同等凭证表）
- 更新：`backend/app/api/deps.py`（从 state/JWT 取 user_id；`require_owned_*` → `require_project_access`）
- 更新：`backend/app/main.py`（prod 禁用 local-user fallback）
- 新增 Alembic：`backend/alembic/versions/*_add_auth_and_project_memberships.py`
- （可选）新增：`docker-compose.yml`、`backend/scripts/entrypoint.sh`（等待 DB → alembic upgrade → uvicorn）

**Phase 4（Vector RAG：4A/4B）**
- 新增：`backend/app/models/vector_collection.py`
- 新增：`backend/app/models/vector_chunk.py`
- 新增：`backend/app/schemas/vector.py`
- 新增：`backend/app/services/vector_ingest_service.py`（分块、embedding、入库、软删重建）
- 新增：`backend/app/services/vector_query_service.py`（hybrid/融合/rerank/去重/超级排序 + logs）
- 新增：`backend/app/api/routes/vector.py`（collections/ingest/query/rebuild）
- 新增 Alembic：`backend/alembic/versions/*_add_vector_tables_and_indexes.py`

**Phase 5（Structured Memory + ChangeSet）**
- 新增：`backend/app/models/entity.py`、`backend/app/models/relation.py`、`backend/app/models/event.py`、`backend/app/models/foreshadow.py`
- 新增：`backend/app/models/evidence.py`、`backend/app/models/memory_change_set.py`
- 新增：`backend/app/schemas/memory_update.py`（严格 JSON op schema）
- 新增：`backend/app/services/memory_update_service.py`（propose/apply/rollback + 安全执行器）
- 新增：`backend/app/api/routes/memory_update.py`
- 新增 Alembic：`backend/alembic/versions/*_add_structured_memory_and_change_sets.py`

**Phase 6（Graph + Fractal）**
- 新增：`backend/app/services/graph_service.py`（GraphContext 构建）
- 新增：`backend/app/services/fractal_memory_service.py`（scene→arc→saga 任务）
- 新增：`backend/app/models/fractal_memory.py`
- 新增：`backend/app/schemas/graph.py`、`backend/app/schemas/fractal.py`
- 新增：`backend/app/api/routes/graph.py`

**Phase 7（Style Profiles + 去味）**
- 新增：`backend/app/models/writing_style.py`、`backend/app/models/project_default_style.py`
- 新增：`backend/app/schemas/writing_styles.py`
- 新增：`backend/app/api/routes/writing_styles.py`
- （可选）新增：`backend/app/services/style_service.py`（选择默认风格/拼装 `style.profile_md`）
- 新增 Alembic：`backend/alembic/versions/*_add_writing_styles_and_project_default_styles.py`

### 16.2 前端（React）

**Phase 0**
- 新增：`frontend/src/components/memory/ContextPreviewDrawer.tsx`
- 更新：`frontend/src/pages/WritingPage.tsx`（入口按钮 + 调试展示）
- 新增：`frontend/src/services/memoryApi.ts`

**Phase 1**
- 新增：`frontend/src/pages/WorldBookPage.tsx`（CRUD + preview_trigger）
- 更新：`frontend/src/App.tsx`、`frontend/src/components/layout/AppShell.tsx`（导航入口）

**Phase 2（标注 UI）**
- 新增：`frontend/src/pages/ChapterAnalysisPage.tsx`
- 新增：`frontend/src/components/memory/AnnotatedText.tsx`、`frontend/src/components/memory/MemorySidebar.tsx`
- 更新：`frontend/src/pages/writing/useChapterAnalysis.ts`（对接新接口）

**Phase 3（Auth/RBAC）**
- 新增：`frontend/src/pages/LoginPage.tsx`
- 新增：`frontend/src/contexts/AuthContext.tsx`（或同等会话管理）
- 新增：`frontend/src/services/authApi.ts`
- 更新：`frontend/src/App.tsx`（路由守卫/未登录跳转）

**Phase 4（RAG 调试）**
- 新增：`frontend/src/pages/RagPage.tsx`
- 新增：`frontend/src/services/vectorApi.ts`

**Phase 5（Memory Update 面板）**
- 新增：`frontend/src/components/memory/MemoryUpdatePanel.tsx`
- 新增：`frontend/src/services/memoryUpdateApi.ts`

**Phase 6（Graph/Fractal）**
- 新增：`frontend/src/pages/GraphPage.tsx`、`frontend/src/components/graph/RelationshipGraph.tsx`
- 新增：`frontend/src/pages/FractalMemoryPage.tsx`（可选）

**Phase 7（Style Profiles）**
- 新增：`frontend/src/pages/StyleProfilesPage.tsx`
- 新增：`frontend/src/services/writingStylesApi.ts`
- 更新：`frontend/src/components/writing/AiGenerateDrawer.tsx`（风格选择/本次覆盖）

### 16.3 Prompt 资源（Prompt Studio 的“推荐预设升级”）

- Phase 1：`backend/app/resources/prompt_presets/chapter_generate_v4/` 新增 `sys.memory.worldbook`
- Phase 4A/4B：`chapter_generate_v4/v5` 新增 `sys.memory.vector_rag`
- Phase 5：新增 `memory_update_v1`（严格 JSON 契约）与 `sys.memory.structured`
- Phase 7：`post_edit_v2` 新增 `sys.style_profile` / `sys.anti_ai_voice`（可选 `sys.consistency_repair`）

---

## 17. 参考实现与资料（实现阶段请在 PR/Issue 里引用）

### 17.1 仓库内参考（代码定位）

- demo（插入点 / 真实约束）
  - `demo/backend/app/services/chapter_context_service.py`
  - `demo/backend/app/services/prompt_presets.py`
  - `demo/backend/app/models/generation_run.py`
  - `demo/backend/app/services/task_queue.py`
  - `demo/backend/scripts/run_rq_worker.py`
- Amily（可迁移算法）
  - `Amily/ST-Amily2-Chat-Optimisation/core/table-system/executor.js`
  - `Amily/ST-Amily2-Chat-Optimisation/core/lore.js`
  - `Amily/ST-Amily2-Chat-Optimisation/core/rag-processor.js`
  - `Amily/ST-Amily2-Chat-Optimisation/core/super-sorter.js`
  - `Amily/ST-Amily2-Chat-Optimisation/core/fractal-memory.js`
  - `Amily/ST-Amily2-Chat-Optimisation/core/relationship-graph/executor.js`
- MuMuAINovel（工程化落地）
  - `mumu/MuMuAINovel/backend/app/models/memory.py`
  - `mumu/MuMuAINovel/backend/app/services/memory_service.py`
  - `mumu/MuMuAINovel/backend/app/services/plot_analyzer.py`
  - `mumu/MuMuAINovel/backend/app/middleware/auth_middleware.py`
  - `mumu/MuMuAINovel/backend/app/models/writing_style.py`
  - `mumu/MuMuAINovel/backend/app/models/project_default_style.py`
  - `mumu/MuMuAINovel/backend/app/api/writing_styles.py`
  - `mumu/MuMuAINovel/frontend/src/pages/ChapterAnalysis.tsx`
  - `mumu/MuMuAINovel/frontend/src/pages/WritingStyles.tsx`

### 17.2 Web 参考（实现时请固定版本/commit）

```text
pgvector（索引 / 查询 / hybrid）
https://github.com/pgvector/pgvector

pgvector 0.8.0（iterative scan、过滤相关改进）
https://pgvector.org/

PostgreSQL 全文检索（tsvector/tsquery/ts_rank）
https://www.postgresql.org/docs/current/textsearch.html
https://www.postgresql.org/docs/current/functions-textsearch.html

ChromaDB（where/where_document 过滤）
https://docs.trychroma.com/

Cohere Rerank（rerank API 形态与限制）
https://docs.cohere.com/
```
