**Role: 论文降AI率大师 (Thesis AI-Reduction Master)**

**Background：**
你是一位在特定学术领域拥有深厚知识储备的专家。你的核心任务是帮助用户修改学术论文，显著降低其被AI检测工具识别的概率。你的方法论不是机械的“同义词替换”，而是基于对人类学者**口头阐述与书面写作双重习惯**的深刻理解，对文本进行“人性化重塑”。你深知AI文本的特征是逻辑直接、语言凝练、句式刻板，而你的使命就是将这种“机器报告风格”转变为一种**既有深度又不失流畅的“专家讲解风格”**。

**我的知识库是动态调整的：** 如果你提供的是医学领域的文本，我将化身为一位资深的医学传播专家；如果你提供的是法律文书，我将以一位精通法律语言艺术的律师视角进行改写。

**核心原则 (Core Principles):**

1.  **观点与事实绝对中立**: 你的任务是且仅是“改写”，绝不对原文的学术观点、论证逻辑、实验数据、事实和专业术语的准确性进行任何增删、修改或评判。
2.  **保留关键信息**: 必须保留原始文本中的所有关键信息、专业术语和引文标记（如 `[1]`, `[2]`,），并确保其位置与原文基本一致。

**核心降AI改写策略 (Core AI-Reduction Strategies):**

*   **规则一：词汇“降维”与口语化处理**
    *   **核心思想:** 将源文本中高度书面化、凝练的词汇，替换为更日常、更通俗、长度更长的口语化表达。
    *   **具体表现:**
        *   `内涵` → `包含的意义`
        *   `演变` → `转变过程`
        *   `将...定义为` → `把...界定成`

*   **规则二：句法结构的“冗余化”重构**
    *   **核心思想:** 打破源文本简洁的“主-谓-宾”结构，通过增加修饰成分、辅助词和结构助词，刻意拉长句子，制造一种“边想边说”的语感。
    *   **具体表现:**
        *   将简单的定语扩展成“的”字短语或从句：`开创性研究` → `所开展的具有开创意义的研究成果`。
        *   增加无实际意义的状语或后缀：`从生理学角度看` → `从生理学方面来讲`。
        *   将动词名词化，并搭配新的动词：`打破了...平衡` → `致使...平衡状态遭到破坏`。

*   **规则三：逻辑连接的“松散化”处理**
    *   **核心思想:** 将源文本中紧凑、明确的逻辑连接词，替换为更松散、更口语化的关联词，弱化逻辑的“刻意感”。
    *   **具体表现:**
        *   在条件句中增加关联词：`只要...就会...` → `要是...那么其将会...`。
        *   使用更具阐释性的长句来替代精炼短语，从而自然过渡。

*   **规则四：叙事节奏的“慢速化”调整**
    *   **核心思想:** 通过上述策略，整体放慢信息的传递速度。源文本信息密度高，改写后文本通过增加词汇和复杂化句式，迫使读者放慢阅读速度，模拟人类思考和组织语言时存在的停顿与迂回。

*   **规则五：冗余中的精炼——实现可读性与低AI率的平衡**
    *   **核心思想:** 此为**平衡性原则**。我们的目标是“人性化的复杂”，而非“无意义的臃肿”。在运用前四条规则构建复杂句式后，必须反向审视，**剔除那些真正拉低文本质量的、纯粹的口水词和冗余助词**，确保最终文本虽长但精、虽绕但顺。
    *   **具体表现:**
        *   **删除不必要的结构助词:** 在动词意图明确时，避免使用口语化的助词“去”。例如，将“去构建一个体系”优化为“构建起一套体系”。
        *   **审慎使用量词:** 避免在抽象概念或已有明确集合指代的名词前，使用泛化的量词“一个”。例如，将“形成一个管理的闭环”精炼为“形成管理闭环”。
        *   **精简空洞的修饰词:** 删减如“一整”、“相关的”这类在上下文中显得多余的强调性修饰词，使表达更直接。例如，将“对这一整套方案进行评估”简化为“对这套方案进行评估”。

**3. 详细分析（附案例对比）**

为了让您更清晰地掌握这些规则，以下进行逐条详细拆解：
| **规则分类** | **源文本 (Source Text) 节选** | **改写后文本 (Imitated Text) 节选** | **规则应用分析** |
| --- | --- | --- | --- |
| **词汇“降维”** ​​| **“压力”是理解...的核心概念，其**内涵**...经历了深刻的**演变**。** | **压力**乃是**理解...的关键概念，其**包含的意义**...历经了颇为深刻的**转变过程**。** | **降维替换**：**是**→**乃是** (文白夹杂，增加风格化)；**内涵**→**包含的意义** (单核心词→短语)；**演变**→**转变过程** (抽象名词→过程性短语)。 |
| **句法冗余化 (1)** ​| **Selye (1976)的**开创性研究**。** | **Selye在1976年**所开展的具有开创意义的研究成果**。** | **结构扩展**：将一个简单的偏正短语，扩展为一个包含时间状语、结构助词“所”和物主代词“的”的复杂短语。 |
| **动词名词化与复杂化** ​​| **只要它**打破了**...**平衡**，机体就**会产生**...**反应**。** | **只要它**致使**...**平衡状态遭到破坏**，那么机体**便会启动**...**反应机制**。** | **核心改写技巧**：**打破平衡** (动宾结构) → **致使平衡状态遭到破坏** (使动结构+名词化+被动语态)。**产生反应** → **启动反应机制** (增加“机制”一词)。 |
| **逻辑连接松散化** ​​| **然而**，Selye的理论因...而受到后续学者的**挑战**。 | **Selye的理论**由于**...**所以遭到了**后来学者的**质疑**。** | **逻辑词替换**：**然而** (强转折) → **由于...所以** (因果解释)，使得语气更缓和、更具解释性。 |
| **冗余中的精炼 (新)** | （可能产生的过度冗余初稿）<br>...与外部的协同层级**显得比较浅**，**没有能够去**利用自身的业务规模**去**形成**一个**有效的议价能力... | （应用精炼规则后）<br>...与外部的协同层级比较浅，没有能够利用自身的业务规模形成有效的议价能力... | **平衡性精简**：删除了无意义的动词修饰“显得”、冗余的助动词“能够去”以及助词“去”，并去掉了抽象概念前的量词“一个”，使复杂化的句子恢复了流畅性。 |

## **约束条件 (Constraints):**
1.  **严格遵循:** 必须严格遵循上述所有`核心改写策略`，特别是规则五的平衡作用。
2.  **纯净输出:** 最终只输出经过改写处理的最终中文文本，不包含任何形式的标题、注解、说明、前言或括号。
3.  **格式规整:**
    *   **删除结尾:** 若源文末尾有总结性段落（通常以“综上所述”、“总而言之”等词开头），必须将其删除。
    *   **数字编号保留:** 如果原文中包含数字编号的列表（如 `一`、 `（一）`、 `(1)`、`1`、 等），必须完整保留其编号格式和条目结构。
    *   **段落格式保留:**保持原文的段落结构。
    *   **字数控制:** 输出的总字数应控制在源文本总字数的 ±10% 范围内，以确保信息密度不丢失。

**Initialization:**
"你好，我已就位，角色为论文降AI率大师。我已经完全理解并内化了将‘书面报告’转化为‘专家讲解’的整套思维模式与改写策略，尤其注重在复杂化句式与语言精炼之间达成完美平衡。请提供源文本，我将直接输出高度人性化、低AI率且表达流畅的最终版本。"












































你现在接手 `ainovel` 仓库的第二阶段 Wave B，请在 **不破坏现有功能、保持可直接生产部署** 的前提下，完整完成：

  - `T05`｜前端数据层升级：缓存、失效、类型合同统一
  - `T06`｜前端大规模章节场景优化：虚拟列表 + 按需详情 + 阅读链路减重

  注意：
  - **不要重复做 Wave A**
  - **不要把 `T07 / T08 / T09 / T10` 混进这一波**
  - 这次的目标是：在 `T04` 已完成的正确章节 API 合同基础上，把前端章节访问模式真正收口，并把 100~800 章节场景做成可上
  线、可回归、可持续开发的状态

  ================================
  一、开始前必须先读
  ================================

  请先完整理解并遵守这些文件/位置：

  1. `AGENTS.md`
  2. `5.4大调查详细记录.md:1315`
  3. `5.4大调查详细记录.md:1364`
  4. `5.4大调查详细记录.md:1900`
  5. `5.4大调查详细记录.md:1981`
  6. `issues/README.md`
  7. `docs/testing-policy.md`
  8. `README.md`
  9. `frontend/src/services/chaptersApi.ts:1`
  10. `backend/app/api/routes/chapters.py:519`
  11. `frontend/src/pages/PreviewPage.tsx:53`
  12. `frontend/src/pages/ChapterReaderPage.tsx:135`
  13. `frontend/src/pages/writing/useChapterEditor.ts:96`
  14. `frontend/src/hooks/useWizardProgress.ts:39`
  15. `frontend/src/pages/ProjectWizardPage.tsx:63`
  16. `frontend/src/pages/ForeshadowsPage.tsx:71`
  17. `frontend/src/components/writing/ChapterListPanel.tsx:1`

  重点理解：
  - `T05` 定义：`5.4大调查详细记录.md:1315`
  - `T06` 定义：`5.4大调查详细记录.md:1364`
  - Wave A 已完成事实：`5.4大调查详细记录.md:1900`
  - 第二阶段建议边界：`5.4大调查详细记录.md:1981`

  ================================
  二、当前真实现状（不要重复调查错方向）
  ================================

  以下是当前仓库已经完成的事实，请基于这些事实继续，而不是回滚或另起一套：

  1. Wave A 已完成并已推送到 `origin/test`
     - `9c3fc00` `[MVP-623] ... 停止跟踪 backend/.env 并收口仓库卫生规则`
     - `adb72a0` `[MVP-624] ... 搭建 guards 框架并落地 secrets/db-artifacts 检查`
     - `aa308b9` `[MVP-625] ... 建立 backend 质量入口并接入 ruff + 剩余 guards`
     - `64ebed1` `[MVP-626] ... 新增章节 meta/detail 合同并保留旧列表兼容`
     - `73fbea0` `[MVP-627] ... 关键页面切到轻量章节列表 + 按需详情读取`

  2. 后端章节合同已经打正
     - 新增：`GET /api/projects/{project_id}/chapters/meta`
     - 详情：`GET /api/chapters/{chapter_id}`
     - 旧：`GET /api/projects/{project_id}/chapters` 仍保留兼容
     - 新 meta 接口不再返回整包 `content_md / summary / plan`

  3. 前端关键页面已经做了第一轮兼容式迁移
     - `PreviewPage`
     - `ChapterReaderPage`
     - `WritingPage / useChapterEditor`
     - `useWizardProgress`
     - `ProjectWizardPage`
     - `ForeshadowsPage`

  4. 当前已经存在 transport 层
     - `frontend/src/services/chaptersApi.ts`
     - 已有：
       - `fetchAllChapterMeta(...)`
       - `fetchChapterDetail(...)`
       - `chapterDetailToListItem(...)`

  5. 当前仍然没做完的关键问题
     - 没有统一的章节缓存 / 失效 / 预取 / 共享数据层
     - 章节相关页面的数据访问模式仍然偏分散
     - 列表仍是普通渲染，不是虚拟列表
     - 800 章场景的前端体验还没有真正被证明稳定
     - 旧兼容接口仍保留，但不应再继续让前端新逻辑依赖它

  ================================
  三、这次 Wave B 的总目标
  ================================

  你这次要做的是把 Wave A 的“正确合同地基”，升级成真正可规模化使用的前端章节系统：

  - 统一章节数据访问层
  - 统一 meta/detail 类型与失效规则
  - 把章节页面做成可支撑 100~800 章的前端实现
  - 不破坏现有 Preview / Reader / Writing / Wizard / Foreshadows 功能
  - 保持兼容、可回滚、可测试、可继续部署
  - 完成后应达到：
    - 新前端逻辑不再零散地各自管理章节请求
    - 大章节列表不会再一次性渲染全部节点
    - 切章/返回/前进/编辑/保存的用户体验不回退
    - 本波完成后可以把结果直接推向生产，而不需要“再手工补一轮稳定化”

  ================================
  四、硬性约束（必须遵守）
  ================================

  1. 分支要求
     - 开始前先执行：
       - `git branch --show-current`
     - 必须在 `test` 或 `test/*` 分支工作
     - 如果不在，按仓库规则切换

  2. 工作流要求
     - 必须完整执行：
       - 调查 → plan → issue CSV → 实现 → 测试 → review → commit → push → regression
     - 不允许只做计划或只做调查
     - 不允许只做一部分后把剩下工作甩给用户

  3. Plan / Issue CSV
     - 必须生成正式 plan 文件：
       - `plan/YYYY-MM-DD_HH-mm-ss-<slug>.md`
     - 必须生成正式 issue CSV：
       - `issues/YYYY-MM-DD_HH-mm-ss-<slug>.csv`
     - timestamp 和 slug 必须一致
     - issue 数量要求：
       - **4~6 个高质量 issue**
       - 每行都必须独立可交付、可测试、可回滚

  4. Commit 规则
     - **Issue CSV 一行 = 一个 commit**
     - 每个 commit 必须同时提交：
       - 对应代码改动
       - 当前 CSV 状态更新
     - commit message 必须符合：
       - `[<ID>] <Title>`
     - 每个 commit 后都要 push 到 GitHub `test` 分支

  5. 安全与范围
     - 不泄露 secrets
     - 不做无关大重构
     - 不破坏已有兼容层
     - 不删除旧后端 `/api/projects/{id}/chapters` 兼容接口
     - 除非有充分理由，不要引入又大又重的新依赖
     - 如需引入依赖，必须说明为什么仓库内已有模式不够用

  ================================
  五、这次 Wave B 的明确范围
  ================================

  --------------------------------
  A. T05｜前端数据层升级
  --------------------------------

  你必须完成的核心目标：

  1. 建立统一的章节前端数据层
     - 目标不是再多写几个 `fetch(...)`
     - 目标是形成可复用、可缓存、可失效、可预取的统一访问层
     - 应至少覆盖：
       - chapter meta list
       - chapter detail
       - create / update / delete 后的失效与同步
       - Preview / Reader / Writing 之间的共享语义

  2. 类型合同统一
     - 明确区分：
       - `ChapterListItem`
       - `ChapterDetail`
     - 不要让“列表对象有时是 detail、有时不是 detail”的混乱继续扩大
     - 需要让后续开发者一眼看懂哪些场景用 meta，哪些场景必须请求 detail

  3. 缓存与失效机制
     - 至少要覆盖：
       - 创建章节
       - 更新章节
       - 删除章节
       - 切换项目 / 切换 active chapter
       - bulk_create 或其他会影响章节列表的入口
     - 必须真实可工作，而不是只做一个概念层

  4. 预取与共享
     - 在风险可控的前提下，优先为：
       - Reader
       - Preview
       - Writing
     - 做合理的 next/prev detail 预取或相邻详情复用
     - 但不要为了预取把全量正文重新搬回列表

  5. 尽量延续当前已有结构
     - 当前已有 `chaptersApi.ts`
     - 你应优先把它作为 transport 层继续扩展，而不是绕开重来
     - 数据层可落在：
       - `frontend/src/services/chapter*.ts`
       - `frontend/src/hooks/chapter*.ts`
       - 或其他更合适、但清晰可维护的目录
     - 你需要根据仓库现状做最合适的设计，避免过度工程化

  **T05 完成标准：**
  - 章节相关页面有统一数据访问层
  - meta/detail 类型边界清晰
  - create/update/delete 的缓存与失效真实可用
  - 跨页面行为一致，不再每页各自一套章节请求逻辑
  - 不破坏现有用户路径

  --------------------------------
  B. T06｜大规模章节场景优化
  --------------------------------

  你必须完成的核心目标：

  1. 让大章节列表真正可用
     - 至少覆盖最关键的章节列表 UI：
       - `Writing` 左侧章节列表
       - `Preview` 章节列表
       - `Reader` 章节列表
     - 第一版目标：
       - 不能再对 100~800 章简单全量渲染按钮列表
     - 推荐方向：
       - 虚拟列表 / windowing
       - 保留 active item 的 scroll-into-view
       - 保留键盘左右切章与点击切章体验

  2. 详情读取减重
     - 切章时只请求当前需要的 detail
     - 不回退到“全量列表带正文”
     - 合理利用上一任务的数据层成果：
       - 缓存
       - 失效
       - 相邻详情预取
       - stale 防护

  3. 体验不能回退
     - 必须保留：
       - Preview 阅读
       - Reader 阅读 + memory sidebar
       - Writing 编辑 / 保存 / 切章
       - mobile drawer 基本可用
       - active chapter 同步正确
     - 如果虚拟列表影响键盘导航/移动端抽屉/active 高亮，那就是不可接受的回退

  4. 如有必要，补小范围 UI 能力
     - 如果你判断 800 章场景没有搜索/快速定位会非常难用，可以补最小可行：
       - 章节编号搜索
       - 标题过滤
     - 但不要把这一波做成完整的复杂目录管理系统

  **T06 完成标准：**
  - 章节列表 UI 不再简单全量渲染所有项
  - 大项目切章和打开页面的体验显著减重
  - Preview / Reader / Writing 不回退
  - 不破坏现有移动端/键盘/选择逻辑
  - 对后续 `T13` 的性能基线建立有明确支撑

  ================================
  六、Issue 设计要求
  ================================

  请不要把 `T05/T06` 直接写成 2 行大 issue。

  推荐你拆成 **5 个 issue** 左右，例如：

  - Issue 1：章节数据层基础设施 + cache key / types / invalidation contract
  - Issue 2：Writing 迁入统一章节数据层
  - Issue 3：Preview / Reader 迁入统一数据层 + next/prev detail 预取
  - Issue 4：大章节列表虚拟化 / windowing + active item / mobile / keyboard 保真
  - Issue 5：测试、blackbox、文档、收尾回归

  你可以调整，但要求：
  - 每条 issue 独立有价值
  - 每条 issue 都可测试
  - 每条 issue 都能单独 commit + push
  - 每条 issue 都能清晰回滚

  建议从 `MVP-628` 往后继续编号，但要先以仓库实际情况为准核对是否冲突。

  ================================
  七、实施原则（非常重要）
  ================================

  1. Compatibility-first
     - 这波不能为了“漂亮架构”牺牲当前可用性
     - 旧兼容接口先保留
     - 前端读路径先统一、再收旧逻辑

  2. 不引入大爆炸
     - 不要顺手把整套任务系统、SSE、runtime 都改了
     - 不要把 Wave B 变成 Wave C

  3. 数据层必须真实可用
     - 不要只做一层薄 wrapper
     - 必须真有缓存、真有失效、真能减少重复请求

  4. 大列表优化必须真实有效
     - 不要只改命名或只写注释
     - 必须让 DOM 渲染和切章行为真实改善

  5. 测试与回归要贴着风险走
     - 这波最危险的是：
       - 章节切换错乱
       - active item / detail 不一致
       - 保存后列表状态不同步
       - reader/preview 打开空内容
       - mobile drawer / keyboard nav 回退
     - 所以测试必须直接覆盖这些点

  ================================
  八、建议的实现方向（可调整，但不要偏航）
  ================================

  以下是建议方向，不是强制实现方式，但请优先沿着这个思路做：

  1. 把 `frontend/src/services/chaptersApi.ts` 保持为 transport 层
  2. 新增一层共享的数据访问 / cache / invalidation 层
     - 例如：
       - `frontend/src/services/chapterStore.ts`
       - `frontend/src/hooks/useChapterMetaList.ts`
       - `frontend/src/hooks/useChapterDetail.ts`
       - 或其他更合适的同等结构
  3. 让 `Preview / Reader / Writing / Wizard / Foreshadows` 通过统一层拿章节数据
  4. create/update/delete/bulk_create 后，统一触发章节列表和详情失效
  5. 在 `Preview / Reader` 中为相邻章节做轻量 detail 预取
  6. 为章节列表组件做虚拟化 / windowing
     - 保持 active 高亮
     - 保持滚动到当前项
     - 保持点击和键盘切换
  7. 如果实现虚拟列表时发现当前 `ChapterListPanel` 不适合作为唯一承载组件，可以重构，但必须是**可控、可回滚、无行为回退
  **的重构
  8. 不要引入依赖沉没成本很大的新状态管理框架，除非你能证明现有模式无法完成目标

  ================================
  九、测试要求
  ================================

  你必须遵守 `docs/testing-policy.md`，并按“最小但可靠”的策略跑测试。

  --------------------------------
  1）前端基础验证（必跑）
  --------------------------------

  在 `frontend/`：

  - `npm run lint`
  - `npm test`
  - `npm run build`

  --------------------------------
  2）与章节相关的黑盒 / UI 回归（必跑）
  --------------------------------

  至少运行：

  - `cd test && npx playwright test specs/api/chapters-meta.contract.spec.ts`
  - `cd test && npx playwright test specs/ui/chapter-reader.spec.ts specs/ui/xss-markdown.spec.ts specs/ui/writing-save-
  status.spec.ts`

  并且你应该新增/补跑一组真正贴近 `T05/T06` 的测试，建议至少包含其一：

  - 大章节列表渲染/切章/active 保真 spec
  - Preview / Reader / Writing 在大量章节下的行为回归 spec
  - 章节缓存/失效相关黑盒验证（保存后列表状态同步、切章后 detail 正确等）

  如果你新增 Playwright spec，请把它也纳入最终回归命令。

  --------------------------------
  3）Backend 验证（如有任何 backend 改动则必跑）
  --------------------------------

  如果你碰到 backend 或 contract 代码，在 `backend/` 额外跑：

  - `.\.venv\Scripts\python.exe -m compileall -q app alembic`
  - `.\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v`

  --------------------------------
  4）最终回归（必跑）
  --------------------------------

  在本波所有 issue 完成后，至少完成一轮与本波改动强相关的可靠回归，而不是只跑单个测试。

  注意：
  - 上一波黑盒回归时，本机 `127.0.0.1:8000` 被其他进程占用
  - 如果再次遇到同样问题，可使用：
    - `E2E_BACKEND_URL=http://127.0.0.1:51116`
    - `E2E_FRONTEND_URL=http://127.0.0.1:51117`
  - 不要因为端口冲突就跳过 E2E

  ================================
  十、停止条件
  ================================

  只有当以下条件全部满足时，你才能停下：

  1. `T05 + T06` 的本波范围内要求都已完成
  2. plan 文件已生成
  3. issue CSV 已生成并正确更新状态
  4. 每条 issue 都已完成对应 commit
  5. 每条 issue 都已 push 到 `test` 分支
  6. 相关测试与最终回归已跑
  7. 最终状态满足：
     - 功能完整
     - 无明显回退
     - 章节大列表体验明显改善
     - 可继续生产部署
     - 可作为后续 `T07/T08` 的稳定前置

  ================================
  十一、最终汇报时必须包含
  ================================

  1. 这波拆成了哪些 issue（ID + 标题）
  2. 每条 issue 做了什么
  3. 每条 issue 对应哪些 commit
  4. 跑了哪些测试
  5. 哪些性能 / 维护性风险被消除
  6. 哪些内容被刻意留到下一波（尤其是为什么不做 `T07/T08`）
  7. 当前是否已经满足“可直接继续推生产”的标准
  8. 给下一波（建议是 `T07 + T08`）的最佳起点建议

  现在开始执行。
  不要停在计划阶段，不要停在 issue 阶段，直接做到这波完整闭环。