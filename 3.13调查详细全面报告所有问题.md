# 3.13调查详细全面报告所有问题

> 审查日期：2026-03-13  
> 审查仓库：`E:\毕设小说项目\项目\new-demo\demo`  
> 当前分支：`test`  
> 审查对象：ainovel AI 小说项目（前端 React + TypeScript + Vite + Tailwind；后端 FastAPI + SQLAlchemy + SQLite/Alembic；黑盒测试 Playwright）  
> 报告定位：面向项目负责人、主程、后续接手开发者的全局可信审查报告  
> 结论一句话：这个项目已经不是“玩具 demo”，而是一个功能密度很高、测试基础明显高于普通学生项目、并且已经具备多模块写作平台雏形的中型工程；但它同时也已经出现了非常典型的“功能长得太快，架构边界和治理没有同步收紧”的问题。它能支撑当前阶段的单机开发、内测、少量真实用户与中小规模协作，但离“工业级、长期演进、能稳妥支撑五六位数用户、支持多 agent / 插件生态”的目标还有明显距离。

---

## 一、审查方法、证据来源与可信度声明

### 1.1 本次到底做了什么

这次不是只打开几个页面看一下外观，也不是只运行一次测试就下结论，而是对整个仓库做了三层审查：

1. **全仓清单级扫描**  
   使用 `rg --files`、目录统计、脚本扫描，对前端、后端、测试、文档、脚本、迁移文件做了完整 inventory，先确认项目到底有多少模块、多少页面、多少接口、多少模型、多少迁移，再决定重点深读区域。

2. **热点文件深读 + 关键流程定点审查**  
   重点阅读了：
   - 前端路由入口、布局、主题、UI 文案、全局 API 客户端、核心页面与写作/大纲/配置/任务/RAG/世界书相关页面
   - 后端入口、配置、数据库 session、迁移入口、任务队列、项目任务服务、生成链路、LLM 客户端、MCP 服务、主要 API 路由
   - 设计文档、部署文档、SQLite 并发文档、任务系统文档、UI/API 映射文档、测试说明文档

3. **自动化验证与实证**  
   实际执行了编译、lint、单测、构建、质量门、Playwright 黑盒测试，不靠“我觉得应该没问题”的主观推断来写报告。

### 1.2 本次执行过的验证命令与结果

已实跑并记录结果如下：

- 后端编译检查  
  `cd backend && .\.venv\Scripts\python.exe -m compileall -q app alembic`  
  结果：通过

- 后端单元测试  
  `cd backend && .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v`  
  结果：`427` 项通过，`1` 项跳过

- 后端质量门  
  `cd backend && .\.venv\Scripts\python.exe scripts\run_quality_gate.py`  
  结果：通过，但暴露大量 warning：超大文件、API 路由中直接调用 LLM 的遗留 allowlist

- 前端 lint  
  `cd frontend && npm run lint`  
  结果：通过

- 前端单元测试  
  `cd frontend && npm test`  
  结果：`14` 个文件，`51` 个测试全部通过

- 前端构建  
  `cd frontend && npm run build`  
  结果：通过，但产出较大的 chunk

- Playwright 子集验证  
  `cd test && npx playwright test specs/ui/navigation.spec.ts specs/api/api-smoke.spec.ts specs/db/db-schema.spec.ts`  
  结果：`5` 项通过

- Playwright 全量黑盒验证  
  `cd test && npm test`  
  结果：`138` 项中 `137` 项通过，`1` 项失败  
  失败用例：`test/specs/ui/import.spec.ts`  
  这不是“环境完全跑不起来”，而是一个非常有价值的真实缺陷信号：导入页的列表/详情状态存在不一致。

### 1.3 覆盖范围与诚实边界

必须坦白说：仓库体量已经不小，按“人工逐行逐字读完所有 900+ 文件每一行”的标准，这次工作量远超单次会话的理性边界。因此，本报告采用的是**全仓结构扫描 + 所有核心热点深读 + 自动化验证 + 关键流程追证**的方法。这种方法对高风险区域的可信度很高，对低风险边角文件则主要依赖结构扫描、命名约定、统一规则和现有测试保护。

换句话说：

- 我对**主路径与高风险区域**的判断可以给出很强结论；
- 对于少量边角工具文件，我给的是高可信的工程判断，而不是伪装成“已经人工检查每个字符”的假话。

这是比“装作全看过”更可信的审查方式。

### 1.4 证据基础

本报告主要基于以下事实来源：

- 仓库源码与文档实读
- 已执行测试与构建结果
- `backend/scripts/run_quality_gate.py` 输出
- `docs/ux/ui-api-map.md`、`docs/design/sqlite-concurrency.md`、`docs/design/task-system.md` 等设计文档
- 关键文件精确定位：
  - `frontend/src/services/theme.ts:7,32`
  - `frontend/src/index.css:40,62,185,216,270`
  - `frontend/src/components/prompts/LlmPresetPanel.tsx:473-489,569-585,772-797`
  - `frontend/src/pages/OutlinePage.tsx:756,770,1006,1046`
  - `frontend/src/pages/writing/useChapterGeneration.ts`
  - `frontend/src/components/writing/AiGenerateDrawer.tsx`
  - `backend/app/db/session.py:17,42-45`
  - `backend/app/main.py:68-74`
  - `backend/app/api/routes/llm_models.py:157-214`
  - `backend/app/api/routes/tables.py:186-196`
  - `backend/app/services/mcp/service.py:87-130`
- 外部权威文档（官方文档为主）：
  - SQLite WAL 官方文档：<https://www.sqlite.org/wal.html>
  - SQLite `busy_timeout` 官方文档：<https://www.sqlite.org/pragma.html#pragma_busy_timeout>
  - SQLAlchemy SQLite threading/pooling 文档：<https://docs.sqlalchemy.org/en/20/dialects/sqlite.html>
  - FastAPI Bigger Applications 文档：<https://fastapi.tiangolo.com/tutorial/bigger-applications/>
  - RQ Workers 官方文档：<https://python-rq.org/docs/workers/>
  - Tailwind 自定义主题变量文档：<https://tailwindcss.com/docs/adding-custom-styles>

---

## 二、项目总体画像：这到底是个什么级别的工程

### 2.1 仓库体量

按本次统计：

- `backend`：约 `502` 个文件，约 `64,034` 行
- `frontend`：约 `184` 个文件，约 `46,248` 行
- `test`：约 `151` 个文件，约 `21,227` 行
- `docs`：约 `60` 个文件，约 `4,067` 行
- `scripts`：约 `15` 个文件，约 `1,072` 行

这说明项目已经远超“毕业设计随便拼起来”的典型规模。尤其是：

- 后端不是简单的 CRUD，而是已经有任务系统、记忆系统、向量检索、图谱、风格、提示词工作室、导入/导出、认证、项目成员、generation runs、MCP 研究工具入口等复杂模块。
- 前端也不是几个页面，而是一个完整写作工作台，拥有路由、布局、主题、抽屉、全局 toast/confirm、任务中心、RAG 调试页、Prompt Studio、风格页、导入页等。

### 2.2 功能密度

按接口统计，`backend/app/api/routes/*.py` 中共识别出 **185 个 HTTP endpoint**。这不是“后端没多少东西”的项目。它已经具备一个小型平台级产品的复杂度。

按页面统计，`frontend/src/pages/**/*.tsx` 共 **39 个页面文件**，覆盖：

- 登录/注册/管理员
- 项目首页与创建向导
- 角色、大纲、写作、预览、阅读、导出
- 提示词配置、提示词工作室、模板
- 任务中心、结构化记忆、数值表格、伏笔
- 世界书、图谱、RAG、Fractal、搜索、导入
- 风格与项目设置

### 2.3 工程成熟度第一印象

这个项目最值得肯定的一点是：**它不是一坨没有测试、没有约束、所有逻辑堆到一个文件的“功能堆积物”。**

它已经具备下面这些明显强于普通项目的工程特征：

- 有后端质量门脚本
- 有前端 UI class 守卫脚本
- 有大量后端单测
- 有较多 Playwright 黑盒测试，且覆盖真实页面流程
- 有部署文档、并发设计文档、UI/API 映射文档、任务系统文档
- 有请求 ID、统一错误响应、日志脱敏、API Key 密文存储
- 有 generation runs、debug bundle、context preview 等可追溯机制

所以，本报告不会把项目简单归类为“烂”。  
真正准确的结论是：**底子不差，甚至很多地方明显用心了；但架构治理已经开始落后于功能增长速度。**

---

## 三、总评结论：强项、硬伤、最终判断

### 3.1 总体评分（按当前状态，不带情绪）

| 维度 | 评分 | 结论 |
| --- | --- | --- |
| 功能完整性 | 8/10 | 功能很多，且不是空壳，很多高级功能确实做了 |
| 前端设计统一性 | 7/10 | 已有设计 token 与组件类约束，但仍未做到完全统一 |
| 前端可维护性 | 5.5/10 | 多个页面过大，编排逻辑过重 |
| 后端工程化 | 7.5/10 | 配置、日志、错误处理、任务系统、测试都不错 |
| 后端架构清晰度 | 5/10 | 业务边界开始塌陷，超大 route/service 明显 |
| 数据模型设计 | 7.5/10 | 覆盖面广，迁移链条完整，领域模型不弱 |
| 测试与回归能力 | 8/10 | 明显高于平均，但仍有遗漏和单点失败 |
| 可观测性 | 5/10 | 有 health、任务事件、debug bundle，但缺少正式监控体系 |
| 扩展性（多 agent / 插件） | 5/10 | 有一些前置基础，但还没形成真正稳定扩展层 |
| 生产承载力（SQLite 模式） | 2/10 | 明确不能上量 |
| 生产承载力（Postgres+Redis+RQ 形态） | 5/10 | 能走向小型生产，但离大规模还有明显差距 |

### 3.2 三个最重要的结论

#### 结论 A：项目有真实工程价值，绝不是空壳

这个项目已经具备：

- 多 Provider LLM 适配
- 大纲/章节流式生成与回退
- Prompt Studio/提示词模板治理
- 结构化记忆、WorldBook、图谱、向量 RAG、Fractal 等记忆与检索体系
- 项目任务中心、事件流、运行时状态
- 用户、角色、项目成员、管理员
- 导入/导出、风格、搜索、调试

这意味着它不是“功能想法列表”，而是真做出来了很多东西。

#### 结论 B：它的主要问题不是“没功能”，而是“功能已经把边界压垮了”

项目真正的主要风险不是功能缺失，而是：

- 超大页面与超大路由越来越像“神文件”
- API route 中还保留了很多直接 LLM 调用与编排逻辑
- 主题、文案、样式虽然有中心化基础，但没有完全贯彻到底
- 高级功能越来越多，但部分流程的产品解释力和交互可控性跟不上

这类问题短期不一定炸，但中长期一定拖死维护效率。

#### 结论 C：它目前不具备“五六位数用户量”的可信承载能力

这个结论不是拍脑袋，而是仓库自己就已经写得很清楚：

- `README.md` 明确说明 SQLite 模式必须单 worker
- README 明确说明 Docker Compose 形态用于“三位数并发的基础需求”
- `docs/performance-baseline.md` 的基线只测到后端并发 `1 / 5 / 20`
- 当前没有成熟的限流、缓存、指标、分布式任务治理、对象存储、租户隔离、压测闭环

所以如果有人说“这个项目现在已经能顶 5 万用户”，那是不可信的。

---

## 四、前端整体审查

### 4.1 前端架构优点

#### 4.1.1 路由组织是有结构的，不是野生拼接

`frontend/src/App.tsx` 使用了 `createBrowserRouter` + `lazy()` + `Suspense` 的组织方式，页面按路由拆分并通过 `importWithChunkRetry` 做 chunk 加载重试。  
这说明前端不是把全部页面一把梭打进主 bundle，而是已经考虑了按页面懒加载与失败恢复。

这是一个明显优点。

#### 4.1.2 布局与导航集中化程度不错

`AppShell` 与 `appShellNavConfig.tsx` 集中维护主导航。  
`frontend/src/lib/routes.ts` 还维护了 route meta。  
这意味着项目不是每个页面自己随便造导航，而是有统一外壳与统一路由语义层。

#### 4.1.3 UI token 基础比大多数同类项目更好

`frontend/src/index.css` 已经定义了：

- 颜色 token：`--color-canvas`、`--color-surface`、`--color-accent` 等
- 字体 token：`--font-ui`、`--font-content`、`--font-mono`
- 动效 token：`--motion-duration-*`、`--motion-ease-standard`
- 组件基类：`.btn`、`.input`、`.select`、`.panel`

而且 `tailwind.config.js` 已经把部分 utility 映射到 CSS 变量上。  
这说明项目不是完全依赖硬编码 Tailwind palette，而是已经有“设计系统底座”的意识。

#### 4.1.4 设计规范不是空文档，至少部分进入了自动化

`frontend/scripts/check-ui-classes.mjs` 会校验：

- `btn-*` 变体不能脱离 `btn` 基类
- 只写 `btn` 而不带 variant 也会报错

这类规则虽然简单，但它说明项目已经在尝试把 UI 规范自动化，而不是靠口头约定。

### 4.2 前端最突出的硬伤

#### 4.2.1 页面级神文件已经很多

本次统计中，前端超大页面包括：

- `frontend/src/pages/PromptsPage.tsx`：`2307` 行
- `frontend/src/pages/SettingsPage.tsx`：`2200` 行
- `frontend/src/pages/WorldBookPage.tsx`：`1824` 行
- `frontend/src/pages/TaskCenterPage.tsx`：`1343` 行
- `frontend/src/pages/OutlinePage.tsx`：`1078` 行
- `frontend/src/pages/WritingPage.tsx`：`813` 行
- `frontend/src/pages/PromptTemplatesPage.tsx`：`797` 行
- `frontend/src/pages/PromptStudioPage.tsx`：`769` 行
- `frontend/src/pages/AdminUsersPage.tsx`：`730` 行
- `frontend/src/pages/StructuredMemoryPage.tsx`：`714` 行
- `frontend/src/pages/RagPage.tsx`：`701` 行

这已经不是“稍微有点大”，而是明确进入了维护成本危险区。

页面大的问题不在于美观，而在于：

- 状态与副作用很难推理
- 局部修改很容易碰到无关逻辑
- 组件测试粒度变粗
- 审查成本暴涨
- 新人接手会直接掉进上下文黑洞

#### 4.2.2 主题系统只有“单主题 + 明暗模式”，不是真正的多主题系统

`frontend/src/services/theme.ts:7` 直接把 `themeId` 固定成 `"paper-ink"`；  
`frontend/src/services/theme.ts:32` 只是在 `documentElement.dataset.theme` 上写入当前 themeId；  
而 `frontend/src/index.css:40` 和 `:62` 也只定义了 `[data-theme="paper-ink"]` 及其 `.dark` 变体。

这代表什么？

- 当前系统支持“纸墨风”的 light/dark 切换；
- 但并不支持“第二套、第三套主题包”的成熟切换；
- 更谈不上“快速做出第三种颜色适配”或者“一整套 RGB/品牌化主题矩阵”。

如果你后续想做：

- 多品牌皮肤
- 用户可切换视觉主题
- 节日活动主题
- 出版社/渠道定制主题

那现在的前端设计系统还不够。

#### 4.2.3 文案中心化做了一半，没彻底做完

`UI_COPY` 是存在的，而且引用次数很多，本次统计：

- `UI_COPY` 引用：约 `612`
- `uiCopy` 相关引用：约 `48`

但仓库中仍然存在大量页面内联文本，静态扫描统计到的 inline text node 量依然很高。  
这说明项目在“统一文案源”这件事上处于**半中心化**状态。

后果是：

- 文案统一性和语气一致性难完全保证
- 后期做国际化会很痛
- 术语替换时需要跨很多页面搜
- 高级页与普通页的文案成熟度会出现落差

#### 4.2.4 样式 token 统一性总体不错，但仍有泄漏点

优点：

- 前端代码里没有泛滥的十六进制颜色硬编码
- 按钮/输入/面板的公共类使用率较高

但仍发现硬编码 palette 泄漏，例如：

- `frontend/src/pages/AdminUsersPage.tsx` 使用 `emerald/slate`
- `frontend/src/components/ui/Overlay.tsx`、`frontend/src/components/layout/AppShell.tsx` 使用 `bg-black/30`
- `frontend/src/pages/SettingsPage.tsx` 使用 `text-red-*`
- `frontend/src/pages/StructuredMemoryPage.tsx`、`frontend/src/pages/structuredMemory/CharacterRelationsView.tsx` 使用 `amber-*`
- `frontend/src/pages/WorldBookPage.tsx` 使用 `text-white`

这些不是灾难级问题，但它们说明：

- 前端还没有做到“全部由 design token 管理”
- 某些页面为了快，仍然绕过了统一主题变量
- 一旦未来要切换第三主题，这些泄漏点会成为返工清单

#### 4.2.5 bundle 体积已经开始提醒你“别再往单页里塞了”

本次构建产物中值得警惕的 chunk：

- `assets/index-BPtckMKS.js` 约 `477.54 kB`（gzip 约 `156.52 kB`）
- `assets/pinyin-tKT4_raX.js` 约 `303.09 kB`（gzip 约 `138.89 kB`）
- `WritingPage` chunk 约 `165.16 kB`
- 另一个 `index-*` chunk 约 `156.61 kB`
- `PromptsPage` chunk 约 `73.31 kB`
- `SettingsPage` chunk 约 `60.21 kB`

路由懒加载确实在做，但大页面本身的体积增长仍然明显。  
这会导致：

- 首次进入某些重型页面的等待更明显
- 弱网环境下体验下降
- 页面内部再次叠加复杂逻辑后更难继续扩展

### 4.3 页面逐页审查

下面按页面职责逐页给出判断。这里不装样子写“全部完美”或“全部糟糕”，而是按真实情况分层。

#### 4.3.1 登录/注册页

- `LoginPage.tsx`、`RegisterPage.tsx` 体量适中，职责单一。
- 黑盒测试覆盖了回车提交等细节，这是加分项。
- 这部分前端不是项目当前主要风险。

#### 4.3.2 Dashboard 与 ProjectWizard

- 首页、项目创建向导整体结构清楚，属于“能用且不算脏”的部分。
- 向导和全局进度条体系（Wizard 系列）说明产品思路是成型的。
- 问题不在“有没有”，而在“后续步骤信息是否足够解释下一步为何不可用”。当前仍有少量状态依赖需要作者自己猜。

#### 4.3.3 SettingsPage

这是前端最明显的“超载页面”之一。

它把以下内容挤在一个页面里：

- 项目基础设定
- vector embedding / rerank 配置
- query preprocessing
- 图相关配置
- membership 管理
- 其他项目设置

问题不是内容多，而是**关注点过多**。  
你后面再接入更多模型参数、第三方插件设置、Agent 策略设置时，这个页面会继续膨胀，最后变成不可维护的配置大杂烩。

#### 4.3.4 PromptsPage

这是整个项目最关键也最复杂的前端页面之一，同时也是最危险的页面之一。

优点：

- 支持主模块 + 任务模块覆盖
- 支持 profile、API Key、测试连接、模型列表、任务级 API Key
- 确实有不少 guard，说明开发者意识到了配置错误的风险

问题：

- 文件过大
- 信息架构已经很重
- 用户容易搞不清“主配置”“任务绑定配置”“API Key 保存到哪里”“模型列表在什么前提下能拉到”

特别是模型列表/测试连接的交互，存在明显认知模糊点：

- `LlmPresetPanel.tsx:473-477` 的“拉取模型列表”按钮在主模块中只受 `loading/saving` 控制，并**不要求已保存 API Key**
- 但 `LlmPresetPanel.tsx:481-485` 的“测试连接”会受 `testDisabledReason` 影响
- 主配置区底部 `LlmPresetPanel.tsx:772-797` 又要求先有 `selectedProfileId` 且有已保存 key 才能清除/处理 key
- 后端 `backend/app/api/routes/llm_models.py:178-214` 在缺少 key 时会返回 `models: []` 和 `warning`，而不是硬错误

这会产生什么体验？

- 用户看到“拉取模型列表”按钮可点，以为可以正常用
- 结果拿到空列表或 warning
- 用户不确定是 base_url、provider、profile、API Key 还是后端环境有问题

这不是代码错误，而是**产品状态表达不够干净**。

#### 4.3.5 OutlinePage

大纲页整体比我预期好，尤其不是你说的“什么都没做”。事实上，这页做了很多：

- AI 生成大纲
- 流式生成
- 章节 JSON 预览
- raw 原始片段预览
- 结果预览后覆盖或另存为新大纲
- 从大纲创建章节骨架
- 自动保存与未保存拦截

证据：

- `OutlinePage.tsx:756` 有“实时章节预览（JSON）”
- `OutlinePage.tsx:770` 有“流式原始片段（raw）”
- `OutlinePage.tsx:1006` 开始是“生成结果预览”
- `OutlinePage.tsx:1046` 使用 `MarkdownEditor` 编辑 `outline_md`

但你的抱怨依然有道理：  
当前最终的主编辑对象仍然是 `outline_md`，而不是一个一等公民的结构化章节编辑器。  
这会导致：

- JSON 结构虽然可看，但不是最好改
- 章节级别的二次调整成本还是高
- 重新生成、局部重生成、结构级 patch 的产品能力还不够强

我的判断是：**这个功能不是没实现，而是实现到了“有预览、有回退、有覆盖”，但没实现到“作者可结构化精修”的完整产品阶段。**

#### 4.3.6 WritingPage

写作页是全项目价值最高的页面之一，也是复杂度极高的页面。

优点：

- 不是简单的“点一下生成正文”
- 有 memory injection、context preview、prompt inspector、plan first、post edit、content optimize、style、stream、append/replace 等多条能力链
- `useChapterGeneration.ts` 有明显的 guard/fallback 设计

例如：

- 没有保存 LLM 配置会报错
- 非 OpenAI provider 开 stream 时会自动回退非流式
- 缺少前置章节内容会给出具体缺失章节号，并支持跳转
- 对 `dropped_params` 做了用户提示
- 对润色、正文优化做了 compare/revert

所以，写作高级功能不是摆设，也不是“按钮都连空方法”。它确实做了。

但问题同样清楚：

- 功能太多，用户理解成本高
- “流式/可靠链路/自动回退/预检/覆盖 prompt”这些概念叠在一起，认知负担很重
- 代码层面 hook 和 drawer 已经非常复杂，维护成本开始逼近极限

也就是说，**功能层面是有料的，产品解释层和代码可维护性层开始吃不消。**

#### 4.3.7 TaskCenterPage

任务中心是加分模块，说明项目已经具备“后台任务不是黑箱”的意识。

优点：

- memory change sets + project tasks + runtime timeline
- SSE 实时事件流
- 能 cancel/retry
- health 信息可见

问题：

- 页面仍然过大
- Task Center 承载了太多不同类型任务的展示逻辑
- 随着自动任务越来越多，信息架构还要继续升级，否则会进入“数据很多但看不懂”的状态

#### 4.3.8 StructuredMemory / NumericTables / Graph / RAG / Fractal 这些高级页

这几页说明项目不只是写作器，而是在尝试做“小说知识底座 + 调试工作台”。

优点：

- 这些页面不是空白壳
- 黑盒测试覆盖了不少高级流程
- 与后端 API 和任务中心的联动是真实存在的

问题：

- 页面的信息层级仍然偏密
- 高级调试文档有了，但一致性治理没有完全自动化
- 某些页的视觉与交互成熟度差异较大

换句话说：  
“高级调试”这条路是走对了，但现在仍处于**有能力、缺治理、缺统一体验模型**的阶段。

#### 4.3.9 WorldBookPage

功能丰富，批量操作、导入导出、搜索排序、预览触发、自动更新都做了。  
黑盒测试覆盖也不少。

但问题很现实：

- 文件太大
- 页面承担的动作太多
- 未来一旦再接人物卡联动、更多规则匹配、第三方词典同步，这页会继续失控

#### 4.3.10 ImportPage

导入页在产品思路上是合理的：

- 上传 txt/md
- 切 chunk
- 预览 worldbook / story_memory proposal
- 选择性应用

这很对。

但本次全量 Playwright 暴露出一个真实缺陷：

- 用例：`test/specs/ui/import.spec.ts`
- 现象：期望在导入记录列表中看到 `import-demo.txt` 对应按钮，但未找到
- 同时错误上下文显示：右侧详情区已经能显示该导入项内容与提案

这代表前端存在**列表状态与详情状态不一致**的问题：

- `createImport()` 会先 `loadList()` 再 `selectDocAndLoad()`
- 但实际执行中，详情已能打开，列表却仍显示“暂无导入记录”

这类 bug 的严重性在于：

- 它不会让系统彻底崩
- 但会严重破坏用户对“导入是否成功”的信任
- 而且这正是黑盒测试最擅长抓到、人工最容易忽略的那种问题

这应该列为明确的 P1 缺陷。

#### 4.3.11 SearchPage / PreviewPage / ReaderPage / ExportPage / StylesPage

这些页面整体质量中上：

- 职责相对明确
- 测试覆盖有一定保障
- 没有看到特别恶性的结构问题

它们当前不是全仓最危险的区域。

#### 4.3.12 AdminUsersPage

后台管理页是有用的，但：

- 文件较大
- 使用了 `emerald/slate` 等硬编码 palette
- 与主写作工作台视觉体系不完全一致

如果后面做真正的后台控制台，建议单独抽成管理视觉子系统，不要继续混进同一套作者工作台语义里。

---

## 五、前端 UI、一致性、工业设计与流程设计审查

### 5.1 UI 统一管理：已有基础，但没到“全面统一”

当前项目在 UI 统一方面，现实结论是：

**已经有明显的统一管理基础，但还没有完成彻底治理。**

证据包括：

- `index.css` 中有主题色、字体、动效 token
- `.btn/.input/.select/.panel` 等公共类已经形成
- `check-ui-classes.mjs` 对按钮类做了 lint 级约束
- `UI_COPY` 试图统一文案

但以下方面仍未完全统一：

- 颜色仍有泄漏点
- 文案仍有大量页面内联
- 某些高级页的信息结构更像“工程面板”，某些页更像“产品页”
- 主题仅支持单视觉系统的 light/dark，而非多主题矩阵

结论：  
**前端统一性不是零分，但也绝对不能说“全部统一管理、随时能换第三套视觉皮肤”。**

### 5.2 能否快速做第三种颜色适配 / RGB 级适配？

#### 当前答案：不能算真正能

原因：

1. `themeId` 只有 `"paper-ink"` 一个
2. `index.css` 只定义了 `paper-ink` 的 light/dark
3. 页面中仍有少量硬编码 palette
4. 没有主题包注册机制、主题 schema、主题 QA 基线
5. 没有针对第三主题的自动截图回归与视觉审查矩阵

如果你现在强行上第三主题，会发生：

- 大部分页面能跑
- 少量页面出现跳色、文字对比度、警告/危险态不一致
- 某些硬编码的 `text-white/bg-black/amber/red` 需要人工清理

#### 更好的修复方向

建议把主题体系升级为：

- `themeId` 从字面量变为可扩展枚举
- 引入 `theme registry`
- 把 danger/warning/success/info/overlay/selection/scrollbar 等全部 token 化
- 增加主题 smoke test + 视觉回归
- 页面禁止直接使用语义外 palette

到那时才叫“第三主题可快速适配”。

### 5.3 工业设计与流程设计评价

#### 做得好的地方

- 核心创作链路有“向导感”
- 保存/覆盖/另存/删除等危险动作多处有 confirm
- 任务中心与调试页说明项目不是把 AI 当黑箱
- 提示词、风格、记忆、RAG、图谱等都进入了同一个写作平台逻辑，而不是互相断裂

#### 做得不够的地方

- 高级功能已经很多，但“为什么要这样用”的解释力跟不上
- 某些按钮在技术上有 guard，在产品上却没有足够清晰的前置状态说明
- 部分页面把“开发者视角的数据结构”直接裸露给作者
- 作者二次修改的主对象仍偏 Markdown/raw，而不是结构化编辑对象

你提到的这些问题是成立的：

- 大纲生成后 JSON 虽然有预览，但不是一等编辑对象
- 某些高级功能“有没有用”的价值展示不够
- 用户未必清楚何时该用润色、何时该用正文优化、何时该用去味/一致性修复

这不是因为代码没写，而是**产品分层和用户教育没跟上工程能力**。

### 5.4 关键按钮与交互细节审查

#### 5.4.1 保存类按钮

总体评价较好。

- 很多页面都有 `dirty` / `saving` 状态控制
- 有自动保存
- 有快捷键保存
- 有未保存离开拦截

这部分说明项目对“作者数据不能轻易丢”是有敬畏心的。

#### 5.4.2 删除/覆盖类按钮

总体也较好。

- 删除大纲
- 覆盖创建章节
- 覆盖生成结果
- 删除世界书条目/批量操作

这些危险动作大多有 confirm 或明显视觉提示。  
这符合写作产品应该有的谨慎。

#### 5.4.3 生成类按钮

这一类问题最多。

原因不是按钮没连上，而是生成类动作本身复杂：

- 会依赖模型配置
- 会依赖 API Key
- 会依赖前置章节、上下文、prompt preset
- 会涉及 token 消耗与回退策略

项目现在的 guard 比较多，但**guard 不是解释**。  
对高级用户足够，对普通用户还不够。

#### 5.4.4 模型列表 / 测试连接按钮

这是当前最容易让用户疑惑的一组按钮。

建议未来明确区分三种状态：

- 未绑定 profile：按钮直接禁用并说明原因
- 已绑定 profile 但未保存 key：按钮可点但显式标注“只能读取本地候选，不会请求远端”或直接禁用
- 已保存 key：允许请求，并在结果区显示来源与错误原因

#### 5.4.5 导入页按钮

导入页目前已经验证出一个真实缺陷：  
“开始导入”成功后，详情和列表不同步。

因此这组按钮当前不能算“完全可靠”。

---

## 六、后端整体审查

### 6.1 后端架构优点

#### 6.1.1 配置系统比较成熟

`backend/app/core/config.py` 虽然很大，但质量不低。

优点：

- 使用 `pydantic-settings`
- 大量字段做了 normalize/validator
- 对 prod 环境有硬性校验
- 对弱口令、CORS、`TASK_QUEUE_BACKEND`、`SECRET_ENCRYPTION_KEY` 等有防呆

这类配置严谨度，明显比一般项目高。

#### 6.1.2 入口层做了统一错误、请求 ID、日志

`backend/app/main.py` 已经具备：

- request id 中间件
- auth session 中间件
- `AppError` / `RequestValidationError` / `SQLAlchemyError` / 未捕获异常统一处理
- 日志脱敏与结构化记录

这说明后端不是一堆随意的 `raise Exception`。

#### 6.1.3 API Key 与敏感信息治理做得比平均水平好

这点必须明确表扬。

证据：

- `backend/app/models/llm_profile.py` 有 `api_key_ciphertext` 和 `api_key_masked`
- `backend/app/api/routes/llm_profiles.py` 在保存 API Key 时走 `encrypt_secret` + `mask_api_key`
- `backend/app/core/secrets.py` 提供加密、脱敏、日志 redaction
- `backend/app/services/project_task_service.py`、`memory_update_service.py`、`generation_runs.py` 等处都有 `redact_api_keys`

说明：

- 前端不是假装“后端加密”
- 后端确实在做密文存储与脱敏输出

这部分是实打实的工程加分项。

#### 6.1.4 LLM Provider 适配层做得比较扎实

`backend/app/llm/client.py` 支持：

- `openai`
- `openai_compatible`
- `openai_responses`
- `openai_responses_compatible`
- `anthropic`
- `gemini`

这不是单一 provider 的硬编码项目，而是已经做了 provider 抽象。  
这对以后扩展自建网关、代理、企业模型都是好事。

#### 6.1.5 任务系统是真实存在并可观测的

项目的后台任务并不是“起个线程就算了”。  
它有：

- `task_queue.py`
- `project_task_service.py`
- `project_task_runtime_service.py`
- Task Center 页面
- SSE 事件流
- runtime timeline
- queued/running/retry/cancel/watchdog

这说明项目对“异步任务可观测、可重试、可恢复”的认知已经建立起来了。

### 6.2 后端最大的硬伤

#### 6.2.1 route 层职责过重，边界已经开始坏掉

这是本项目当前后端架构的头号问题之一。

`backend/scripts/run_quality_gate.py` 本次实跑结果明确给出 warning：

- `no-direct-llm-call-in-api` 规则存在大量 legacy allowlist
- 典型文件包括：
  - `backend/app/api/routes/chapters.py`
  - `backend/app/api/routes/chapter_analysis.py`
  - `backend/app/api/routes/llm.py`
  - `backend/app/api/routes/memory.py`
  - `backend/app/api/routes/outline.py`

这说明什么？

说明 route 层仍然直接接触：

- LLM client
- generation pipeline
- 输出解析/重试/回退
- 复杂业务编排

按比较理想的架构，这些应该逐步沉到应用服务层。  
route 应该负责：

- 参数接收
- 权限检查
- 调用应用服务
- 返回结果

而不是继续膨胀为“第二个 service 层”。

#### 6.2.2 超大后端文件已经到了明确危险区

质量门输出的超大文件 warning 包括：

- `backend/app/api/routes/outline.py`：`3047` 行
- `backend/app/services/vector_rag_service.py`：`2472` 行
- `backend/app/api/routes/chapters.py`：`2207` 行
- `backend/app/services/memory_update_service.py`：`1439` 行
- `backend/app/services/project_task_service.py`：`1425` 行
- `backend/app/services/import_export_service.py`：`1109` 行
- `backend/app/services/fractal_memory_service.py`：`1063` 行
- `backend/app/services/search_index_service.py`：`1033` 行

这种规模一旦继续发展，后果是：

- 单文件理解成本极高
- 新 bug 更容易由“修 A 伤 B”引入
- 审查和测试边界不清晰
- 很多逻辑只能靠人脑记忆，无法形成模块清晰度

#### 6.2.3 配置和服务虽然强，但模块切分仍不够细

`backend/app/core/config.py` 本身接近 `800` 行。  
这不代表配置写得差，而是说明配置项已经非常多。

项目现在正处于这样一个阶段：

- 功能扩展速度很快
- 配置数量、任务种类、记忆模块、向量模块都在增长
- 但服务切分和语义边界没有同步收紧

如果不在这时候开始主动拆分，后面“每加一个功能就得改 5 个大文件”的状态会越来越严重。

#### 6.2.4 MCP / 插件能力目前仍是演示级

用户非常关心多 agent、插件、第三方扩展。  
这里必须明确：

**当前仓库里并没有成熟的插件运行时。**

`backend/app/services/mcp/service.py` 当前只内置了：

- `mock.echo`
- `mock.sleep`
- `mock.fail`

也就是说，它更像一个：

- research tool call 记录器
- mock 工具调试壳
- 运行结果写入 generation run 的演示实现

它还不是：

- 真正的多工具注册中心
- 可热插拔插件生态
- 有权限边界/沙箱/审计策略的扩展系统
- 多 agent orchestration engine

所以如果你后面要做：

- 多 agent 协作
- 插件商店
- 自定义外部工具
- Agent 工具权限控制

那现在只能算“有一个方向上的起点”，远远不算现成可用。

#### 6.2.5 缺少正式的生产级可观测性体系

当前项目有：

- `/api/health`
- 任务运行时信息
- request id
- generation runs
- debug bundle

这已经比很多项目强。

但真正意义上的生产可观测性还缺少：

- Prometheus 指标
- OpenTelemetry tracing
- Sentry / 异常聚合
- 面向 provider、route、队列、数据库、缓存的统一 dashboard
- 长期容量趋势与告警机制

因此当前项目属于“有调试能力”，但还不属于“有完善运维观测能力”。

#### 6.2.6 缺少真正的全局限流与缓存策略

本次扫描没有发现成熟的应用级 rate limit / throttle / quota 体系。  
存在的配额主要集中在 `batch_generation` 相关逻辑，而不是全局请求治理。

同样，也没有看到成体系的后端缓存层：

- 没有统一 response cache
- 没有热数据缓存层
- 没有检索结果缓存治理
- 没有 provider 能力缓存策略

这意味着：

- 并发上来后，热点读取会直接压数据库和服务层
- LLM 配置、检索、聚合类页面在高访问量下会更快暴露性能问题

---

## 七、数据库、数据存储、检索与并发审查

### 7.1 数据模型设计：覆盖度高，说明项目不是“拍脑袋”

后端模型层覆盖了相当多领域对象：

- 项目、章节、角色、大纲
- Prompt preset / block / llm profile / llm task preset
- project settings / project membership / user / auth external account
- generation run / project task / project task event / memory task
- worldbook / story_memory / structured_memory / fractal_memory
- knowledge_base / search_index / project_table / writing_style

而且 Alembic migration 已有 **48** 个版本，说明数据演化并非一次性写死。

这代表两个结论：

1. 数据模型覆盖面是真实的；
2. 项目是有演化历史的，不是最后一天拍脑袋拼出来的。

### 7.2 SQLite 并发策略：代码与文档基本一致

从代码看：

- `backend/app/db/session.py:17` 使用 `check_same_thread=False`
- `backend/app/db/session.py:42-45` 设置 `foreign_keys=ON`、`journal_mode=WAL`、`busy_timeout=5000`
- `backend/app/main.py:68-74` 明确在 SQLite 模式下发出单 worker 警告

从文档看：

- `README.md` 明确写了 SQLite 模式必须单 worker
- `docs/design/sqlite-concurrency.md` 明确要求 LLM 调用不要持有长事务

这点是好的：**项目在 SQLite 约束上没有自相矛盾。**

### 7.3 但 SQLite 路线本身决定了它不能承载五六位数用户

SQLite + WAL 并不等于“就能水平扩展”。  
官方文档也明确说明 WAL 虽能改善读写并发，但 WAL 文件仍然只有一个，**同一时刻只有一个 writer**。  
这与项目中大量写入型操作天然冲突：

- 章节保存
- 大纲保存
- 任务状态更新
- 记忆变更集写入
- generation run 写入
- worldbook / graph / structured memory / vector dirty 标记

再考虑到你的产品强依赖异步任务与 AI 写入回流，单机 SQLite 路线在高用户规模下会非常吃力。

### 7.4 为什么 README 自己都不敢说它能上大规模

因为 README 说得很实在：

- SQLite：本地单机调试
- Compose（Postgres + Redis + rq_worker）：承载三位数并发的基础需求

这已经是仓库作者对现实容量的自我约束。  
所以“当前架构是否能稳妥顶五六位数用户量”的答案只能是：**不能。**

### 7.5 当前数据检索与长期记忆底座的评价

项目在检索和长期记忆上，不是空泛概念，而是已经形成了多层底座：

- WorldBook
- StoryMemory
- Structured Memory
- Graph
- Vector RAG
- Fractal
- Search index

这是一个非常有价值的方向，因为小说写作真正痛点就在“长期一致性”与“上下文召回”。

但问题在于：

- 各模块能力都在增长
- 数据组织越来越复杂
- 前后端解释层并没有完全统一

结果就是：  
从工程视角看，这是丰富；  
从作者视角看，可能会觉得“功能很多，但不知道什么时候该信哪个”。

### 7.6 一个明确的数据层瑕疵：`schema` 字段命名触发 Pydantic warning

本次后端测试过程中暴露了明确 warning：

- `backend/app/api/routes/tables.py:186-196`

这里的 `TableCreateRequest` 和 `TableUpdateRequest` 使用了字段名：

- `schema: dict[str, Any]`

这会 shadow `BaseModel` 自带属性名 `schema`，运行时已有 warning。

这不是马上炸库的 bug，但它是典型的**语义命名不够谨慎**问题。  
它应该修，因为：

- 会污染日志与测试输出
- 容易让 IDE、类型、反射语义变混
- 不符合高标准代码规范

建议改名为：

- `table_schema`
- `column_schema`
- 或保持外部 API 名 `schema`，内部字段用 alias 映射

### 7.7 导入、向量、记忆更新链路的数据风险

这些链路总体上做了很多 fail-soft 和异步化处理，这是加分项。  
但随着能力叠加，也出现几个风险：

- 数据落库点多，排查链路长
- 配置项非常多，用户很难分辨“功能没开”还是“配置不完整”
- 某些页面默认暴露的是 JSON/raw，而不是面向作者的结构化产品对象
- 导入链路已经出现列表/详情不同步的实证 bug

这说明系统的“数据底座”并不弱，但“数据体验层”还不够成熟。

---

## 八、异步处理、多线程处理、竞争读写与任务系统审查

### 8.1 当前任务系统的优点

任务系统是这个项目真正的工程亮点之一。

它已经具备：

- `inline` 和 `rq` 两种 backend
- watchdog
- heartbeat
- queued/running/succeeded/failed/canceled
- retry/cancel
- runtime timeline
- 任务事件 SSE
- 对 project task / memory task 的兼容处理

这些设计说明开发者是理解异步系统基本盘的。

### 8.2 代码中对 SQLite 事务边界的认识是存在的

`docs/design/sqlite-concurrency.md` 的主张是正确的：

- 读库阶段和写库阶段分开
- LLM 网络调用不能持有长事务
- SQLite 写阶段尽量短

从代码与文档结合看，这条原则并没有被完全无视。  
这是项目能稳定跑到今天的重要原因之一。

### 8.3 但“并发设计良好”不等于“可以上大规模”

你问的是：

> 异步处理、多线程处理、同时读写、竞争读写是否全部优异，能否撑得起强大的五六位数用户量

答案必须拆开：

#### 对当前体量/开发阶段：还可以

- 对单机、开发环境、小团队内测，这套任务体系是有效的
- 对小规模生产，如果上 Postgres + Redis + RQ，也能比 SQLite 稳很多

#### 对五六位数用户量：完全不够

缺什么？

- 分布式任务分片策略
- 多队列隔离
- provider 级限流与 backpressure
- 请求级 rate limit
- 用户级 quota
- 热点缓存
- 长时间任务的状态持久化治理
- 统一 metrics / queue lag / worker saturation 监控
- 多实例部署与幂等性压测闭环

也就是说，当前任务系统是**合格的中小型系统任务层**，不是大规模平台任务基础设施。

### 8.4 一个额外的部署风险：RQ Windows 路线需要谨慎

README 给了 Windows PowerShell 启动 RQ worker 的示例，这对开发者很友好。  
但需要注意的是，RQ 官方文档当前强调：

- 对 Windows 兼容的 worker 需要关注 `SpawnWorker`
- 官方文档专门点名了 `SpawnWorker: Windows compatibility using spawn() instead of fork()`

而当前仓库的 `backend/scripts/run_rq_worker.py` 直接导入的是：

- `from rq import Queue, Worker`

这意味着：

- 当前脚本在你本地也许能跑
- 但它与官方关于 Windows worker 兼容性的推荐并不完全一致

因此这至少是一个**部署可靠性风险**，需要补充验证或改造。

---

## 九、API 设计与后端功能分配审查

### 9.1 API 数量与覆盖范围说明后端已经平台化

当前 185 个 endpoint 覆盖了：

- 认证与管理员
- 项目与成员
- 大纲/章节/分析/预览/导出
- prompt preset / prompt preview / llm profile / llm preset / llm models / llm capabilities
- worldbook / story_memory / memory preview / structured memory / graph / fractal / vector
- tables / search / imports / generation runs / tasks / batch generation

这已经接近一个小型创作平台后端，而不是单点工具后端。

### 9.2 API 错误与 guard 整体评价

整体评价偏正面：

- 有统一 `AppError`
- 多数接口对参数与权限有明确校验
- generation 与 memory 相关功能多数会返回结构化错误
- provider / API Key / config 缺失时，很多地方不是直接崩溃

但也有两个问题：

#### 问题 1：guard 很多，但 guard 表达层不总是清晰

很多 guard 在代码里存在，但用户感知上仍可能觉得：

- “按钮能点，但为什么不好用？”
- “明明配置了，为什么这里还报 key missing？”
- “为什么模型列表是空的，不是报错？”

换句话说，guard 代码是有的，**guard 的产品可解释性不足**。

#### 问题 2：部分 route 自己承担了太多 guard + 编排 + LLM 细节

这会让 route 层越来越像“巨型 orchestrator”。  
这不是 API 设计应该长期承受的负担。

### 9.3 “获取模型列表”这类 API 交互为什么容易让用户迷惑

这是一个非常典型的产品/后端协作边界问题。

后端 `backend/app/api/routes/llm_models.py` 的设计思路是：

- 允许通过 `profile_id`
- 或 `project_id`
- 或 `X-LLM-API-Key` 头

来拿模型列表。

如果缺少 key，不一定硬失败，而可能返回：

- `models: []`
- `warning: {...}`

这个设计从 API 角度是合理的，因为它把“没有 key”当成一种可表达状态。  
但从前端按钮体验看，就很容易让用户误以为“按钮可用 = 功能可正常完成”。

所以这里真正需要修的不是某一行代码，而是：

- 前端状态提示
- 按钮禁用策略
- warning 的 UI 展示方式
- profile / key / model list 三者关系的产品文案

---

## 十、提示词系统、高级调试与 AI 功能有效性审查

### 10.1 提示词系统不是摆设，而是项目核心资产之一

从 Prompt Studio、Prompt Templates、Prompt Presets、Prompt Preview、Prompt Inspector 的存在，以及相关 API / tests 来看，提示词系统已经是项目真正的核心资产，而不是临时写几段 prompt string。

这点很好，因为未来做：

- 多 agent
- 提示词约束
- Prompt A/B
- 提示词版本治理
- 可视化调试

都离不开这层基础。

### 10.2 但提示词系统也已经复杂到需要“二次产品化”

当前用户面对的不是一个简单设置项，而是一整套：

- 主模型
- 任务模块覆盖
- prompt 预览
- override
- 模型参数
- reasoning effort / text verbosity / extra JSON
- 测试连接 / 模型列表 / API key 保存

从能力角度这很强；  
从使用角度这已经接近一个给 power user 的配置工作台。

这意味着后续要做两件事：

1. 给普通作者提供更简化的“推荐模式”
2. 给高级作者保留完整能力但加强可解释性

否则高级能力越多，误配就越多。

### 10.3 高级调试功能的实现度：不是空，但并不“工业级收口”

你点名的高级功能包括：

- 润色
- 规划
- 优化
- 去味/一致性修复
- 高级调试页面

结论是：

#### 不是没实现

证据很明显：

- `useChapterGeneration.ts` 有 `plan_first`、`post_edit`、`content_optimize`
- `AiGenerateDrawer.tsx` 有相关开关与 compare/revert 按钮
- Prompt Preview/Prompt Inspector 可做预检
- RAG / Graph / Structured Memory / Task Center 都有高级调试页

#### 但实现成熟度不统一

具体问题：

- 功能之间的协同逻辑已经复杂
- 用户不一定知道何时该开哪个功能
- 某些功能对结果价值的可见性不强
- “格式报错”“结构化输出不稳定”“需要 fallback”的现实仍然存在

这说明项目已经进入 AI 产品的真实阶段：  
不是“有没有 AI”，而是“AI 能力如何被可控地收口成可靠工作流”。

### 10.4 关于“这些高级功能到底有用没用”

我的判断是：

- 它们不是无用功能
- 也不是已经成熟到可以不加解释就放心交给所有用户

更准确地说：

- 对重度用户、调试型用户，这些功能是有价值的
- 对普通作者，如果没有“推荐路径”和“结果解释”，就会觉得它们只是多出来的复杂开关

因此产品上应分层：

- 默认路径：更少开关、更少概念
- 高级路径：完整能力 + 明确风险说明 + 效果对比

---

## 十一、可扩展性与长期演进审查

### 11.1 这个项目有没有扩展潜力？

有，而且不低。

原因：

- provider abstraction 已存在
- prompt 系统已存在
- 任务系统已存在
- generation runs 与 debug bundle 已存在
- 多种知识模块已存在
- 前端已有工作台形态，不是散页工具

这些都说明：  
项目未来往平台化、多 agent 化走，并不是没有基础。

### 11.2 但为什么我仍然只给扩展性 5/10

因为当前扩展更多是“能加功能”，而不是“能优雅地加功能”。

差别很大。

#### 当前能加的方式

- 往超大 route/service/page 里继续加判断
- 再加一个 task kind
- 再加一组 prompt 入口
- 再加一个高级页面

#### 真正好的扩展方式应该是

- 有清晰的应用服务边界
- 有 Agent/Tool/Memory/Planner 的独立语义接口
- 有插件注册、权限声明、沙箱执行、审计日志
- 有跨模块事件总线或编排层
- 有领域模型级 contract，而不是只靠页面字段拼接

当前仓库离后者还有不小距离。

### 11.3 如果你要做多 agent，当前结构哪里有基础，哪里不够

#### 有基础的部分

- Prompt 体系
- Provider 抽象
- 任务系统
- generation run 记录
- memory / worldbook / graph / vector / fractal 等可供 agent 使用的知识源

#### 不够的部分

- 没有真正的 agent runtime
- 没有 agent state machine
- 没有 tool sandbox / permission model
- 没有 planner / executor / reviewer 的独立角色协议
- 没有跨 agent 的共享工作记忆 contract
- 没有可靠的中间产物 schema 注册中心

所以结论是：

**当前项目适合继续向多 agent 演进，但不适合直接在现状上硬堆多 agent 功能。**

### 11.4 如果你要支持第三方插件，当前最大阻碍是什么

不是 UI，不是按钮，不是能不能多写几个接口。  
最大阻碍是：

- 缺少插件生命周期管理
- 缺少运行时沙箱
- 缺少权限边界
- 缺少插件版本与兼容性 contract
- 缺少宿主与插件之间的稳定 API

现在的 MCP 服务更像证明“工具调用是有价值的”，而不是已经有“插件平台”。

---

## 十二、容量、稳定性与生产化判断

### 12.1 是否能支撑强大的五六位数用户量？

直接结论：**当前不能。**

#### 证据链

1. `README.md` 明确要求 SQLite 单 worker  
2. README 明确说明 Compose 形态用于“三位数并发的基础需求”  
3. `docs/performance-baseline.md` 只做了并发 `1/5/20` 基线  
4. 当前没有成熟的：
   - 全局限流
   - 正式缓存层
   - 分布式监控
   - 任务队列分片治理
   - 多实例幂等与压测闭环
   - 大规模对象存储与静态内容治理

#### 更准确的现实判断

- **SQLite 模式**：仅适合本地开发、演示、小范围试用
- **Postgres + Redis + RQ Compose 模式**：可走向小型生产，适合内测、灰度、有限用户群
- **大规模平台化生产**：还需要一整轮架构升级

### 12.2 性能基线说明了什么

`docs/performance-baseline.md` 与归档结果说明：

- 已对 `100 / 300 / 800` 章节规模做性能基线
- 后端并发只测到 `1 / 5 / 20`
- 环境仍是 isolated E2E SQLite + single-worker uvicorn

这很好，因为说明团队不是完全不测性能。  
但它也清楚说明：

- 当前性能验证还停留在基线阶段
- 这不是生产容量证明

#### 一个很重要的区别

“有性能基线”  
不等于  
“已经证明可承载大规模用户”。

这个项目目前属于前者，不属于后者。

---

## 十三、问题总表：按优先级、难度、数据影响综合排序

下表列的是我认为最关键、最值得立刻进入 issue/路线图的问题。不是所有问题都同级，但这些是最影响长期健康度的。

| 编号 | 问题 | 主要位置 | 优先级 | 难度 | 升级/数据影响 | 建议方向 |
| --- | --- | --- | --- | --- | --- | --- |
| P0-01 | SQLite 单 worker 路线无法支撑大规模用户 | `README.md`、`backend/app/db/session.py`、`backend/app/main.py` | P0 | 中 | 生产架构升级会影响部署与数据迁移 | 默认生产切 Postgres + Redis + RQ，并补压测与迁移工具 |
| P0-02 | 后端存在多个超大 route/service 神文件 | `outline.py`、`chapters.py`、`vector_rag_service.py` 等 | P0 | 高 | 代码拆分本身不影响数据，但回归风险高 | 按用例/阶段/子域拆服务与 route facade |
| P0-03 | API route 仍直接调用 LLM / generation 细节 | `chapters.py`、`chapter_analysis.py`、`memory.py`、`outline.py` | P0 | 高 | 无数据迁移，但影响稳定性与可维护性 | 强制 route 只调 application service |
| P0-04 | 缺少正式监控、限流、缓存，无法生产上量 | 全局 | P0 | 高 | 对已部署实例影响大 | 增加 metrics、rate limit、cache、告警、provider backpressure |
| P1-05 | 主题系统仅支持 `paper-ink`，不是真多主题 | `frontend/src/services/theme.ts`、`index.css` | P1 | 中 | 主要影响前端样式层 | 引入 theme registry、完整语义 token、主题回归测试 |
| P1-06 | 页面级文件过大，前端维护成本高 | `PromptsPage.tsx`、`SettingsPage.tsx`、`WorldBookPage.tsx` 等 | P1 | 中高 | 不影响数据，影响持续开发效率 | 页面拆 panel/container/hooks，做状态分层 |
| P1-07 | Prompt/模型配置 UX 存在前置条件表达不清 | `LlmPresetPanel.tsx`、`llm_models.py` | P1 | 中 | 无数据影响 | 统一“未绑定 profile / 未保存 key / 可请求远端”三态 |
| P1-08 | 大纲结构预览存在，但结构化二次编辑仍不够强 | `OutlinePage.tsx`、`outline.py` | P1 | 中高 | 会影响已有用户编辑习惯 | 引入结构化章节编辑器、局部重生成、结构 patch |
| P1-09 | ImportPage 列表/详情不同步，已被全量 E2E 证实 | `frontend/src/pages/ImportPage.tsx`、`test/specs/ui/import.spec.ts` | P1 | 中 | 无数据迁移，但会影响用户信任 | 创建后做 optimistic insert 或确保 list API 立即可见 |
| P1-10 | MCP/插件能力仍是 mock 级，不适合直接承载插件平台 | `backend/app/services/mcp/service.py` | P1 | 高 | 未来扩展架构影响大 | 定义 tool registry、权限、沙箱、审计、插件 contract |
| P1-11 | RQ Windows 启动脚本与官方兼容性建议存在偏差 | `backend/scripts/run_rq_worker.py` | P1 | 中 | 影响部署可靠性 | 评估 `SpawnWorker` 或明确平台支持矩阵 |
| P1-12 | 高级功能做了很多，但产品解释层不足 | `AiGenerateDrawer.tsx`、`useChapterGeneration.ts` 等 | P1 | 中 | 无数据影响 | 默认模式/高级模式分层，增加“何时使用”说明 |
| P1-13 | 后端配置文件过大，继续增长会成治理负担 | `backend/app/core/config.py` | P1 | 中 | 无数据影响 | 分域配置模块化，如 auth/vector/task/graph/fractal |
| P2-14 | `schema` 字段命名触发 Pydantic warning | `backend/app/api/routes/tables.py` | P2 | 低 | 可能影响 API schema 文档兼容 | 用 alias 改名，消除 warning |
| P2-15 | 前端仍存在少量硬编码 palette 泄漏 | `AdminUsersPage.tsx` 等 | P2 | 低 | 无数据影响 | 全面 token 化，禁止语义外 palette |
| P2-16 | UI 文案仍未彻底中心化 | 多个页面与组件 | P2 | 中 | 无数据影响 | 扩大 `UI_COPY` 覆盖并建立术语审查清单 |
| P2-17 | 大页面 chunk 偏大，进入成本上升 | 构建产物与大页面 | P2 | 中 | 无数据影响 | 继续拆 panel、抽重型逻辑、优化依赖加载 |
| P2-18 | 高级调试页规范有文档，但未形成自动化一致性守卫 | `docs/ux/advanced-debug-page-guidelines.md` | P2 | 中 | 无数据影响 | 增加测试/校验规则/视觉基线 |
| P2-19 | 缺少多 agent 与插件的领域级 contract | 全局 | P2 | 高 | 长期架构影响大 | 定义 agent/tool/memory/planner API 与事件模型 |
| P2-20 | 批量/任务限额已有雏形，但全局配额治理不足 | `batch_generation.py` 等 | P2 | 中高 | 生产影响大 | 扩展到 user/provider/project/global 四层 quota |

---

## 十四、我认为最应该先做的修复路线

### 14.1 第一阶段：立刻做，优先止血（1 到 2 周）

1. 修 `ImportPage` 列表/详情状态不一致  
   这是已被全量黑盒证实的真实问题，优先级高。

2. 修 `schema` 字段 warning  
   低成本、立刻提升代码洁净度。

3. 整理模型配置页状态表达  
   尤其是“拉取模型列表/测试连接/API Key”的前置条件。

4. 为大纲页补结构化编辑改造方案设计稿  
   先定产品对象，再动代码。

5. 给高级写作抽屉补“推荐使用场景”与默认模式  
   减少普通用户认知负担。

### 14.2 第二阶段：架构收边（2 到 6 周）

1. 把 `outline.py`、`chapters.py`、`memory.py` 中的直接 LLM 编排逐步下沉到 application service
2. 拆分 `PromptsPage`、`SettingsPage`、`WorldBookPage`、`TaskCenterPage`
3. 拆分 `vector_rag_service.py` 与 `project_task_service.py`
4. 把主题系统升级为 registry 化，而不是只有 `paper-ink`
5. 把 UI copy 再往中心化推进一轮

### 14.3 第三阶段：面向长期平台化（1 到 3 个月）

1. 生产默认部署切 Postgres + Redis + RQ
2. 补监控、限流、缓存、压测、worker saturation dashboard
3. 定义 Agent/Tool/Memory/Planner 领域接口
4. 把 MCP 从 mock service 升级为真实工具运行时
5. 为插件和多 agent 提前设计权限模型与审计模型

---

## 十五、这套项目现在最值得保留的东西

这部分很重要，因为审查不是为了把项目全盘否定，而是为了看清哪些是资产。

### 15.1 真正值得保留并继续加强的资产

- Prompt 体系
- Task Center 与 project task runtime
- generation runs / debug bundle / request id / redaction
- 多 Provider LLM abstraction
- WorldBook / StoryMemory / Structured Memory / Graph / Vector / Fractal 的整体方向
- 设计 token 基础与组件类体系
- Playwright 黑盒测试资产
- UI/API 映射与设计文档

这些东西不是包袱，而是你未来继续做大的“地基”。

### 15.2 最不该继续的增长方式

- 再往超大页面里堆功能
- 再往超大 route 里堆分支
- 再把高级功能直接暴露成更多 checkbox
- 再把多 agent 做成“多一个 if else”

如果继续这么长，项目会变成“功能很多，但谁也不敢改”的那种系统。

---

## 十六、最终结论

如果用最直白的话说：

1. **这个项目并不差，甚至在很多方面明显优于大多数同规模 AI 应用仓库。**  
   它有真实功能、有测试、有设计文档、有任务系统、有长期记忆底座、有提示词治理，这些都不是表面功夫。

2. **当前最大问题已经不再是“有没有功能”，而是“架构治理和产品解释是否跟得上功能增长”。**  
   神文件、边界模糊、UX 状态表达不够清晰、主题系统不够彻底、插件/多 agent 仍是雏形，这些都是真问题。

3. **它目前适合作为强功能写作平台继续打磨，不适合被误判为已经完成工业级平台化。**  
   尤其是容量问题，不要自欺欺人。SQLite 路线、当前监控能力、缓存/限流缺位，都决定了它现在不能被当成五六位数用户量的成熟系统。

4. **如果你愿意在接下来 1 到 3 个月集中做“收边、拆分、治理、观测、主题和扩展层设计”，这个项目很有机会从“强功能写作器”进化成“真正可长期演进的平台”。**

这才是我对它最真实、也最严肃的评价。

---

## 附录 A：本次验证结果摘要

### A.1 编译、测试、构建

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 后端编译 | `cd backend && .\.venv\Scripts\python.exe -m compileall -q app alembic` | 通过 |
| 后端单测 | `cd backend && .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v` | 427 通过，1 跳过 |
| 后端质量门 | `cd backend && .\.venv\Scripts\python.exe scripts\run_quality_gate.py` | 通过，有 warning |
| 前端 lint | `cd frontend && npm run lint` | 通过 |
| 前端单测 | `cd frontend && npm test` | 14 文件，51 测试通过 |
| 前端构建 | `cd frontend && npm run build` | 通过 |
| Playwright 全量 | `cd test && npm test` | 137 通过，1 失败 |

### A.2 Playwright 全量失败项

- 失败用例：`test/specs/ui/import.spec.ts`
- 失败点：等待列表中 `import-demo.txt` 对应按钮出现
- 已定位现象：详情面板已出现，但左侧列表仍显示空态，说明页面局部状态不同步

---

## 附录 B：后端热点文件（按行数）

| 行数 | 文件 |
| ---: | --- |
| 3047 | `backend/app/api/routes/outline.py` |
| 2472 | `backend/app/services/vector_rag_service.py` |
| 2207 | `backend/app/api/routes/chapters.py` |
| 1439 | `backend/app/services/memory_update_service.py` |
| 1425 | `backend/app/services/project_task_service.py` |
| 1109 | `backend/app/services/import_export_service.py` |
| 1063 | `backend/app/services/fractal_memory_service.py` |
| 1033 | `backend/app/services/search_index_service.py` |
| 969 | `backend/app/services/batch_generation_service.py` |
| 946 | `backend/app/services/plot_analysis_service.py` |
| 945 | `backend/app/services/memory_retrieval_service.py` |
| 926 | `backend/app/services/worldbook_auto_update_service.py` |
| 893 | `backend/app/services/prompt_presets.py` |
| 885 | `backend/app/api/routes/memory.py` |
| 847 | `backend/app/api/routes/auth.py` |
| 804 | `backend/app/services/table_ai_update_service.py` |
| 797 | `backend/app/core/config.py` |
| 793 | `backend/app/services/characters_auto_update_service.py` |
| 734 | `backend/app/services/graph_auto_update_service.py` |
| 731 | `backend/app/api/routes/batch_generation.py` |

---

## 附录 C：前端页面清单（含行数）

| 行数 | 页面文件 |
| ---: | --- |
| 2307 | `frontend/src/pages/PromptsPage.tsx` |
| 2200 | `frontend/src/pages/SettingsPage.tsx` |
| 1824 | `frontend/src/pages/WorldBookPage.tsx` |
| 1343 | `frontend/src/pages/TaskCenterPage.tsx` |
| 1078 | `frontend/src/pages/OutlinePage.tsx` |
| 813 | `frontend/src/pages/WritingPage.tsx` |
| 797 | `frontend/src/pages/PromptTemplatesPage.tsx` |
| 769 | `frontend/src/pages/PromptStudioPage.tsx` |
| 730 | `frontend/src/pages/AdminUsersPage.tsx` |
| 714 | `frontend/src/pages/StructuredMemoryPage.tsx` |
| 711 | `frontend/src/pages/structuredMemory/CharacterRelationsView.tsx` |
| 701 | `frontend/src/pages/RagPage.tsx` |
| 634 | `frontend/src/pages/ImportPage.tsx` |
| 618 | `frontend/src/pages/ChapterReaderPage.tsx` |
| 502 | `frontend/src/pages/promptStudio/PromptStudioPresetEditorPanel.tsx` |
| 490 | `frontend/src/pages/DashboardPage.tsx` |
| 476 | `frontend/src/pages/CharactersPage.tsx` |
| 461 | `frontend/src/pages/GraphPage.tsx` |
| 446 | `frontend/src/pages/ProjectWizardPage.tsx` |
| 423 | `frontend/src/pages/rag/RagQueryPanel.tsx` |
| 410 | `frontend/src/pages/StylesPage.tsx` |
| 370 | `frontend/src/pages/SearchPage.tsx` |
| 352 | `frontend/src/pages/rag/RagKnowledgeBasePanel.tsx` |
| 334 | `frontend/src/pages/ForeshadowsPage.tsx` |
| 321 | `frontend/src/pages/FractalPage.tsx` |
| 299 | `frontend/src/pages/PreviewPage.tsx` |
| 277 | `frontend/src/pages/rag/RagAdvancedDebugPanel.tsx` |
| 251 | `frontend/src/pages/ChapterAnalysisPage.tsx` |
| 238 | `frontend/src/pages/taskCenter/ProjectTaskRuntimePanel.tsx` |
| 229 | `frontend/src/pages/promptStudio/PromptStudioPresetListPanel.tsx` |
| 217 | `frontend/src/pages/ExportPage.tsx` |
| 213 | `frontend/src/pages/LoginPage.tsx` |
| 198 | `frontend/src/pages/NumericTablesPage.tsx` |
| 196 | `frontend/src/pages/RegisterPage.tsx` |
| 165 | `frontend/src/pages/promptStudio/PromptStudioPreviewPanel.tsx` |
| 111 | `frontend/src/pages/rag/RagHeaderPanel.tsx` |
| 52 | `frontend/src/pages/NotFoundPage.tsx` |
| 50 | `frontend/src/pages/RouteErrorPage.tsx` |
| 41 | `frontend/src/pages/rag/RagStatusPanel.tsx` |
| 28 | `frontend/src/pages/taskCenter/StatusBadge.tsx` |

---

## 附录 D：后端路由模块与 endpoint 数量

| 数量 | 路由模块 |
| ---: | --- |
| 17 | `backend/app/api/routes/prompts.py` |
| 15 | `backend/app/api/routes/memory.py` |
| 13 | `backend/app/api/routes/auth.py` |
| 13 | `backend/app/api/routes/chapters.py` |
| 12 | `backend/app/api/routes/vector.py` |
| 11 | `backend/app/api/routes/projects.py` |
| 11 | `backend/app/api/routes/tables.py` |
| 11 | `backend/app/api/routes/worldbook.py` |
| 8 | `backend/app/api/routes/batch_generation.py` |
| 7 | `backend/app/api/routes/writing_styles.py` |
| 6 | `backend/app/api/routes/glossary.py` |
| 6 | `backend/app/api/routes/story_memory.py` |
| 6 | `backend/app/api/routes/tasks.py` |
| 5 | `backend/app/api/routes/import_export.py` |
| 5 | `backend/app/api/routes/outlines.py` |
| 4 | `backend/app/api/routes/chapter_analysis.py` |
| 4 | `backend/app/api/routes/characters.py` |
| 4 | `backend/app/api/routes/outline.py` |
| 4 | `backend/app/api/routes/llm_profiles.py` |
| 3 | `backend/app/api/routes/generation_runs.py` |
| 3 | `backend/app/api/routes/llm_task_presets.py` |
| 2 | `backend/app/api/routes/export.py` |
| 2 | `backend/app/api/routes/fractal.py` |
| 2 | `backend/app/api/routes/graph.py` |
| 2 | `backend/app/api/routes/llm_preset.py` |
| 2 | `backend/app/api/routes/mcp.py` |
| 2 | `backend/app/api/routes/settings.py` |
| 1 | `backend/app/api/routes/health.py` |
| 1 | `backend/app/api/routes/llm.py` |
| 1 | `backend/app/api/routes/llm_capabilities.py` |
| 1 | `backend/app/api/routes/llm_models.py` |
| 1 | `backend/app/api/routes/search.py` |

总计：**185** 个 endpoint。

---

## 附录 E：模型文件清单

- `backend/app/models/__init__.py`
- `backend/app/models/auth_external_account.py`
- `backend/app/models/batch_generation_task.py`
- `backend/app/models/chapter.py`
- `backend/app/models/character.py`
- `backend/app/models/fractal_memory.py`
- `backend/app/models/generation_run.py`
- `backend/app/models/glossary_term.py`
- `backend/app/models/knowledge_base.py`
- `backend/app/models/llm_preset.py`
- `backend/app/models/llm_profile.py`
- `backend/app/models/llm_task_preset.py`
- `backend/app/models/memory_task.py`
- `backend/app/models/outline.py`
- `backend/app/models/plot_analysis.py`
- `backend/app/models/project.py`
- `backend/app/models/project_default_style.py`
- `backend/app/models/project_membership.py`
- `backend/app/models/project_settings.py`
- `backend/app/models/project_source_document.py`
- `backend/app/models/project_table.py`
- `backend/app/models/project_task.py`
- `backend/app/models/project_task_event.py`
- `backend/app/models/prompt_block.py`
- `backend/app/models/prompt_preset.py`
- `backend/app/models/search_index.py`
- `backend/app/models/story_memory.py`
- `backend/app/models/structured_memory.py`
- `backend/app/models/user.py`
- `backend/app/models/user_activity_stat.py`
- `backend/app/models/user_password.py`
- `backend/app/models/user_usage_stat.py`
- `backend/app/models/worldbook_entry.py`
- `backend/app/models/writing_style.py`

其中最复杂的是：`backend/app/models/structured_memory.py`（约 275 行）。

---

## 附录 F：Alembic migration 清单

- `026e378fe453_add_project_tables.py`
- `039b3f10165b_add_project_memberships_role_check.py`
- `055a86b053de_add_vector_rerank_config_to_project_.py`
- `09930c9ce083_add_embedding_provider_config_to_.py`
- `09e76a9bafd4_repair_integer_datetimes.py`
- `0f24b611cf21_init.py`
- `166a3fc3e56a_add_context_optimizer_enabled.py`
- `171d29cc0501_add_auth_external_accounts.py`
- `1c2a0e6b4c2d_multi_outlines_and_llm_profiles.py`
- `2662fe2e5a01_add_fractal_memory_table.py`
- `2c72bcfbae89_import_prompt_templates_into_prompt_.py`
- `2f6b9f4a0c1e_drop_llm_profiles_api_key.py`
- `31c0665707d6_add_project_memberships.py`
- `33aadf6df492_add_prompt_preset_resource_key.py`
- `3d1c1e8a9f0b_add_llm_profiles_api_key_ciphertext.py`
- `478d3bb289da_add_memory_tasks.py`
- `4858b08a6519_add_pgvector_vector_chunks.py`
- `4c971683d6cb_add_project_tasks.py`
- `64ef818ef343_add_admin_user_stats_tables.py`
- `69b84930b3f8_add_vector_embedding_config_to_project_.py`
- `6a97ad76d9c1_add_llm_task_presets.py`
- `709f879b1f13_add_structured_memory_list_indexes.py`
- `714c1b58c136_add_knowledge_base_priority_group.py`
- `76954388cbe7_add_worldbook_entries.py`
- `8b4c2f3a1d9e_add_batch_generation_tasks.py`
- `902bd83f86ed_add_vector_rerank_project_config.py`
- `9b6366ef8065_add_postgres_extensions.py`
- `a4f9e5c7d2b1_add_llm_profile_template_fields.py`
- `a8467b6f0518_add_prompt_preset_category.py`
- `af542aa52b66_add_query_preprocessing_config_to_.py`
- `b368a03dc4b5_add_knowledge_bases.py`
- `b7c4f2e6a901_add_project_task_events_and_runtime_fields.py`
- `b9c7a5c2e1f3_add_vector_index_dirty_fields.py`
- `c036de4694b3_add_search_index_tables.py`
- `c4a2b7e91d13_add_batch_generation_runtime_linkage.py`
- `c9f4cca35a13_add_project_tables_auto_update_enabled.py`
- `ca30c45d18e1_add_plot_analysis_and_story_memories.py`
- `d8993729ae5a_add_prompt_presets_and_blocks.py`
- `d8ffc3ff79f5_add_project_source_documents.py`
- `dfb3c3ed0847_merge_heads.py`
- `e17a2e45e8f0_add_user_passwords_and_admin_flag.py`
- `e1c65f9a82c6_add_auto_update_flags_to_project_.py`
- `ea09718bdf1e_add_writing_styles.py`
- `ed12de854702_extend_change_set_target_table_project_.py`
- `ee23bd2770fc_add_glossary_terms.py`
- `f078e253d338_add_generation_runs_prompt_render_log_.py`
- `f0e7b256e514_timestamps_to_datetime.py`
- `f89622011fdb_add_structured_memory_tables.py`

---

## 附录 G：本次使用的外部参考资料

1. SQLite Write-Ahead Logging 官方文档  
   <https://www.sqlite.org/wal.html>

2. SQLite `PRAGMA busy_timeout` 官方文档  
   <https://www.sqlite.org/pragma.html#pragma_busy_timeout>

3. SQLAlchemy SQLite 方言与 threading/pooling 文档  
   <https://docs.sqlalchemy.org/en/20/dialects/sqlite.html>

4. FastAPI Bigger Applications / APIRouter 官方文档  
   <https://fastapi.tiangolo.com/tutorial/bigger-applications/>

5. RQ Workers 官方文档（含 `SpawnWorker` 的 Windows 兼容说明）  
   <https://python-rq.org/docs/workers/>

6. Tailwind 自定义样式/主题变量文档  
   <https://tailwindcss.com/docs/adding-custom-styles>

---

## 附录 H：逐页补充点评

这一节补充正文里没有逐一展开到位的页面，按“页面职责、当前质量、主要风险、后续建议”四个角度快速扫一遍。

### H.1 DashboardPage

首页承担项目列表、入口聚合与快速跳转的职责，逻辑上不算重，当前状态稳定。它的问题不是功能不足，而是随着项目模块越来越多，首页未来会承受更多“入口导航 + 项目概览”的压力，需要提前规划卡片层级和信息密度，否则会逐渐长成第二个综合控制台。

### H.2 ProjectWizardPage

创建向导是项目里产品味比较强的页面，说明项目不是纯工程人的后台工具。它当前最大的价值在于帮助新项目快速完成起步，但后续如果要接入更多“项目模板、预设风格、默认模型策略、写作模式”，必须避免直接把这些东西堆成更多表单项，应该以模板卡片或推荐方案驱动。

### H.3 CharactersPage

角色页本身不算失控，体量也还能接受。它最大的中长期价值不在 CRUD，而在于与大纲、写作、图谱、结构化记忆的多向联动。目前联动能力是有的，但还不够显式，未来可以强化“角色设定是否实际进入生成上下文”“角色关系是否回流图谱”的可视反馈。

### H.4 ChapterAnalysisPage

剧情记忆页是这个项目与普通写作器拉开差距的关键页面之一。它不仅是分析页，还是 story_memory 的可视入口和应用页。当前问题不是“没有能力”，而是这个页面承载了分析、重写、应用、标注、story memory 编辑等多种心智模型，后续最好继续拆分面板语义，减少作者首次进入的认知负担。

### H.5 ChapterReaderPage

阅读页负责“只读视角 + 记忆命中 + 跳回编辑”的场景，定位明确，是值得保留的。它说明项目并没有把“编辑器”当作唯一视图，而是开始考虑阅读体验和复盘体验。后续可以继续强化旁注、命中来源、章节间连续阅读的稳定性，但当前不是主要技术债区。

### H.6 PreviewPage

预览页是输出落地的重要页面，它让项目不至于只是一堆模型和数据结构。当前页的风险不大，但它未来会越来越依赖前面所有模块的质量。如果上游大纲、写作、导出规则不统一，预览页会首先把这些裂缝暴露出来，因此它适合作为最终视觉回归和用户体验回归的重点观察窗口。

### H.7 ExportPage

导出页职责单一，当前实现思路合理。它在未来的真正挑战不在“如何下载 markdown”，而在“导出格式规范、元数据附加、长篇项目导出性能、增量导出与 bundle 一致性”。如果项目未来面向真正的出版/外发流程，导出页会从小工具升级为重要产品节点。

### H.8 PromptStudioPage

提示词工作室是典型的 power-user 页面，项目把它做出来本身就说明目标不是简单写作助手，而是想做一个可调优的创作系统。它现在的主要问题不是功能少，而是作为高级页，它还没有足够清晰地对齐“作者工作台”和“提示词工程台”之间的边界。建议以后把“作者日常编辑”与“提示词研发调试”这两套模式分得更开。

### H.9 PromptTemplatesPage

模板页已经承担了一部分治理职责，这很好，因为 prompt 体系没有模板管理就很难长期维护。当前主要风险是模板数量和分类一旦继续增长，现有列表式信息架构可能会变得拥挤。未来建议引入更明显的标签、版本、用途分组，避免模板页退化成长列表仓库。

### H.10 RagPage

RAG 页从黑盒测试和代码结构看都不是空壳，状态页、知识库管理、查询页、高级调试面板都在。它的问题在于概念密度太高，尤其是 embedding、rerank、kb priority、query preview、rebuild、ingest 混在一个工作台时，对普通作者极不友好。这个页面更适合定位成“高级调试台”，而不是普通作者默认入口。

### H.11 GraphPage

图谱页是一个值得保留的中长期资产，因为人物/组织/地点关系网络对长篇小说非常重要。当前页的技术能力已经有查询、关系编辑、证据回放等实证，但产品层仍然偏工程化。后续如果要提升可用性，重点应该放在“从写作视角看关系变化”而不是继续堆更多调试字段。

### H.12 FractalPage

Fractal 页体现了项目在“长程记忆/分形记忆”方向上的探索，这一点很难得。它现在的问题不在代码是否存在，而在于用户是否真正知道这个页面的价值边界。未来若继续保留，建议把它明确标成“实验功能/高级记忆视图”，否则作者会把它误判成必须操作的主路径页面。

### H.13 StructuredMemoryPage

结构化记忆页是底座型页面，价值很高，因为它把 AI 产出的中间状态变成了可见的实体、关系、证据与变更集。问题在于它天然更像“数据管理台”而不是“普通作者页”，所以必须严控视觉复杂度和操作数量，否则只会让作者感觉“我看到了很多结构，但不知道该不该碰它”。

### H.14 NumericTablesPage

数值表格是很有潜力的特色功能，它可以把战力、势力、资源、时间线等量化设定纳入统一系统。当前实现已经够用，但未来如果要支撑更强自动化，建议把 schema、校验规则、AI update、上下文注入之间的关系设计得更显式，不要只停留在“表格能改、AI 能写”的层面。

### H.15 ForeshadowsPage

伏笔页属于非常贴近小说创作实际痛点的模块，因为它直接面对“有没有埋而未收”的问题。当前功能方向是对的，而且黑盒测试也覆盖了跳转和 resolve 流程。后续更大的挑战是如何把伏笔状态自然嵌回写作主路径，而不是让它成为一个需要作者手动记得去看的孤岛页面。

### H.16 SearchPage

全项目搜索页是效率型功能，实际价值很高。它比传统“术语表”更进一步，是更接近 IDE 风格的全项目检索。当前结构不错，但长期要注意搜索来源增加之后的排序策略、结果卡片统一性、来源类型标签，以及跳转后上下文定位的一致性。

### H.17 StylesPage

风格页不是装饰页，它和写作生成质量直接相关。当前它已经支持系统预设与用户风格，且可设置项目默认风格，这是正确方向。未来要留意的是风格定义、prompt 模板、生成链路之间的耦合度，避免风格页变成另一个隐藏很深的提示词配置页。

### H.18 AdminUsersPage

管理员页从功能上是必要的，但从视觉和语义上更像后台控制台。建议后续明确它属于“管理域”，与作者工作台分层；否则作者工作台的设计系统很容易被管理页的状态色、表格风格和权限逻辑反向污染。

### H.19 NotFoundPage / RouteErrorPage

这些页虽然体量小，但它们决定了异常路径的用户感受。当前不算问题源，但建议长期保持与主站语气一致，不要出现“主流程精致，错误页像默认模板”的割裂感。

### H.20 Login / Register / Admin / Help 这些外围页的统一性

项目当前的主力都压在写作工作台，因此外围页没有明显拖后腿，这可以接受。但未来如果想真正对外运营，外围页也会成为第一印象的一部分，需要继续补品牌表达、一致术语和视觉统一，而不是只把精力放在核心内页。

---

## 附录 I：关键流程补充拆解

这一节把用户最常走的流程串起来重新审视，不按文件而按“实际使用顺序”看问题。

### I.1 新项目创建流程

从首页进入项目创建向导的主路径是成立的，基本不会让人完全迷路。这一步最大的改进空间不是技术实现，而是模板化能力不足。现在新项目更像“空白项目 + 手工慢慢配置”，未来如果能加入题材模板、默认提示词套件、默认记忆策略，会更像成熟产品。

### I.2 模型配置流程

当前模型配置能力强，但初次使用成本高。作者需要理解 profile、provider、base_url、model、API Key、任务覆盖、任务绑定 profile 等概念。对于懂模型的人这是优势，对只想写小说的人则是阻力。所以这条流程需要“双层模式”：简化模式用于快速开写，高级模式用于深调。

### I.3 大纲生成流程

大纲流程已经有“生成、流式预览、覆盖、另存、创建章节骨架”等关键动作，说明主流程不是空的。但它的问题在于：真正便于二次精修的对象仍然是 markdown 文本，而不是结构化章节树。对于严肃创作用户，这会限制后期迭代效率，也会让局部重生成难做得优雅。

### I.4 章节生成流程

章节生成流程的代码明显下了很多功夫，尤其是 stream fallback、缺失前置章节提示、对比回退、prompt 覆盖、memory injection 等。这条流程的真正风险不是“没 guard”，而是“guard 太多但用户不一定懂”。后续应该把复杂性继续藏到默认模式后面，让高级作者自己打开更多控制面板。

### I.5 记忆注入与上下文预览流程

这是项目很有特色的部分。用户不仅能生成，还能看到记忆注入从哪里来、注入了什么、向量查询如何影响上下文。这在 AI 写作器里是非常宝贵的，因为它让模型调用变得可审视。缺点是模块太多，世界书、story memory、structured、vector、graph、fractal 同时出现时，很容易超过普通作者的理解阈值。

### I.6 任务中心回看流程

任务中心的存在让后台自动更新不再是黑箱，这非常正确。用户能看到任务、状态、错误、运行时细节，这是平台化的关键一步。但未来随着任务数量继续增长，必须进一步做聚类、过滤、默认折叠和“推荐关注项”，否则任务中心会从“透明”变成“嘈杂”。

### I.7 导入资料流程

导入流程的产品逻辑是成立的：上传文本、切分 chunk、生成提案、选择性应用。这个流程比很多项目里粗暴的“全量直接灌库”要成熟得多。问题在于，真实测试已经证明当前状态同步存在缺陷，所以这条流程在用户信任层面还不够稳。它必须修，因为导入功能天生就是第一印象功能，一旦导入体验含糊，用户会直接怀疑整套长期记忆体系。

### I.8 导出与预览流程

预览和导出说明项目有“结果落地”意识，而不只是生成一堆中间状态。这部分整体方向是对的。后续如果要走向更成熟的创作平台，建议继续增强导出格式管理、章节元数据、批量导出、审稿格式和外发包的一致性。

### I.9 高级调试流程

项目有高级调试页面，这是加分项。但高级调试的最大风险是：如果没有非常明确的“它给谁用、解决什么问题、什么时候该看它”，那它很容易从加分项变成复杂度来源。当前仓库已经有这方面迹象，所以后续建议把高级页统一标识为“高级/实验/调试”层，并尽量减少普通作者误入后的压迫感。

### I.10 整条作者主路径是否顺畅

综合来看，主路径可以概括为：

1. 新建项目  
2. 配模型  
3. 生成大纲  
4. 生成章节骨架  
5. 进入写作  
6. 通过记忆/图谱/RAG/任务中心做增强和调试  
7. 预览与导出

这条路径在结构上是成立的，并且比很多“只有聊天框”的 AI 小说项目成熟得多。它的主要问题不是路径断裂，而是中间每一步都已经长出较多高级分支，需要产品层继续做收口和推荐化。

---

## 附录 J：一句话版总判断

这是一个**有真东西、有工程底子、有未来潜力，但已经明显需要架构收边和产品收口**的 AI 小说平台项目。  
现在最不该做的，是继续盲目加功能；现在最该做的，是把已经做出来的东西整理成一个真正可长期维护、可稳定扩展、可解释、可生产演进的系统。

---

## 附录 K：全量任务拆解、详细分类与开发周期（18 个任务包，作为后续开发总标准）

这一节不是“再写一遍建议”，而是把前文所有关键问题落成可执行的后续开发体系。  
写法参考了 `5.4大调查详细记录.md` 的“全量任务拆解”思路，但我这里会更贴合当前 `3.13` 报告里的问题分布，重点覆盖：

- 前端设计系统、主题、页面拆分、数据层
- 后端 route/service 边界、任务系统、观测、部署
- 大纲、写作、导入、提示词配置这些主路径问题
- 多 agent / 插件底座和长期演进路线

### K.1 使用口径与排期假设

为了让周期估算有实际意义，这里先明确口径：

- 周期按“有效开发周”估算，不含长期卡在需求反复、频繁插单、紧急线上故障。
- 一个“开发周”按 5 个工作日计算。
- 估算按两种团队规模给出：
  - `单主程模式`：1 名主力全栈开发，偶尔有人配合测试
  - `小组模式`：1 名前端 + 1 名后端 + 1 名全栈/测试协助
- 如果中途继续大量插入新功能，下面所有周期都会被拉长。
- 任务之间存在依赖，不是 18 个包平铺并行。

### K.2 总体分类与开发波次建议

按当前项目实际情况，我建议拆成 5 个波次：

| 波次 | 核心目标 | 对应任务包 | 单主程周期 | 小组周期 |
| --- | --- | --- | --- | --- |
| Wave A | 止血与产品可解释性修复 | `T01 T03 T04 T05 T08` | 3-5 周 | 2-3 周 |
| Wave B | 前端可维护性与主路径重构 | `T06 T07 T09 T10` | 5-8 周 | 3-5 周 |
| Wave C | 后端边界收拢与任务工业化 | `T02 T11 T12 T13` | 6-10 周 | 4-6 周 |
| Wave D | 数据层与生产化底座升级 | `T14 T15 T16 T18` | 6-10 周 | 4-7 周 |
| Wave E | 长期平台化与扩展层 | `T17` | 4-8 周 | 3-5 周 |

### K.3 任务包总览

| 任务 | 分类 | 优先级 | 主要解决的问题 | 预计周期（单主程 / 小组） | 依赖 |
| --- | --- | --- | --- | --- | --- |
| T01 | 仓库与安全卫生 | P0 | 敏感信息、环境文件、开发产物、部署口径漂移 | 0.5-1 / 0.5 周 | 无 |
| T02 | 后端质量门禁 | P0 | 超大文件、route 直调 LLM、静态质量基线弱 | 1-2 / 1 周 | T01 |
| T03 | 设计系统与多主题 | P1 | 主题单一、颜色泄漏、第三主题不可快速适配 | 1.5-3 / 1-2 周 | 无 |
| T04 | 文案、术语与状态表达统一 | P1 | UI_COPY 半中心化、按钮状态解释不足 | 1-2 / 1 周 | T03 |
| T05 | 模型配置中心收口 | P1 | profile/key/model list/test connection 认知混乱 | 1-2 / 1 周 | T04 |
| T06 | 大纲流程重构 | P1 | markdown 为主，结构化二改弱，局部重生成差 | 2-4 / 1.5-3 周 | T05 |
| T07 | 写作高级生成收口 | P1 | 高级功能多但不够解释，默认路径过重 | 2-4 / 1.5-3 周 | T05 |
| T08 | 导入链路稳定化 | P0/P1 | ImportPage 列表/详情不同步，导入成功感不稳 | 1-1.5 / 0.5-1 周 | 无 |
| T09 | 前端数据层统一 | P1 | 缓存、失效、预取、跨页共享数据不足 | 2-4 / 1.5-3 周 | T05 |
| T10 | 前端热点页面拆分 | P1 | Prompts/Settings/WorldBook/TaskCenter 等神页 | 3-6 / 2-4 周 | T09 |
| T11 | 后端主链路边界重构 | P0 | chapters/outline/memory route 过重 | 3-6 / 2-4 周 | T02 |
| T12 | 知识子域拆分与调试统一 | P1 | RAG/Graph/Structured/Fractal 复杂度散乱 | 3-5 / 2-3 周 | T11 |
| T13 | 任务系统工业化 | P0/P1 | quota、watchdog、event、recovery、provider 压力控制 | 3-6 / 2-4 周 | T11 |
| T14 | 数据层与部署升级 | P0 | SQLite 无法上量，需 Postgres/Redis/RQ 规范化 | 3-6 / 2-4 周 | T01 T13 |
| T15 | 观测体系建设 | P0/P1 | 缺 metrics/tracing/Sentry/dashboard | 2-4 / 1.5-3 周 | T13 T14 |
| T16 | 配置中心合同化 | P1 | provider/model/capability/secret/prod contract 不够强 | 2-4 / 1.5-3 周 | T05 T14 |
| T17 | 多 agent / 插件底座 | P2 战略 | 缺 tool registry、权限、沙箱、agent runtime | 4-8 / 3-5 周 | T11 T13 T16 |
| T18 | 测试矩阵、CI、压测门禁 | P0/P1 | 回归分层、性能基线、交付门槛不足 | 2-4 / 1.5-3 周 | T02 T09 T14 T15 |

### K.4 问题映射：前文问题对应哪些任务包

| 前文问题编号 | 对应任务包 |
| --- | --- |
| `P0-01` SQLite 路线无法上量 | `T14 T15 T18` |
| `P0-02` 后端超大文件 | `T02 T11 T12` |
| `P0-03` API route 直调 LLM | `T02 T11` |
| `P0-04` 缺正式监控、限流、缓存 | `T09 T13 T15 T18` |
| `P1-05` 主题系统只有 `paper-ink` | `T03` |
| `P1-06` 前端神页过多 | `T09 T10` |
| `P1-07` 模型配置 UX 不清晰 | `T04 T05 T16` |
| `P1-08` 大纲结构化编辑不足 | `T06` |
| `P1-09` ImportPage 状态不同步 | `T08` |
| `P1-10` MCP/插件还是 mock 级 | `T17` |
| `P1-11` RQ Windows 兼容风险 | `T14 T18` |
| `P1-12` 高级功能解释层不足 | `T04 T07` |
| `P1-13` 配置文件过大 | `T02 T16` |
| `P2-14` `schema` 字段 warning | `T02` |
| `P2-15` 前端颜色 token 泄漏 | `T03` |
| `P2-16` 文案未彻底中心化 | `T04` |
| `P2-17` bundle 偏大 | `T09 T10` |
| `P2-18` 高级调试页缺自动化一致性守卫 | `T04 T10 T18` |
| `P2-19` 缺多 agent / 插件 contract | `T17` |
| `P2-20` 全局配额治理不足 | `T13 T15` |

### K.5 详细任务包

#### T01｜仓库卫生、敏感信息与本地产物治理（P0）

目标：让仓库状态与安全规范一致，避免 `.env`、数据库文件、日志、截图、调试产物继续污染主线开发。

主要内容：

- 统一检查所有被误跟踪的本地配置文件、测试产物、数据库文件。
- 确认 `.gitignore`、`README`、部署文档、开发脚本的口径完全一致。
- 建立“本地允许生成但禁止提交”的文件白名单。
- 对 API Key、token、密码、session key、导出 bundle 等做仓库级扫描。

涉及范围：

- 根目录与 `backend/`、`frontend/`、`test/`
- `.gitignore`
- `README.md`
- 后端/前端环境样例文件

验收标准：

- `git ls-files` 不再跟踪任何本地敏感配置文件
- 仓库新增 guard 后，对 secrets / db artifacts 的误提交会直接失败
- 文档中明确哪些文件是本地生成、哪些文件可提交

数据/部署影响：

- 不改业务数据
- 会影响开发流程，需要所有协作者统一习惯

开发周期：

- 单主程：0.5 到 1 周
- 小组模式：0.5 周

#### T02｜后端静态质量门禁与热点文件熵控制（P0）

目标：把后端从“规则写在文档里”升级成“规则在脚本里可执行”，并开始限制超大文件继续恶化。

主要内容：

- 增加或强化 `ruff`、基础类型检查、后端 guard 入口。
- 固化 `no-direct-llm-call-in-api`、`file-line-count-guard`、`backend-no-print-guard`。
- 把当前 warning 模式逐步过渡到 enforce 模式。
- 修复已知低风险 warning，例如 `tables.py` 的 `schema` 字段命名问题。

涉及范围：

- `backend/scripts/run_quality_gate.py`
- `scripts/guards/`
- `backend/app/api/routes/*`
- `backend/app/core/config.py`

验收标准：

- backend 一条命令可执行静态质量门禁
- 新增 route 不允许直接碰底层 LLM 客户端
- 新增超大文件会被 guard 明确拦截或警告
- `schema` 命名 warning 清零

数据/部署影响：

- 基本不影响已部署数据
- 主要影响代码提交和 CI 流程

开发周期：

- 单主程：1 到 2 周
- 小组模式：1 周

#### T03｜前端设计系统升级：多主题、语义 token 与视觉统一（P1）

目标：把现有 `paper-ink + dark` 的半主题系统升级为真正可扩展的主题体系。

主要内容：

- 引入 `theme registry`
- 让 `themeId` 不再是固定字面量
- 全面补齐 `danger/warning/info/success/overlay/selection/scrollbar` 等语义 token
- 清理 `text-white`、`bg-black/30`、`text-red-*`、`amber-*` 之类的硬编码泄漏点
- 增加至少第二主题样例，验证第三主题适配路径

涉及范围：

- `frontend/src/services/theme.ts`
- `frontend/src/index.css`
- `tailwind.config.js`
- 高风险页面与通用组件

验收标准：

- 可配置多个主题 ID
- 新主题无需全仓搜索替换硬编码颜色
- 所有页面在两套主题下基本可用
- 主要按钮、表单、危险态、覆盖层全部走语义 token

数据/部署影响：

- 仅前端样式层
- 不影响数据库

开发周期：

- 单主程：1.5 到 3 周
- 小组模式：1 到 2 周

#### T04｜文案、术语、状态表达统一治理（P1）

目标：把“有 UI_COPY，但还不彻底”的状态升级成真正可维护的文案与状态表达体系。

主要内容：

- 扩大 `UI_COPY` 覆盖范围
- 把模型配置、大纲生成、写作高级功能、任务中心等高认知负担页面的状态提示统一
- 建立术语表与 UI 语气约束
- 为高级调试页增加统一的标题、说明、风险提示格式

涉及范围：

- `frontend/src/lib/uiCopy.ts`
- `frontend/src/pages/*`
- `frontend/src/components/*`
- `docs/ux/*`

验收标准：

- 核心页面不再到处散落近义词和重复提示
- “未绑定 / 未保存 / 可用 / 警告 / 失败”的状态表达统一
- 高级调试页至少在标题、说明、危险提示格式上统一

数据/部署影响：

- 无数据影响
- 主要提升可用性和可维护性

开发周期：

- 单主程：1 到 2 周
- 小组模式：1 周

#### T05｜模型配置中心收口：Profile / API Key / Model List / Test Connection 合同化（P1）

目标：把当前最容易让用户困惑的一组配置操作彻底收口。

主要内容：

- 明确“未绑定 profile”“已绑定但未保存 key”“可请求远端”的三态
- 重新设计“拉取模型列表”和“测试连接”的按钮可用条件
- 对 warning / empty models / provider mismatch / fallback profile mismatch 做明确 UI 反馈
- 强化前后端配置 contract，减少页面猜状态

涉及范围：

- `frontend/src/components/prompts/LlmPresetPanel.tsx`
- `frontend/src/pages/PromptsPage.tsx`
- `backend/app/api/routes/llm_models.py`
- `backend/app/api/routes/llm_profiles.py`

验收标准：

- 用户能清楚知道按钮为什么可用/不可用
- 空模型列表不再造成“像坏了”的体验
- profile/provider/key 关系可以在界面中被解释清楚

数据/部署影响：

- 可能影响配置页交互与接口响应字段展示
- 不应破坏已有 profile 数据

开发周期：

- 单主程：1 到 2 周
- 小组模式：1 周

#### T06｜大纲流程重构：结构化编辑、局部重生成、应用策略（P1）

目标：把大纲流程从“生成后主要改 markdown”升级为“结构化编辑优先、markdown 作为结果表现”。

主要内容：

- 建立大纲章节树的一等编辑对象
- 支持章节级增删改、顺序调整、局部重生成
- 支持结构 patch 与 markdown 同步
- 优化“覆盖当前 / 另存为新大纲 / 从结构创建章节骨架”的策略解释

涉及范围：

- `frontend/src/pages/OutlinePage.tsx`
- 大纲解析与保存相关逻辑
- `backend/app/api/routes/outline.py`
- `backend/app/api/routes/outlines.py`

验收标准：

- 用户可直接编辑章节结构，而不是只看 JSON 预览
- 局部重生成不需要整份大纲推翻重来
- markdown 与结构化数据保持一致

数据/部署影响：

- 可能影响 outline `structure` 字段的内部组织方式
- 必须保证兼容已有项目的大纲数据

开发周期：

- 单主程：2 到 4 周
- 小组模式：1.5 到 3 周

#### T07｜写作高级生成收口：默认模式 / 高级模式 / 效果证明（P1）

目标：保留高级能力，但降低普通作者使用成本。

主要内容：

- 把写作生成 UI 拆成“默认模式”和“高级模式”
- 明确 `plan_first`、`post_edit`、`post_edit_sanitize`、`content_optimize` 的使用场景
- 给 compare/revert、dropped params、fallback 行为增加更清楚的解释
- 增加“这一步为什么有价值”的结果回显设计

涉及范围：

- `frontend/src/components/writing/AiGenerateDrawer.tsx`
- `frontend/src/pages/writing/useChapterGeneration.ts`
- Prompt preview / prompt inspector 相关链路

验收标准：

- 普通作者可以只看简化模式完成主流程
- 高级用户仍能使用完整配置
- 润色/优化/去味不再只是 checkbox，而是有结果价值可见性

数据/部署影响：

- 无数据库结构影响
- 主要是交互与流程层改造

开发周期：

- 单主程：2 到 4 周
- 小组模式：1.5 到 3 周

#### T08｜导入链路稳定化：状态同步、提案应用与失败恢复（P0/P1）

目标：把导入流程从“逻辑正确但体验不稳”变成用户可相信的功能。

主要内容：

- 修复 ImportPage 列表/详情不同步
- 为导入记录列表增加 optimistic update 或刷新后稳定一致性
- 强化导入状态机：queued/running/done/failed/retry
- 完善提案应用成功后的反馈与跳转联动

涉及范围：

- `frontend/src/pages/ImportPage.tsx`
- `backend/app/api/routes/import_export.py`
- 导入相关服务
- `test/specs/ui/import.spec.ts`

验收标准：

- 当前全量 Playwright 中的导入页失败用例修复
- 导入完成后，列表、详情、提案按钮状态一致
- 失败和重试路径可解释

数据/部署影响：

- 不影响已有数据结构
- 可能影响导入状态管理逻辑

开发周期：

- 单主程：1 到 1.5 周
- 小组模式：0.5 到 1 周

#### 2026-03-13 第一批次实做结果（对应 T02 / T04 / T05 / T08）

本节记录这份报告发布后第一批实际落地的收口结果，只写真实已做项，不回填“计划中但未做”的内容。

- 执行载体：
  - `plan/2026-03-13_09-45-52-wave-2026-03-13-a-config-import-quality.md`
  - `issues/2026-03-13_09-45-52-wave-2026-03-13-a-config-import-quality.csv`
- 已完成的 task 子项：
  - `T08`：`ImportPage` 改为 optimistic merge + request-seq guard，修复“右侧详情已出、左侧列表仍空/不稳定”的真实竞态；提案按钮改为按导入状态显式禁用/启用。
  - `T04`：模型配置页与导入页补齐关键按钮状态表达；任务模块“模型列表”按钮文案统一为“拉取模型列表”。
  - `T05`：主模块与任务模块共用同一套 LLM 访问状态机，明确展示“未绑定 profile / 未保存 Key / provider 不匹配 / 可请求远端”，并统一“拉取模型列表”“测试连接”的可用条件。
  - `T02`（低风险子项）：`backend/app/api/routes/tables.py` 内部字段改为 `table_schema`，对外继续保持 `schema` alias，消除 Pydantic shadow warning。
- 已补的回归：
  - `frontend/src/pages/importState.test.ts`
  - `frontend/src/components/prompts/llmConnectionState.test.ts`
  - `backend/tests/test_tables_schema_alias.py`
  - `test/specs/ui/import.spec.ts`
  - `test/specs/ui/prompts-test-connection.spec.ts`
  - `test/specs/api/tables.contract.spec.ts`
- 本批最终实跑结果：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`429 passed, 1 skipped`
  - 后端 `quality_gate`：通过，且不再出现 `tables.py schema` warning
  - 前端 `lint` / `vitest` / `build`：全部通过
  - Playwright 全量：`139/139` 通过
- 因而本报告中以下问题可视为已在第一批关闭：
  - `P1-07` 模型配置 UX 不清晰
  - `P1-09` ImportPage 状态不同步
  - `P2-14` `schema` 字段 warning
- 仍明确未做、留给后续波次的内容：
  - `T03` 多主题与 token 泄漏治理
  - `T06` / `T07` 主路径重构
  - `T09` 前端统一数据层
  - `T11+` 后端主链路边界重构与任务工业化
  - `T14+` 数据层/部署升级
  - `T17` 多 agent / 插件底座

#### 2026-03-13 第二次全量复核与推荐第二批次（Wave 2026-03-13 B）

本节是第一批实际落地后的再次复核结论，只写真实已验证结果与下一批推荐，不回填“理想中应该如何”。

- 第二次全量复核（2026-03-13 实跑）：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`429 passed, 1 skipped`
  - 后端 `quality_gate`：通过；仍仅保留既有 legacy allowlist warning 与超大文件 warning
  - 前端 `lint`：通过
  - 前端 `vitest`：`16` 文件 / `58` 测试通过
  - 前端 `build`：通过
  - Playwright 全量：`139/139` 通过
- 因而，本报告中第一批涉及的关闭结论仍成立：
  - `P1-07` 模型配置 UX 不清晰：已关闭
  - `P1-09` ImportPage 状态不同步：已关闭
  - `P2-14` `schema` 字段 warning：已关闭
- 同时可以更明确地说：
  - `Wave 2026-03-13 A` 已闭环
  - 当前仓库在现有范围内已达到“可直接推送生产端”的验证状态
  - 下一批不应回到“继续补用户可见 bug”，而应转向“降低前端复杂度与 review 风险”

- 推荐第二批次严格只做以下范围：
  1. `T10` 局部执行：拆 `frontend/src/pages/PromptsPage.tsx`，保持路由、行为、接口 contract 不变
  2. `T10` 局部执行：拆 `frontend/src/pages/SettingsPage.tsx`，保持 query preprocessing / rerank / feature defaults / context optimizer 等行为不回退
  3. `T04` 残项：把 Prompts / Settings 两页散落的 helper text、warning、danger confirm 文案进一步中心化
  4. `T03` 低风险子项：仅清理本批触达组件中的颜色/token 硬编码，统一到现有语义 token；不启动全站多主题

- 为什么推荐这批，而不是直接跳到更大的波次：
  - `PromptsPage.tsx` 当前约 `2193` 行，构建产物 chunk 约 `76.07 kB`
  - `SettingsPage.tsx` 当前约 `2075` 行，构建产物 chunk 约 `60.21 kB`
  - 两页已经是当前前端 review 最容易被打中的热点文件
  - 现有目录 `frontend/src/pages/prompts/` 与 `frontend/src/pages/settings/` 已存在，是低风险拆分落点
  - 这两页已有稳定回归可兜底：
    - `test/specs/ui/prompts-test-connection.spec.ts`
    - `test/specs/ui/prompts-rag-config.spec.ts`
    - `test/specs/ui/settings-query-preprocess.spec.ts`
    - `test/specs/ui/settings-rerank-config.spec.ts`
    - `test/specs/ui/navigation.spec.ts`
    - `test/specs/ui/a11y-form-fields.spec.ts`
    - `test/specs/ui/visual.spec.ts`
  - 相比之下，`T09`、`T11+`、`T14+` 都会带来明显更大的爆炸半径，不适合作为紧接着的下一批

- 第二批明确不应混入：
  - `T09` 全局前端数据层统一
  - `T06` / `T07` 大纲与写作主路径重构
  - `T11+` 后端 route/application service 大拆
  - `T14+` 数据层、部署、观测、CI/压测升级

- 第二批建议额外交付标准：
  1. `PromptsPage.tsx` 与 `SettingsPage.tsx` 入口文件体量显著下降，主要状态逻辑与面板逻辑抽到 page-local hooks/panels/models
  2. 路由、保存、测试连接、跳转、导航、锚点、视觉快照行为都不回退
  3. 对拆出的纯逻辑补 `vitest`
  4. 至少重跑上述定向 Playwright，并在最终回到 `cd test && npm test`
  5. 本报告与 `5.4大调查详细记录.md` 继续如实更新，不得只改代码不记账

#### 2026-03-13 第二批次实际完成结果（Wave 2026-03-13 B）

本节记录推荐第二批次在同日内的真实落地结果，只写已完成并经回归验证的内容，不回填未做项。

- 执行载体：
  - `plan/2026-03-13_21-00-53-wave-2026-03-13-b-prompts-settings-closeout.md`
  - `issues/2026-03-13_21-00-53-wave-2026-03-13-b-prompts-settings-closeout.csv`
- 已完成的 task 子项：
  - `T10` 局部执行（Prompts）：
    - `frontend/src/pages/PromptsPage.tsx` 从超大热点页收口为 105 行入口，主要状态与副作用下沉到 `frontend/src/pages/prompts/usePromptsPageState.ts`，Vector RAG 区块落到独立 section，保留 `/projects/:id/prompts` 路由与 `#rag-config` 锚点。
    - 主模块与任务模块的 profile / API key / 拉模型列表 / 测连接三态合同继续沿用既有状态机，未改后端 API contract。
  - `T10` 局部执行（Settings）：
    - `frontend/src/pages/SettingsPage.tsx` 收口为 141 行入口，项目设置加载与保存、feature defaults、query preprocessing、context optimizer、vector/rerank 表单映射改为 page-local state + sections 分层。
    - `/projects/:id/settings` 路由、保存语义、刷新语义、跳转到 prompts 链路、rerank API key 掩码/清除行为保持兼容。
  - `T04` 残项：
    - Prompts / Settings 触达范围内的 helper text、warning、danger confirm、disabled reason 进一步收口到 `frontend/src/pages/prompts/promptsCopy.ts`、`frontend/src/pages/settings/settingsCopy.ts` 与 `frontend/src/lib/uiCopy.ts`。
    - 同一概念不再混用多套近义词；危险操作与 fail-soft 提示语气在两页内已统一到同一风格。
  - `T03` 低风险子项：
    - 仅修这批触达组件中的明显颜色/token 硬编码，尤其是 danger 文案从 `text-red-*` / `dark:text-red-*` 收口到现有语义 class `text-danger`。
    - 未启动全站多主题、theme registry 或第三主题工程。
  - 测试稳定性补丁：
    - `test/specs/ui/visual.spec.ts` 的 request-id badge 清理改为 `MutationObserver` 持续移除异步插入节点，目的是保持视觉基线稳定，不放宽断言、不改业务行为。
- 本批新增/更新的回归：
  - 前端 `vitest` 新增/扩充：
    - `frontend/src/pages/prompts/llmApiError.test.ts`
    - `frontend/src/pages/prompts/promptsCopy.test.ts`
    - `frontend/src/pages/settings/queryPreprocessing.test.ts`
    - `frontend/src/pages/settings/settingsCopy.test.ts`
  - 黑盒重点回归继续覆盖：
    - `test/specs/ui/prompts-test-connection.spec.ts`
    - `test/specs/ui/prompts-rag-config.spec.ts`
    - `test/specs/ui/settings-query-preprocess.spec.ts`
    - `test/specs/ui/settings-rerank-config.spec.ts`
    - `test/specs/ui/navigation.spec.ts`
    - `test/specs/ui/a11y-form-fields.spec.ts`
    - `test/specs/ui/visual.spec.ts`
- 本批最终实跑结果（2026-03-13 实跑）：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`429 passed, 1 skipped`
  - 后端 `quality_gate`：通过；仍仅保留既有 legacy allowlist warning 与超大文件 warning
  - 前端 `lint`：通过
  - 前端 `vitest`：`20` 文件 / `71` 测试通过
  - 前端 `build`：通过
  - 定向 Playwright：上述 7 个 spec 共 `8/8` 通过
  - Playwright 全量：`139/139` 通过
- 因而，本批范围内以下问题可视为已关闭：
  - `Wave 2026-03-13 B` 定义内的 `PromptsPage` / `SettingsPage` 热点页入口职责收口
  - Prompts / Settings 两页触达范围内的 `T04` 文案、状态表达残项
  - Prompts / Settings 两页触达范围内的 `T03` 明显 token 泄漏低风险子项
- 仍明确未做、且不属于本批范围的内容：
  - `T09` 全局前端数据层统一
  - `T10` 其它热点页（`WorldBook` / `TaskCenter` / `Outline` / `Writing`）的拆分
  - `T03` 全站多主题 / 第三主题系统
  - `T04` 仓库级文案中心化工程
  - `T06` / `T07` 主路径重构
  - `T11+` 后端主链路边界重构与任务工业化
  - `T14+` 数据层 / 部署 / 可观测性升级
  - `T17` 多 agent / 插件底座
- 当前仍需诚实记录但不阻断生产的遗留点：
  - `PromptsPage` 与 `SettingsPage` 的路由 chunk 仍分别约为 `78.24 kB` 与 `67.18 kB`，说明“入口职责收口”已经完成，但更深层的 bundle/预取/共享状态治理仍需后续 `T09` / `T10` 深水区工作。
  - 该遗留不影响本批 contract、黑盒回归与当前生产可推结论。

#### 2026-03-13 Wave B 完成后的客观现状与推荐第三批次（Wave 2026-03-13 C）

截至 `Wave 2026-03-13 B` 完成并再次核对后，可以更明确地标记当前真实现状：

- 已经完全关闭、无需下一批重开的范围：
  - `Wave 2026-03-13 A` 已关闭：
    - `ImportPage` 列表/详情不同步黑盒失败
    - 模型配置页 `profile / API key / 拉模型列表 / 测连接` 三态合同不清
    - `backend/app/api/routes/tables.py` 的 `schema` warning
  - `Wave 2026-03-13 B` 已关闭：
    - `PromptsPage` 热点页入口职责过载
    - `SettingsPage` 热点页入口职责过载
    - Prompts / Settings 触达范围内的 `T04` 文案、状态表达残项
    - Prompts / Settings 触达范围内的 `T03` 明显颜色/token 泄漏低风险子项
- 当前仓库仍维持“可直接推送生产端”的客观状态：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`429 passed, 1 skipped`
  - 后端 `quality_gate`：通过；仍仅保留既有 legacy allowlist warning 与超大文件 warning
  - 前端 `lint` / `vitest` / `build`：通过，其中 `vitest` 为 `20` 文件 / `71` 测试
  - Playwright 全量：`139/139` 通过

- 因而，推荐第三批次严格只做以下范围：
  1. `T10` 局部执行：拆 `frontend/src/pages/WorldBookPage.tsx`
  2. `T10` 局部执行：拆 `frontend/src/pages/TaskCenterPage.tsx`
  3. `T04` 残项：把这两页触达范围内散落的 helper text / warning / disabled reason / danger confirm 进一步收口
  4. `T03` 低风险子项：仅清理这两页触达组件中的明显颜色/token 硬编码，统一到现有语义 token

- 为什么第三批次应选这组，而不是直接进入更深的主路径改造：
  - `WorldBookPage.tsx` 当前约 `1824` 行，构建产物 route chunk 约 `45.15 kB`
  - `TaskCenterPage.tsx` 当前约 `1343` 行，构建产物 route chunk 约 `39.29 kB`
  - 两页都已经有 page-local 落点可以承接拆分：
    - `frontend/src/pages/worldbook/`
    - `frontend/src/pages/taskCenter/`
  - 两页都已有较完整黑盒回归，适合继续做“结构收口但不改 contract”：
    - `test/specs/ui/worldbook.spec.ts`
    - `test/specs/ui/worldbook-bulk-disable.spec.ts`
    - `test/specs/ui/worldbook-import-export.spec.ts`
    - `test/specs/ui/worldbook-large-list-pagination.spec.ts`
    - `test/specs/ui/worldbook-auto-update-success.spec.ts`
    - `test/specs/ui/worldbook-auto-update-failsoft.spec.ts`
    - `test/specs/ui/task-center.spec.ts`
    - `test/specs/ui/taskcenter-projecttasks.spec.ts`
    - `test/specs/ui/taskcenter-projecttasks-sse.spec.ts`
    - `test/specs/ui/batch-generation-runtime-sync.spec.ts`
    - `test/specs/ui/navigation.spec.ts`
    - `test/specs/ui/a11y-form-fields.spec.ts`
    - `test/specs/ui/visual.spec.ts`
  - 相比之下，`OutlinePage` / `WritingPage` 更接近创作主路径，虽然也仍有体量问题，但当前爆炸半径明显更大，不适合作为紧跟 Wave B 的下一批

- 第三批次明确不应混入：
  - `T09` 全局前端数据层统一
  - `T10` 的 `OutlinePage` / `WritingPage` 拆分
  - `T03` 全站多主题 / 第三主题系统
  - `T04` 全仓文案中心化工程
  - `T06` / `T07` 大纲与写作主路径重构
  - `T11+` 后端 route / application service 大拆
  - `T14+` 数据层、部署、观测、CI/压测升级

- 第三批次完成后的正确验收方向应是：
  - `WorldBookPage.tsx` 与 `TaskCenterPage.tsx` 入口文件体量显著下降
  - 世界书 CRUD / 搜索 / 排序 / 批量操作 / import/export / auto update 链路不回退
  - TaskCenter 的 change sets / project tasks / SSE / batch runtime / rollback/retry/cancel 链路不回退
  - 文案、状态表达、danger token 在触达范围内继续收口
  - 跑完针对性 Playwright 后最终回到 `cd test && npm test`

#### 2026-03-14 第三批次实际完成结果（Wave 2026-03-13 C）

本批已按原定 Wave C 范围完成，并且没有重开已经在 Wave A / Wave B 关闭的问题。

- 本批实际关闭项：
  - `T10` 局部子批次：`WorldBookPage` 完成 page-local 拆分，入口文件已收口到 `26` 行；页面初始化、筛选/分页、批量操作、导入导出、预览、编辑 drawer、auto update 与 TaskCenter 跳转职责已拆入 `frontend/src/pages/worldbook/` 下的 state / sections / models / copy 模块。
  - `T10` 局部子批次：`TaskCenterPage` 完成 page-local 拆分，入口文件已收口到 `41` 行；筛选状态、详情 drawer、queue health、project task 操作、runtime、SSE live status 与 reload 恢复职责已拆入 `frontend/src/pages/taskCenter/` 下的 state / sections / models / copy 模块，且未重写 `useProjectTaskEvents`。
  - `T04` 残项：WorldBook / TaskCenter 触达范围内的 helper text、warning、fail-soft、loading、confirm 与错误摘要表达继续收口，减少同义散落和“像坏了”的提示。
  - `T03` 低风险子项：只处理本批触达组件里的明显 token 泄漏；WorldBook 批量勾选态已从 `text-white` 收口到 `color-on-accent` 语义 token，未扩展为全站主题工程。

- 本批验证结果（真实实跑）：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`429 passed, 1 skipped`
  - 后端 `quality_gate`：通过；仍只有既有 legacy allowlist warning 与超大文件 warning
  - 前端 `lint`：通过
  - 前端 `vitest`：`23` 文件 / `83` 测试通过
  - 前端 `build`：通过
  - 定向 Playwright（本批要求的 15 个 spec 文件）：`20/20` 通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过

- 仍需诚实记录、但不属于本批范围的事项：
  - `T09` 全局前端数据层统一未做；本批没有引入新的全局 store，也没有改跨页共享状态方案。
  - `OutlinePage` / `WritingPage` 拆分未做；这两页仍属于更高爆炸半径主路径，应放在后续 `T10` 深水区处理。
  - `T06` / `T07` 主路径重构未做；本批未碰大纲/写作主链路。
  - `T03` 全站多主题 / registry 工程未做；本批仅清理触达范围内明显 token 泄漏。
  - `T04` 全仓文案中心化工程未做；本批只在 WorldBook / TaskCenter 触达范围内统一 copy。
  - 更深层的 bundle/data-layer 治理未做；本次构建中 `WorldBookPage` route chunk 约 `48.74 kB`、`TaskCenterPage` route chunk 约 `48.13 kB`，说明“入口职责收口”已完成，但更深层优化仍应归入后续 `T09` / `T10`，不应在本批扩 scope。

- 生产可推结论：
  - 现阶段仓库仍达到“可直接推送更新到生产端”的标准。
  - 原因是本批没有改路由 contract、search params contract、ARIA contract、按钮合同或后端 API contract，且后端、前端、定向黑盒与全量黑盒回归均已通过。

#### T09｜前端统一数据层：缓存、失效、预取、跨页共享（P1）

目标：让前端不再主要依靠页面级 `useProjectData` 做松散的数据获取，而是形成统一数据层。

主要内容：

- 抽象 query/store 层
- 统一 chapters、project、task、worldbook、memory、settings 等关键资源的缓存与失效
- 支持列表与详情分离、预取、跨页复用
- 为大页面拆分创造更稳定的数据基础

涉及范围：

- `frontend/src/hooks/useProjectData.ts`
- 各种 `services/*Api.ts`
- 写作/预览/阅读/任务中心等主要页面

验收标准：

- 页面切换不再频繁重复拉同一份数据
- 数据更新后的失效行为可预测
- 写作、预览、阅读等页面共享章节基础数据

数据/部署影响：

- 无数据库影响
- 对前端行为和缓存一致性影响较大，需要补测试

开发周期：

- 单主程：2 到 4 周
- 小组模式：1.5 到 3 周

#### T10｜前端热点页面拆分：Prompts / Settings / WorldBook / TaskCenter / Outline / Writing（P1）

目标：压低单页复杂度，减少神页继续膨胀。

主要内容：

- 对超大页面做 panel/container/hook/domain 级拆分
- 拆分后保持路由和用户体验不变
- 为各子面板补单元测试和黑盒关键路径测试
- 顺带优化 bundle 切分

涉及范围：

- `PromptsPage.tsx`
- `SettingsPage.tsx`
- `WorldBookPage.tsx`
- `TaskCenterPage.tsx`
- `OutlinePage.tsx`
- `WritingPage.tsx`

验收标准：

- 高风险页面都降到合理维护体量
- 页面职责边界更清晰
- 拆分后测试不回退，路由行为不变

数据/部署影响：

- 不影响数据库
- 前端回归面较大

开发周期：

- 单主程：3 到 6 周
- 小组模式：2 到 4 周

#### T11｜后端主链路边界重构：Chapters / Outline / Memory 从 route 下沉到 application service（P0）

目标：把最危险的 route 巨石拆开，建立真正的应用服务边界。

主要内容：

- 为章节生成、章节计划、大纲生成、记忆预览/更新等链路建立 application service
- route 层只做权限、参数、调用、响应
- 把当前 route 中的 LLM 调用、解析、回退逻辑逐步下沉
- 保持对旧接口兼容，禁止大爆炸重写

涉及范围：

- `backend/app/api/routes/chapters.py`
- `backend/app/api/routes/outline.py`
- `backend/app/api/routes/memory.py`
- generation / parsing / key resolve / runtime services

验收标准：

- `no-direct-llm-call-in-api` allowlist 显著缩小
- 主路径 route 行数下降
- 行为不回退，现有 API contract 维持兼容

数据/部署影响：

- 不直接改数据模型
- 对业务逻辑调用栈影响大，需要充分回归

开发周期：

- 单主程：3 到 6 周
- 小组模式：2 到 4 周

#### T12｜知识子域拆分与高级调试统一：RAG / Graph / Structured / Fractal（P1）

目标：把知识底座模块从“都能用，但各自长法不同”变成统一的可维护子域。

主要内容：

- 拆分 `vector_rag_service.py` 等超大服务
- 建立 RAG/Graph/Structured/Fractal 的统一调试页信息架构
- 统一 query/preview/status/result/debug payload 的展示模型
- 明确“作者模式”和“高级调试模式”的分层

涉及范围：

- `backend/app/services/vector_rag_service.py`
- graph / fractal / structured memory 相关服务
- 对应前端高级页
- `docs/ux/advanced-debug-page-guidelines.md`

验收标准：

- 高级页之间的结构更统一
- RAG/Graph/Structured/Fractal 主要服务不再是超巨石
- 作者不会在普通流程里被迫面对大量调试字段

数据/部署影响：

- 可能涉及部分响应结构统一
- 必须保持现有底层数据兼容

开发周期：

- 单主程：3 到 5 周
- 小组模式：2 到 3 周

#### T13｜任务系统工业化：事件流、配额、自愈、provider backpressure（P0/P1）

目标：把当前已经有雏形的任务系统升级为更稳的生产基础设施。

主要内容：

- 明确 project task / batch generation / memory task 的事件模型
- 完善 watchdog、heartbeat、reconcile、重试、取消传播
- 增加 provider/user/project/global 多层 quota
- 增加 provider backpressure 和异常恢复策略

涉及范围：

- `backend/app/services/task_queue.py`
- `backend/app/services/project_task_service.py`
- `backend/app/services/project_task_runtime_service.py`
- `batch_generation` 相关服务与 route
- Task Center 页面

验收标准：

- 任务状态可以恢复、可以解释、可以限额、可以观测
- 批量生成不再只是“小批处理工具”，而更接近真正批次编排
- Redis / worker 波动下任务不容易长期卡死

数据/部署影响：

- 可能增加 runtime 字段、事件表字段或任务 contract
- 对部署和运维有直接影响

开发周期：

- 单主程：3 到 6 周
- 小组模式：2 到 4 周

#### T14｜数据层与部署升级：Postgres / Redis / RQ 默认化，SQLite 退居开发（P0）

目标：从根上承认 SQLite 的定位，把可上线口径切到更合理的生产形态。

主要内容：

- 明确生产只推荐 Postgres + Redis + RQ
- 完善 SQLite 到 Postgres 的迁移说明与验证脚本
- 收敛 Docker Compose / prod overlay / secrets / network exposure 规范
- 评估 Windows worker 兼容与平台支持矩阵

涉及范围：

- `README.md`
- `docker-compose.yml`
- `docker-compose.prod.yml`
- `backend/scripts/*`
- 数据迁移与部署文档

验收标准：

- 项目不会再对外暗示“SQLite 也适合生产上量”
- Postgres/Redis/RQ 的启动、迁移、回滚路径文档齐全
- 生产环境默认配置更安全

数据/部署影响：

- 高
- 直接影响已部署实例的升级路径和数据迁移计划

开发周期：

- 单主程：3 到 6 周
- 小组模式：2 到 4 周

#### T15｜观测体系建设：Metrics / Tracing / Error Aggregation / Runtime Dashboard（P0/P1）

目标：把当前“靠 health + logs + debug bundle 排障”的模式升级为真正可运维的可观测体系。

主要内容：

- 增加指标采集：请求量、错误率、时延、队列长度、任务时长、provider 调用结果
- 增加 tracing 或最少链路追踪能力
- 接入 Sentry 或同级异常聚合
- 增加任务运行时 dashboard

涉及范围：

- 后端入口、日志系统、任务系统、队列
- 部署文档与运维脚本

验收标准：

- 能回答“哪里慢、哪里炸、哪个 provider 异常、哪个队列积压”
- 线上排障不再主要靠翻文本日志

数据/部署影响：

- 对代码侵入中等
- 对部署和监控环境有新增要求

开发周期：

- 单主程：2 到 4 周
- 小组模式：1.5 到 3 周

#### T16｜配置中心合同化：Provider / Model / Capability / Secret / Prod Guard（P1）

目标：把当前配置很多但仍偏手工维护的状态，升级成更严格的配置合同。

主要内容：

- 抽离 provider/model/capability registry
- 对 model list、capability、pricing、reasoning 参数做 schema 化
- prod guard 与 dev fallback 口径进一步收紧
- 拆分 `backend/app/core/config.py` 的子域配置

涉及范围：

- `backend/app/core/config.py`
- llm capability / llm models / llm preset / profile routes
- 前端模型配置页

验收标准：

- 配置项不再主要靠页面和 route 自己解释
- model/provider/capability 关系有统一合同
- `config.py` 不再继续无序膨胀

数据/部署影响：

- 低到中
- 主要影响配置结构与接口返回语义

开发周期：

- 单主程：2 到 4 周
- 小组模式：1.5 到 3 周

#### T17｜多 Agent 与插件底座：Tool Registry / Permission / Sandbox / Agent Runtime（P2 战略）

目标：为未来的多 agent、外部工具、第三方插件做好真正的底座，而不是继续在 mock MCP 上加 if/else。

主要内容：

- 设计统一 tool registry
- 设计插件元数据、权限声明、版本兼容、审计模型
- 引入运行时沙箱和调用边界
- 设计 agent runtime：planner / executor / reviewer / memory coordinator 等角色协议
- 统一中间产物 schema 与上下文协议

涉及范围：

- `backend/app/services/mcp/service.py`
- generation run / task runtime / prompt system / memory modules
- 未来插件 API 与前端扩展点

验收标准：

- MCP 不再只是 `mock.echo/mock.sleep/mock.fail`
- 多 agent / 插件扩展不需要改核心业务大文件
- 工具调用有权限边界与审计

数据/部署影响：

- 中到高
- 是战略任务，不建议在前面基础未稳时提前硬上

开发周期：

- 单主程：4 到 8 周
- 小组模式：3 到 5 周

#### T18｜测试矩阵、CI 回归分层与压测门禁（P0/P1）

目标：把当前不错但还不够制度化的测试基础，升级为真正能挡住回退的交付门槛。

主要内容：

- 建立 smoke / contract / critical / full / perf-smoke 分层
- 将全量 Playwright、关键 API contract、数据库快照、性能基线纳入统一 gate
- 为主题、多主题、导入、任务中心、大纲结构化编辑等新能力补测试
- 增加压测场景与数据基线

涉及范围：

- `test/`
- `backend/tests/`
- `frontend` 单测
- CI / 脚本入口

验收标准：

- 每个任务包都有明确回归集
- 关键路径回归时间可控
- 性能基线不再只是文档，而是有持续可对比结果

数据/部署影响：

- 不影响业务数据
- 直接影响交付流程与研发节奏

开发周期：

- 单主程：2 到 4 周
- 小组模式：1.5 到 3 周

### K.6 推荐执行顺序

#### 第一波：先修“用户会直接撞上的坑”

优先执行：

- `T01`
- `T03`
- `T04`
- `T05`
- `T08`

原因：

- 这批任务不需要先推翻架构
- 能快速修掉最影响用户感知和团队协作质量的问题
- 对后续更大规模改造几乎都是正收益

#### 第二波：收紧前端主路径

优先执行：

- `T06`
- `T07`
- `T09`
- `T10`

原因：

- 当前前端复杂度是最直观的维护成本来源
- 大纲和写作是项目主路径，优先级必须高
- 数据层统一是拆大页面的前提

#### 第三波：收紧后端主链路与任务系统

优先执行：

- `T02`
- `T11`
- `T12`
- `T13`

原因：

- 不先把 route/service 边界收好，后面继续扩展只会更痛
- 任务系统的工业化直接决定系统稳定性与上限

#### 第四波：生产化与可观测性

优先执行：

- `T14`
- `T15`
- `T16`
- `T18`

原因：

- 这一波是从“能开发、能演示”走向“能稳上线、能排障、能回归”的关键
- 必须放在主路径与边界收口之后做，否则会在不稳定地基上堆生产能力

#### 第五波：多 agent / 插件战略波次

优先执行：

- `T17`

原因：

- 这是未来价值很高的方向
- 但它明显不是现在最先做的事
- 没有前面几波，做出来也只会更难维护

### K.7 开发周期总估算

按推荐顺序，粗略估算如下：

#### 单主程模式

- Wave A：3 到 5 周
- Wave B：5 到 8 周
- Wave C：6 到 10 周
- Wave D：6 到 10 周
- Wave E：4 到 8 周

总计：

- 不含多 agent/插件：约 `20 到 33 周`
- 含多 agent/插件：约 `24 到 41 周`

#### 小组模式

- Wave A：2 到 3 周
- Wave B：3 到 5 周
- Wave C：4 到 6 周
- Wave D：4 到 7 周
- Wave E：3 到 5 周

总计：

- 不含多 agent/插件：约 `13 到 21 周`
- 含多 agent/插件：约 `16 到 26 周`

### K.8 如果必须压缩周期，应该怎么压

如果你当前资源有限，不要试图一次做完 18 个包。  
更务实的压缩方案是：

#### 最小可落地提升包

- `T01`
- `T04`
- `T05`
- `T08`
- `T10`（只先拆 `PromptsPage` 与 `SettingsPage`）
- `T11`（先收 `chapters/outline`）

这样做的好处：

- 周期短
- 用户感知强
- 维护收益明显
- 不会一脚踏进超大改造泥潭

#### 最小生产可信包

在上面基础上再加：

- `T13`
- `T14`
- `T15`
- `T18`

这样才算开始具备“可信上线”的基础。

### K.9 每一波完成后必须做的回归

#### 所有波次通用

- `cd backend && .\.venv\Scripts\python.exe -m compileall -q app alembic`
- `cd backend && .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v`
- `cd frontend && npm run lint`
- `cd frontend && npm test`
- `cd frontend && npm run build`

#### Wave A / Wave B 重点

- Playwright UI：
  - 大纲主路径
  - 写作主路径
  - 模型配置主路径
  - 导入主路径
  - 世界书 / 任务中心关键链路

#### Wave C / Wave D 重点

- API contract：
  - tasks / runtime / events
  - llm models / profiles / presets
  - outline / chapters / memory
- DB schema snapshot
- perf baseline

### K.10 最终推荐

如果只给一句最现实的执行建议，我推荐：

1. 先做 `Wave A`
2. 然后立即做 `Wave B`
3. 接着做 `Wave C`
4. 在你真的准备上线前，再做 `Wave D`
5. `Wave E` 只在前四波完成且团队进入平台化阶段后再启动

这套顺序最符合你当前项目的真实状态，也最符合投入产出比。

### 附录 L：2026-03-14 第四批次实际完成结果（Wave 2026-03-14 D）

本批已在 `Wave 2026-03-13 C` 完整回归通过的基础上继续推进，没有重开已关闭问题，也没有借拆分名义引入行为回退。

- 本批实际关闭项：
  - `T10` 局部子批次：`OutlinePage` 完成 page-local 拆分；入口文件已收口到 `28` 行，页面初始化、active outline 切换、保存/rename/create/delete、autosave/save hotkey/dirty guard、生成 modal 状态、stream/fallback/raw/final preview、overwrite/save-as-new/create chapter skeleton 与触达范围内 copy 职责已拆入 `frontend/src/pages/outline/`。
  - `T10` 局部子批次：`WritingPage` 完成 page-local 拆分；入口文件已收口到 `27` 行，`chapterId` / `applyRunId` 语义、chapter list 与 editor 编排、save/done/dirty、generate/apply/history/batch/context/memory/foreshadow/tables/analysis 与页面级 copy 职责已拆入 `frontend/src/pages/writing/`，且未重写现有写作 hooks 主合同。
  - `T04` 残项：Outline / Writing 触达范围内的 helper text、warning、disabled reason、confirm、fail-soft、advanced-mode 提示继续收口，减少同义散落与“像坏了”的状态表达。
  - `T03` 低风险子项：只清理本批触达组件里的明显 token 泄漏与危险色/前景色硬编码，统一回现有语义 token；未扩成全站主题工程。
  - 回归补口时同步关闭两个真实阻断竞态：
    - `fractal_memory.project_id` 首次重建插入竞态，避免 `fractal/rebuild` 在后台 `chapter_done` 重建并发时返回 `500`
    - `user_usage_stats.user_id` 首次 generation run 计数竞态，避免 `fractal_v2` / auto update 等链路把真实 parse fallback 误打成 `internal_error`

- 本批验证结果（真实实跑）：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`431 passed, 1 skipped`
  - 后端 `quality_gate`：通过；仍只有既有 legacy allowlist warning 与超大文件 warning
  - 前端 `lint`：通过
  - 前端 `vitest`：`25` 文件 / `87` 测试通过
  - 前端 `build`：通过
  - 定向 Playwright（本批要求的 31 个 spec 文件）：`34/34` 通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过

- 仍需诚实记录、但不属于本批范围的事项：
  - `T09` 全局前端数据层统一未做；本批没有引入新的跨页共享状态方案。
  - `T06` 大纲结构化编辑与主路径重构未做；本批不改大纲主路径产品模型。
  - `T07` 写作高级模式全面收口未做；本批只做 compatibility-first 的入口收口与 copy/state 清理。
  - `T03` 全站多主题 / theme registry 工程未做；本批仅修触达范围内明显 token 泄漏。
  - `T04` 全仓文案中心化工程未做；本批只统一 Outline / Writing 触达范围内 copy。
  - 更深层 bundle/data-layer 治理未做；当前构建中 `OutlinePage` route chunk 约 `27.71 kB`、`WritingPage` route chunk 约 `170.70 kB`，说明入口职责收口已完成，但更深层性能与切分优化仍应归入后续 `T09` / `T10` 深水区。

- 生产可推结论：
  - 现阶段仓库仍达到“可直接推送更新到生产端”的标准。
  - 原因是本批没有改路由 contract、search params contract、ARIA contract、按钮合同或后端 API contract，且后端、前端、定向黑盒与全量黑盒回归均已通过。

### 附录 M：2026-03-14 第五批次实际完成结果（Wave 2026-03-14 E）

本批在 `Wave 2026-03-14 D` 完整回归通过的基础上继续推进，严格限定在“写作高级生成解释层 + 项目任务运行时数据层”的局部收口，没有重开已关闭问题，也没有借统一数据层、产品重构或主题工程之名引入行为回退。

- 本批实际关闭项：
  - `T09` 局部子批次：为 `Writing` 批量生成与 `TaskCenter` project task runtime 收口共享的 query / snapshot / invalidation 边界；新增 `projectTaskStore` 与 `useProjectTaskRuntimeResource`，把列表、详情、runtime 请求缓存、SSE 驱动刷新、轮询 fallback、手动失效与跨页同步从页面级重复逻辑中抽出，但未改 SSE 路由、payload、后端 API contract、`?chapterId=`、`?applyRunId=` 或 TaskCenter 主路径行为。
  - `T07` 局部子批次：`AiGenerateDrawer` 完成 feature-local 拆分；入口编排、默认模式区块、高级模式区块、copy/model 映射与 style 加载边界已分离到 `frontend/src/components/writing/aiGenerateDrawer/`，但未重写 `useChapterGeneration` / `useApplyGenerationRun` 主合同，也未改按钮文案、ARIA、ESC、save-before-generate、compare/revert、prompt override、memory/context 或 fallback 行为。
  - `T04` 残项：本批触达范围内的 helper text、warning、disabled reason、fail-soft、dropped params、bundle 说明与 runtime recommendation copy 已继续收口；同一概念不再散落多套近义词。
  - `T03` 低风险子项：只清理本批触达范围内的样式 contract 残项，继续沿用现有语义 token / semantic class；未启动全站多主题、theme registry 或 Tailwind 大工程。
  - 回归补口时同步修复两个直接阻断验证的前端测试类型问题：`aiGenerateDrawerModels.test.ts` 的 provider 取值改回合法枚举，`projectTaskStore.test.ts` 的 mock 签名调整为与共享 store 合同一致；修复范围仅限测试与本批触达链路。

- 本批验证结果（真实实跑）：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`431 passed, 1 skipped`
  - 后端 `quality_gate`：通过；仍只有既有 legacy allowlist warning 与超大文件 warning
  - 前端 `lint`：通过
  - 前端 `vitest`：`28` 文件 / `97` 测试通过
  - 前端 `build`：通过
  - 定向 Playwright（本批要求的 `34` 个 spec 文件）：`37/37` 通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过

- 本批未做且不应借机混入的内容：
  - `T09` 全站统一数据层重写、跨页全局 store 工程或 SSE 契约改造未做；本批只补了 project task runtime 这条链路的最小共享资源层。
  - `T07` 写作高级模式产品级全面重构未做；本批只拆本地职责与解释层。
  - `T10` 之外的其他热点页再大拆、`T11+` 后端 route / application service 大拆、`T14+` Postgres / Redis / RQ / 部署 / 观测 / CI / 压测升级均未做；这些不属于本批兼容优先边界。
  - 全仓文案中心化工程与全站多主题工程未做；本批只统一触达范围内 copy 与语义 token。
  - 更深层 bundle 治理未做；当前构建中 `WritingPage` route chunk 约 `170.30 kB`，说明本批目标是数据流与解释层收口，不是 bundle 工程。

- 生产可推结论：
  - 现阶段仓库仍达到“可直接推送更新到生产端”的标准。
  - 原因是本批没有改路由、search params、ARIA、按钮文案或后端 API contract，且后端、前端、定向黑盒与全量黑盒回归均已实跑通过。

### 附录 N：2026-03-15 第六批次实际完成结果（Wave 2026-03-15 F）

本批严格承接 `2026-03-15` 对 `Wave 2026-03-13 A/B/C` 与 `Wave 2026-03-14 D/E` 的全量复核结果，只处理后端 `T11` 的局部主链路边界与 `T18` 的低风险回归/文档闭环，没有重开已关闭问题，也没有借“统一后端架构”“任务系统工业化”或部署升级之名扩 scope。

- 本批实际关闭项：
  - `T11` 局部子批次：`backend/app/api/routes/chapters.py` 的 chapter plan / generate-precheck / generate / generate-stream LLM orchestration 已下沉到 `backend/app/services/chapter_generation_app_service.py`、`backend/app/services/chapter_generation_stream_service.py` 与 `backend/app/services/chapter_generation_models.py`；route 现仅保留鉴权、请求接入、service 调用、响应映射与最薄错误边界。
  - `T11` 局部子批次：`backend/app/api/routes/outline.py` 的 outline generate / generate-stream / segmented / repair / fill / aggregate-run orchestration 已下沉到 `backend/app/services/outline_generation_app_service.py`；普通生成、长篇 segmented、repair/fill、`generation_run_id` / `generation_sub_run_ids`、raw preview 与 stream/fallback 语义保持兼容。
  - `T11` 局部子批次：`backend/app/api/routes/memory.py` 的 `POST /chapters/{chapter_id}/memory/propose/auto` LLM orchestration 已下沉到 `backend/app/services/memory_auto_update_app_service.py`；route 不再直接准备 LLM 调用或解析 contract。
  - `T18` 低风险子项：补齐本批新增 application service 的后端单测与主链路 contract/UI 回归；同时把本批触达范围内 parse failure / validation details / run-id 传递口径收得更一致，其中 memory auto-propose 的 parse failure details 现在同时携带 `generation_run_id` 与 `llm_generation_run_id`，但未改对外 schema。

- 本批验证结果（真实实跑）：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`439 passed, 1 skipped`
  - 后端 `quality_gate`：通过
  - 前端 `lint`：通过
  - 前端 `vitest`：`28` 文件 / `97` 测试通过
  - 前端 `build`：通过
  - 定向 Playwright（本批要求的 `24` 个 spec 文件）：`26/26` 通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过

- 本批实际缩小的 warning / route 边界：
  - `backend/app/api/routes/chapters.py` 已从 `no-direct-llm-call-in-api` warning 列表移除。
  - `backend/app/api/routes/outline.py` 已从 `no-direct-llm-call-in-api` warning 列表移除。
  - `backend/app/api/routes/memory.py` 已从 `no-direct-llm-call-in-api` warning 列表移除。
  - `quality_gate` 当前仅剩既有 legacy allowlist warning：`backend/app/api/routes/chapter_analysis.py`、`backend/app/api/routes/llm.py`。

- 本批未做且不属于本批范围的内容：
  - `T11` 全量后端主链路大重写未做；本批只收 `chapters.py` / `outline.py` / `memory.py` 指定链路，不触碰无关 CRUD endpoint、API contract、SSE contract、status code、error code 或 generation run 类型。
  - `T12` 知识子域统一、高级调试页统一、`T13` 任务系统工业化、`T14+` Postgres / Redis / RQ / 部署 / 观测 / CI / 压测升级均未做；这些依旧属于后续更大工程，不应混入本批 compatibility-first 收口。
  - `file-line-count-guard` 仍提示大文件，包括 `backend/app/services/outline_generation_app_service.py`、`backend/app/services/chapter_generation_app_service.py`、`backend/app/api/routes/outline.py`、`backend/app/api/routes/memory.py` 等；这些 warning 已从 direct-LLM 热点转成“后续继续细拆的大文件热点”，但继续拆分会明显超出本批边界。
  - `backend/app/api/routes/chapter_analysis.py` 的 direct-LLM allowlist 尚未收口；该链路属于独立后端主链路批次，不阻断当前 Wave F 的 chapters/outline/memory 主路径闭环。

- 生产可推结论：
  - 现阶段仓库仍达到“可直接推送更新到生产端”的标准。
  - 原因是本批 route 边界真实收紧、`no-direct-llm-call-in-api` 在触达链路上已显著缩小，而且后端、前端、定向 Playwright 与全量 Playwright 均已实跑通过。

### 附录 O：2026-03-15 Wave F 完成后再次全量核对与推荐下一批次（Wave 2026-03-15 G）

本节只记录 `Wave 2026-03-15 F` 完成后的真实客观现状，以及最适合继续保持 compatibility-first 的下一批建议；不回填“理想中应该如何”，也不重开已经关闭的 Wave A-F 问题。

- 当前已确认完成且不应下一批重开的范围：
  - `Wave 2026-03-13 A`：`ImportPage` 列表/详情不同步、模型配置页三态合同、`tables.py` schema warning
  - `Wave 2026-03-13 B/C`：Prompts / Settings / WorldBook / TaskCenter 热点页拆分及对应 copy/token 残项
  - `Wave 2026-03-14 D/E`：Outline / Writing 热点页拆分、`AiGenerateDrawer` 解释层收口、project task runtime 共享资源边界
  - `Wave 2026-03-15 F`：chapters / outline / memory route 的主 LLM orchestration application service 化

- Wave F 完成后的再次核对结果（真实状态）：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`439 passed, 1 skipped`
  - 后端 `quality_gate`：通过；当前 `no-direct-llm-call-in-api` 只剩 `backend/app/api/routes/chapter_analysis.py`、`backend/app/api/routes/llm.py` 两个 legacy allowlist warning
  - 前端 `lint`：通过
  - 前端 `vitest`：`28` 文件 / `97` 测试通过
  - 前端 `build`：通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过

- 因而，下一批最值得做、且最适合继续保持 compatibility-first 的范围是：
  1. `T11` 局部子批次：把 `backend/app/api/routes/chapter_analysis.py` 中 `analyze` / `rewrite` / auto memory update 的 LLM orchestration 下沉到 application service，保留现有 route / payload / error / fail-soft 语义。
  2. `T11` 局部子批次：把 `backend/app/api/routes/llm.py` 的 `/llm/test` orchestration 下沉到 application service，保留现有请求头/body 合同、重试策略、details 语义与前端测试连接主路径。
  3. `T02/T11` 低风险子项：只在本批触达链路里继续缩小 Wave F 新增大文件热点，优先拆 `chapter_generation_app_service.py`、`outline_generation_app_service.py` 及必要的 route-local helper，不扩成全仓超大文件治理。
  4. `T18` 低风险子项：为新增 service/models/parser 补单测与 contract test，并对本批触达的 API/UI 主路径重跑定向黑盒和全量黑盒。

- 选择这批的原因：
  - 它直接命中当前 `quality_gate` 里最明确、最容易在 review 中继续被打中的剩余 route-level warning。
  - `chapter_analysis`、`chapter_rewrite`、`auto memory update`、`/llm/test` 都已有较强的 API contract 或 unittest/Playwright 覆盖，适合做“结构收口但不改 contract”的下一波。
  - 相比 `T12`、`T13`、`T14+`，这批不会引入新的基础设施形态、配额系统、事件总线或知识子域统一工程，爆炸半径可控。

- 下一批明确不应混入：
  - `T12` 知识子域统一与高级调试页统一
  - `T13` 任务系统工业化、provider backpressure、quota、watchdog
  - `T14+` Postgres / Redis / RQ / 部署 / 观测 / CI / 压测升级
  - `vector_rag_service.py`、`project_task_service.py` 等无关巨石的大拆
  - 改后端 API contract、SSE contract、status code、error code、generation run 类型
  - 改前端 URL、search params、ARIA、按钮文案、测试断言或 timeout

- 下一批完成后的正确验收方向应是：
  - `chapter_analysis.py` 与 `llm.py` 不再承担直接 LLM orchestration，route 层只保留接入/映射职责。
  - `no-direct-llm-call-in-api` 在当前仓库中不再对任何 API route 报 direct-LLM legacy allowlist warning；若仍有残留，必须有明确证据证明不属于本批范围。
  - 本批触达的大文件 warning 至少显著缩小，并通过真实测试证明没有引入 chapter analysis、rewrite、memory update 或 test-connection 回退。
  - 本批完成后仓库仍达到“可直接推送更新到生产端”的标准。


### 附录 P：2026-03-15 第七批次实际完成结果（Wave 2026-03-15 G）

本批严格承接附录 O 对 `Wave 2026-03-15 G` 的推荐范围，只做 `T11` 的两个局部 route allowlist 收口、`T02/T11` 的本批触达热点缩口，以及 `T18` 的低风险测试/文档闭环；没有重开 Wave A-F 已关闭问题，也没有借“统一后端架构”“知识子域统一”“任务系统工业化”或部署升级之名扩 scope。

- 本批实际关闭项：
  - `MVP-678` / `T11` 局部子批次：`backend/app/api/routes/chapter_analysis.py` 的 chapter analyze / rewrite / auto memory update orchestration 已下沉到 `backend/app/services/chapter_analysis_app_service.py` 与 `backend/app/services/chapter_analysis_models.py`；route 仅保留鉴权、header/body 接入、service 调用、响应映射与最薄错误边界，`/chapters/{chapter_id}/analysis/apply`、`/annotations` 主路径合同未改。
  - `MVP-679` / `T11` 局部子批次：`backend/app/api/routes/llm.py` 的 `/api/llm/test` key 解析、provider/base_url 归一、retry/backoff、details 组装与 LLM 调用已下沉到 `backend/app/services/llm_test_app_service.py` 与 `backend/app/services/llm_test_models.py`；请求 body/headers、masked key、preview 截断、retryable / non-retryable 与前端 Prompts 页测试连接链路保持兼容。
  - `MVP-680` / `T02/T11` 低风险子项：`backend/app/services/chapter_generation_app_service.py` 已拆成 chapter-local prepare / plan-prepare / prompt / memory / post-process 模块，主文件收口到 `245` 行；`backend/app/services/outline_generation_app_service.py` 已拆成 prepare / segment / fill / gap-repair / final-sweep / stream / models / route-bridge 等 feature-local 模块，主文件收口到 `206` 行，`outline_generation_segment_service.py` 收口到 `387` 行。
  - `MVP-681` / `T18` 低风险子项：补齐新增 application service / helper module 的 backend unittest，补跑 chapter analysis、chapter rewrite、auto memory update、`/llm/test`、chapter generation、outline generation、generation-runs、prompt preview、TaskCenter 等 API/UI 定向黑盒与全量黑盒，并如实更新主文档、辅文档与 Wave G issue CSV。

- 本批验证结果（真实实跑）：
  - 后端 `compileall`：通过
  - 后端 `unittest`：`446 passed, 1 skipped`
  - 后端 `quality_gate`：通过
  - 前端 `lint`：通过
  - 前端 `vitest`：`28` 文件 / `97` 测试通过
  - 前端 `build`：通过
  - 定向 Playwright（本批要求的 `26` 个 spec 文件）：`30/30` 通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过
  - `python .codex/skills/plan/scripts/validate_issues_csv.py issues/2026-03-15_18-54-48-wave-2026-03-15-g-remaining-route-boundary-closeout.csv`：通过

- 本批实际关闭或缩小的 warning / 热点：
  - `backend/app/api/routes/chapter_analysis.py` 已从 `no-direct-llm-call-in-api` warning 列表移除。
  - `backend/app/api/routes/llm.py` 已从 `no-direct-llm-call-in-api` warning 列表移除。
  - `quality_gate` 当前 `no-direct-llm-call-in-api` 已无任何 route direct-LLM warning。
  - `backend/app/services/chapter_generation_app_service.py` 不再触发 `file-line-count-guard` warning。
  - `backend/app/services/outline_generation_app_service.py` 不再触发 `file-line-count-guard` warning。
  - `backend/app/services/outline_generation_segment_service.py` 已压到阈值内（`387` 行），不再属于本批热点。

- 本批仍未做且不属于本批范围的内容：
  - `backend/app/services/outline_generation_stream_service.py` 当前仍约 `480` 行，属于本批拆分后残留的单个 helper 热点；继续拆会直接触碰 outline stream/fallback/event 语义，已超出“本批只做 feature-local shrink、不得改 stream contract”的边界。
  - `backend/app/api/routes/outline.py`（约 `1108` 行）与 `backend/app/api/routes/memory.py`（约 `759` 行）仍保留 file-line warning，但继续拆将重新打开 Wave F 已完成的 route/application service 范围，不属于本批四个 issue 包。
  - `plot_analysis_service.py`、`memory_update_service.py`、`vector_rag_service.py`、`project_task_service.py` 等其他巨石 warning 未处理；这些不在本批触达链路内，继续拆会明显扩 scope。
  - `T12` 知识子域统一、`T13` 任务系统工业化、`T14+` Postgres / Redis / RQ / 部署 / 观测 / CI / 压测升级均未做；本批仍保持 compatibility-first。
  - 未改任何既有 API contract、SSE contract、status code、error code、generation run 类型、前端 URL/search params/ARIA/按钮文案合同。

- 生产可推结论：
  - 现阶段仓库仍达到“可直接推送更新到生产端”的标准。
  - 原因是本批已把剩余 route direct-LLM allowlist 清零、把本批最容易继续被 review 命中的 generation/outline 热点显著缩口，同时后端、前端、定向 Playwright 与全量 Playwright 均已真实实跑通过，且没有引入 API/UI contract 回退。

### 附录 Q：2026-03-15 第八批次实际完成结果（Wave 2026-03-15 H）

本批严格承接附录 P 对 `Wave 2026-03-15 H` 的推荐范围，只做 `T02/T11` 的三个局部热点缩口（outline stream、outline route、memory route）与 `T18` 的测试/文档闭环；没有重开 Wave A-G 已关闭问题，也没有借“统一后端架构”“知识子域统一”“任务系统工业化”或 `T14+` 基础设施升级之名扩 scope。

- 本批实际关闭项：
  - `MVP-682` / `T02/T11` 局部子批次：`backend/app/services/outline_generation_stream_service.py` 已继续拆成 stream finalize 与 stream progress 两个 feature-local helper 模块（`outline_generation_stream_finalize_service.py`、`outline_generation_stream_progress_service.py`），主文件只保留最核心编排；outline stream/fallback、SSE event、segmented/fill/repair 兼容字段以及 `generation_run_id` / `generation_sub_run_ids` / `warnings` / `parse_error` / `finish_reason` / `dropped_params` 语义保持不变。
  - `MVP-683` / `T02/T11` 局部子批次：`backend/app/api/routes/outline.py` 中残留的 route-local policy / chapter helper / prompt helper 已抽到 `outline_route_policy.py`、`outline_route_chapter_helpers.py`、`outline_route_prompt_helpers.py`；route 层收口为鉴权、header/params 接入、service 调用与最薄响应边界，没有把 Wave F 已完成的 outline app-service 编排拉回 route。
  - `MVP-684` / `T02/T11` 局部子批次：`backend/app/api/routes/memory.py` 中 preview / retrieve pack mapping、auto-update touched normalize/helper 与 route-local request model 已抽到 `memory_route_helpers.py`、`memory_route_models.py`；`/memory/preview`、`/memory/retrieve` 与 memory update UI 主路径的 pack structure / warnings / fail-soft 合同保持兼容。
  - `MVP-685` / `T18` 低风险子项：补齐 outline stream helper 与 memory route helper 的 backend unittest，如实更新主文档、辅文档与 Wave H issue CSV，并完成本批要求的 backend / frontend / Playwright 定向与全量回归闭环。

- 本批验证结果（真实实跑）：
  - 后端 `compileall`：通过
  - 后端 `unittest discover -s tests -p "test_outline*_*.py" -v`：通过（`40` 测试）
  - 后端 `unittest discover -s tests -p "test_memory*_*.py" -v`：通过（`28` 测试）
  - 后端 `quality_gate`：通过；`no-direct-llm-call-in-api` 仍无 route direct-LLM warning
  - 前端 `lint`：通过
  - 前端 `vitest`：通过（`28` 文件 / `97` 测试）
  - 前端 `build`：通过
  - 定向 Playwright（本批要求的 `6` 个 spec）：`6/6` 通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过
  - `python .codex/skills/plan/scripts/validate_issues_csv.py issues/2026-03-15_21-13-01-wave-2026-03-15-h-outline-memory-hotspot-shrink.csv`：通过

- 本批实际关闭或缩小的 file-line 热点：
  - `backend/app/services/outline_generation_stream_service.py` 已从 Wave G 时约 `480` 行降到 `364` 行，已压回 `file-line-count-guard` 阈值内，不再是本批残留 warning。
  - `backend/app/api/routes/outline.py` 已从 Wave G 时约 `1108` 行降到 `271` 行；`outline_route_chapter_helpers.py` 也控制在 `393` 行，outline route 热点本批已实质关闭。
  - `backend/app/api/routes/memory.py` 已从 Wave G 时约 `759` 行降到 `699` 行，preview / retrieve / auto-update touched helper 负担已显著缩小，但文件本身仍高于 `400` 行阈值，因此仍属于残留 warning。

- 本批仍未做且不属于本批范围的内容：
  - `backend/app/api/routes/memory.py` 仍保留结构化记忆、导入导出、foreshadow、graph 相关 route 负担；继续把它彻底压回阈值内，需要单独一批继续拆剩余 memory route 局部 helper，但这已超出本批只收 preview / retrieve / auto-update touched 范围。
  - `backend/app/services/memory_update_service.py`、`backend/app/services/memory_retrieval_service.py`、`backend/app/services/vector_rag_service.py`、`backend/app/services/project_task_service.py` 等既有巨石 warning 未处理；它们不在本批触达链路内，继续拆会明显扩 scope。
  - `T12` 知识子域统一、`T13` 任务系统工业化、`T14+` Postgres / Redis / RQ / 部署 / 观测 / CI / 压测升级均未做；本批仍保持 compatibility-first。
  - 未改任何既有 API contract、SSE contract、status code、error code、generation run 类型、前端 URL/search params/ARIA/按钮文案合同，也未修改任何 guard 规则掩盖 warning。

- 生产可推结论：
  - 现阶段仓库仍达到“可直接推送更新到生产端”的标准。
  - 原因是本批已关闭 outline stream 与 outline route 的 file-line 热点、把 memory route 的 touched warning 面继续缩小，并且后端、前端、定向 Playwright 与全量 Playwright 均已真实实跑通过，未引入 API/UI/SSE contract 回退。


### 附录 R：2026-03-17 第九批次实际完成结果（Wave 2026-03-16 I）

本批严格承接附录 Q 与 `5.4大调查详细记录.md` 中对 `Wave 2026-03-16 I` 的推荐范围，只做 `memory.py` 的 remaining route-helper closeout；没有重开 Wave A-H 已关闭问题，也没有借“知识子域统一”“任务系统工业化”或 `T14+` 基础设施升级之名扩 scope。

- 本批实际关闭项：
  - `MVP-686` / `T02/T11` 局部子批次：`/memory/structured` 的 table 参数校验、keyword/before normalize、count 查询、各 table query builder、cursor 计算、row -> response mapping 已抽到 `memory_route_structured_helpers.py`、`memory_route_structured_mappers.py`、`memory_route_structured_models.py`；`StructuredMemoryPage`、`CharacterRelationsView`、`MemoryUpdateDrawer` 依赖的 `counts` / `cursor` / table key / `include_deleted` / `q` / `before` 语义保持兼容。
  - `MVP-687` / `T02/T11` 局部子批次：`story_memories/import_all`、`story_memories/foreshadows/open_loops`、`story_memories/foreshadows/{id}/resolve` 的 schema/version 校验、query/order normalize、chapter 校验、dirty mark、preview mapper、response payload 已抽到 `memory_route_story_helpers.py` 与 `memory_route_story_mappers.py`；`ImportPage`、`ForeshadowsPage`、`ForeshadowDrawer` 主路径保持兼容。
  - `MVP-688` / `T02/T11` 局部子批次：structured/story helper 已再上收为顶层 payload builder，`backend/app/api/routes/memory.py` 只保留鉴权、params/body/header 接入、helper/service 调用与最薄 `ok_payload(...)` 包装；主文件已降到 `356` 行，并从 `file-line-count-guard` warning 列表移除，`memory.py` 热点本批已真实关闭。
  - `MVP-689` / `T18` 低风险子项：补齐 `test_memory_route_structured_helpers.py`、`test_memory_route_story_helpers.py` 等 backend unittest，完成后端/前端/定向 Playwright/全量 Playwright 回归，并如实更新主文档、辅文档与 Wave I issue CSV。

- 本批验证结果（真实实跑）：
  - 后端 `compileall`：通过
  - 后端全量 `unittest discover -s tests -p "test_*.py" -v`：`464 passed, 1 skipped`
  - 后端 `quality_gate`：通过；`no-direct-llm-call-in-api` 仍无任何 route direct-LLM warning，且 `backend/app/api/routes/memory.py` 已不再出现在 `file-line-count-guard` warning 中
  - 前端 `lint`：通过
  - 前端 `vitest`：`28` 文件 / `97` 测试通过
  - 前端 `build`：通过
  - 定向 Playwright（本批要求的 `9` 个 spec）：`9/9` 通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过
  - `python .codex/skills/plan/scripts/validate_issues_csv.py issues/2026-03-17_14-11-01-wave-2026-03-16-i-memory-route-remaining-closeout.csv`：通过

- 本批实际关闭或缩小的热点：
  - `backend/app/api/routes/memory.py` 已从 Wave H 时约 `699` 行降到 `356` 行，已压回 `400` 行阈值内，不再属于本系列残留 warning。
  - `memory.py` 中原本残留的 structured/story-memory/foreshadow route-local normalize、mapper、count/query/cursor builder 已迁出到 feature-local route helper / mapper 模块，不再混杂在 route 主文件中。

- 本批仍未做且不属于本批范围的内容：
  - `backend/app/services/memory_update_service.py`（`1439` 行）、`backend/app/services/memory_retrieval_service.py`（`945` 行）、`backend/app/services/vector_rag_service.py`、`backend/app/services/project_task_service.py` 等既有巨石 warning 未处理；它们不属于这条 `memory.py` route-helper shrink closeout 的最小闭环目标。
  - `T12` 知识子域统一、`T13` 任务系统工业化、`T14+` Postgres / Redis / RQ / 部署 / 观测 / CI / 压测升级均未做。
  - 未改任何既有 API contract、status code、error code、response schema、pack structure、fail-soft 语义、前端 URL/search params/ARIA/按钮文案，也未修改任何 guard 规则掩盖 warning。

- 生产可推结论：
  - 现阶段仓库仍达到“可直接推送更新到生产端”的标准。
  - 原因是本批已真实关闭 `memory.py` route 热点，且后端全量、前端三件套、定向 Playwright 与全量 Playwright 均已实跑通过，没有引入 API/UI contract 回退，也没有扩成 memory 子域统一工程。

### 附录 S：2026-03-17 第十批次实际完成结果（Wave 2026-03-17 J）

本批严格承接附录 R 与 `5.4大调查详细记录.md` 中对 `Wave 2026-03-17 J` 的推荐范围，只做 `prompts.py` 与 `tables.py` 的 route hotspot closeout；没有重开 Wave A-I 已关闭问题，也没有借“知识子域统一”“任务系统工业化”或 `T14+` 基础设施升级之名扩 scope。

- 本批实际关闭项：
  - `MVP-690` / route-local prompts shrink：`backend/app/api/routes/prompts.py` 中原本混在 route 内的 preset/block row -> response mapping、baseline preset ensure、resource metadata join、export / import / export_all / import_all payload builder、preview response builder、block reorder 校验，已抽到 `prompt_route_helpers.py`、`prompt_route_mappers.py`、`prompt_route_import_export.py`、`prompt_route_preview.py`、`prompt_route_models.py`；Prompt Studio / Prompt Templates / Prompt Inspector / Context Preview 依赖的 contract 未改。
  - `MVP-691` / route-local tables shrink：`backend/app/api/routes/tables.py` 中原本混在 route 内的 table schema normalize、row data validate、table / row public mapper、list/detail/count/row_count builder、row create/update payload、ai_update chapter resolve 与 request builder，已抽到 `table_route_helpers.py`、`table_route_mappers.py` 与 `table_route_models.py`；Numeric Tables / Tables Panel / table_ai_update 主路径 contract 未改。
  - `MVP-692` / final thin：prompts route 剩余的 preset/block detail、create/update/reset/delete builder 已继续上收到 `prompt_route_helpers.py`；tables route 剩余的 delete payload 与 request models 已继续上收到 `table_route_helpers.py` / `table_route_models.py`。最终 `backend/app/api/routes/prompts.py` 已从约 `687` 行降到 `188` 行，`backend/app/api/routes/tables.py` 已从约 `601` 行降到 `217` 行，二者都只保留鉴权、请求接入、helper/service 调用与最薄 `ok_payload(...)` 包装。
  - `MVP-693` / docs-regression：补齐 `test_prompt_route_helpers.py`、`test_table_route_helpers.py` 等 route-helper 单测，完成本批要求的 backend / frontend / Playwright 定向与全量回归，并如实更新主文档、辅文档与 Wave J issue CSV。

- 本批验证结果（真实实跑）：
  - 后端 `compileall`：通过
  - 后端全量 `unittest discover -s tests -p "test_*.py" -v`：`473 passed, 1 skipped`
  - 后端 `quality_gate`：通过；`no-direct-llm-call-in-api` 仍无任何 route direct-LLM warning，且 `backend/app/api/routes/prompts.py`、`backend/app/api/routes/tables.py` 已不再出现在 `file-line-count-guard` warning 中
  - 前端 `lint`：通过
  - 前端 `vitest`：`28` 文件 / `97` 测试通过
  - 前端 `build`：通过
  - 定向 Playwright（本批要求的 `17` 个 spec）：均已拿到通过结果；其中 `specs/ui/prompts-rag-config.spec.ts` 首轮单跑出现 1 次未复现失败，未改代码直接复跑后通过，且后续全量 Playwright 仍通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过
  - `python .codex/skills/plan/scripts/validate_issues_csv.py issues/2026-03-17_15-36-10-wave-2026-03-17-j-prompts-tables-route-closeout.csv`：通过

- 本批实际关闭的热点：
  - `backend/app/api/routes/prompts.py` 已从本系列收尾前约 `687` 行降到 `188` 行，并已从 `file-line-count-guard` warning 列表移除；prompts route hotspot 本批已真实关闭。
  - `backend/app/api/routes/tables.py` 已从本系列收尾前约 `601` 行降到 `217` 行，并已从 `file-line-count-guard` warning 列表移除；tables route hotspot 本批已真实关闭。
  - 本批关闭的是 route-local helper / mapper / payload hotspot；没有把复杂逻辑反向塞回 `prompt_presets.py` 或 `table_ai_update_service.py` 形成新的 service 巨石。

- 为什么本批选择 prompts / tables，而不是 auth / batch_generation：
  - `prompts.py` 与 `tables.py` 的剩余负担主要还是 route-local normalize / validate / mapper / payload builder，适合沿用已被 Wave G/H/I 验证过的 feature-local helper 拆分模式。
  - 这两条链路已有较强 backend unittest、API contract 与 UI Playwright 护栏，能在 compatibility-first 前提下真实收口，而不是只做文件搬家。
  - `auth.py` 涉及登录/会话/RBAC，安全敏感性更高；`batch_generation.py` 涉及 runtime / queue / pause-resume / retry 语义，编排复杂度更高，都不适合作为 Wave I 之后的首个 route closeout 批次。

- 本批仍未做且不属于本批范围的内容：
  - `backend/app/api/routes/auth.py`（约 `847` 行）与 `backend/app/api/routes/batch_generation.py`（约 `731` 行）未动；前者安全敏感，后者 runtime/queue 复杂，均不属于本批 prompts/tables closeout 范围。
  - `backend/app/services/prompt_presets.py`（`893` 行）、`backend/app/services/table_ai_update_service.py`（`804` 行）等既有 service warning 仍在，但它们不是这两个 route hotspot closeout 的剩余部分。
  - `T12` 知识子域统一、`T13` 任务系统工业化、`T14+` 基础设施 / 部署 / 观测 / CI / 压测升级均未做。
  - 未改任何既有 API contract、status code、error code、response schema、pack structure、fail-soft 语义、前端 URL/search params/ARIA/按钮文案，也未修改任何 guard 规则掩盖 warning。

- 生产可推结论：
  - 现阶段仓库仍达到“可直接推送更新到生产端”的标准。
  - 原因是本批已真实关闭 `prompts.py` 与 `tables.py` 两个 route hotspot，且后端全量、前端三件套、定向 Playwright、全量 Playwright 与 issue CSV 校验均已实跑通过，没有引入 API/UI contract 回退，也没有把任务扩成 prompt 域或 numeric table 域统一工程。

### 附录 T：2026-03-17 第十一批次实际完成结果（Wave 2026-03-17 K）

本批严格承接附录 S 与 `5.4大调查详细记录.md` 中对 `Wave 2026-03-17 K` 的推荐范围，只做 `worldbook.py` 的 route hotspot closeout；没有重开 Wave A-J 已关闭问题，也没有借“知识子域统一”“任务系统工业化”或 `T14+` 基础设施升级之名扩 scope。

- 本批实际关闭项：
  - `MVP-694` / route-local import-export-bulk shrink：`backend/app/api/routes/worldbook.py` 中原本混在 route 内的 row -> response mapping、keywords / entry_ids normalize、`export_all` builder、`import_all` schema/version 校验与 action/conflict collector、`bulk_update` / `bulk_delete` / `duplicate` builder，已抽到 `worldbook_route_mappers.py`、`worldbook_route_helpers.py`、`worldbook_route_import_export.py`、`worldbook_route_mutations.py` 与 `worldbook_route_models.py`；`WorldBookPage`、worldbook import/export、bulk action、duplicate 主路径 contract 未改。
  - `MVP-695` / route-local preview-auto_update-crud shrink：`worldbook.py` 中原本混在 route 内的 `auto_update` chapter resolve / latest-done fallback / token builder、`preview_trigger` preprocess + `raw_query_text` / `normalized_query_text` / `preprocess_obs` / `match_config` wrapper、create/update/delete payload builder 与 dirty-mark / rebuild schedule wrapper，已抽到 `worldbook_route_preview.py` 与扩展后的 `worldbook_route_mutations.py` / `worldbook_route_helpers.py`；WorldBookPage、ContextPreviewDrawer、writing auto update、search / memory context preview 主路径 contract 未改。
  - `MVP-696` / final thin：bulk-update 的“至少提供一个更新字段”校验也已继续下沉到 route-local mutation helper，最终 `backend/app/api/routes/worldbook.py` 已从本批开始前约 `583` 行降到 `237` 行，只保留鉴权、request params/body 接入、helper 调用与最薄 `ok_payload(...)` 包装，并已从 `file-line-count-guard` warning 列表移除；`worldbook.py` hotspot 本批已真实关闭。
  - `MVP-697` / docs-regression：补齐 `test_worldbook_route_import_export.py`、`test_worldbook_route_preview_mutations.py` 等 route-helper 单测，完成本批要求的 backend / frontend / Playwright 定向与全量回归，并如实更新主文档、辅文档与 Wave K issue CSV。

- 本批验证结果（真实实跑）：
  - 后端 `compileall`：通过
  - 后端全量 `unittest discover -s tests -p "test_*.py" -v`：`483 passed, 1 skipped`
  - 后端 `quality_gate`：通过；`no-direct-llm-call-in-api` 仍无任何 route direct-LLM warning，且 `backend/app/api/routes/worldbook.py` 已不再出现在 `file-line-count-guard` warning 中
  - 前端 `lint`：通过
  - 前端 `vitest`：`28` 文件 / `97` 测试通过
  - 前端 `build`：通过
  - 定向 Playwright（本批要求的 `13` 个 spec，共 `19` 个测试）：通过
  - `cd test && npm test`：Playwright 全量 `139/139` 通过
  - `python .codex/skills/plan/scripts/validate_issues_csv.py issues/2026-03-17_18-31-36-wave-2026-03-17-k-worldbook-route-closeout.csv`：通过

- 本批实际关闭的热点：
  - `backend/app/api/routes/worldbook.py` 已从约 `583` 行降到 `237` 行，并已从 `file-line-count-guard` warning 列表移除；worldbook route hotspot 本批已真实关闭。
  - 本批关闭的是 route-local helper / mapper / payload / wrapper hotspot；没有把复杂逻辑反向塞回 `worldbook_service.py`、`project_task_service.py`、`vector_rag_service.py` 或 `search_index_service.py` 形成新的 service 巨石。

- 为什么本批选择 worldbook，而不是 auth / batch_generation：
  - `worldbook.py` 剩余负担主要还是 route-local import/export、bulk、preview、manual auto_update、CRUD builder 与 dirty/schedule wrapper，最适合继续沿用已经在 prompts / tables / memory / outline 上验证有效的“feature-local helper / mapper 拆分 + route shell 变薄”模式。
  - 这条链路已有较强的 backend unittest、API contract 与 UI Playwright 护栏，能在 compatibility-first 前提下真实收口，而不是只做文件搬家。
  - `auth.py` 涉及登录/会话/RBAC/OIDC，安全敏感性更高；`batch_generation.py` 涉及 runtime / queue / pause-resume / retry / quota 语义，编排复杂度更高，都不适合作为 Wave J 之后的首个 route closeout 批次。

- 本批仍未做且不属于本批范围的内容：
  - `backend/app/api/routes/auth.py`（约 `847` 行）与 `backend/app/api/routes/batch_generation.py`（约 `731` 行）未动；前者安全敏感，后者 runtime/queue 复杂，均不属于本批 worldbook route closeout 范围。
  - `backend/app/services/worldbook_auto_update_service.py`、`vector_rag_service.py`、`project_task_service.py`、`search_index_service.py` 等既有 service warning 仍在，但它们不是这条 `worldbook.py` route hotspot 的剩余部分。
  - `T12` 知识子域统一、`T13` 任务系统工业化、`T14+` 基础设施 / 部署 / 观测 / CI / 压测升级均未做。
  - 未改任何既有 API contract、status code、error code、response schema、pack structure、fail-soft 语义、前端 URL/search params/ARIA/按钮文案，也未修改任何 guard 规则掩盖 warning。

- 生产可推结论：
  - 现阶段仓库仍达到“可直接推送更新到生产端”的标准。
  - 原因是本批已真实关闭 `worldbook.py` route hotspot，且后端全量、前端三件套、定向 Playwright、全量 Playwright 与 issue CSV 校验均已实跑通过，没有引入 API/UI contract 回退，也没有把任务扩成新的 worldbook / knowledge 子域统一工程。
