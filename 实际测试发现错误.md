# 实际测试发现错误

## 环境
- 日期：2026-01-13
- OS/浏览器：Microsoft Windows 11 家庭中文版 (10.0.26100) / Chrome 143.0.0.0（chrome-devtools）
- 前端地址：http://127.0.0.1:5173
- 后端地址：http://127.0.0.1:8000
- 启动方式（引用 README 对应命令名/脚本名即可）：
  - 后端：README「本地启动（开发）- 后端」的 `uvicorn app.main:app --workers 1 --port 8000`
  - 前端：README「本地启动（开发）- 前端」的 `npm run dev`

## 覆盖清单
- [x] 登录/退出/刷新会话
- [x] 主要导航跳转
- [x] 项目创建/切换
- [x] 世界书 CRUD
- [x] 角色卡 CRUD
- [x] 大纲编辑保存
- [x] 写作章节创建/切换/保存/状态切换
- [x] Memory Update 仅 done 章可用
- [x] 模型配置保存/测试
- [x] Prompt/预设编辑与预览
- [x] 导出/下载
- [ ] 控制台/网络无异常

## 问题列表
### [P0] 后端按 README 启动会在 lifespan 阶段退出（AUTH_ADMIN_PASSWORD 过短触发校验异常）
- 复现步骤：
  1) `cd backend`
  2) 运行 README 提供的后端启动命令：`.\.venv\Scripts\python.exe -m uvicorn app.main:app --workers 1 --port 8000`
  3) 观察进程退出，`http://127.0.0.1:8000/docs` 无法访问（连接被拒绝）
- 期望：后端启动成功并监听 8000 端口，打开 `/docs` 正常。
- 实际：启动日志停留在 `Waiting for application startup` / `DB_SCHEMA upgrade head`，随后进程退出（exit code=1），端口未监听。
- 证据（脱敏）：
  - Console（节选）：
    - `INFO: Waiting for application startup.`
    - `{"event":"DB_SCHEMA","action":"upgrade","target":"head"}`
  - Network：N/A（服务未启动）
  - 额外定位：`ensure_admin_user()` 在启动时会对管理员密码做最小长度校验；当前环境中 `AUTH_ADMIN_PASSWORD` 长度为 5（<8），触发 `AppError.validation("密码长度至少 8 位")` 并导致 lifespan 失败退出。
- 影响范围：本地开发启动 / 任何依赖后端的前端功能、E2E。
- 临时规避方案（如有）：启动前通过环境变量覆盖 `AUTH_ADMIN_PASSWORD` 为长度 ≥ 8 的值，或修正 `backend/.env` 中的 `AUTH_ADMIN_PASSWORD`。

### [P1] 世界书「预览触发」按钮无效：未发起预览请求且误触编辑区复选框（导致“未保存”状态）
- 复现步骤：
  1) 进入任一项目的「世界书」
  2) 新建并保存一个世界书条目（任意标题/关键词/内容均可）
  3) 在「预览触发」区填写 `query_text`
  4) 点击「预览」
- 期望：发起预览请求并展示“触发了哪些条目 / 拼接后的 text_md”等结果；不应修改编辑区的条目字段。
- 实际：
  - 点击「预览」后页面未出现预览结果
  - 未观察到任何预览相关网络请求
  - 点击行为会误触右侧编辑区的「阻止递归」复选框（从未选中变为选中），导致「保存」按钮变为可点击（进入未保存状态）
- 证据（脱敏）：
  - Console：无报错（仅出现 1 条 a11y issue，见下一条）
  - Network（节选）：
    - `GET /api/projects/<project_id>/worldbook_entries -> 200`
    - `POST /api/projects/<project_id>/worldbook_entries -> 200`
    - `PUT /api/worldbook_entries/<entry_id> -> 200`
    - 点击「预览」后未出现任何 `*/worldbook*preview*` 相关请求（仅观察到上述 entries CRUD 请求）
- 影响范围：世界书预览链路不可用；且容易造成误编辑。
- 临时规避方案（如有）：暂无（只能手动避免点击或关闭编辑面板后再尝试，但本次测试中仍未触发预览请求）。

### [P2] 前端控制台存在 a11y issue：表单字段缺少 id/name（count: 5）
- 复现步骤：
  1) 正常浏览/操作页面（如登录页、世界书编辑等含表单区域）
  2) 打开控制台（本次用 chrome-devtools 采集）
- 期望：控制台无异常/issue。
- 实际：控制台出现 `[issue] A form field element should have an id or name attribute (count: 5)`。
- 证据（脱敏）：
  - Console：`[issue] A form field element should have an id or name attribute (count: 5)`
  - Network：N/A
- 影响范围：可访问性/表单自动填充/测试稳定性（选择器策略）。
- 临时规避方案（如有）：N/A

### [P1] 写作页「Memory Update」在非 done 章节无提示且按钮看似可用（点击无任何反馈）
- 复现步骤：
  1) 进入项目「写作」页，确保存在至少 1 个章节
  2) 选中一个 `status=planned` 或 `status=drafting` 的章节
  3) 点击顶部工具栏「Memory Update」
- 期望：非 `done` 章节应禁止打开/操作，并给出明确提示（例如 toast/tooltip/禁用态），引导用户先将章节标记为 `done`。
- 实际：按钮呈可点击状态，但点击后无任何 UI 反馈（不弹窗、不 toast、不跳转）。
- 复核结论（2026-01-14）：confirmed；根因：`WritingPage` 调用 `toastWarning` 但 `ToastApi/ToastProvider` 未实现该方法，导致无提示；已修复（见 Issue `MVP-CONF-004`）。
- 证据（脱敏）：
  - Console：无报错
  - Network：点击后未观察到与 memory update 相关的请求
  - 对照：将章节状态切到 `done` 并保存后，再点「Memory Update」会正常打开面板（因此更像是“缺少非 done 的禁用/提示”）。
- 影响范围：用户难以理解为何无法进行记忆更新；易误以为按钮失效。
- 临时规避方案（如有）：先将章节状态保存为 `done` 后再使用「Memory Update」。

### [P1] 部分页内导航路由异常：URL 已切换但页面主体未更新（需手动刷新）
- 复现步骤：
  1) 进入项目「写作」页
  2) 点击侧边栏「模型配置」
  3) 观察 URL 已变为 `/projects/<id>/prompts`，但页面主体仍显示「写作」内容
  4) 手动刷新浏览器后，才会显示正确的「模型配置」页面
  5) （同类复现）在「Prompt Studio」页点击侧边栏「导出」，URL 变为 `/projects/<id>/export`，但页面主体仍停留在「Prompt Studio」；刷新后才进入「导出」
- 期望：点击侧边栏后立即渲染目标页面（heading 与内容应匹配 URL），不需要手动刷新。
- 实际：URL 变化但页面主体不刷新，仍停留在写作页面；需要手动刷新才正常。
- 复核结论（2026-01-13）：not_reproduced；Playwright `test/specs/ui/navigation.spec.ts` 覆盖上述跳转路径，URL 与页面主体渲染一致。该现象更可能与历史 `PersistentOutlet` 导致多个 `useBlocker` 并存的路由不稳定/告警（已在 Issue `MVP-FE-002` 修复），或 chrome-devtools 点击伪影有关。
- 证据（脱敏）：
  - Console：无报错（本次复现未观察到异常堆栈）
  - Network：点击后无关键请求变化；刷新后才会触发目标页加载请求（如 `GET /api/llm_profiles`、`GET /api/projects/<id>/llm_preset` 或导出页相关请求）
  - 见对话中的页面 snapshot 证据：URL=/projects/.../prompts 但 heading=写作；URL=/projects/.../export 但 heading=Prompt Studio
- 影响范围：主要导航可靠性；用户可能误以为“入口坏了/数据没刷新”，且对新手向导影响较大（流程跳转不可信）。
- 临时规避方案（如有）：点击后手动刷新页面。

### [P1] 多个页面「保存/保存大纲」按钮点击无效（无请求/无提示）；只能用 Ctrl/Cmd+S 才能保存
- 复现步骤（以「设定」页为例，其他页同理）：
  1) 进入项目「设定」页
  2) 修改任意字段，使顶部「保存」按钮变为可点击（例如编辑「世界观」）
  3) 点击「保存」
  4) 对照：按快捷键 `Ctrl/Cmd + S`
- 期望：点击「保存」后发起保存请求并提示成功/失败；不依赖键盘快捷键。
- 实际：
  - 点击「保存」无任何反馈（本次 chrome-devtools 点击多次超时）；Network 未出现保存请求
  - `Ctrl/Cmd + S` 可正常保存（保存按钮恢复 disabled，刷新后数据持久化）
- 复核结论（2026-01-13）：not_reproduced；Playwright 复核下在「设定/大纲/写作/模型配置」分别编辑产生 dirty 后，点击保存按钮均可触发对应 `PUT -> 200`，且 `Ctrl/Cmd + S` 同样可保存。该条更可能是 chrome-devtools 点击伪影（或当时环境偶发阻塞）导致的误判。
- 证据（脱敏）：
  - Network（成功保存对照）：`PUT /api/projects/<project_id>/settings -> 200 | request_id:91c9fd86-a687-4e18-8499-900ae32dd6fd`
  - 现象覆盖范围（均出现“按钮点不动/无反馈，但 Ctrl+S 可保存”）：写作页「保存」、大纲页「保存大纲」、模型配置页「保存」、设定页「保存」
- 影响范围：严重影响可用性（用户习惯点击按钮保存；移动端/无键盘环境无法使用快捷键）。
- 临时规避方案（如有）：使用 `Ctrl/Cmd + S` 保存。

### [P1] 模型配置「保存为新配置」按钮无效：未创建后端配置且无任何请求/提示
- 复现步骤：
  1) 进入「模型配置」页
  2) 在「API 配置库」区域的「新建配置名」输入任意名称（例如 `local-test-profile` / `dummy-profile-1`）
  3) 点击「保存为新配置」
  4) 尝试在「选择配置」下拉框中选择新建配置
- 期望：发起创建请求并生成一条后端配置；下拉框出现新配置并自动选中/可选；随后可保存 Key。
- 实际：点击后无提示；Network 未出现创建请求；下拉框仍只有「（未绑定后端配置）」。
- 复核结论（2026-01-13）：not_reproduced；Playwright 复核下点击「保存为新配置」可触发 `POST /api/llm_profiles -> 200`，且新配置会出现在「选择配置」下拉框中。
- 证据（脱敏）：
  - Network（节选）：仅观察到 `GET /api/llm_profiles -> 200`、`PUT /api/projects/<id>/llm_preset -> 200` 等；未观察到任何 `POST /api/llm_profiles`（或类似创建接口）请求。
  - Console：无报错
- 影响范围：无法在 UI 中创建/管理后端配置库，导致无法保存 API Key、无法“测试连接”、无法进行生成链路。
- 临时规避方案（如有）：暂无（需要后端已有预置 profile 或修复该按钮）。

### [P2] 开工向导步骤「补齐设定」疑似误判为“已完成”（新项目设定为空仍显示完成）
- 复现步骤：
  1) Dashboard 新建一个项目（例如「测试项目2」）
  2) 进入该项目「向导」
  3) 观察步骤清单中「补齐设定」显示为“已完成”
  4) 点击该步骤的「打开」进入「设定」页，确认「世界观/风格/约束」均为空
- 期望：「补齐设定」应在相关字段为空时显示“待完成/下一步”，并引导用户补齐设定。
- 实际：新项目设定字段为空，但向导显示“已完成”（完成度也因此偏高）。
- 复核结论（2026-01-13）：not_reproduced；新建项目仅填写「项目名」后进入「开工向导」，「补齐设定」会显示为“下一步”，且设定页 `world_setting/style_guide/constraints` 均为空。若仍能复现，优先排查是否为旧版本 snapshot 或浏览器缓存/本地存储污染导致的误判。
- 证据（脱敏）：
  - Console：无报错
  - Network：N/A
  - 见对话页面 snapshot：向导页显示「补齐设定 已完成」，但设定页 `world_setting/style_guide/constraints` 为空。
- 影响范围：新手流程误导，降低向导可信度。
- 临时规避方案（如有）：忽略该步骤状态，仍需手动补齐设定字段。

### [P2] 写作页 Memory Update 面板「关闭」按钮无效（需按 Esc 才能关闭）
- 复现步骤：
  1) 进入项目「写作」页，选中 `status=done` 的章节
  2) 点击「Memory Update」打开面板
  3) 点击面板内的「关闭」按钮
  4) 对照：按键盘 `Esc`
- 期望：点击「关闭」即可收起/关闭 Memory Update 面板。
- 实际：点击「关闭」后面板仍保持展开；按 `Esc` 才能关闭。
- 复核结论（2026-01-14）：not_reproduced；点击「关闭」可正常收起；另外 Drawer 已统一支持 `Esc` 关闭（见 Issue `MVP-FE-004`）。
- 证据（脱敏）：
  - Console：无报错
  - Network：点击「关闭」未观察到请求；按 `Esc` 后面板关闭（纯前端交互）。
- 影响范围：交互困扰（尤其是面板占用页面空间时）。
- 临时规避方案（如有）：使用 `Esc` 关闭面板。

## 调查补充（2026-01-14，Playwright headless 复核）

> 说明：以下为在**不修改任何业务/测试代码**前提下，使用 Playwright headless（非 chrome-devtools 工具点击）对上述问题做的复核补充。
> 结论分级：`confirmed | partial | not_reproduced | needs_confirmation`。

### 复核环境与前置
- 后端：按 README 的 `uvicorn app.main:app --workers 1 --port 8000` 启动。
  - 为进入 UI 复核，临时通过**进程级环境变量**覆盖 `AUTH_ADMIN_PASSWORD` 为长度 ≥ 8（不落盘、不提交）；否则会命中本文件 [P0] 所述启动失败。
- 前端：`npm run dev`，访问 `http://127.0.0.1:5173`（API 走 `/api`）。
- E2E：`test/` 下 Playwright（示例命令：`cd test; npx playwright --version`）。

### 复核结果摘要
- `confirmed`：后端启动失败（当 `AUTH_ADMIN_PASSWORD` 长度 < 8 且执行 `ensure_admin_user()` 时会导致 lifespan 退出，进程非 0 退出；`/docs` 不可达）。
- `partial`：世界书「预览触发」并非全链路失效：
  - Drawer **关闭**时：点击「预览」可触发 `POST /api/projects/<project_id>/worldbook_entries/preview_trigger -> 200` 并渲染结果。
  - Drawer **打开**时：点击「预览」在 4s 内未观测到 `preview_trigger` 的 request/response（疑似被遮罩/布局覆盖导致点击未命中预览按钮），与“误触编辑区”现象一致，需要进一步确认点击命中点与层级关系（见 plan/Issue）。
- `confirmed`：Settings 页存在 **5 个**表单字段缺少 `id/name`（与本文件 [P2] 的 count:5 一致；Playwright DOM scan 复核）：
  - 向量检索（Vector RAG）：Base URL / Model / API Key（3 个）
  - 协作成员邀请：邀请 user_id / 角色 select（2 个）
- `confirmed`：React Router 警告 `A router only supports one blocker at a time` 可复现：
  - 复现步骤（示例）：在「设定」编辑但不保存 → 点击「大纲」并选择“离开” → 在「大纲」编辑但不保存 → 点击「模型配置」并选择“离开” → 在「模型配置」编辑任意字段。
  - 观测：控制台出现上述 warning（多次），疑似与 `PersistentOutlet` 缓存导致“离开页面未卸载”从而残留多个 `useBlocker` 有关（见 plan/Issue）。
- `not_reproduced`（需复核是否为工具点击伪影）：以下条目在 Playwright 下均可正常工作或可观察到预期反馈，建议标记 `needs_confirmation` 并用真实鼠标复核：
  - 多页面「保存/保存大纲」按钮点击无效：设定/大纲/写作页均可点击保存，均能看到对应 `PUT -> 200`。
  - 模型配置「保存为新配置」：可触发 `POST /api/llm_profiles -> 200`。
  - 写作页「Memory Update」非 done：可 toast 提示“仅定稿章节（status=done）允许记忆更新…”。
  - 写作页 Memory Update 面板「关闭」：点击「关闭」可收起（无需 Esc）。
- 额外发现（`needs_confirmation`）：世界书编辑 Drawer 当前**不支持 Esc 关闭**（只能点击「关闭」或点遮罩），这会放大“点击预览误触/遮罩覆盖”的体感问题。
