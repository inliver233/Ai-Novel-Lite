# 前端完全优化问题发现

> 目标：在不改变既有 Paper & Ink / Atelier 视觉方向的前提下，提升全站一致性、可用性、可维护性与排障体验。
> 
> 约束：本仓库审计仅输出本报告，不做任何业务/样式/结构代码修改。

## 0. 范围、证据与行号说明

### 0.1 覆盖范围（以路由为准）

- 路由来源：`frontend/src/App.tsx`
- 页面文件来源：`frontend/src/pages/**`
- 额外覆盖：全局布局与覆盖层（AppShell/Drawer/Modal/Toast/Confirm/WizardNextBar/MarkdownEditor/写作抽屉群等）

### 0.2 参考规范（必须遵守）

- `ui设计规范.md`
- `docs/ux/glossary.md`（中文优先；“中文（key）”；请求 ID（request_id）需可复制）
- `docs/ux/navigation-ia-v1.md`（侧边栏分组与“高级调试”开关默认折叠/隐藏）
- `docs/ux/advanced-debug-page-guidelines.md`（高级调试页分层结构：Header → Overview → Actions → Results → Debug Details）

### 0.3 行号说明

- 本报告的 `file:line` 均为“近似行号”，用于快速定位；若后续代码有改动，请以实际行号为准。

## 1. 覆盖矩阵（Routes → Page Files）

| 路由 | 页面组件 | 文件 |
| --- | --- | --- |
| `/login` | LoginPage | `frontend/src/pages/LoginPage.tsx` |
| `/` | DashboardPage | `frontend/src/pages/DashboardPage.tsx` |
| `/admin/users` | AdminUsersPage | `frontend/src/pages/AdminUsersPage.tsx` |
| `/projects/:projectId/wizard` | ProjectWizardPage | `frontend/src/pages/ProjectWizardPage.tsx` |
| `/projects/:projectId/settings` | SettingsPage | `frontend/src/pages/SettingsPage.tsx` |
| `/projects/:projectId/characters` | CharactersPage | `frontend/src/pages/CharactersPage.tsx` |
| `/projects/:projectId/outline` | OutlinePage | `frontend/src/pages/OutlinePage.tsx` |
| `/projects/:projectId/writing` | WritingPage | `frontend/src/pages/WritingPage.tsx` |
| `/projects/:projectId/tasks` | TaskCenterPage | `frontend/src/pages/TaskCenterPage.tsx` |
| `/projects/:projectId/structured-memory` | StructuredMemoryPage | `frontend/src/pages/StructuredMemoryPage.tsx` |
| `/projects/:projectId/foreshadows` | ForeshadowsPage | `frontend/src/pages/ForeshadowsPage.tsx` |
| `/projects/:projectId/chapter-analysis` | ChapterAnalysisPage | `frontend/src/pages/ChapterAnalysisPage.tsx` |
| `/projects/:projectId/preview` | PreviewPage | `frontend/src/pages/PreviewPage.tsx` |
| `/projects/:projectId/reader` | ChapterReaderPage | `frontend/src/pages/ChapterReaderPage.tsx` |
| `/projects/:projectId/prompts` | PromptsPage | `frontend/src/pages/PromptsPage.tsx` |
| `/projects/:projectId/prompt-studio` | PromptStudioPage | `frontend/src/pages/PromptStudioPage.tsx` |
| `/projects/:projectId/prompt-templates` | PromptTemplatesPage | `frontend/src/pages/PromptTemplatesPage.tsx` |
| `/projects/:projectId/export` | ExportPage | `frontend/src/pages/ExportPage.tsx` |
| `/projects/:projectId/worldbook` | WorldBookPage | `frontend/src/pages/WorldBookPage.tsx` |
| `/projects/:projectId/graph` | GraphPage | `frontend/src/pages/GraphPage.tsx` |
| `/projects/:projectId/fractal` | FractalPage | `frontend/src/pages/FractalPage.tsx` |
| `/projects/:projectId/styles` | StylesPage | `frontend/src/pages/StylesPage.tsx` |
| `/projects/:projectId/rag` | RagPage | `frontend/src/pages/RagPage.tsx` |
| `/projects/:projectId/import` | ImportPage | `frontend/src/pages/ImportPage.tsx` |
| `/projects/:projectId/glossary` | GlossaryPage | `frontend/src/pages/GlossaryPage.tsx` |
| `*` | NotFoundPage | `frontend/src/pages/NotFoundPage.tsx` |

## 2. 系统性问题（Cross-cutting，影响全站）

本节列的是“跨页面/跨组件”的一致性问题：一旦修复，会显著降低后续页面迭代与回归成本。

### 2.1 “A4 纸面”宽度/留白缺少统一约束（阅读与写作体验不稳定）

- 位置/证据：
  - `frontend/src/components/layout/AppShell.tsx:822`、`frontend/src/components/layout/AppShell.tsx:875` 使用 `max-w-screen-xl`（全站主容器偏宽）。
  - `frontend/src/pages/PreviewPage.tsx:262`、`frontend/src/pages/ChapterReaderPage.tsx:549` 使用 `max-w-none` 导致超宽屏行长失控。
  - `frontend/src/pages/WritingPage.tsx:339`（近似）编辑区未形成“纸张居中+阴影”的稳定隐喻。
- 问题：Paper & Ink 的核心“纸在桌上”的阅读体验需要稳定的行长与留白；当前不同页面宽度策略分裂，导致同一字体/字号在不同页面观感差异很大。
- 影响：阅读疲劳、排版不一致、截图/演示时风格不统一。
- 建议（不改设计方向，做“收敛”）：
  - 为“内容型页面”（写作/大纲/阅读/预览/导出）统一采用 `max-w-4xl mx-auto` 级别的纸面宽度；“工具型页面”（高级调试/配置）可保留更宽，但需明确分层。
  - 在 `AppShell` 层提供可复用容器：`<PaperContent>`（窄）与 `<ToolContent>`（宽）。
  - 动效：页面进入统一使用 `fadeUpVariants`（`frontend/src/lib/motion.ts:20`）。

### 2.2 输入控件风格与 `ui设计规范.md` 3.2 不一致（Underline/Minimal Block）

- 位置/证据：
  - 规范：`ui设计规范.md` “3.2 输入框（Input / Textarea）”要求“去掉四周灰边框，仅保留底边/极淡填充；Focus 底边变陶土色”。
  - 现状：`frontend/src/index.css:151`～`frontend/src/index.css:170` 的 `.input/.textarea/.select` 为完整边框 + 背景块。
- 问题：配置/写作场景里大量盒状输入会造成“表格/工程表单感”，与 Atelier 的“填空题/纸面批注”意象相冲突。
- 影响：视觉噪音上升，重要信息层级不清；长表单更累。
- 建议：
  - 保留 `.input` 作为通用控件，但为写作/设置型页面引入“下划线式”变体（例如 `.input-underline` / `.textarea-underline`），并在关键页面优先使用。
  - 对 JSON/长文本编辑，优先用 “Minimal Block”（极淡底色 + focus-within 边框微强调），避免大块边框堆叠。
  - 伪代码见“Appendix A：Underline Input”。

### 2.3 Focus（键盘）策略：按钮可保留环形强调，文本输入建议弱化 ring

- 位置/证据：
  - 规范：`ui设计规范.md` 4.3 允许 `focus-visible` 的陶土色柔和外圈（无蓝色默认 ring）。
  - 现状：`frontend/src/index.css:106`～`frontend/src/index.css:108` 的 `.ui-focus-ring` 使用 `ring-2 ring-accent`。
- 问题：对按钮/卡片而言 ring-2 合理；对文本输入而言 ring+完整边框会叠加成“强强调”，破坏写作沉浸。
- 建议：
  - 维持 `.ui-focus-ring` 用于 pressable（按钮/可点击卡片）。
  - 对文本输入（尤其写作标题/关键字段）优先采用“下划线变色 + 轻微背景变化”的 focus-within 方案。

### 2.4 请求 ID（request_id）展示与复制不一致（排障路径断裂）

- 位置/证据：
  - Toast 支持复制：`frontend/src/components/ui/ToastProvider.tsx:120`～`:130`。
  - 页面正文缺失/不可复制：
    - `frontend/src/pages/ChapterAnalysisPage.tsx:108`（Header）缺少 request_id（仅 toast 有）。
    - `frontend/src/pages/ForeshadowsPage.tsx:191`（description）展示 request_id，但无复制按钮。
    - `frontend/src/pages/GraphPage.tsx`、`frontend/src/pages/FractalPage.tsx` 多为“仅错误态显示 request_id”。
- 问题：排障时用户需要“稳定可见且可复制”的请求 ID（request_id），只靠 toast 很容易错过；成功态同样需要保留，便于复盘。
- 影响：定位问题成本高；支持/运营复现困难。
- 建议：
  - 统一组件：`<RequestIdBadge requestId=... />`（含复制按钮 + UI_COPY 文案）。
  - 高级调试页：固定放在 Overview/Status 区；业务页：放在“结果摘要/错误卡片”右侧。
  - 伪代码见“Appendix B：RequestIdBadge”。

### 2.5 复制（Clipboard）实现分裂，部分退化方案破坏沉浸

- 位置/证据：
  - 现代实现：`navigator.clipboard.writeText` 广泛使用（例如 `frontend/src/pages/TaskCenterPage.tsx:182`、`frontend/src/pages/rag/RagQueryPanel.tsx:145`）。
  - 退化实现：`document.execCommand("copy")` 仍存在（`frontend/src/pages/AdminUsersPage.tsx:44`、`frontend/src/components/writing/contextPreview/utils.ts:15`）。
  - 强破坏沉浸的 fallback：`window.prompt`（`frontend/src/pages/AdminUsersPage.tsx:204`）。
- 问题：复制能力是调试/配置页的高频操作；实现分裂会导致“某些页能复制、某些页复制失败/弹原生 prompt”的体验断裂。
- 建议：
  - 收敛到一个 `copyText(text, opts)` 工具函数，内部优先 `navigator.clipboard`，失败时弹出应用内 Modal（可选中复制），禁止 `window.prompt`。
  - 伪代码见“Appendix C：copyText()”。

### 2.6 Loading / Empty / Error 状态缺少统一“可行动”模板

- 位置/证据：
  - 多页仍是纯文本加载：例如 `frontend/src/pages/CharactersPage.tsx:245`、`frontend/src/pages/PromptStudioPage.tsx:583`、`frontend/src/pages/SettingsPage.tsx:591`、`frontend/src/pages/PromptsPage.tsx:745`。
  - Skeleton 基础能力已存在：`frontend/src/index.css:195`～`:197` 定义 `.skeleton`。
- 问题：纯文本加载/空态缺少层级与下一步指引；高级调试页更需要“下一步按钮”。
- 建议：
  - 列表/卡片页：使用 Skeleton 占位（与真实布局同结构）。
  - 空态：必须给出“下一步 CTA”（例如“新建角色”“去写作”“触发入库”）。
  - 错误态：除 toast 外，应有页内可见 Error 卡片（带 request_id + copy）。

### 2.7 滚动与高度：`calc(100vh-xxx)` + 多层 `overflow-auto` 易造成 scroll trap

- 位置/证据：
  - `frontend/src/pages/ChapterAnalysisPage.tsx:184` 使用 `max-h-[calc(100vh-320px)] overflow-auto`。
  - `frontend/src/components/chapterAnalysis/MemorySidebar.tsx:93` 使用 `max-h-[calc(100vh-340px)] overflow-auto`。
  - `frontend/src/components/ui/Modal.tsx:25` 也有 `max-h-[calc(100vh-2rem)] overflow-y-auto`（此处合理，但需要避免再嵌套一层滚动）。
- 问题：高度魔法数对 header/底栏变化非常敏感，容易出现双滚动条/底部内容被遮挡。
- 建议：
  - 页面级布局优先用 `flex` + `min-h-0` + 单一滚动容器。
  - 大块 JSON/日志默认折叠（DebugDetails）或移入 Drawer，减少嵌套滚动。
  - 伪代码见“Appendix D：FlexFillScrollable”。

### 2.8 颜色语义：仍大量使用 amber/red/rose（与 token 化方向不一致）

- 位置/证据：
  - `frontend/src/index.css:19` 明确写了迁移方向：warning/danger/info 应语义化而不是 hard-coded amber/red。
  - 仍存在大量 hard-coded：
    - `frontend/src/components/writing/AiGenerateDrawer.tsx:131`（bg-amber-50 / text-amber-800）
    - `frontend/src/pages/WorldBookPage.tsx:80`（bg-amber-200/60）
    - `frontend/src/pages/TaskCenterPage.tsx:50`（bg-red-50 / text-red-700）
    - `frontend/src/index.css:143`（`.btn-danger` 用 `bg-red-600`）
- 问题：hard-coded 色值会在浅色/深色、不同背景层级下产生对比不稳定；也会造成“页面各自为政”的警告/危险视觉语言。
- 建议：
  - 统一语义类：`text-warning/bg-warning`, `text-danger/bg-danger` 等（基于 CSS variables）。
  - 统一“风险提示块”组件（标题/正文/可折叠/复制 request_id）。

### 2.9 WizardNextBar 固定底栏可能遮挡内容（需要全站策略）

- 位置/证据：
  - `frontend/src/components/atelier/WizardNextBar.tsx:141`（fixed bottom）。
  - 受影响页面：`frontend/src/pages/OutlinePage.tsx:906`、`frontend/src/pages/CharactersPage.tsx:415`、`frontend/src/pages/PreviewPage.tsx`/`ChapterReaderPage.tsx`/`ExportPage.tsx` 等。
- 建议：
  - 页面根容器统一预留 `pb-24`（或按实际 bar 高度计算）；或在 AppShell 根据 bar 可见性动态设置 main 的 padding-bottom。

### 2.10 A11y 基线问题：键盘可达性、表单 id、表格语义

- 位置/证据：
  - 点击片段不可键盘触发：`frontend/src/components/chapterAnalysis/AnnotatedText.tsx:77`～`:99`（span onClick 无 role/tabIndex/onKeyDown）。
  - 表单字段缺 id 引发可访问性/测试问题：`frontend/src/pages/WorldBookPage.tsx`（多个字段无 id；详见 WorldBook 专项）。
  - 表格 header 缺 `scope`：`frontend/src/pages/AdminUsersPage.tsx:317`。
- 建议：
  - 统一“可点击=可聚焦=可键盘触发”的规范。
  - 所有输入控件必须具备稳定的 `id`（便于 label/aria/测试选择器）。

### 2.11 性能：长列表与批量选择的可扩展性不足

- 位置/证据：
  - `frontend/src/pages/WorldBookPage.tsx:381`～`:391` 通过 `slice` 做分页渲染，数据大时 DOM 膨胀。
  - `frontend/src/pages/WorldBookPage.tsx:571`（bulkSelectAll）一次性生成大量 id 数组。
- 建议：
  - 长列表用虚拟化（react-window/virtuoso）；批量选择用“all_except”模型。

## 3. 页面级问题清单（按路由）

以下每页列出 P0/P1/P2 级别问题：

- P0：阻塞体验/高概率出错/严重不一致
- P1：明显影响观感与效率
- P2：一致性与可维护性优化

> 说明：为避免重复，本节会引用第 2 节的复用模式（Appendix）。

### 3.1 `/login`（LoginPage）

- [P1] Dev fallback 提示块信息密度偏高、缺少“非技术用户”读法
  - 位置：`frontend/src/pages/LoginPage.tsx:42`
  - 问题：dev_fallback 场景下连续三行风险说明 + 一个链接按钮，视觉上像“故障告警”，容易吓退第一次体验的用户。
  - 影响：登录前就产生不安全/不稳定感；评审/演示时容易被误解为系统异常。
  - 建议：将风险提示改为可折叠（默认收起），并把“继续进入（dev）”做成更明确的 CTA。
  - 交互/动效：折叠区使用 `DebugDetails` 风格，但文案更“人话”。

- [P2] 缺少“忘记密码/恢复路径”
  - 位置：`frontend/src/pages/LoginPage.tsx:83`
  - 问题：除“重置表单”外，没有任何恢复/提示；对于非开发场景属于死路。
  - 影响：用户遇到凭证问题只能求助管理员/直接改 DB。
  - 建议：在页脚提示中加入“联系管理员/重置密码”的指引（即使 MVP 只支持管理员操作，也应给出路径）。

- [P2] next 跳转逻辑正确，但缺少“将跳转到哪里”的可解释性
  - 位置：`frontend/src/pages/LoginPage.tsx:21`、`frontend/src/components/layout/AuthGuard.tsx:23`
  - 问题：实现正确（`/login?next=...` + `safeNextPath`），但用户看不到登录成功后的去向。
  - 建议：当 nextPath 非 `/` 时，在副标题补一句：“登录后将返回：xxx”。

### 3.2 `/`（DashboardPage）

- [P0] 主内容容器过宽，弱化“纸面/书架”隐喻
  - 位置：`frontend/src/components/layout/AppShell.tsx:822`
  - 问题：`max-w-screen-xl` 导致卡片与正文视觉更像管理后台。
  - 影响：Paper & Ink 的“精致感”被冲淡，尤其在大屏。
  - 建议：对 Dashboard 这类“项目入口页”可保留更宽，但项目列表（书脊/封面）区建议限定宽度或使用居中网格（明确“书架”版式）。

- [P1] 缺少问候/近期创作引导（与 `ui设计规范.md` 的 dashboard wireframe 不一致）
  - 位置：`frontend/src/pages/DashboardPage.tsx:106`
  - 问题：没有“时间问候 + 继续写作”类顶层引导。
  - 影响：首次进入缺少情绪/方向；老用户也缺少“下一步”。
  - 建议：增加 Header（Serif + 大字号），并把“继续写作/打开最近项目”作为首个 CTA。
  - 动效：Header 轻微 fade-in；卡片列表使用 `duration.stagger = 0.03`（`frontend/src/lib/motion.ts:5`）。

- [P1] 项目卡片未建立“书封面”隐喻
  - 位置：`frontend/src/pages/DashboardPage.tsx:259`
  - 问题：当前多为通用 `panel-interactive`，更像设置卡。
  - 影响：项目辨识度低；Paper & Ink 的“书”主题没有落地。
  - 建议：统一一个 `BookCard`：纵向比例、标题/进度条位置固定、可选“书脊阴影”。

- [P2] 新建项目虚线卡已存在，但尺寸/层级需与项目卡对齐
  - 位置：`frontend/src/pages/DashboardPage.tsx:118`
  - 问题：虚线卡存在（符合规范），但需要保证与项目卡同栅格、同高度。
  - 建议：将新建卡作为“书架的空位”，在空态时更突出。

- [P2] ProjectSwitcher 使用原生 `<select>`（跨平台不一致）
  - 位置：`frontend/src/components/atelier/ProjectSwitcher.tsx:29`
  - 问题：Windows/Mac 渲染差异明显，无法完全贴合 token。
  - 建议：替换为自定义下拉（可用现有 UI 组件实现），并提供“创建新项目”入口。
  - 伪代码：见 `ses_4062c3a30ffe1dubVDcczrNVDs` 的建议（已在本报告 2.1/2.2 思路覆盖）。

### 3.3 `/admin/users`（AdminUsersPage）

- [P0] 小屏表格不可用：强制横向滚动
  - 位置：`frontend/src/pages/AdminUsersPage.tsx:316`
  - 问题：`min-w-[720px]` 导致手机/小屏必须横向滚动。
  - 影响：管理操作高摩擦。
  - 建议：小屏改为卡片列表；或隐藏低优先列（`hidden md:table-cell`）。

- [P1] 全局 loading/saving 锁死所有行（并发操作被阻塞）
  - 位置：`frontend/src/pages/AdminUsersPage.tsx:58`
  - 问题：对 User A 的操作会阻塞 User B。
  - 建议：按 row/action 维度维护 loading 状态。

- [P1] Clipboard fallback 使用 `window.prompt` / `execCommand` 破坏沉浸
  - 位置：`frontend/src/pages/AdminUsersPage.tsx:44`、`frontend/src/pages/AdminUsersPage.tsx:204`
  - 问题：原生 prompt/过时 API。
  - 影响：体验割裂；在部分环境下复制失败。
  - 建议：统一用 `copyText()`（Appendix C），失败时用应用内 Modal 显示可选中文本。

- [P1] 表单 Enter 键无法提交
  - 位置：`frontend/src/pages/AdminUsersPage.tsx:304`
  - 问题：按钮 `type="button"` 且未使用 `<form onSubmit>`。
  - 建议：改为表单提交模型（不改变样式即可提升效率）。

- [P2] 表格语义与 a11y 细节
  - 位置：`frontend/src/pages/AdminUsersPage.tsx:317`
  - 问题：表头缺 `scope="col"`；label/id 绑定也不够稳定。
  - 建议：补齐 scope；输入加稳定 id。

### 3.4 `/projects/:projectId/wizard`（ProjectWizardPage）

- [P0] “下一步”与 `WizardNextBar` 存在双主 CTA（注意力被撕裂）
  - 位置：`frontend/src/pages/ProjectWizardPage.tsx:249`（Header Next）与 `frontend/src/components/layout/ProjectWizardShell.tsx`（Footer WizardNextBar）
  - 问题：同屏出现两个“下一步”，且位于不同位置。
  - 影响：用户不知道以哪个为准；移动端更混乱。
  - 建议：保留一种主 CTA（推荐保留底栏作为稳定锚点），Header 仅保留次要动作（刷新/返回）。

- [P1] “一键开工/Auto Mode”与手动流程并列，缺少明确选择点
  - 位置：`frontend/src/pages/ProjectWizardPage.tsx:273`
  - 问题：Auto Mode 在视觉层级上与步骤列表竞争。
  - 影响：用户不确定该点哪里开始。
  - 建议：把 Auto Mode 变成“开场二选一”或“快速通道卡片”（可折叠），并给出将执行的步骤预览。

- [P1] 覆盖章节是高破坏操作，确认交互不够“重”
  - 位置：`frontend/src/pages/ProjectWizardPage.tsx:178`
  - 问题：确认弹窗对“清空/覆盖”类动作风险提示不足。
  - 建议：增加二次确认（例如要求输入项目名/输入“覆盖”）或至少在文案中强调影响范围。

- [P2] WizardNextBar 在 overview 页隐藏，导致底栏“忽隐忽现”
  - 位置：`frontend/src/components/layout/ProjectWizardShell.tsx:29`
  - 问题：overview 缺底栏，进入步骤页才出现。
  - 建议：overview 也显示底栏（指向第一个未完成步骤），让节奏更稳定。

### 3.5 `/projects/:projectId/settings`（SettingsPage）

- [P1] Loading 用纯文本，缺少 Skeleton
  - 位置：`frontend/src/pages/SettingsPage.tsx:591`
  - 影响：布局跳变、缺乏精致感。
  - 建议：按 section 显示 skeleton（Appendix E）。

- [P1] 数字输入的 clamp 逻辑让编辑困难
  - 位置：`frontend/src/pages/SettingsPage.tsx:738`、`frontend/src/pages/SettingsPage.tsx:746`
  - 问题：用户删除数字再输入新值时会被强制回填。
  - 建议：允许空字符串的中间态；失焦时再 clamp。

- [P1] query_preprocessing 的校验错误只走 toast，缺少就地反馈
  - 位置：`frontend/src/pages/SettingsPage.tsx:300`
  - 影响：用户不知道哪一项错。
  - 建议：字段旁显示 inline error（并保留 toast 作为补充）。

- [P2] 术语与 glossary 不一致/中英混排不稳
  - 位置：`frontend/src/pages/SettingsPage.tsx:700`、`:792`、`:805`、`:823`
  - 问题：“provider/deployment/api_version”等大小写与中文解释不一致。
  - 建议：按 `docs/ux/glossary.md` 使用“中文（key）”格式；缩写首次解释。

- [P2] request_id 在 Debug 结果区仅静态展示，缺复制
  - 位置：`frontend/src/pages/SettingsPage.tsx:1047`
  - 建议：使用 `RequestIdBadge`（Appendix B）。

### 3.6 `/projects/:projectId/prompts`（PromptsPage）

- [P1] Loading 用纯文本
  - 位置：`frontend/src/pages/PromptsPage.tsx:745`
  - 建议：Skeleton（Appendix E）。

- [P1] LLM 额外参数/JSON 编辑“对用户不友好”
  - 位置：`frontend/src/components/prompts/LlmPresetPanel.tsx:231`（来自配置体验审计）
  - 问题：一处 JSON 错就会让连接测试失败。
  - 建议：提供 JSON Editor 包装（至少“实时校验 + 错误定位 + 一键格式化”），或提供 key-value builder。
  - 伪代码：见 `ses_4062c0852ffe060sz0pPOtnmWz` 的 `JsonConfigEditor` 草图。

- [P2] 连接测试成功缺 request_id
  - 位置：`frontend/src/pages/PromptsPage.tsx:655`
  - 建议：成功 toast 也可带 request_id（可选），或至少在页内保留“最近一次测试结果 + request_id”。

### 3.7 `/projects/:projectId/prompt-studio`（PromptStudioPage）

- [P0] request_id 不在页面正文保留（仅 error toast）
  - 位置：`frontend/src/pages/PromptStudioPage.tsx:79`（toastError）、`:540`～`:557`（preview 请求）
  - 问题：预览是高频排障入口，但 request_id 不可追溯。
  - 建议：在预览结果区固定展示 `RequestIdBadge`（Appendix B），并为“渲染日志/render_log”提供 copy。

- [P1] Loading 用纯文本
  - 位置：`frontend/src/pages/PromptStudioPage.tsx:583`
  - 建议：用 skeleton 模拟左侧 preset 列表与右侧编辑器骨架（Appendix E）。

- [P1] 文案治理与术语一致性不足（大量 hard-coded）
  - 位置：`frontend/src/pages/PromptStudioPage.tsx:570`～`:578`（任务 label）、`:590`（beta 文案）
  - 问题：面向用户的文案未走 `UI_COPY`，也不符合 glossary “中文（key）”策略。
  - 影响：后续维护/测试选择器不稳定；术语漂移。
  - 建议：逐步迁移到 `frontend/src/lib/uiCopy.ts`，并对任务名采用“中文（task_key）”或“中文（英文名）”。

- [P2] Help 区域同时使用 `DebugDetails` + 原生 `<details>`（组件语言不统一）
  - 位置：`frontend/src/pages/PromptStudioPage.tsx:611`、`:620`
  - 问题：两种折叠样式并存，造成视觉层级不一致。
  - 建议：统一为 `DebugDetails` 或统一为“HelpSection”组件（可沿用 DebugDetails 的视觉，但文案更写作向）。

- [P2] 提示词编辑缺少“变量高亮/模板语法支持”
  - 位置（来自配置体验审计）：`frontend/src/pages/promptStudio/PromptStudioPresetEditorPanel.tsx:407`
  - 问题：`{{...}}` 类模板变量不可视；纯 textarea 容易写错。
  - 建议：即使不引入大型编辑器，也可以用“overlay 高亮层”实现变量着色（见 `ses_4062c0852ffe060sz0pPOtnmWz` 的实现草图）。

### 3.8 `/projects/:projectId/prompt-templates`（PromptTemplatesPage）

- [P1] 模板块编辑以纯 textarea 为主，缺少语法/变量提示
  - 位置：`frontend/src/pages/PromptTemplatesPage.tsx:502`
  - 影响：对模板系统不熟的用户很难写对；出错只能靠试。
  - 建议：最小增强：
    - 在 textarea 上方显示可用变量清单（可复制）；
    - 提供“预览渲染”（调用同 PromptStudio 的 preview 能力）并显示 request_id。

- [P2] dirty 状态提示过轻，缺少“未保存离开”保护
  - 位置：`frontend/src/pages/PromptTemplatesPage.tsx:471`～`:499`
  - 问题：当前仅在块内显示“未保存”字样（近似），没有全局离开保护。
  - 建议：引入页面级 UnsavedChangesGuard（与 Outline/Writing 一致），或在切换 preset 时强提示。

### 3.9 `/projects/:projectId/characters`（CharactersPage）

- [P0] WizardNextBar 遮挡风险（缺少统一 bottom safe area）
  - 位置：`frontend/src/pages/CharactersPage.tsx:219`（root）与 `frontend/src/pages/CharactersPage.tsx:415`（WizardNextBar）
  - 问题：页面根容器缺 `pb-*`，底栏固定时可能遮挡最后一行内容/按钮。
  - 建议：页面 root 增加 `pb-24` 或由 AppShell 统一管理（见 2.9）。

- [P1] 搜索输入仍是“盒状 input”，与 Underline/Minimal Block 方向不一致
  - 位置：`frontend/src/pages/CharactersPage.tsx:228`
  - 建议：角色页属于“设定/资料”类，优先用下划线式输入（Appendix A）。

- [P1] 删除动作使用 `text-accent`，危险语义不清
  - 位置：`frontend/src/pages/CharactersPage.tsx:307`
  - 问题：accent 同时承载“主要强调”与“危险操作”。
  - 建议：危险动作改用 `text-danger` / `btn-danger`（语义化）。

- [P2] 角色卡片内边距偏紧
  - 位置：`frontend/src/pages/CharactersPage.tsx:281`（p-5）
  - 建议：提升到 p-6/p-8，使卡片更“纸面”。

- [P2] Drawer 动效与层级需要统一 token
  - 位置：`frontend/src/pages/CharactersPage.tsx:339`
  - 建议：Drawer 进入/退出统一使用 `transition.slow`（`frontend/src/lib/motion.ts:13`），并确保 focus trap/esc 行为一致。

### 3.10 `/projects/:projectId/outline`（OutlinePage）

- [P0] WizardNextBar 遮挡风险（缺少 bottom safe area）
  - 位置：`frontend/src/pages/OutlinePage.tsx:422`（root）与 `frontend/src/pages/OutlinePage.tsx:906`（WizardNextBar）
  - 建议：root 加 `pb-24`（见 2.9）。

- [P1] 留白偏小，纸面感不足
  - 位置：`frontend/src/pages/OutlinePage.tsx:424`、`frontend/src/pages/OutlinePage.tsx:506`（panel p-4）
  - 问题：16px padding 更像紧凑管理页。
  - 建议：提高至 p-6/p-8（与规范“至少 32px 留白”一致）。

- [P1] 删除动作语义不清（同样使用 accent）
  - 位置：`frontend/src/pages/OutlinePage.tsx:472`
  - 建议：危险动作统一使用 danger 语义（2.8）。

- [P2] 工具栏按钮主次关系可能需要重排
  - 位置：`frontend/src/pages/OutlinePage.tsx:443`、`:457`
  - 问题：“新建/重命名”等行为的主次在不同项目状态下不同。
  - 建议：当无大纲时，“新建”应为 primary；当已有大纲，“保存/生成”才是 primary。

### 3.11 `/projects/:projectId/writing`（WritingPage）

- [P0] “纸面”隐喻缺失：编辑区背景与画布接近、缺少白纸层级
  - 位置（近似）：`frontend/src/pages/WritingPage.tsx:343`
  - 问题：编辑容器使用 `.panel`（bg-surface）而不是更亮的“纸”。
  - 影响：写作主场景不像在纸上写，而像在表单里填。
  - 建议：中间写作区用 `bg-canvas` 上的 `bg-white`/更亮 surface，并用轻阴影强调。

- [P0] 写作区宽度未收敛（缺少 A4 居中约束）
  - 位置（近似）：`frontend/src/pages/WritingPage.tsx:339`
  - 建议：写作正文容器 `max-w-4xl mx-auto`（见 2.1）。

- [P1] 页面进入缺少统一过渡动效
  - 位置（近似）：`frontend/src/pages/WritingPage.tsx:287`
  - 建议：用 `fadeUpVariants`（`frontend/src/lib/motion.ts:20`）。

- [P1] 章节列表侧栏宽度为 magic number
  - 位置（近似）：`frontend/src/pages/WritingPage.tsx:331`（`w-[240px]`）
  - 建议：抽成常量并与 AppShell 侧栏策略对齐（200/260 的规范取一个并全站统一）。

- [P1] 写作标题输入仍是“盒状 input”，缺少“文档标题”的气质
  - 位置（近似）：`frontend/src/pages/WritingPage.tsx:405`
  - 建议：标题使用透明/下划线式输入 + Serif 大字号（Appendix A）。

- [P2] 表格/列表编辑噪音（表格单元格用完整 input）
  - 位置（近似）：`frontend/src/components/writing/TablesPanel.tsx:656`
  - 建议：单元格默认无边框，仅 hover/focus 出现底边或轻背景，降低视觉噪音。

### 3.12 `/projects/:projectId/preview`（PreviewPage）

- [P0] 行长失控：`.atelier-content` 使用 `max-w-none`
  - 位置：`frontend/src/pages/PreviewPage.tsx:262`
  - 影响：超宽屏阅读疲劳，破坏“纸面”隐喻。
  - 建议：正文容器 `max-w-4xl mx-auto`，并保持左右留白（2.1）。

- [P1] 留白不足（p-6=24px，不达规范“至少 32px”）
  - 位置：`frontend/src/pages/PreviewPage.tsx:248`
  - 建议：提升到 p-8；或在 Paper 容器层统一。

- [P1] sidebar 高度用魔法数，易双滚动
  - 位置：`frontend/src/pages/PreviewPage.tsx:243`
  - 建议：改用 flex 布局（Appendix D）。

- [P2] 导航“返回首页/返回项目”缺显性入口
  - 位置：`frontend/src/pages/PreviewPage.tsx:188`
  - 建议：Header 增加 breadcrumb 或“返回写作/返回项目”。

### 3.13 `/projects/:projectId/reader`（ChapterReaderPage）

- [P0] 行长失控：正文使用 `max-w-none`
  - 位置：`frontend/src/pages/ChapterReaderPage.tsx:549`
  - 建议：同 Preview，限制纸面宽度；若 3 列布局会挤压，可将右侧记忆面板改为可折叠 Drawer。

- [P1] 留白不足
  - 位置：`frontend/src/pages/ChapterReaderPage.tsx:536`
  - 建议：p-8（或 Paper 容器统一）。

- [P2] 3 列布局在小屏笔记本上过挤
  - 位置：`frontend/src/pages/ChapterReaderPage.tsx:559`
  - 建议：将右侧面板显示阈值从 `xl` 提高到 `2xl`，或增加“阅读模式：沉浸/对照”切换。

### 3.14 `/projects/:projectId/export`（ExportPage）

- [P1] 留白不足
  - 位置：`frontend/src/pages/ExportPage.tsx:73`
  - 建议：p-8。

- [P2] 表单控件（checkbox）视觉仍偏“原生表单”
  - 位置：`frontend/src/pages/ExportPage.tsx:98`、`:135`
  - 建议：在不引入新依赖的前提下，提供一个轻量 Checkbox 样式封装（沿用 token + focus-visible）。

### 3.15 `/projects/:projectId/foreshadows`（ForeshadowsPage）

- [P0] has_more=true 但无“加载更多”（数据被截断且无法访问）
  - 位置：`frontend/src/pages/ForeshadowsPage.tsx:87`（limit=80）、`frontend/src/pages/ForeshadowsPage.tsx:285`（hasMore 提示）
  - 问题：UI 提示“已截断”，但缺少 Load More。
  - 影响：用户无法管理更早的伏笔 open loops。
  - 建议：增加“加载更多/分页/无限滚动”。

- [P1] request_id 展示但不可复制
  - 位置：`frontend/src/pages/ForeshadowsPage.tsx:193`
  - 建议：改为 `RequestIdBadge`（Appendix B）。

### 3.16 `/projects/:projectId/chapter-analysis`（ChapterAnalysisPage）

- [P0] 缺少 request_id（与 glossary/排障要求冲突）
  - 位置：`frontend/src/pages/ChapterAnalysisPage.tsx:108`～`:120`
  - 问题：页面无 request_id；只在 toast error 里出现（`frontend/src/pages/ChapterAnalysisPage.tsx:59`）。
  - 建议：Header/Status 区展示 `RequestIdBadge`。

- [P1] 高度使用魔法数，易出现双滚动/被遮挡
  - 位置：`frontend/src/pages/ChapterAnalysisPage.tsx:184`
  - 建议：改用 flex 填充（Appendix D）。

- [P1] Loading 体验较粗糙
  - 位置：`frontend/src/pages/ChapterAnalysisPage.tsx:176`
  - 建议：Skeleton（AnnotatedText/Sidebar 各自骨架）。

- [P0] A11y：可点击标注片段不可键盘操作
  - 位置：`frontend/src/components/chapterAnalysis/AnnotatedText.tsx:77`～`:99`
  - 问题：span onClick 无 role/tabIndex/onKeyDown。
  - 影响：键盘用户无法选择标注；测试选择器也不稳定。
  - 建议：改为 button 或补齐 ARIA（Enter/Space 触发）。

- [P1] Sidebar 同样使用魔法数高度
  - 位置：`frontend/src/components/chapterAnalysis/MemorySidebar.tsx:93`
  - 建议：同主区，用 flex。

### 3.17 `/projects/:projectId/styles`（StylesPage）

- [P1] 长 prompt 在 Modal 内编辑偏拥挤
  - 位置：`frontend/src/pages/StylesPage.tsx:342`（Modal）
  - 问题：中心 Modal 对“长文本编辑”不友好。
  - 建议：改为侧边 Sheet/Drawer（更符合写作工具的“旁注”感）；或至少支持全屏编辑。

- [P2] 文案与 badge 体系仍可统一
  - 位置：`frontend/src/pages/StylesPage.tsx:258`～`:260`（预设标签）
  - 建议：复用统一 `<Badge>` 组件（见 2.8）。

### 3.18 `/projects/:projectId/worldbook`（WorldBookPage）

- [P0] Drawer 可能无法滚动（表单超出屏幕即不可达）
  - 位置：`frontend/src/pages/WorldBookPage.tsx:1283`
  - 建议：Drawer panel 增加 `overflow-y-auto` 或将内容区包一层可滚动容器。

- [P0] Preview Trigger 误点/点击劫持（用户实测 bug）
  - 位置（近似）：`frontend/src/pages/WorldBookPage.tsx:1322`（Run Preview）与 `frontend/src/pages/WorldBookPage.tsx:1490`（Prevent Recursion）
  - 问题：点击 Run Preview 触发了下方 checkbox。
  - 影响：关键功能不可用/不可信。
  - 建议：
    - 所有表单控件加稳定 `id` 并用 `label htmlFor` 显式绑定；避免大面积 `<label>` 包裹。
    - 检查 z-index/布局重叠，确保 preview 区域不被点击穿透。

- [P1] 表单字段缺少 `id`（a11y/自动化测试风险）
  - 位置：`frontend/src/pages/WorldBookPage.tsx` 多处（例如 1344/1449/1462/1473/1484/1495/1505/1546，近似）
  - 建议：为所有 input/textarea/select 添加稳定 id。

- [P1] 大数据量性能：slice 渐进渲染会造成 DOM 膨胀
  - 位置：`frontend/src/pages/WorldBookPage.tsx:381`～`:391`
  - 建议：虚拟化列表（react-window/virtuoso）。

- [P1] 批量选择的可扩展性与可解释性不足
  - 位置：`frontend/src/pages/WorldBookPage.tsx:571`（bulkSelectAll）与 `frontend/src/pages/WorldBookPage.tsx:775`（bulk selected 文案，近似）
  - 问题：一次性写入大量 id；且可能“选中包含不可见项”。
  - 建议：采用 `all_except` 选择模型，并在 UI 明确提示“选中 X（含 Y 个未显示）”。

- [P2] Preview 按钮在 dirty 时 disabled 容易困惑
  - 位置：`frontend/src/pages/WorldBookPage.tsx:1324`
  - 问题：预览运行于 DB 状态而非本地草稿，disabled 合理但缺解释。
  - 建议：disabled 时显示 tooltip/说明文本（当前 hint 在 `:1335` 附近，但建议更显性）。

- [P2] hard-coded warning 色与标签体系不统一
  - 位置：`frontend/src/pages/WorldBookPage.tsx:80`、`:1107`、`:1334`、`:1405`
  - 建议：用 warning token（2.8），统一 `<Badge>`。

### 3.19 `/projects/:projectId/rag`（RagPage，高级调试）

- [P0] 未使用 `DebugPageShell`（结构不统一）
  - 位置：`frontend/src/pages/RagPage.tsx`（参见 `docs/ux/audit-report-2026-01-26.md`）
  - 问题：页面自行拼装 header/actions/results，导致层级与风格偏离规范。
  - 建议：整体迁移到 `DebugPageShell`，并按“Header→Overview→Actions→Results→Debug Details”拆分。

- [P0] ingest/rebuild/status 等操作缺少“成功态 request_id + copy”
  - 位置（近似）：`frontend/src/pages/rag/RagHeaderPanel.tsx`（参见 `docs/ux/audit-report-2026-01-26.md`）
  - 建议：每个操作完成后在 Overview 区展示 `RequestIdBadge`，并提供 copy。

- [P1] `RagQueryPanel` 存在多层滚动容器（scroll trap）
  - 位置（近似）：`frontend/src/pages/rag/RagQueryPanel.tsx:304`/`:313`/`:322`/`:359`/`:391`（来自审计报告）
  - 建议：减少嵌套 overflow；大块 JSON 移入 DebugDetails 或 Drawer。

### 3.20 `/projects/:projectId/graph`（GraphPage，高级调试）

- [P1] 成功态缺少 request_id 展示
  - 位置：`frontend/src/pages/GraphPage.tsx:159`（当前仅错误态显示）
  - 建议：成功态也显示 `RequestIdBadge`。

- [P1] 多处预览/JSON 缺少复制按钮
  - 位置：`frontend/src/pages/GraphPage.tsx:171`（Injection Preview）、`:239`（Debug JSON）
  - 建议：每个预览块右上角提供 Copy（Appendix C）。

- [P2] 空态缺少“下一步”引导
  - 位置（近似）：`frontend/src/pages/GraphPage.tsx:201`/`:219`/`:235`
  - 建议：在空态提供可点击 CTA（例如“运行一次查询”）。

### 3.21 `/projects/:projectId/fractal`（FractalPage，高级调试）

- [P1] 成功态缺少 request_id；预览/Debug JSON 缺复制
  - 位置：`frontend/src/pages/FractalPage.tsx:160`（仅错误态）与预览区（参见 `docs/ux/audit-report-2026-01-26.md`）
  - 建议：同 GraphPage，统一 `RequestIdBadge` + Copy。

- [P2] 多处 `max-h-* overflow-auto` 易 scroll trap
  - 建议：将长内容折叠/抽屉化。

### 3.22 `/projects/:projectId/structured-memory`（StructuredMemoryPage，高级调试）

- [P0] 未使用 `DebugPageShell`
  - 位置：`frontend/src/pages/StructuredMemoryPage.tsx`（参见 `docs/ux/audit-report-2026-01-26.md`）
  - 影响：header/风险提示/操作区样式不统一。
  - 建议：迁移到 `DebugPageShell`，并把“批量操作”放入 Drawer 或固定底栏，避免挤压内容。

- [P1] request_id 不在正文保留
  - 建议：Overview 区固定显示 `RequestIdBadge`。

### 3.23 `/projects/:projectId/tasks`（TaskCenterPage，高级调试）

- [P0] 未使用 `DebugPageShell`
  - 位置：`frontend/src/pages/TaskCenterPage.tsx`（参见 `docs/ux/audit-report-2026-01-26.md`）
  - 建议：迁移到 `DebugPageShell`，统一标题/风险提示/操作区。

- [P1] Drawer 内 JSON 预览可能 scroll trap
  - 位置（近似）：`frontend/src/pages/TaskCenterPage.tsx:441`
  - 建议：长 JSON 默认折叠，或提供“全屏查看/复制”。

- [P2] 复制文案建议与 debug 规范统一
  - 位置（近似）：`frontend/src/pages/TaskCenterPage.tsx:430`（来自审计报告）
  - 建议：统一为“复制 Debug 信息/复制请求 ID（request_id）”。

### 3.24 `/projects/:projectId/import`（ImportPage）

- [P0] 无实时进度（需要手动刷新）
  - 位置：`frontend/src/pages/ImportPage.tsx:301`（刷新列表）、`:405`（刷新详情）
  - 影响：大文件导入时用户无感知，体验很差。
  - 建议：最小改动：导入任务 running 时自动轮询；或 SSE/WS。

- [P1] 过早暴露工程 JSON（写作者不友好）
  - 位置：`frontend/src/pages/ImportPage.tsx:467`～`:481`
  - 问题：WorldBook/story_memory 提案直接 dump JSON。
  - 建议：默认展示“可读预览”（条目数/摘要/差异），JSON 放 DebugDetails 折叠区。

- [P1] 术语解释偏“自解释”，不利于首次使用
  - 位置：`frontend/src/pages/ImportPage.tsx:462`
  - 建议：用 glossary 的“中文（key）”解释，并给出示例（导入后会出现在哪里/怎么用）。

- [P2] 失败重试/卡死恢复能力不足
  - 位置：`frontend/src/pages/ImportPage.tsx:395`
  - 建议：为 running 超时提供“取消/重试”入口，并提示用户下一步。

### 3.25 `/projects/:projectId/glossary`（GlossaryPage，高级调试）

- [P1] 缺少 DebugDetails（raw JSON）与 copy
  - 位置：`frontend/src/pages/GlossaryPage.tsx:270`（使用 DebugPageShell）
  - 建议：增加折叠 DebugDetails，提供“复制 JSON/复制术语 ID”。

### 3.26 `*`（NotFoundPage）

- [P2] 标题/说明文案未完全走 `UI_COPY`
  - 位置：`frontend/src/pages/NotFoundPage.tsx:18`～`:19`
  - 影响：术语一致性与未来国际化/测试选择器稳定性。
  - 建议：将标题与说明迁移到 `UI_COPY`；保留当前 path 展示（用于排障）。

## 4. 非路由关键界面/组件（Global Surfaces）

### 4.1 AppShell（全局布局与导航）

- [P0] “高级调试”开关放在导航列表中，交互层级错位
  - 位置：`frontend/src/components/layout/AppShell.tsx:436`（移动端）、`frontend/src/components/layout/AppShell.tsx:674`（桌面端）
  - 问题：看起来像一个导航项，但实际是偏好设置。
  - 建议：移动到侧边栏底部/设置页；改为更明确的 Toggle（并默认隐藏/折叠，符合 IA）。

- [P1] 内容宽度策略需要“内容型/工具型”分流
  - 位置：`frontend/src/components/layout/AppShell.tsx:822`、`:875`
  - 建议：提供可复用容器（PaperContent/ToolContent），避免每页自带 max-w 策略。

- [P2] 路由标题硬编码维护成本高
  - 位置：`frontend/src/components/layout/AppShell.tsx:39`
  - 建议：与 router 配置合并（例如 route handle.title），或集中到 `lib/routes.ts`。

### 4.2 Drawer / Modal / Overlay（覆盖层基础设施）

- [P0] Drawer 内容区必须保证可滚动
  - 证据：WorldBook Drawer 在 `frontend/src/pages/WorldBookPage.tsx:1283` 出现不可滚动风险。
  - 建议：Drawer 组件提供一致的 `panelClassName` 默认滚动策略（例如 body 区 `overflow-y-auto`）。

- [P1] Escape / 焦点栈（stack）需要覆盖多重 overlay
  - 证据：WorldBook 报告“Esc 不关闭 Drawer”（见 `ses_4062a46ccffepyO8bqjF67Rd6N`）。
  - 相关：`frontend/src/components/ui/Overlay.tsx`（esc listener）。
  - 建议：确保 confirm/modal/drawer 的 esc 优先级可预期，并在 dirty confirm 时给出明确反馈。

### 4.3 Toast / Confirm

- [P1] request_id copy 能力已在 Toast 层实现，但应下沉为通用组件
  - 位置：`frontend/src/components/ui/ToastProvider.tsx:120`～`:130`
  - 建议：Toast 继续保留；但页面正文也应通过 `RequestIdBadge` 提供稳定入口。

### 4.4 MarkdownEditor（核心编辑器）

- [P1] focus/ring 与写作沉浸的“弱强调”目标可能冲突
  - 位置：`frontend/src/components/atelier/MarkdownEditor.tsx:81`（使用 `ui-focus-ring`）
  - 建议：工具栏按钮可保留 ring；编辑区域 focus-within 更建议走“边框/下划线微变”。

### 4.5 WizardNextBar（向导底栏）

- [P0] fixed bottom 需要全站 padding 策略
  - 位置：`frontend/src/components/atelier/WizardNextBar.tsx:141`
  - 建议：由 AppShell 统一加 padding-bottom；避免每页手工 pb。

### 4.6 写作抽屉群（AI/排障工具）

- [P1] 多处 hard-coded amber/rose 用于提示/错误
  - 位置示例：
    - `frontend/src/components/writing/AiGenerateDrawer.tsx:131`
    - `frontend/src/components/writing/PromptInspectorDrawer.tsx:250`、`:256`
    - `frontend/src/components/writing/GenerationHistoryDrawer.tsx:254`
  - 建议：统一 warning/danger 语义色（2.8），并统一提示块组件。

## 5. 优先级路线图（P0/P1/P2）

### 5.1 P0（先救火：阻塞/高风险/强不一致）

- request_id：所有“高级调试/预览/分析”结果区都必须展示并可复制（Appendix B）。
- Foreshadows：`has_more=true` 必须可加载更多（`frontend/src/pages/ForeshadowsPage.tsx:285`）。
- WorldBook：Drawer 可滚动 + 表单 id 补齐 + Preview 误点修复（`frontend/src/pages/WorldBookPage.tsx:1283`/`:1322`/`:1490`）。
- ChapterAnalysis：标注片段 a11y 修复（`frontend/src/components/chapterAnalysis/AnnotatedText.tsx:77`）。
- Advanced Debug：Rag/StructuredMemory/TaskCenter 迁移到 `DebugPageShell`（参见 `docs/ux/audit-report-2026-01-26.md`）。

### 5.2 P1（统一体验：纸面、加载、滚动、语义色）

- 纸面宽度/行长：Preview/Reader/Writing/Outline 统一 `max-w-4xl mx-auto`（2.1）。
- Underline Input：为写作/设置型页面引入输入变体（Appendix A）。
- Loading/Empty：用 Skeleton 替代纯文本加载（Appendix E），空态提供 CTA。
- 滚动：用 flex-fill 替代 `calc(100vh-xxx)`（Appendix D）。
- 语义色迁移：减少 amber/red hard-coded（2.8），统一 risk hint 组件。

### 5.3 P2（长期优化：文案治理、编辑器增强、性能）

- 文案集中：逐步把 hard-coded 文案迁移到 `frontend/src/lib/uiCopy.ts`。
- Prompt Studio：模板变量高亮/预览增强（不强依赖大型编辑器）。
- WorldBook 大数据量：列表虚拟化 + 批量选择 all_except。

## 6. Appendix（可复用模式 / 伪代码）

### Appendix A：Underline Input（下划线式输入）

参考：`ui设计规范.md` 3.2。

```css
/* 概念示例：可在现有 Tailwind 体系中用 class 组合实现 */
.input-underline {
  /* bg-transparent + bottom border only */
}
```

```tsx
// 典型用法：标题/关键字段
<label className="grid gap-1">
  <span className="text-xs text-subtext">项目名</span>
  <input
    className="w-full bg-transparent border-b border-border px-0 py-2 text-lg text-ink outline-none ui-transition-fast focus:border-accent"
  />
</label>
```

### Appendix B：RequestIdBadge（请求 ID（request_id）可复制）

```tsx
import { UI_COPY } from "../lib/uiCopy";

export function RequestIdBadge({ requestId }: { requestId?: string | null }) {
  if (!requestId) return null;
  return (
    <div className="inline-flex items-center gap-2 rounded-atelier border border-border bg-surface px-2 py-1 text-xs">
      <span className="text-subtext">{UI_COPY.common.requestIdLabel}</span>
      <span className="atelier-mono text-ink">{requestId}</span>
      <button className="btn btn-ghost btn-sm" type="button" onClick={() => void copyText(requestId)}>
        {UI_COPY.common.copy}
      </button>
    </div>
  );
}
```

### Appendix C：copyText()（统一复制 + 统一 fallback）

```ts
export async function copyText(text: string): Promise<boolean> {
  try {
    await navigator.clipboard.writeText(text);
    return true;
  } catch {
    // Fallback: show app Modal with selectable text; avoid window.prompt
    return false;
  }
}
```

### Appendix D：FlexFillScrollable（替代 calc(100vh-xxx)）

```tsx
// 用单一滚动容器承载长内容，避免 scroll trap
<div className="flex min-h-0 flex-1 flex-col">
  <header className="shrink-0">...</header>
  <div className="min-h-0 flex-1 overflow-auto">...</div>
</div>
```

### Appendix E：Skeleton（用现有 .skeleton）

参考：`frontend/src/index.css:195`。

```tsx
<div className="grid gap-2">
  <div className="skeleton h-6 w-1/3" />
  <div className="skeleton h-4 w-full" />
  <div className="skeleton h-4 w-5/6" />
</div>
```
